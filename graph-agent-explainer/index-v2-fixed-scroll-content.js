Object.assign(SLIDES, {
  intro_summary: {
    tag: '前言 · 产品心智与设计亮点',
    title: '开篇：核心设计亮点与产品心智',
    text: `
      <p>在人机协同的智能开发生态中，创作体验的敏捷性与生产执行的稳定性往往存在物理屏障。本章节致力于打破这一隔阂，通过确立创作与生产执行的物理一致性，建立 Studio、Engine 与 Gateway 三模块协同的整体产品认知。</p>

      <h3>一、 产品概念：创作与生产执行的物理一致性承诺</h3>
      <p>Studio 作为一个可视化的 Skill 创作、编译与测试工作台，其底层承载着核心的“物理一致性承诺”：<strong>在设计端测试通过的 Skill，在任何异构的生产执行端运行都具备绝对一致的位置表现。</strong></p>
      <p>这种一致性基于“创作与执行彻底分离”的设计理念。在设计期，系统将本地源码编译为具备唯一内容指纹的不可变临时产物，交由执行引擎进行调试。当进行版本发布时，该不可变快照被赋予唯一的版本标识并写入只读成品库，而源码则交由版本控制系统进行管理。生产运行环境只从成品库获取只读的编译产物，不接触、不解析任何源码。这一物理隔离消除了由于环境差异、源码解析版本差异或运行时状态残留导致的执行分叉，确保了“所见即所运行”的物理契约。</p>

      <h3>二、 系统行为：Studio、Engine、Gateway 三模块协同拓扑</h3>
      <p>为了保障上述一致性承诺，系统在物理结构上划分为三个核心模块，它们以位置无关的方式进行协同：</p>
      <ul>
        <li><strong>Engine（执行引擎）</strong>：纯粹的执行逻辑库。它负责将源码编译为不可变快照，并提供运行、预测干跑与断点续跑的核心计算能力。Engine 不具备独立的网络服务接口，它完全依赖宿主环境注入的模型解析器、事件订阅器以及存储提供方来完成计算。</li>
        <li><strong>Gateway（连接网关）</strong>：专门负责凭证与路由的安全逻辑库。它管理着运行期的凭证解析、角色路由决策、备用路由（Fallback）方案以及节点注册表的处理。Gateway 负责持有配置的唯一真相源，在保护敏感密钥的同时为 Engine 提供模型调用映射。</li>
        <li><strong>Studio（消费工作台）</strong>：包含可视化前端与本地后端（Sidecar）。Studio 充当 Engine 与 Gateway 的适配器，负责将画布上的可视化编辑转换为标准的 <code>GRAPH.md</code> 源码，驱动本地编译管道，订阅运行事件流并将其推送到画布进行渲染，同时向 Engine 和 Gateway 注入所需的本地或远程存储实现。</li>
      </ul>

      <div class="lesson-alert alert-amber"><b>【界面占位符】：协同拓扑在 Studio 的系统配置面板中呈现。面板顶部为三维节点拓扑图，展示 Studio 本端、编译执行单元（Engine）与凭证网关（Gateway）的连接状态与网络延迟。点击拓扑节点可展开抽屉，在此配置网关地址、切换凭证来源以及管理底座存储路径。</b></div>

      <h3>三、 用户工作流：从可视化设计到确定性运行</h3>
      <p>用户在 Studio 中的完整生命周期包含以下闭环步骤：</p>
      <ol>
        <li><strong>可视化画布设计</strong>：用户在画布上拖拽节点、连接连线。Studio 将用户的编辑行为实时序列化为 <code>GRAPH.md</code> 源码文件。画布渲染直接读取源码，从而避免中间格式导致的信息丢失。</li>
        <li><strong>本地调试与干跑</strong>：用户点击“调试”或“干跑”按钮。Studio 将当前的 <code>GRAPH.md</code> 源码编译成临时的不可变产物，交由 Engine 运行。Engine 结合 Gateway 解析的开发凭证执行流式计算，并将中间路径差异与执行事件回传给 Studio，Studio 在画布上高亮相应的执行路径。</li>
        <li><strong>版本发布</strong>：在调试完成后，用户在发布面板中填写发布版本号。Studio 执行正式编译，生成带有内容指纹的只读快照并上传至成品库，同时将对应的源码提交至 Git 仓库，完成设计版本与运行版本的双轨归档。</li>
        <li><strong>生产运行</strong>：独立部署的 headless 运行实例通过网关获取成品库中的指定版本快照，直接加载运行，无需 Studio 界面参与。</li>
      </ol>

      <h3>四、 工程契约与边界</h3>
      <p>系统通过明确的物理边界和契约防止行为分叉：</p>
      <ul>
        <li><strong>存储职责分治</strong>：存储接口被划分为三条互不交叉的管道，坚决不设通用接口，不引入旁路缓存：
          <ul>
            <li><em>配置真相线</em>：读写凭证、角色和全局配置。网关每次读取都直接触达持久化介质，不进行任何本地缓存，以确保全局配置的唯一一致性。</li>
            <li><em>运行产物线</em>：存储运行记录（Runs）、评测数据与执行日志。此类数据一次写入后不可更改，支持使用本地副本进行加速。</li>
            <li><em>运行态线</em>：保存断点续跑的临时快照，由单实例独占，在会话生命周期结束后自动清理。</li>
          </ul>
        </li>
        <li><strong>成品库隔离</strong>：成品库只接收通过发布流程提交的、带有确定版本号的编译快照。设计期的临时编译快照被禁止进入成品库，避免污染运行期版本。</li>
      </ul>

      <h3>五、 错误和边界</h3>
      <p>系统在异常边界下表现出清晰、确定的行为模式，不以静默失败处理异常：</p>
      <ul>
        <li><strong>网关连接中断</strong>：当 Studio 无法连接至远程凭证网关时，本地的凭证输入与修改功能会自动锁定，Studio 拒绝执行任何编译与运行请求。系统会抛出连接超时异常，并在底部通知区弹出红色的状态警告。</li>
        <li><strong>发布版本冲突</strong>：若用户尝试向成品库发布已存在的版本号，成品库会拒绝写入并抛出版本冲突异常。Studio 的发布界面会中止流程，并替换显示冲突的版本号，要求用户递增版本或切换发布分支。</li>
        <li><strong>存储路径适配</strong>：在非 POSIX 兼容的远程对象存储环境下，Engine 放弃使用裸物理路径，全部切换为基于逻辑对象标识符的读写。系统启用本地暂存区来缓冲大规模 I/O 写入，避免因直接读写远程存储而导致链路超时。</li>
      </ul>

      <h3>六、 物理分卷：三卷分治的知识定位</h3>
      <p>为满足不同角色的工程师和协作人员的阅读诉求，本手册在结构上划分为三个独立分卷，以清晰定位核心内容：</p>
      <ul>
        <li><strong>第一卷：《设计手册》（本卷）</strong>：专注于阐述产品心智、用户工作流、交互界面规范和体验打磨细节。主要面向产品设计与前端开发人员。</li>
        <li><strong>第二卷：《架构手册》</strong>：专注于阐述底座拓扑、编译执行引擎的计算管道设计、网关策略以及三线存储提供方的接口契约。主要面向系统架构师与后端研发人员。</li>
        <li><strong>第三卷：《开发指南》</strong>：专注于阐述 API 适配器实现、SDK 接入方法、调试脚本以及测试用例的编写规范。主要面向日常参与功能迭代与缺陷修复的工程师。</li>
      </ul>
    `
  },

  init_repair: {
    tag: '第一卷 · Skill Studio 设计与打磨手册',
    title: '1.1 起步：打开工作区与引导修复',
    text: `
      <h3>1. 产品概念：本地文件夹即工作区</h3>
      <p>Studio 采用以本地物理文件夹为核心的工作区模型。这一模型打破了传统的中心化数据库与应用注册表机制，使用户能够以极轻量的方式管理本地项目，实现类似主流现代集成开发环境的无注册表体验。</p>
      <p>工作区直接映射为操作系统中的一个具体目录。当用户在此目录中开展工作时，Studio 仅将其作为本地路径进行操作。在应用欢迎界面中，系统利用本地存储记录最近打开的工作区路径（最长保留 10 条记录），而不在本地系统注册表中写入任何持久性应用元数据。退出或移出工作区仅意味着从应用最近历史中清除路径引用，绝不修改或删除文件夹下的物理文件。</p>

      <h3>2. 系统行为：宽松导入扫描与修复状态激活</h3>
      <p>为最大化保障用户进入开发环境的效率，系统在导入项目时采取不设物理门槛的宽松扫描策略：</p>
      <ul>
        <li><strong>非阻塞式导入：</strong>当用户选择并打开任意本地文件夹时，底层文件系统接口对目录进行基础扫描。即使选定目录为空文件夹，或者缺失核心规范文件（如 <code>GRAPH.md</code> 与 <code>SKILL.md</code>），系统依旧会成功进入工作区主窗口，绝不在前置入口处阻碍用户的导入操作。</li>
        <li><strong>修复状态激活：</strong>进入工作区后，编译器在后台对当前目录结构展开轻量化诊断。若检测到关键结构缺失（例如无法定位技能定义文件 <code>SKILL.md</code> 或工作流描述文件 <code>GRAPH.md</code>），系统会立刻将当前工作区置为修复状态。</li>
        <li><strong>资源与子图扫描：</strong>系统根据路径解析子图依赖。若子图绝对路径包含在当前工作区树形目录内（默认子图存放于工作区 <code>subgraphs/</code> 目录下），系统自动将其视作当前工作区成员进行加载；若子图路径位于当前工作区之外，资源管理器会在面板中将该路径高亮显示，并提供将其关联至当前工作区的便捷操作。</li>
      </ul>

      <h3>3. 用户工作流：自动化目录重构</h3>
      <p>在日常使用中，用户通过简便的图形向导或嵌入式智能助手完成项目的开启与一键重构：</p>
      <ul>
        <li><strong>新建工作区：</strong>用户通过启动页的对话框指定新工作区名称与父级目录。文件系统服务自动执行目录创建、初始化默认脚手架文件、在后台执行 <code>git init</code> 完成版本控制初始化，并自动切换并打开沉浸式开发主窗口。</li>
        <li><strong>打开已有文件夹：</strong>用户可通过系统原生目录选择器选取本地任意文件夹，或直接点击欢迎页中的最近工作区卡片进入。</li>
        <li><strong>一键结构重构：</strong>一旦进入处于修复状态的工作区，右侧的智能助手会自动识别结构缺陷并弹出修复方案。用户只需点击修复按钮，系统便会在后台自动补齐标准的 <code>GRAPH.md</code> 和 <code>SKILL.md</code> 描述文件，并将存在的脚本结构重新编排至规范目录中。在此期间，界面状态在重构完成后实时刷新，错误诊断面板中的警示自动消除，画布恢复为正常编辑状态。</li>
      </ul>

      <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现欢迎页的“最近工作区”卡片列表（各卡片显示工作区名称与本地绝对路径）与新建/打开快捷控制板。对于缺失必要骨架文件的非标文件夹，用户进入工作区后，界面顶部显示带有黄色警示横条，并在右侧 Copilot 助手面板中显示“一键补齐规范文件”的修复向导卡片，支持用户通过交互按钮原地生成 GRAPH.md 及重排目录。</b></div>

      <h3>4. 工程契约：独立运行期隔离</h3>
      <p>为了保障系统的健壮度与用户资产安全，Studio 确立了清晰的底层服务与生命周期契约：</p>
      <ul>
        <li><strong>三模块分离架构：</strong>系统架构解耦为壳层与本地文件系统、语言模型网关、以及图智能运行引擎。其中，本地文件系统模块使用 Rust 实现，作为唯一的物理文件写入者，负责所有新建、重组和 Git 事务操作；模型网关与运行引擎作为无状态的后台进程，在主应用启动时即在后台加载，不阻塞主应用界面的渲染。</li>
        <li><strong>最近卡片移出逻辑：</strong>用户在欢迎页对最近卡片执行移出操作时，系统仅清除本地存储中的路径记录。如果目标物理路径在外部已被用户手动移动或删除，点击该卡片将触发路径不存在的提示信息，并将失效项自动从最近工作区列表中移除。</li>
        <li><strong>会话上下文清理：</strong>当用户点击“返回主页”关闭工作区时，系统会卸载当前画布、重置分屏面板与编辑器状态，并清除该工作区的临时会话数据，防止跨项目的上下文数据污染。退出工作区仅清除运行时的内存上下文，对于最近的对话进度则在本地执行存储，以便用户重新进入该工作区时恢复历史。</li>
      </ul>

      <h3>5. 错误和边界</h3>
      <p>系统针对开发过程中的异常边界实施了全面的防御设计，确保运行时的连续性：</p>
      <ul>
        <li><strong>物理路径缺失：</strong>点击最近记录但对应目录在磁盘上已不存在时，系统捕获操作系统文件异常，弹出通知提示路径已不存在，并同步清理失效的卡片记录，不触发白屏故障。</li>
        <li><strong>物理写入权限受限：</strong>若用户选择无写入权限的目录新建项目，底层写入接口将错误反馈上传至前端，在新建弹窗中以高亮红色警示文案形式展现具体错误信息，引导用户重新选择写入路径。</li>
        <li><strong>后台进程连接延迟：</strong>当后台大语言模型网关或运行引擎正在拉起或因连接异常未就绪时，需要后台计算的服务（如模型列表、在线编译服务等）将自动降级为等待态，并以骨架屏形式展现进度，保障用户依然可以照常操作本地文件与编辑本地脚本，直到后台连接自动恢复。</li>
      </ul>
    `
  },

  config_credentials: {
    tag: '第一卷 · Skill Studio 设计与打磨手册',
    title: '1.2 配置：API 凭证与模型就绪检查',
    text: `
      <h3>一、产品概念</h3>
      <p>在 Skill Studio 的架构中，API 凭证与密钥被彻底从具体的 Skill 流程定义中剥离。这一设计的核心是将“流程编排的逻辑”与“具体运行环境的凭证安全”进行物理隔离。Skill 流程定义中仅声明抽象的模型角色与性能意图，例如声明某节点需要使用一个具有推理能力的模型，而非直接指定某特定的 API 密钥或服务商终端。具体的 API 凭证、三方服务端点以及物理模型实例的绑定关系，统一由全局的设置进行纳管。</p>
      <p>这种解耦机制确保了 Skill 流程可以作为无状态、无密钥的安全配置文件进行自由导入、导出和共享，避免了密钥泄露的风险。同时，当服务商端点发生故障或需要执行密钥轮换时，用户只需在全局设置中修改凭证，所有引用该模型角色的 Skill 流程便会自动应用新的凭证，实现运行时无缝切换。</p>

      <h3>二、系统行为</h3>
      <p>模型与端点的就绪状态并非由前端界面计算或保存，而是由网关服务提供统一的就绪检查与投影机制。网关服务作为单一事实源，综合评估凭证存在性、网络连通性、历史探测记录以及运行期熔断记录，为每个路由实时投影出标准的六态健康模型：</p>
      <ul>
        <li><code>ready</code>（绿色）：表示当前端点与路由均已通过实时探测验证，能够正常执行推理调用。</li>
        <li><code>historical_ready</code>（蓝色）：表示在历史探测证据库中该路由曾成功连通，但当前运行期尚未发起实时测试。此状态介于“未测试”与“完全就绪”之间，允许系统快速复用历史就绪信息，避免每次启动都进行高开销的全量测试。</li>
        <li><code>untested</code>（灰色）：中性状态，表示配置完整且未被熔断，但尚未有任何成功或失败的探测证据。</li>
        <li><code>failed</code>（红色）：表示存在阻碍正常使用的故障。其包含两类细分原因：其一是配置缺口（例如缺失 API 密钥或未探通协议类型），其二是探测失败（连接超时或服务商接口报错）。此类路由仍会保留在可用列表中，允许用户继续进行配置与拖拽，不会因为一次瞬时故障导致配置数据在界面中意外消失。</li>
        <li><code>cooling_down</code>（灰色带倒计时）：表示由于触发了限流、配额耗尽或临时网络超时，路由被网关自动置入熔断冷却状态。系统会根据网关反馈的重试时间计算出冷却时间，在倒计时结束后自动恢复探测权限。</li>
        <li><code>off</code>（灰色且不可选）：表示端点或路由被用户主动停用，或者服务商明确返回该模型已下线。被弃用的模型会被移入弃用区，限制其在角色卡片中的拖拽。</li>
      </ul>
      <p>网关服务在解析模型角色时，依据 <code>off &gt; failed &gt; cooling_down &gt; ready &gt; historical_ready &gt; untested</code> 的优先级顺序对路由进行过滤与排序。在执行角色物化时，系统将自动跳过 <code>failed</code> 与 <code>off</code> 的路由，对 <code>cooling_down</code> 路由输出调用警告，并确保只有处于健康就绪状态的路由被编译进最终的候选链中。</p>

      <h3>三、用户工作流</h3>
      <p>在设置面板中，用户可以对 API 凭证和模型角色进行配置与状态恢复：</p>
      <ol>
        <li>用户进入 API 凭证页面，添加官方或第三方服务商。对于官方服务商，用户填入 API 密钥后，直接点击测试，系统便会向服务商端点发送获取模型列表的请求；对于第三方服务商，用户还需填入 Base URL，系统会自动排列组合不同的协议类型，去尝试访问其推理端点以自动判定协议兼容性。</li>
        <li>系统将获取到的模型列表与本地历史证据库进行差异比对，将证据库中已证实的模型特性与元数据回填至当前列表中，并将本次新发现的模型与能力写入证据库，作为下一次探测的历史依据。</li>
        <li>服务商卡片下挂的多个 Base URL 经探测和协议识别后，会被拆分为独立的标准化端点。用户在模型角色页面，可以将这些按模型类型合并的模型组拖拽入具体的角色配置卡中，并设置其故障转移链。</li>
        <li>当端点在测试中被判定为已下线并归入弃用区时，用户可通过点击行旁的“重新探测”按钮手动触发即时检测，一旦连通，该路由将被即时捞回到可用模型列表中。</li>
      </ol>
      <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现 API Keys 与 LLM Roles 配置面板。API Keys 界面支持用户添加服务商并输入 Key 与 URL，右侧状态指示器固定展示 inline 检查结果（✓ 或 错误诊断文案）；LLM Roles 界面左侧为可用模型侧栏（支持按模型家族折叠，底部包含可折叠的弃用模型区），右侧为角色配置卡片，用户可将模型组拖拽入卡片中，并使用单选三态控件（Off / Preferred / Required）配置 Thinking 意图，以及通过 inline 按钮为特定角色发起一键批量测试。</b></div>

      <h3>四、工程契约</h3>
      <p>Studio 后端服务与网关服务之间基于标准的数据契约传递凭证和投影状态：</p>
      <pre><code>interface ProviderModelStateProjection {
  ui_state: 'ready' | 'historical_ready' | 'untested' | 'failed' | 'cooling_down' | 'off';
  reason_code?: 'missing_config' | 'endpoint_unreachable' | 'model_failed';
  retry_at?: string;
  ui_detail?: string;
}</code></pre>
      <p>在保存第三方端点时，Studio 后端服务将原始 URL 传递给网关服务。网关服务完成协议的探测与路由解析，为不同的协议生成符合命名规则 <code>{slug}-{protocol}[-{n}]</code> 规范的端点标识符，并回传给 Studio 后端进行统一的平铺展示。所有的状态评估与角色解析，均直接调用网关服务的解析接口，以确保 Studio 编辑界面看到的状态与运行期执行链看到的状态保持绝对一致。</p>

      <h3>五、错误和边界</h3>
      <p>系统针对凭证检查和运行期调用制定了严密的边界防御逻辑：</p>
      <ul>
        <li><b>熔断计时器边界：</b>当发生连接超时、429 频控限制或临时网络抖动时，网关服务会在内部的健康仓储中开启对应的熔断器，记录冷却截止时间。Studio 编辑界面以及运行期调用会根据此截止时间判定路由为 <code>cooling_down</code>，运行期调度器将临时跳过该端点以保护外部调用频次，直至倒计时归零，避免频繁的超时重试拖慢整个工作流的执行。</li>
        <li><b>连接失败红标与诊断常驻：</b>若发生 API 密钥错误、URL 无法解析或探测失败等确定性错误，该端点与对应路由在列表组内会被标红，并呈现出醒目的连接失败指示状态。为了避免通知信息闪烁或消失，详细的连接错误原因与诊断文案将常驻于对应配置行下方，鼠标悬浮时可通过顶层的 Tooltip 浮窗查阅详细的技术诊断日志。</li>
        <li><b>意图匹配失败与降级：</b>如果用户为角色配置了必须满足的 Token 意图（例如设定了 Required 模式的最小上下文），且当前就绪端点中没有任何一个实例能够满足该参数要求，该角色状态会自动从 Using 变更为 Not Fit。此时，系统会阻断对该角色的直接运行，并在配置卡片上通过红标警告用户更新配置。</li>
      </ul>
    `
  },

  authoring_source: {
    tag: '第一卷 · Skill Studio 设计与打磨手册',
    title: '1.3 设计：编辑 Skill、Steps 与属性白名单',
    text: `
      <h4>1. 产品概念</h4>
      <p>系统采用“画布直读直写 Markdown 源码”的无损编辑架构。在工作区中，所有的 Phase 节点（包括 Agent 节点与 Logic 节点）以及 Subgraph 子图，均以标准的 Markdown 文本格式（例如 <code>SKILL.md</code>、<code>LOGIC.md</code> 和 <code>SUBGRAPH.md</code>）直接存储于本地文件系统。</p>
      <p>系统完全摒弃了中间编译包或临时缓存格式。画布上的每一次拓扑连线、节点参数配置、或是步骤顺序的调整，都会通过 Rust 后端实时解析并直接写回至对应的 Markdown 源码文件中。这种设计确保了 Markdown 文件是唯一的、可被第三方文本编辑器自由编辑的数据源，使得项目版本控制与多端协作无需进行复杂的格式转换。</p>

      <h4>2. 系统行为</h4>
      <p>当用户在画布中选中特定节点时，右侧的属性面板仅暴露出该节点类型的属性白名单字段，确保配置界面的清爽与准确：</p>
      <ul>
        <li><strong>Agent 节点 (<code>SKILL.md</code>)</strong>：属性面板仅提供元数据表单，包括 <code>name</code>、<code>schema_version</code>、<code>llm_role</code> 和 <code>description</code> 等元数据字段。系统在属性面板中不提供诸如运行模式、退出契约或运行尝试限制等非白名单字段的编辑入口。</li>
        <li><strong>Logic 节点 (<code>LOGIC.md</code>)</strong>：属性面板仅允许编辑其输入与输出的描述信息及 Schema。</li>
        <li><strong>Subgraph 节点 (<code>SUBGRAPH.md</code>)</strong>：属性面板中仅提供子图本地文件路径 <code>path</code> 的配置项。</li>
      </ul>
      <p>属性面板严格限制为仅编辑前置元数据字段（YAML Frontmatter），系统禁止通过属性面板直接编辑 Markdown 源码中的底层步骤（如 <code>&lt;step&gt;</code> 或 <code>&lt;action&gt;</code> 标记）与运行验证所依赖的基准数据。这些微观内容的管理被隔离在画布的内联编辑区及专属的工作流组件中，以防止元数据编辑意外损坏核心的逻辑步骤。</p>

      <div data-demo="demo-nodes"></div>

      <h4>3. 用户工作流</h4>
      <p>系统为用户提供直观的图形化编辑体验：</p>
      <ul>
        <li><strong>内联编辑</strong>：双击画布上的 Phase 节点，即可在画布原位展开该节点的内联编辑区，用户可以直接在此处阅读和改写该节点的 Markdown 描述或核心提示词。</li>
        <li><strong>步骤卡片重排</strong>：节点展开后，其内部所包含的步骤块（即 <code>&lt;step&gt;</code> / <code>&lt;action&gt;</code> 块）会转换为可视化的步骤卡片。用户可以通过拖拽卡片直接调整步骤执行的先后顺序。每一次位置变更，系统都会通过 Rust 解析器重新生成节点文本正文并写回本地文件。</li>
        <li><strong>输入输出与连线映射</strong>：在画布中，用户通过在上游节点的输出引脚与下游节点的输入引脚之间进行连线，来显式声明数据流转关系。子图节点的输入直接从共享黑板中按 Schema 定义的字段进行过滤与切片，不再强制要求子图与其父图保持 1:1 的严格输入输出绑定。用户可以随时为节点的输出定义具体的文件落盘路径，使运行时产生的数据（Markdown 或 JSON 格式）自动写入工作区目录的 artifacts 文件夹中。</li>
      </ul>

      <div data-demo="demo-subgraph"></div>

      <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现子图的内联展开（Subgraph Inline Expansion），其周围包裹虚线容器，展示子图内部节点的拓扑结构与实时状态，交互上支持在虚线边界内直接拖拽、连线和下钻聚焦。</b></div>

      <h4>4. 工程契约</h4>
      <p>当多个上游节点向共享黑板输出同名变量键（Key）时，系统将触发变量冲突警告机制：</p>
      <ul>
        <li><strong>画布红线警告</strong>：如果一个节点的输入变量键可能被其他执行路径上的同名输出覆盖，且未得到用户的确认授权，系统会在受影响的节点边缘及连线处显示红色高亮警告线，并限制编译的通过。</li>
        <li><strong>显式勾选覆盖契约</strong>：开发者双击警告的节点或打开其属性配置，必须在提示中显式勾选“确认覆盖该黑板变量键”的协议复选框。一旦勾选，系统会在写入节点配置时附加 <code>overwrite=true</code> 的标记，画布上的红色警告随即消除，编译限制得以解除。这建立了清晰的共享变量覆盖契约，防止隐式变量覆写导致运行时逻辑错误。</li>
      </ul>

      <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现共享黑板变量的字段勾选面板，列出当前上下文状态机中的所有可用字段，用户可直接勾选所需字段以建立输入映射，并以红色高亮线警示同名覆盖冲突。</b></div>

      <h4>5. 错误和边界</h4>
      <p>系统提供完备的容错与恢复机制，确保在各种异常状态下工作流依然稳定：</p>
      <ul>
        <li><strong>物理写入失败回滚</strong>：系统在向本地 Markdown 写入新状态时执行基于内容哈希的乐观并发校验。如果由于磁盘权限、外部冲突等原因导致文件物理写入失败，系统将自动丢弃内存中的未提交状态并回滚画布，防止画布渲染状态与磁盘实际数据不一致。</li>
        <li><strong>元数据解析损坏错误</strong>：当用户在外部编辑器中手动修改文件且破坏了 Markdown 顶部的 YAML Frontmatter 格式，Studio 画布将使对应的节点呈现损坏状态。用户在底部的编译诊断抽屉中可以看到具体的 YAML 解析错误，点击“打开文件”即可直接在编辑器中定位到该文件的异常行。</li>
        <li><strong>逻辑环路阻断</strong>：如果用户的连线操作在拓扑中引入了闭环（循环依赖），系统将触发全屏环路阻断提示，暂停一切编译操作，直至用户切断构成循环的任意连线。</li>
        <li><strong>子图路径丢失</strong>：如果子图节点引用的 <code>path</code> 相对路径对应的文件在工作区中不存在或被删除，该子图节点将在画布和资源树中标记为红色失效状态。用户可以右键点击该节点，选择本地磁盘中的有效 Markdown 文件重新导入工作区并修复引用。</li>
      </ul>
    `
  },

  compile_gate: {
    tag: '第一卷 · Skill Studio 设计与打磨手册',
    title: '1.4 检验：一键编译与分级门控',
    text: `
      <h4>1. 产品概念：拓扑完整性与类型校验</h4>
      <p>在工作流开发中，静态编译是保证技能（Skill）在执行前具备拓扑完整性与类型安全性的前置机制。系统在画布搭图与配置编辑过程中，通过静态编译对以下关键要素进行校验，从源头上杜绝逻辑拓扑与数据流的配置缺陷：</p>
      <ul>
        <li><strong>拓扑连通性</strong>：检查是否存在悬空节点、未连接的输入输出端口、或者不符合逻辑的闭环死循环。</li>
        <li><strong>数据流契约匹配</strong>：校验前序节点的输出数据格式（Schema）与后续节点接收的输入数据格式是否兼容，检测潜在的数据类型冲突。</li>
        <li><strong>变量引用（Mention）有效性</strong>：检查文本输入或模板中引用的上文变量是否合法存在，防止因节点重命名或删除导致引用失效。</li>
      </ul>

      <h4>2. 系统行为：实时 Lint 与全量编译的区别</h4>
      <p>为了兼顾编辑时的即时反馈与沉浸式操作体验，系统将静态校验分为实时 Lint 与全量 Compile 检查两个级别：</p>

      <h5>实时 Lint 机制</h5>
      <p>当用户在画布上拖拽节点、修改连线或在属性面板中输入参数时，系统将自动触发实时 Lint。其运行逻辑如下：</p>
      <ul>
        <li><strong>防抖等待</strong>：编辑操作停止后，系统会进行 800 毫秒的防抖等待，随后在后台静默发起校验请求。</li>
        <li><strong>局域提示</strong>：校验结果仅以轻量化的小标志或行内气泡的形式，呈现在对应的画布节点、属性面板输入框旁或代码编辑器行内。</li>
        <li><strong>无打扰设计</strong>：实时 Lint 失败时，系统不会弹出任何全局对话框、抽屉面板或全局 Toast，以确保用户的编辑思路不被频繁中断。</li>
      </ul>

      <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现画布节点和属性面板的实时 Lint 提示。当节点参数缺失时，节点右上角显示红色感叹号小图标，鼠标悬浮（Tooltip）显示“参数 Schema 缺失”；同时属性面板的对应输入框下方出现红色警示文本。</b></div>

      <h5>全量 Compile 检查与编译抽屉（Drawer）</h5>
      <p>当用户手动点击工具栏的“编译（Compile）”按钮时，系统会触发全量编译检查：</p>
      <ul>
        <li><strong>全局状态更新</strong>：顶部动作条的状态转为 <code>Checking</code>（编译中）。</li>
        <li><strong>拉出错误抽屉</strong>：右侧拉出专门的编译错误抽屉（Compile Drawer）。该抽屉与属性配置面板采用并排或独立布局，不会覆盖当前的节点配置信息。</li>
        <li><strong>格式化列表</strong>：抽屉内详尽列出当前画布上的所有编译错误与警告，包含<code>文件/行号</code>、<code>错误字段</code>、<code>严重级别（Error/Warning）</code>以及<code>诊断描述</code>。</li>
      </ul>

      <div data-demo="demo-compile"></div>

      <h4>3. 用户工作流：异常排查与 AI 辅助修复</h4>
      <p>用户依据编译诊断机制，通过以下标准化步骤快速排除技能配置缺陷：</p>
      <ol>
        <li><strong>定位错误</strong>：通过画布节点上的红色警示图标或编译错误抽屉，快速锁定存在异常的节点和参数项。</li>
        <li><strong>获取诊断详情</strong>：在编译抽屉中，每条编译错误信息旁均提供一键拷贝按钮。用户可一键将包含节点 ID、字段路径、错误描述以及错误码在内的诊断文本拷贝至剪贴板。</li>
        <li><strong>协同修复</strong>：用户将拷贝 of 诊断详情直接粘贴至右侧的 AI 辅助工具（Copilot）输入框中。Copilot 会根据接收到的静态编译错误信息，分析上下文并给出具体的修改建议，如修复后的 JSON Schema 或正确的表达式模板。用户确认后即可快速应用修复。</li>
      </ol>

      <h4>4. 工程契约：逐级递进的分级门控</h4>
      <p>为了保障执行环境的稳定性，动作条上的控制按钮遵循严密的分级门控约束，具体表现为状态转换与交互行为的强绑定：</p>
      <ul>
        <li><strong>第一级门控（编译门锁）</strong>：工作流初始或编辑后，编译状态默认为未校验。只有在静态编译全量校验通过（即状态为 <code>Passed</code>，无阻塞性错误）之后，工具栏上的“试飞（Predict）”按钮才会解锁并呈现可点击状态。如果编译未通过或正处于编译中，试飞按钮将被强制置灰，无法触发任何执行请求。</li>
        <li><strong>第二级门控（试飞门锁）</strong>：试飞是工作流发布的必要验证步骤。只有在试飞成功通过（即试飞状态正常返回）之后，工具栏上的“运行（Run）”按钮才会解锁激活。在未完成试飞或试飞失败的情况下，运行按钮将被硬置灰，禁止用户直接上线运行未验证的技能。</li>
        <li><strong>异常退路保障</strong>：若网络异常或引擎编译校验服务响应失败，顶部动作条的状态会立刻切换为 <code>Failed</code>，并在按钮下方或提示区域渲染具体的通信错误信息，便于开发人员排查环境问题。</li>
      </ul>
    `
  },

  predict_mock: {
    tag: '第一卷 · Skill Studio 设计与打磨手册',
    title: '1.5 试飞：Predict 模拟运行与防污染隔离',
    text: `
      <h3>1. 产品概念</h3>
      <p>试飞（Predict）是指在不产生真实大语言模型 API 调用成本的前提下，对技能（Skill）的工作流逻辑、数据流转拓扑以及节点间输入输出契约进行预演的验证机制。当工作流编译成功并获得绿灯指示后，系统解锁试飞功能。试飞是正式运行（Run）的前置条件。它允许用户在零大模型 Token 成本的开销下，确认整个工作流从输入节点到输出节点的数据路由是否正确、Schema 约束是否满足，为后续的正式运行提供逻辑正确性保障。</p>

      <h3>2. 系统行为</h3>
      <p>启动试飞后，系统根据节点的类型及其配置状态，采用分流的执行行为：</p>
      <ul>
        <li><strong>确定性逻辑节点（Logic Node）</strong>：由于其完全由确定性的规则、脚本或数学运算组成，不依赖外部大语言模型服务，因此在试飞过程中系统对其进行真实运行。真实运行的结果会完整落盘，并作为真实输入传递给下游节点。</li>
        <li><strong>代理节点（Agent Node）</strong>：系统自动根据该代理节点是否绑定了黄金期望值（Golden Case）来决定其模拟输出的数据来源：
          <ul>
            <li><strong>存在黄金期望值</strong>：如果代理节点已配置了用户设定的黄金期望值，试飞引擎将直接提取该期望值并进行重放，将其作为该节点的输出。</li>
            <li><strong>不存在黄金期望值</strong>：如果代理节点没有黄金期望值，试飞引擎将读取该节点定义的输出 Schema，并采用启发式逻辑自动生成一份符合该 Schema 结构规范的占位模拟数据作为当前节点的输出。</li>
          </ul>
        </li>
      </ul>
      <p>通过此种分流机制，系统能够确保在无大模型调用的情况下，下游节点也能接收到符合格式规范的输入数据，使整个拓扑结构能够顺畅执行至终点。</p>

      <h3>3. 用户工作流</h3>
      <p>用户在画布中完成编译并获得绿灯后，即可进入试飞流程：</p>
      <ul>
        <li><strong>配置与导入输入</strong>：用户打开右侧的 I/O 配置面板，在测试输入区域，选择或直接导入预先编写好的本地测试 JSON 文件。该文件的数据会被解析并注入到系统的全局状态黑板中。</li>
        <li><strong>发起试飞</strong>：用户点击顶部工具栏的 <code>Predict</code> 按钮。系统将首先对输入的测试数据进行输入 Schema 校验。</li>
        <li><strong>运行状态观察</strong>：校验通过后，画布中的节点将根据执行顺序依次闪烁呼吸灯。逻辑节点输出真实计算结果，代理节点则输出模拟占位或黄金案例数据。</li>
      </ul>

      <div class="lesson-alert alert-amber"><b>【界面占位符】：此处在 Studio 界面中展示 I/O 配置面板。面板内包含“导入测试用例”按钮，点击后弹出本地文件选择器，支持导入 JSON 格式的测试文件。导入成功后，面板列表中会显示文件名与数据结构预览。顶部控制栏中的 Predict 按钮处于激活状态，点击后会在画布节点周围展示流式的执行呼吸灯动画，逻辑节点旁会实时显示真实计算结果的悬浮标签，代理节点旁则会展示标有“Mock 重放”或“Schema 自动生成”字样的输出预览卡片。</b></div>

      <h3>4. 工程契约</h3>
      <p>为保障测试数据的纯净性，防止开发者在试飞过程中产生的模拟、占位数据污染真实的生产黄金案例，系统在数据层执行强隔离的工程契约：</p>
      <ul>
        <li><strong>Trace 属性标记</strong>：试飞运行产生的每一次执行痕迹（Trace）都会在元数据中被标记为 <code>source: 'predict'</code>，与正式运行产生的 <code>source: 'run'</code> 进行物理区分。</li>
        <li><strong>隔离防污染拦截</strong>：系统关闭了从试飞 Trace 直接保存或提升为黄金期望值的通道。在 API 写入层，如果接收到将 Trace 固化为 Golden 的请求，后端服务会强制校验 Trace 的源属性。一旦发现源属性为 <code>predict</code>，系统会拒绝该请求并返回状态冲突错误，以确保黄金案例必须通过人工手动配置或通过正式运行（Run）的真实输出产生。</li>
        <li><strong>临时性沙箱存储</strong>：试飞产生的 Trace 数据不进入生产历史记录，仅在当前的试飞沙箱中进行临时存储，用户刷新页面或切换运行上下文后，该临时 Trace 即被释放。</li>
      </ul>

      <h3>5. 错误和边界</h3>
      <p>系统在试飞过程中遵循以下错误拦截与边界判定规则：</p>
      <ul>
        <li><strong>输入 Schema 校验未通过</strong>：当导入的测试数据与根节点或特定节点的输入 Schema 不匹配时（例如缺失必要字段或数据类型冲突），试飞流程在启动阶段即被终止，控制台抛出 Schema 校验异常，并高亮指示错误的字段位置。</li>
        <li><strong>Schema 变更致 Golden 失效</strong>：若代理节点配置了黄金期望值，但用户随后修改了该节点的输出 Schema，导致现有的黄金期望值数据缺失了 Schema 中新增的必填字段，系统会在编译阶段直接判定该黄金案例失效并触发编译错误。用户必须在 I/O 面板中补全缺失的字段或重新定义黄金案例，否则系统将限制该节点进入试飞流程。</li>
        <li><strong>逻辑节点真跑异常</strong>：当逻辑节点在真实执行过程中发生运行时错误（如脚本语法错误、计算溢出等），试飞会立即中断，故障节点在画布上亮起红灯，且详细的执行堆栈信息会直接渲染在 Trace 日志控制台中。</li>
      </ul>
    `
  },

  run_execution: {
    tag: '第一卷 · Skill Studio 设计与打磨手册',
    title: '1.6 真跑：Run 真实执行与事件状态灯',
    text: `
      <h3>1. 产品概念</h3>
      <p>在 Skill Studio 中，<strong>真跑（Run）</strong>是对已编译且通过验证的技能进行端到端生产级验证的核心阶段。其硬前提为：技能图必须通过编译校验，且必须成功执行过至少一次试飞（Predict）阶段的完整校验。</p>
      <p>与仅在 Agent 节点使用模拟（Mock）数据以验证逻辑连贯性的试飞阶段不同，真跑阶段会真实调用大语言模型（LLM）等外部服务。在此阶段，系统会把输入参数下发至引擎，产生真实的 Token 消耗与网络耗时，从而评估技能在真实环境下的执行稳定性、API 延迟以及成本消耗。</p>

      <h3>2. 系统行为</h3>
      <p>真跑任务由后端的运行引擎独立拉起并管理，其系统行为包含以下核心逻辑：</p>
      <ul>
        <li><strong>数据分发：</strong>系统支持单次运行和批量运行（Batch Run）。对于单次运行，引擎仅派发单组输入；对于批量运行，引擎从 I/O 面板读取多组不同的输入样例，并采用串行或并行排队的方式调度执行。</li>
        <li><strong>事件流驱动：</strong>引擎在执行 <code>run_skill</code> 命令时，会实时将每一个执行步骤的节点状态、中间变量变更以及流式输出，封装为结构化的事件流（Event Stream），通过长连接向 Studio 前端推送。</li>
        <li><strong>节点状态转换：</strong>前端接收事件流后，更新画布中各节点的执行状态（空闲、运行中、执行成功、执行失败）。各个节点根据接收到的状态事件，在画布上触发对应组件的视觉状态转换。</li>
      </ul>

      <h3>3. 用户工作流</h3>
      <p>用户在画布和控制面板中，通过以下直观的步骤与反馈回路完成真跑的验收：</p>
      <ul>
        <li><strong>触发真跑：</strong>用户在 I/O 面板中选择一组或多组测试样例，点击顶部工具栏的 <strong>Run</strong> 按钮。</li>
        <li><strong>执行监控与聚焦追踪：</strong>当运行启动时，系统会自动打开实时监控面板。画布视口随执行进度自动平移，将当前的视觉焦点聚焦在正在运行的节点上，使用户能够清晰掌握当前的执行位置。</li>
        <li><strong>画布视觉反馈：</strong>正在运行的节点其外边框会呈现淡蓝色的呼吸动画效果，两节点之间的数据连线则呈现向数据流动方向传输的粒子流动动画。运行成功的节点会亮起绿色的状态指示灯，运行失败的节点则会亮起红色的警告指示灯。</li>
        <li><strong>批量运行进度：</strong>在批量运行过程中，I/O 面板显示当前的总体进度条（包含已完成数、总数、以及各个样例的状态）。如果某一个样例在运行中报错，系统不会静默跳过，而是高亮标记该失败项，并在下方展示对应的错误堆栈信息。</li>
      </ul>

      <div data-demo="demo-run"></div>

      <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现运行历史列表与时间线面板，用户可以查看每次历史运行的唯一标识、最终执行状态、消耗的 Token 总量以及执行总耗时。点击任意一次历史记录，画布和面板将以只读只看模式还原该次运行的状态灯与数据流，并提供一键重跑（Replay）按钮重新提交相同的输入参数。</b></div>

      <h3>4. 工程契约</h3>
      <p>真跑成功后，系统与底层版本控制系统建立紧密的工程契约，引入<strong>自动提交（Autocommit）机制</strong>：</p>
      <ul>
        <li><strong>触发条件：</strong>当且仅当一次真跑任务完整结束且所有节点均执行成功（即状态为 <code>Success</code>）时，该机制被触发。</li>
        <li><strong>存档动作：</strong>Studio 后端服务会在底层本地 Git 仓库中自动执行一次 commit 操作。该提交会将当前时刻的画布拓扑图结构、节点参数配置以及该次真跑的输入输出 Schema 统一打包存档。</li>
        <li><strong>版本追踪：</strong>此机制保证了每一次经过真实调用验证的成功配置都会在 Git 历史上留下无损的快照记录，从而支持无缝的版本追溯与回滚，防止配置丢失。</li>
      </ul>

      <h3>5. 错误和边界</h3>
      <p>为了保证系统的稳定运行并防止非预期的 Token 开销，系统在边界和异常情况下执行以下防护性策略：</p>
      <ul>
        <li><strong>门控拦截：</strong>如果当前技能的编译状态为失败，或者之前未通过试飞流程，工具栏上的真跑按钮保持禁用状态，并悬浮提示用户先完成编译和试飞校验。</li>
        <li><strong>数据防冲突策略：</strong>试飞产生的追踪记录不能被用户作为验收的黄金样本直接捕获，黄金样本的创建必须通过明确的用户主动行为（如手动填写或基于输出 Schema 模板的生成确认）来产生，确保测试数据的权威性。</li>
        <li><strong>Payload 截断防护：</strong>如果节点输出的数据量或错误堆栈内容过于庞大，为防止前端浏览器渲染高频长文本时发生卡顿或崩溃，前端会对流式推送的 Payload 进行截断。用户若需要查看完整的数据细节，可以点击节点，通过只读编辑器基于运行标识异步按需懒加载完整的 Trace 记录。</li>
      </ul>
    `
  },

  trace_observability: {
    tag: '第一卷 · Skill Studio 设计与打磨手册',
    title: '1.7 去黑盒：Trace 瀑布流、线上 Dot 与人能读文档',
    text: `
      <h3>一、 产品概念：打开黑盒以追踪 Agent 决策路线</h3>
      <p>在大语言模型驱动的 Agent 复合系统中，非确定性的决策流转、黑板数据的改写以及复杂的工具调用往往处于黑盒状态。为了使整个执行过程变得清晰、可追踪且易于调试，Studio 引入了去黑盒化的可观测性设计，主要包含以下核心概念：</p>
      <ul>
        <li><strong>线上 Dot（转移节点）</strong>：连接线中间的 Dot 代表前一节点结束之后、后一节点开始之前的状态操作。它承载了黑板数据的聚合更新、输入数据注入、状态截断以及变量路由过滤等微观行为。</li>
        <li><strong>可折叠的人能读 Trace 文档</strong>：系统不直接倾倒冗长的原始 JSON 行数据，而是将执行轨迹实时转换并生成结构化的 Trace 文档。该文档以轻度格式化的文本形式承载于 Monaco 编辑器中，支持层级折叠与行内高亮，使复杂的逻辑流转具备高度的可读性。</li>
        <li><strong>模型与路由指标</strong>：多模型配置下的 Token 消耗、执行耗时以及 Prompt 模板渲染等细节，通过结构化指标面板与对比工具对外公开，便于进行横向评估与比对。</li>
      </ul>

      <h3>二、 系统行为：流式 Trace 的结构化聚合与 Monaco 虚拟 Trace 文件渲染</h3>
      <p>系统在运行中与运行后分别采取不同的事件聚合与渲染策略，确保在不同阶段均能提供最佳的信息密度：</p>
      <ul>
        <li><strong>流式 Trace 聚合与渲染</strong>：运行过程中，Studio 实时拉取底层执行引擎生成的微观事件流。对于 Agent 节点，系统对其交互过程进行分类折叠渲染。如推理步骤被折叠在 <code>Worked for</code>、<code>Explored</code>、<code>Thought</code> 等分类下，仅在末尾提供自然语言形式的最终输出。用户可展开任意分类查看底层的详细决策细节。
          <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现流式输出的 Trace 瀑布流，包含 Worked for / Explored / Thought 的折叠摘要面板。</b></div>
        </li>
        <li><strong>画布焦点联动</strong>：
          <ul>
            <li><strong>全局聚焦</strong>：当用户点击画布的空白处（未选中任何节点或连线）时，Trace 控制台与编辑器呈现整场运行的全局概要。</li>
            <li><strong>节点聚焦</strong>：当用户选中画布上的某一节点时，Trace 面板自动过滤并仅展示该节点的生命周期（从 Start 到 End）区间，编辑器光标同步跳转到该节点对应的只读 Trace 文档段落。</li>
            <li><strong>状态机转移点聚焦</strong>：当用户选中连接线中点的 Dot 时，系统展示当前转移点关联的黑板数据切片与数据分发过滤规则。</li>
          </ul>
        </li>
        <li><strong>虚拟 Trace 文件生成</strong>：运行结束后，系统将散乱的事件日志格式化为一份虚拟 Trace 文件。该文件由前端的虚拟文件系统承载，通过只读的 Monaco 编辑器进行渲染，保证用户能以熟悉的文档阅读体验来翻阅复杂的执行历史。
          <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现由只读 Monaco 编辑器承载的虚拟 Trace 文档，支持随着画布聚焦的变化自动滚动到对应代码行。</b></div>
        </li>
      </ul>

      <h3>三、 用户工作流：中间状态回溯与模型指标比对</h3>
      <p>开发人员通过以下工作流，实现对 Agent 运行过程的无缝监督与调试：</p>
      <ul>
        <li><strong>点击线上 Dot 查阅黑板数据 JSON 切片</strong>：用户点击两节点连接线上的 Dot 按钮，系统在属性面板中展示该时刻黑板状态机的完整 JSON 数据快照。用户可以直接检查前一节点的输出是如何通过合并规则并入全局状态的，以及多路分发时的数据过滤是否符合预期。
          <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现点击线上 Dot 后弹出的黑板数据 JSON 树形浏览器，支持字段级的折叠与检索。</b></div>
        </li>
        <li><strong>多模型与多路由 Token 对比</strong>：当同一个节点由于路由分支或重试机制调用了不同的模型配置时，用户可以通过顶部的模型对比 Tab，切换并排查看不同大语言模型配置在相同输入下的运行表现。面板提供各个模型的 Token 消耗、耗时分布以及 Prompt 差异对比。
          <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现顶部的模型对比 Tab，支持横向分栏对比不同模型在 Token 消耗和 Prompt 表现上的差异。</b></div>
        </li>
        <li><strong>Prompt 深度透视</strong>：在 Trace 瀑布流中点击 LLM 调用事件（<code>llm_call</code>），将弹出 Prompt 深度透视面板。该面板提供三视图展示：模板原文、传入的变量参数、以及经过插值渲染后的最终 Prompt，辅助开发人员快速定位模型由于 Prompt 格式或变量错误导致的异常响应。</li>
      </ul>

      <h3>四、 工程契约：只读投影与数据量级安全</h3>
      <p>为了保障 Studio 的轻量化和高性能，前端与引擎侧建立了清晰的工程契约：</p>
      <ul>
        <li><strong>只读投影架构</strong>：Studio 不负责 Trace 数据的物理计算与永久存储，所有执行事件和状态快照均物理落盘于 Engine（引擎侧）。Studio 仅通过特定的运行标识符（<code>run_id</code>）向后端发起查询或订阅流式事件，在前端仅做数据的只读投影渲染。</li>
        <li><strong>数据截断协议</strong>：为防止大文本与超长 JSON 在传输与渲染时造成前端内存溢出（OOM），引擎与 Studio 之间约定了 Payload 截断规则。当单次事件输出体积超过指定安全值时，数据在后端进行自动截断，前端以占位符的形式提醒用户该节点数据已进行有损压缩。</li>
      </ul>

      <h3>五、 错误和边界：异常传播与缓存清理</h3>
      <p>在面对执行失败和状态切换时，系统遵循以下边界逻辑进行处理：</p>
      <ul>
        <li><strong>执行异常与验证器重试捕获</strong>：当节点运行抛出错误，或验证器（Validator）拦截到非法输出时，对应节点在画布上亮起红灯，Trace 瀑布流在此处中断。系统在故障节点的 Trace 细节中直观显示重试徽章（例如 <code>2/3</code> 次重试）以及完整的异常调用栈（Error Stack），无需开发人员查阅服务器后台日志。</li>
        <li><strong>运行切换时的缓存重置</strong>：当用户在运行历史列表中切换到另一条 <code>run_id</code> 历史记录时，系统立即中断当前的流式网络连接，丢弃前一次运行的所有临时缓存，并在 Monaco 编辑器中重新载入新运行对应的 Trace 虚拟文档，杜绝多批次历史数据在界面上发生重叠或交叉污染。</li>
        <li><strong>测试保护守卫</strong>：从运行痕迹或调试过程中捕获的临时状态数据不被允许直接晋升为黄金测试集（Golden Case），黄金测试集的创建必须由开发人员根据预设的输出 schema 进行显式确认或手动编辑，从而避免污染验收标准。</li>
      </ul>
    `
  },

  debug_resume: {
    tag: '第一卷 · Skill Studio 设计与打磨手册',
    title: '1.8 调试：节点 Resume 续跑与 HitL 人工注入',
    text: `
      <p>在复杂的多节点 Agent 工作流中，局部调试与状态恢复是保证研发效率的核心能力。本章阐述了在运行失败或挂起时，如何通过节点级 Checkpoint 进行局部续跑、如何在人工介入点（Human-in-the-Loop）注入交互数据，以及如何通过篡改上下文状态实现调试的系统设计与人机工作流。</p>

      <h3>1.8.1 产品概念：节点级局部重算</h3>
      <p>在长链条、高开销的 Agent 工作流中，如果某一步骤因为网络波动、模型响应格式异常、下游依赖超时或人类打断（Human-in-the-Loop）而失败或挂起，系统支持对工作流进行<b>节点级局部重算</b>。用户可以在修改故障节点的 Prompt 或内部脚本后，从受损节点就地启动续跑，而无需从头运行整个工作流。该机制能够完整复用故障节点上游的所有执行结果和历史 Trace 数据，避免了重复调用高昂的语言模型 API，从而大幅降低了调试阶段的 Token 开销与等待时间。</p>

      <h3>1.8.2 系统行为：基于检查点重塑状态</h3>
      <p>当用户触发续跑请求时，计算引擎会通过以下机制重塑内存状态并驱动工作流：</p>
      <ul>
        <li><b>检查点（Checkpoint）定位</b>：引擎基于 <code>checkpoint_id</code> 或 <code>checkpoint_ns</code>（检查点命名空间）精确定位历史状态。对于普通的单层图结构、嵌套子图（Subgraph）以及图级循环迭代（Iterate），引擎均使用统一的 Thread Checkpointer 基座，通过嵌套命名空间（例如 <code>G/iter{k}/subgraph_node</code>）来实现状态的全局寻址与加载。</li>
        <li><b>两层状态分治</b>：
          <ul>
            <li><b>外层黑板（Blackboard）状态</b>：由外层图管理，主要保存工作流节点之间流转的结构化数据（<code>WorkflowState.data</code>），续跑时系统从断点处的黑板数据镜像直接恢复上下文。</li>
            <li><b>内层消息（Messages）状态</b>：由 Agent 节点内部的多轮 LLM-Tool 交互图管理，利用 <code>NamespaceCheckpointer</code> 分层归档。在遇到中断时，引擎暂停在该命名空间的 super-step。</li>
          </ul>
        </li>
        <li><b>增量与有界管理</b>：在多次循环或大规模执行场景下，黑板数据通道会启用 Delta Reducer 机制仅保存增量差异，并周期性生成状态快照，以防止本地存储体积呈平方级爆涨；同时，通过有界累加器（Accumulator）对长上下文进行滚动摘要，避免续跑时大模型上下文窗口溢出。</li>
      </ul>

      <h3>1.8.3 用户工作流：局部续跑、HitL 注入与状态篡改</h3>
      <p>在 Studio 画布中，用户主要通过以下三个核心调试流程与系统交互：</p>

      <h4>1. 失败节点的就地 Resume 续跑</h4>
      <p>当工作流运行失败时，故障节点会在画布中呈现红色警示状态，运行时间轴（Timeline）停滞在该节点。用户可在右侧配置面板中直接修改该节点的 Prompt 模板或 Python 脚本，保存后点击该节点控制悬浮栏上的 <code>[Resume]</code> 按钮。系统将自动提取上游节点的最后一个有效检查点，重塑工作流上下文并仅让故障节点及其下游节点重新投入计算，上游节点则继续保持成功的绿色状态而不被重复调用。</p>

      <h4>2. HitL（Human-in-the-Loop）人工介入与数据注入</h4>
      <p>当工作流中的 Agent 节点调用了请求人类回答的原语（例如 <code>ask_clarification</code> 或 <code>interrupt()</code>）时，整个工作流会自动转换为挂起状态。</p>
      <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现悬浮在挂起节点上方的富文本输入框（而非固定在顶栏或侧边栏），输入框展示当前 Agent 挂起的原因与上下文提问。用户在此输入框中填写回复内容，并点击“发送并续跑”按钮。</b></div>
      <p>用户提交文本后，Studio 将回复内容封装为 <code>ToolMessage</code>，通过 <code>POST /api/skills/{skill_id}/runs/{run_id}/resume</code> 接口投递给引擎。引擎将此消息注入到当前正在挂起的 Pending Tool Call 中，唤醒并恢复 Agent 的后续推理流程。</p>

      <h4>3. 点边 Dot 篡改黑板 JSON 状态续跑</h4>
      <p>为了验证不同边界数据下工作流的健壮性，用户可以在 Trace 状态下手动修改节点间流转的黑板数据。</p>
      <div class="lesson-alert alert-amber"><b>【界面占位符】：用户点击两个节点之间连线上的连接点（Dot），界面在右侧弹出状态详情抽屉。系统在此抽屉中复用只读的 Monaco 编辑器并自动将其切为可写模式，将该位置的 Blackboard JSON 结构化数据完整渲染出来。</b></div>
      <p>用户在编辑器中直接篡改 JSON 字段（例如模拟下游系统返回的特定异常参数），点击“保存覆盖”后，点击下游节点的 <code>[Resume]</code> 按钮.引擎会加载该被篡改后的上下文数据，直接开始执行下游节点的计算。</p>

      <h3>1.8.4 工程契约：变更轴判定与下游置灰失效机制</h3>
      <p>当用户在画布中修改了工作流定义后，系统通过变更轴与置脏契约防止因使用失效检查点续跑而导致运行崩溃：</p>
      <ul>
        <li><b>变更轴分类（ChangeAxis）</b>：引擎实现了一个差分检测器 <code>diff_skill(old, new)</code>，在用户修改 Skill 定义并保存时，自动将变更归入以下分类：
          <ul>
            <li><code>A1</code>：修改了 Prompt 文本、关联的语言模型、参数或绑定的工具列表。</li>
            <li><code>A2</code>：输出契约变更（包括 <code>A2a</code>：必填输出字段增删；<code>A2b</code>：数据类型修改；<code>A2c</code>：非必填输出字段增删）。</li>
            <li><code>A3</code>：本节点输入契约（<code>io.inputs</code>）变更。</li>
            <li><code>A4</code>：本节点依赖的上游节点输出契约发生变化。</li>
            <li><code>A5</code>：工作流拓扑结构变更（例如删除连接线、调换节点顺序或新增节点）。</li>
          </ul>
        </li>
        <li><b>下游 Checkpoint 失效契约</b>：Checkpoint 失效服务订阅了 <code>A2</code>、<code>A4</code> 和 <code>A5</code> 三类变更轴。一旦检测到这三类涉及数据结构与拓扑的变更，系统将判定该节点的所有下游既有检查点均已置脏（Stale）。</li>
        <li><b>界面表现契约</b>：当检测到下游检查点失效时，Studio 画布对应下游节点上的 <code>[Resume]</code> 按钮会被软置灰，并且鼠标悬停时会显示提示信息（例如：“上游契约已变更，此检查点已失效，请重新从上游运行”）。由于 <code>A1</code> 轴（仅修改 Prompt 或模型参数）不属于失效订阅范围，因此仅微调 Prompt 不会导致下游节点被置灰，以此保证非破坏性改动时的顺跑体验。</li>
      </ul>

      <h3>1.8.5 错误与边界条件</h3>
      <p>在运行调试续跑时，系统对以下边界情况进行了严格的安全规约与异常处理：</p>
      <ul>
        <li><b>长挂起任务中的版本漂移</b>：如果工作流因等待人工输入（HitL）而长时间挂起，期间用户发布了不兼容的 Skill 新版本，当用户最终提交人工回复尝试续跑时，引擎会比对检查点存储的编译指纹与当前内存中的编译指纹。如果二者不一致，系统拒绝继续运行，并在 Studio 的错误抽屉中抛出 <code>Compile Fingerprint Mismatch</code> 异常，提示用户必须重跑工作流。</li>
        <li><b>持久化状态的边界约束</b>：在每次将执行上下文写入 Checkpoint 时，引擎中的 Middleware、运行时回调函数以及已编译的 Graph 对象本身均不得被序列化。检查点仅允许存储纯 JSON 安全的数据和 Message 结构。如果检测到包含运行时活跃对象（例如未关闭的文件句柄、网络套接字或未编译的代码块对象），系统将触发 Fail-Fast 报错，拒绝写入检查点，以保障数据不被污染。</li>
        <li><b>多 Pending 冲突校验</b>：在并发或多 Agent 嵌套的复杂工作流中，如果当前检查点内包含多个处于 Pending 状态的打断请求，Studio 发起的 <code>/resume</code> 请求必须显式传入对应的 <code>tool_call_id</code>；只有当挂起的打断请求唯一时，才可以缺省该标识，由引擎的路由算法完成自解析，否则接口返回 <code>400 Bad Request</code>。</li>
      </ul>
    `
  },

  golden_eval: {
    tag: '第一卷 · Skill Studio 设计与打磨手册',
    title: '1.9 打磨：基于 Golden 的比对评估',
    text: `
      <h3>1. 产品概念</h3>
      <p>在技能（Skill）的打磨与调试过程中，开发者需要客观评估节点的输出质量是否随提示词（Prompt）或模型配置的调整而发生改善或退化。为了提供精准的评估依据，Skill Studio 引入了“理想样本”（Golden）机制。</p>
      <p>理想样本是指针对特定智能体节点（Agent Node）的输入，开发者期望该节点输出的理想 JSON 数据。系统不采用整次运行的完整状态快照作为全局比对基准，而是以智能体节点为粒度进行精细化管理。逻辑节点（Logic Node）因其执行结果具有确定性，不参与理想样本的比对与评估。</p>

      <h3>2. 系统行为</h3>
      <h4>智能体节点的三态心智</h4>
      <p>系统中的智能体节点在画布与配置面板中具有明确的三种状态，并在开发过程中进行流转：</p>
      <ul>
        <li><strong>未测试（Untested）</strong>：节点自创建后从未在预估（Predict）或实际运行（Run）中被执行过，未绑定任何理想样本。</li>
        <li><strong>逻辑 OK（Logic OK）</strong>：节点已通过至少一次预估或实际运行，业务逻辑连通，但目前尚未配置理想样本。此时画布上的节点会展示“逻辑 OK”的浅蓝色状态标签。</li>
        <li><strong>有 Golden（Has Golden）</strong>：节点已成功绑定有效的理想样本。画布上的节点会展示绿色的“有 Golden”状态标签，表明该节点已建立评估基准。</li>
      </ul>

      <h4>自动模拟选择</h4>
      <p>在预估（Predict）模式下，系统根据节点的状态自动选择模拟数据的来源：</p>
      <ul>
        <li>当节点处于“未测试”或“逻辑 OK”状态（无理想样本）时，预估引擎自动根据节点的输出架构（Output Schema）生成符合结构定义的占位模拟数据（Placeholder Mock）。</li>
        <li>当节点处于“有 Golden”状态（有理想样本）时，预估引擎直接回放（Replay）该理想样本作为节点的模拟输出，跳过实际的语言模型接口调用。</li>
      </ul>

      <h4>编辑器差异比对</h4>
      <p>在实际运行（Run）结束后，系统将智能体节点的实际输出与理想样本进行字段级的逐项比对，并在分屏编辑器中以高亮形式呈现差异。</p>
      <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现 Monaco Diff Editor 双栏对比视图。左侧展示当前智能体节点的 Golden 理想样本 JSON 文件，右侧展示当前运行（Run）的实际输出 JSON 数据。差异点包括：
      1. 实际输出中新增的字段会以绿色背景高亮显示，并标注 'Extra' 标签；
      2. 实际输出中缺失的 Golden 字段会以红色背景高亮显示，并标注 'Missing' 标签；
      3. 值不一致的字段会以黄色背景高亮显示，并标注 'Changed' 标签，方便用户逐项调试。</b></div>

      <h3>3. 用户工作流</h3>
      <h4>提升运行输出为理想样本</h4>
      <p>当用户完成一次实际运行（Run）并对某个智能体节点的真实输出感到满意时，可以在追踪面板中点击“提升为 Golden”按钮，将该次实际输出一键转化为该节点的理想样本文件。</p>
      <p>为了保证评估基准的真实性，系统严格拦截将预估（Predict）阶段生成的模拟数据直接提升为理想样本的操作。当用户尝试提升预估数据时，系统抛出诊断错误，阻止该行为，并在前端弹出提示告知用户模拟数据不可作为评估基准。</p>

      <h4>创建与编辑理想样本</h4>
      <p>开发者可以通过以下三种工作流创建理想样本：</p>
      <ul>
        <li><strong>基于架构生成模板</strong>：在输入输出（I/O）配置面板中点击生成按钮，系统会根据该节点的输出架构自动构建一份包含所有必填字段的空 JSON 模板，供开发者在编辑器中手工填写。</li>
        <li><strong>单节点 Copilot 辅助设计</strong>：在追踪调试区域，开发者可以激活 Copilot 助手。Copilot 会自动读取技能拓扑图（<code>GRAPH.md</code>）和技能定义文件（<code>SKILL.md</code>），分析该节点在工作流中的角色与职能，自动为开发者设计并填充高水平的理想样本。</li>
        <li><strong>Copilot 批量分析建议</strong>：预估或运行完成后，系统支持批量评估入口。</li>
      </ul>
      <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在 Copilot 面板上方呈现“Golden 批量分析状态栏”。当运行或预估结束后，若检测到流程中存在尚未配置 Golden 样本的智能体节点，该状态栏会自动展开，列出所有无 Golden 的节点，并提供“一键生成 Golden 建议”的交互按钮。</b></div>

      <h3>4. 工程契约</h3>
      <h4>物理存储布局</h4>
      <p>所有的理想样本文件均不写入技能（Skill）的源码目录树中。系统将理想样本统一存储在工作空间目录下的 <code>.workspace/golden/</code> 隐藏文件夹中。理想样本文件与智能体节点之间的绑定关系由执行引擎统一分配的节点物理标识符（<code>phase_id</code>）进行映射，这确保了即使节点在画布中被移动或被重命名，关联的理想样本文件也不会发生丢失或错配。</p>

      <h4>失效判定与边界</h4>
      <p>理想样本文件的生命周期与智能体节点的输出架构（Output Schema）保持强绑定，而与 Prompt、节点内部的具体代码实现无关：</p>
      <ul>
        <li><strong>非失效边界</strong>：当开发者仅修改了智能体节点的提示词（Prompt）、更改了模型参数，或者重构了节点内部的数据转换逻辑，只要输出架构（Output Schema）的结构未发生改变，理想样本依然保持有效。</li>
        <li><strong>失效边界</strong>：一旦开发者修改了输出架构的定义（如增加了一个必填字段，或者更改了已有字段的类型），系统在执行编译诊断或运行后比对时，若发现现有的理想样本缺少必填字段或类型不符，将立即判定该理想样本失效。系统会在诊断面板中抛出编译诊断错误并阻止预估或运行，直至开发者更新或重新生成理想样本以对齐新的输出架构。</li>
      </ul>
      <div data-demo="demo-invalid"></div>
    `
  },

  publish_release: {
    tag: '第一卷 · Skill Studio 设计与打磨手册',
    title: '1.10 发布：Artifact Release 与版本提交',
    text: `
      <p>在工作流的设计与校验工作完成后，用户可以通过发布流水线将当前的本地源码编译并打包输出为生产环境可运行的只读成品。需要明确，<b>发布操作与日常的本地代码提交具有本质区别</b>，系统在产品架构上实现了源码 Git 控制与编译发布包的双命名空间物理隔离与两阶段提交。</p>
      
      <h3>一、产品概念：设计资产固化与保存发布的分离</h3>
      <p>系统将用户的开发创作态与生产执行态进行彻底解耦，并针对两类不同性质的数据实施分治策略：</p>
      <ul>
        <li><b>设计资产源码（可变状态）</b>：用户在画布上编辑的图形结构与配置文件均属于设计资产。系统使用本地的 Git 版本管理进行轨迹追踪。每次用户执行成功运行（Run）时，系统便会自动在本地发起一次快照提交（Autocommit），以此保障日常设计轨迹的安全保存，这并不对外部发布任何可用成品。</li>
        <li><b>发布成品包（不可变状态）</b>：用于生产环境实际运行的成品包由编译系统生成。其本质是内容寻址的只读构建文件包。一旦编译完成，其内容和哈希指纹便完全固化。生产环境在执行时仅读取成品包，完全不依赖、也不接触任何开发期的设计源码。</li>
      </ul>

      <h3>二、系统行为：双命名空间隔离机制</h3>
      <p>为防止设计期的临时代码对生产执行环境造成污染，系统在底层构建了两个完全隔离的版本命名空间：</p>
      <ul>
        <li><b>设计与源码命名空间</b>：由本地 Git 版本库承载。自动提交产生的哈希版本仅在开发工作区内部可见，用于支持画布的撤销、历史追溯与协同开发，完全屏蔽在生产端之外。</li>
        <li><b>发布产物命名空间</b>：由发布成品库（Artifact Registry）统一管理。发布成品打包成 ZIP 格式，通过内容哈希算法（如 SHA-256）计算出全球唯一的物理包指纹（<code>artifact_ref</code>），并附带语义化版本号进行登记。成品库仅接纳通过发布流水线的正式包，开发调试期间的任何临时编译包均被物理隔离，绝不写入成品库。</li>
      </ul>

      <h3>三、用户工作流：发布流水线与两阶段提交</h3>
      <p>由于发布的物理入口被统一集成于工作台顶栏的下拉功能项中，为了保证发布交互的稳定性与透明度，其发布阶段表现为如下逻辑与界面行为：</p>
      
      <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现发布确认面板。当用户点击顶栏“Release”发布选项时，Studio 将弹出侧边抽屉，列出当前编译包的临时哈希值、目标版本号输入框以及依赖项校验结果，用户在填写版本号并点击确认后方正式启动提交流水线。</b></div>

      <p>后台发布流水线严格按照<b>两阶段提交</b>原则执行，确保发布过程的原子性：</p>
      <ol>
        <li><b>第一阶段：暂存验证（Stage）</b>：
          <ul>
            <li>系统对当前源码发起全量编译，生成不可变的二进制打包文件，并计算其唯一的哈希指纹。</li>
            <li>系统同步发起前置条件校验，检查必要的发布凭证（如 <code>user_id</code>、注册表主机地址 <code>registry host</code>、鉴权令牌 <code>token</code> 等）。如果发现任何一项凭证缺失，系统将中止流程，向用户抛出明确的错误诊断，并指引用户前往系统设置页面补全配置。</li>
            <li>当前置校验通过后，系统调用成品库的暂存接口（<code>stage_release</code>），将带有幂等键（<code>idempotency_key</code>）的包写入暂存区，此时该版本对生产端仍不可见。</li>
          </ul>
        </li>
        <li><b>第二阶段：确认提交（Commit）</b>：
          <ul>
            <li>暂存成功后，系统执行确认提交指令（<code>commit_release</code>），将该包在成品库中的状态标记为正式生效，并将该版本号向外界暴露，供生产运行端按版本调取执行。</li>
          </ul>
        </li>
      </ol>

      <h3>四、错误和边界：发布失败的物理补偿机制</h3>
      <p>在网络抖动或远端存储发生异常的场景下，两阶段发布流水线通过提供物理补偿手段，保障成品库的强一致性：</p>
      <ul>
        <li><b>版本冲突保护</b>：在发布流程启动时，系统调用 <code>has_release</code> 进行版本冲突检测。如果发现成品库中已存在同名版本，系统将直接抛出 <code>PublishReleaseConflict</code> 异常并退出流程，防止历史成品被意外覆盖。</li>
        <li><b>两阶段失败回滚</b>：若在第二阶段执行 <code>commit_release</code> 时因各种意外引发错误，系统会捕获该异常并立即触发物理回滚操作（<code>rollback_release</code>），从成品库暂存区中彻底移除该半成品，接着抛出 <code>PublishPartialFailure</code> 异常以便前端呈现报错。</li>
        <li><b>垃圾回收补偿</b>：对于因不可抗力（如进程被强行杀死）导致未完成回滚的孤立暂存包，成品库的自动清理程序将通过定时任务对未提交的临时构建文件进行物理垃圾回收（GC），杜绝无效的临时文件在磁盘或云端对象存储中堆积。</li>
      </ul>
    `
  },

  arch_boundary: {
    tag: '第二卷 · GraphAgent Harness 架构与机制手册',
    title: '2.1 系统架构与三模块物理边界',
    text: `
      <p>在系统的全局设计中，核心执行引擎（Engine）、智能网关（Gateway）与可视化工作台（Studio）之间的物理调用边界和协同模式是保障系统灵活性、高可用性和可移植性的基石。本节将详细阐述其系统架构的物理拓扑、SDK 模块化规范、Adapter 抽象层以及在不同生产环境下的部署形态与责任分工。</p>

      <h3>1. 架构拓扑与物理依赖</h3>
      <p>系统由三个核心模块构成，各模块各司其职，并通过明确的物理边界进行解耦：</p>
      <ul>
        <li><strong>Studio（可视化工作台）</strong>：处于应用层和控制顶层。它管理本地源码，并为用户提供可视化的画布编辑、编译诊断、运行测试和评估基线判定等产品级功能。Studio 不直接参与图结构的底层状态执行，而是通过服务调用来调度底层的 Engine 和 Gateway。</li>
        <li><strong>Engine（核心执行引擎）</strong>：处于图执行层。它负责将规范化的图结构编译为不可变的运行期成品，并在特定的输入下驱动状态机进行节点推导与数据流转。Engine 自身是无状态且位置无关的，它通过外部注入的接口与网关和存储进行交互。</li>
        <li><strong>Gateway（智能网关）</strong>：处于基础设施和路由层。它维护了注册表、模型路由规则、凭证映射以及故障降级决策逻辑。当 Engine 执行到具体的模型节点时，会向 Gateway 发起查询，以获得执行期所需的模型物理地址、解析协议和鉴权密钥。</li>
      </ul>
      <p>这三个模块之间的依赖关系是单向的。Studio 依赖 Engine 和 Gateway，Engine 依赖 Gateway 提供的模型路由与解析服务，而 Gateway 不依赖 Engine 和 Studio 的任何内部逻辑。这种单向依赖保证了每个模块均可独立进行测试和升级。</p>

      <h3>2. SDK 模块化设计</h3>
      <p>为了使核心逻辑不与特定的网络协议或 Web 框架强绑定，系统将核心的 Engine 与 Gateway 声明为<strong>不含 API 接口的纯粹 SDK 包</strong>。也就是说，Engine SDK 与 Gateway SDK 本身仅提供底层计算和状态转换的本地类与函数，并不启动任何 HTTP、gRPC 或 WebSocket 服务。</p>
      <p>这一设计规范带来了以下两点技术优势：</p>
      <ul>
        <li><strong>高度内聚的本地能力</strong>：Engine SDK 只专注于如何快速、稳定地编译和执行图结构，其核心模块可以无缝集成到任何支持 Python 的本地环境或服务器环境中，而无须引入复杂的网络协议栈。</li>
        <li><strong>自由定义的通信载体</strong>：当需要将 SDK 暴露给外部网络时，无须修改 SDK 内部的任何代码，仅需在外层包裹一层适配器即可。这避免了因为网络协议变更或服务框架升级而导致核心状态机逻辑受到污染的问题。</li>
      </ul>

      <h3>3. Adapter 抽象层与部署模式切换</h3>
      <p>为了衔接纯 SDK 包与外部的网络调用，系统引入了 <code>EngineAdapter</code> 与 <code>GatewayAdapter</code> 抽象层。Adapter 负责实现具体的通信协议（如 RESTful API），处理序列化与反序列化，并将传入的网络请求映射为对 SDK 本地 API 的函数调用。</p>
      <p>基于 Adapter 的抽象，系统支持两种物理部署形态的无缝切换，仅需修改配置中的连接端点即可：</p>
      
      <h4>3.1 本地同进程模式</h4>
      <p>在本地单机开发和调试环境下，系统采用同进程部署。此时，Studio 的本地后端服务直接在本地导入 Engine SDK 和 Gateway SDK，调用其 <code>EngineAdapter</code> 和 <code>GatewayAdapter</code> 的本地实现。数据在同一个进程的内存中流动，不产生任何网络传输开销，能极大提升本地调试和高频测试的响应速度。</p>
      
      <h4>3.2 环回网络模式（远程运行态）</h4>
      <p>在团队协作或生产运行环境下，Engine SDK、Gateway SDK 及其对应的 Adapter 运行在远程服务器或独立的容器中。Studio 本地工作台通过环回网络调用（例如网络请求或反向代理端口）与远程的 Adapter 进行通信。在这种部署模式下：</p>
      <ul>
        <li>运行所消耗的计算资源和内存全部由远程服务器承担，Studio 本地仅负责轻量级的前端界面渲染与配置修改。</li>
        <li>敏感的访问凭证由部署在服务器端的 <code>GatewayAdapter</code> 直接读取其存储的凭证库，本地工作台只能获取脱敏后的配置信息，提高了系统的整体安全性。</li>
      </ul>

      <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现远程连接状态指示器。当与远端 EngineAdapter / GatewayAdapter 连接中断时，指示器显示为红色并伴有闪烁警示，点击可查看连接诊断报告并尝试重新连接。</b></div>

      <h3>4. 原材料生产与产品消费分工</h3>
      <p>为了简化生产环境下的无界面运行器（Headless Runner），系统在 Engine 和 Studio 之间建立了清晰的“原材料与消费”契约，杜绝了渲染逻辑与执行逻辑的混杂：</p>
      <ul>
        <li><strong>Engine 只产出基础数据信封</strong>：在编译时，Engine 只输出不含展现逻辑的图结构数据以及编译诊断的行号与错误级别；在执行时，Engine 仅根据图逻辑生成纯粹的事件轨迹数据、运行状态快照和最终的运行结果。它不包含任何关于“这幅图在画布上的排版”、“错误信息在哪个抽屉里显示”或者“运行结果是否达到了某种业务合格标准”的逻辑。</li>
        <li><strong>Studio 负责产品特定消费</strong>：所有与用户界面展现、图的交互式编辑、编译错误的红线高亮提示，以及基于基线数据对比的判定逻辑（如黄金评估测试 of 差异分析），全部都放在 Studio 模块中进行实现。</li>
      </ul>
      <p>通过这种分工，生产环境中的 Headless Runner 只需要加载纯净的 Engine SDK，即可高效运行已编译好的 Skill 包并输出结果。它不需要加载任何与 Studio 相关的 UI 库、渲染引擎或业务判定规则，从而实现了运行效率的最大化和执行环境的极简配置。</p>

      <h3>5. 错误和边界处理</h3>
      <p>在物理边界的协同中，系统设计了严密的异常与安全边界：</p>
      <ul>
        <li><strong>通信中断处理</strong>：若 <code>EngineAdapter</code> 在运行中途与 Studio 断开连接，Studio 会根据最近一次接收到的运行状态快照和断点数据进行状态恢复，并向用户报告断开期间的异常，保护用户的历史执行记录。</li>
        <li><strong>路由与凭证隔离</strong>：所有敏感凭证仅在 <code>GatewayAdapter</code> 内部的凭证解析器中进行实时解密并直接注入到具体的模型调用上下文中，绝不通过网络以明文形式向 Studio 前端或 Engine 传递，确保了生产环境下鉴权信息的物理隔离。</li>
      </ul>
    `
  },

  storage_partition: {
    tag: '第二卷 · GraphAgent Harness 架构与机制手册',
    title: '2.2 存储三线分治机制与成品库',
    text: `
      <p>在系统架构中，数据根据其生命周期、读写模式和一致性要求，在物理上被拆分为三条职责单一的逻辑存储线以及一个专用的成品库。这种物理分治设计能够有效防范状态分叉、数据损坏与高并发冲突。</p>

      <h3>一、物理分线架构</h3>
      <p>系统针对不同性质的数据，采用了三种物理分离的存储线：</p>
      <ul>
        <li>
          <strong>配置真相线 (ConfigTruthStore)</strong>：
          保存凭证信息、角色路由表、全局设置与草稿配置。这类数据体积小，但一致性要求极高。为防止状态分叉，该存储线完全剔除任何形式的缓存，所有读取操作均直接请求底层的物理介质（在本地为 SQLite 数据库或 JSON 文件，在远程为数据库）。
        </li>
        <li>
          <strong>运行产物线 (RunArtifactStore)</strong>：
          保存技能执行所产生的运行历史（Runs）、对比基准数据、执行轨迹（Trace）以及产物文件。这类数据体积庞大且写完不改。该存储线使用内容寻址机制，即数据键值完全由其内容哈希决定。由于内容不可变，系统在本地建立了只读的暂存区作为缓存副本。
        </li>
        <li>
          <strong>运行态线 (RuntimeStateStore)</strong>：
          保存技能在执行过程中的活状态，例如中间断点快照（Checkpoint）和会话状态。这类数据具有高频读写、单实例独占的特点，仅在执行期存在，不作为配置信息的备份。
        </li>
      </ul>

      <h3>二、成品库机制</h3>
      <p>成品库是只读的发布产物存储仓库，用来存放已通过编译并准备用于生产环境的不可变编译包：</p>
      <ul>
        <li>
          <strong>唯一代码真相</strong>：
          在运行端引擎执行技能时，其唯一的代码真相源只能是成品库中的编译包。运行端引擎不支持读取或执行处于编辑状态的本地源码，这保证了开发环境的编辑行为不会干扰生产环境的稳定运行。
        </li>
        <li>
          <strong>独立命名空间</strong>：
          版本管理分为两条独立的线：开发期间的源码版本由 Git 进行跟踪和协同；而已发布的编译包版本由成品库分配的版本号进行独立管理。两者逻辑相互隔离，消成了多环境协同下的版本解析冲突。
        </li>
      </ul>

      <h3>三、并发写入保护</h3>
      <p>在本地开发态下，系统架构包含基于 Rust 构建的原生桌面进程与负责执行具体算法逻辑的 Python 边车进程。为防范并发修改同一配置文件导致文件损坏，系统实施了严格的写入保护：</p>
      <ul>
        <li>
          <strong>锁控制收口</strong>：
          所有针对配置真相线的物理文件写入行为，统一收口于 Rust 进程，并采用文件排他锁进行物理保护。
        </li>
        <li>
          <strong>读写插座隔离</strong>：
          Python 边车进程仅被授予对只读配置信息的快速读取权限。当 Python 边车需要更新某些配置或生成临时草稿时，必须将写请求传递给持有文件锁的 Rust 进程，或在受锁保护的局部隔离目录中进行操作，严禁直接对全局配置文件执行未加锁的写操作。
        </li>
      </ul>

      <h3>四、用户工作流</h3>
      <p>在 Studio 中，用户的开发、测试与发布流程按照如下数据流向运转：</p>
      <ol>
        <li>
          <strong>配置更新流</strong>：
          用户在设置界面配置第三方 API 密钥。请求被发送到 Studio 后端并穿透写入配置真相线。此后运行端每次读取都直达物理层，保证密钥的修改立刻生效。
        </li>
        <li>
          <strong>执行与审计流</strong>：
          用户点击“试运行”或“干跑”。编译器加载源码，而引擎则拉取最新的配置启动执行。在执行过程中，引擎向运行态线实时写入 Checkpoint；在运行结束后，引擎将 Trace 写入运行产物线。Studio 后端拉取 Trace 并向用户提供运行轨迹。
          <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现运行审计视图。以图形化拓扑展示当前技能的执行节点，从运行产物线拉取 trace 数据进行逐节点重放，支持用户在出错节点点击“恢复运行”发起二次续跑。</b></div>
        </li>
        <li>
          <strong>发布流水线</strong>：
          用户确认技能测试无误后点击“发布”。系统启动发布流程：对本地源码进行编译，产出结构契约文件；生成不可变的编译包并分配发布版本号；将编译包存入成品库，并将关联的源码通过 Git 管理提交。
          <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现发布历史看板与版本回溯界面。展示成品库中所有已发布的版本列表、包体指纹及发布时间，并提供一键回滚与部署到指定运行端的按钮。</b></div>
        </li>
      </ol>

      <h3>五、工程契约</h3>
      <p>所有存储操作必须遵守以下定义的底层接口契约。系统通过依赖注入方式，为本地环境与远程服务环境提供不同的存储提供者 (Provider) 实现：</p>
      <pre><code># 核心存储接口定义（Python 伪代码）

class ConfigTruthStore:
    def get_config(key: str) -> dict:
        """从强一致性介质读取配置"""
        pass

    def put_config(key: str, val: dict) -> None:
        """更新强一致性介质的配置，无中间缓存"""
        pass


class RunArtifactStore:
    def put_artifact(content: bytes) -> str:
        """将内容寻址的数据存入，返回 SHA-256 哈希值"""
        pass

    def get_artifact(hash_key: str) -> bytes:
        """根据内容哈希值拉取只读数据"""
        pass


class RuntimeStateStore:
    def acquire_lease(session_id: str, client_id: str, duration: int) -> bool:
        """申请指定会话的租约，防止多实例并发写"""
        pass

    def checkpoint(session_id: str, state: dict, token: int) -> None:
        """写入执行状态，要求携带单调递增的防护令牌"""
        pass</code></pre>

      <h3>六、错误与边界情况</h3>
      <p>针对存储层的异常情况，系统定义了明确的边界处理逻辑：</p>
      <ul>
        <li>
          <strong>租约失效与冲突 (LeaseExpiredException)</strong>：
          若运行端实例因网络卡顿而使得租约过期，另一实例可能会抢占该租约并以更高的防护令牌写入。原实例再次写入时会因为防护令牌失效而被拒绝，确保运行状态不会被过期数据覆盖。
        </li>
        <li>
          <strong>写锁获取超时 (LockAcquireTimeoutException)</strong>：
          如果 Python 边车在短时间内发起大量并发的配置修改请求，或者 Rust 进程由于执行排他性任务而长时间持有文件锁，Python 边车会抛出写锁获取超时异常，由 Studio 前端展示对应的网络提示而不会导致文件损坏。
        </li>
        <li>
          <strong>暂存区空间溢出 (ScratchStorageFullException)</strong>：
          在配置了远程对象存储的运行环境下，若遭遇网络中断，运行产物线的异步批处理同步将会挂起，所有产物在本地暂存区持续累积。当暂存区空间达到设定的阈值时，系统将阻断新的试运行请求以防止磁盘被耗尽。
        </li>
      </ul>
    `
  },

  gateway_brain: {
    tag: '第二卷 · GraphAgent Harness 架构与机制手册',
    title: '2.3 Gateway 健康状态投影与路由大脑',
    text: `
      <h3>1. 配置与凭证隔离</h3>
      <p>网关扮演着凭证解耦隔离源的物理边界角色。在系统架构设计中，网关被设计为纯库（SDK）形式，不直接提供 HTTP 接口，而是由应用适配层进行统一的二次封装与接口发布。网关坚守“单一真相源”与“存储分治”的原则，将系统数据划分为配置真相线、运行产物线、运行态线三条物理管道，并配合不可变发布产物库进行执行：</p>
      <ul>
        <li><strong>配置真相线</strong>：包含用户凭证、路由设置以及导入草稿，其生命周期长，读多写少。网关直接从物理介质读写，不允许引入任何旁路缓存，防止出现版本不一致的并发冲突。</li>
        <li><strong>运行产物线</strong>：记录执行流水及评估快照，写入后不可更改，支持面向对象存储的逻辑抽象。</li>
        <li><strong>运行态线</strong>：存储运行期的动态快照与会话断点，由单个运行实例独占。</li>
        <li><strong>不可变发布产物库</strong>：只装载已发布版本的不可变快照产物，作为引擎运行的依据。</li>
      </ul>
      <p>在安全性上，网关实施了彻底的明文脱敏机制。客户端与应用适配层仅持有凭证引用（Credential Reference），无法获取真正的物理密钥或 API 密钥明文。在运行时，<code>CredentialResolver</code> 组件根据传入的引用从底层的物理配置文件或安全的秘钥管理库中动态检索真正的明文凭证。明文凭证仅在物理调用链的内存生命周期内有效，调用结束后立即从内存中销毁，不会持久化至日志中，也不会向客户端或应用层返回，从物理结构上杜绝了凭证泄露的隐患。</p>

      <h3>2. 探测数据库与熔断机制</h3>
      <p>网关利用 SQLite 数据库对动态健康度进行物理持久化，该数据库由 <code>SqliteLlmHealthStore</code> 组件直接读写，路径由应用适配层进行注入。当发生暂时性失败（例如网络超时、第三方限流、API 配额临时超额）时，探测机制绝不会直接修改持久化的物理路由状态为失败，而是调用 <code>open_circuit</code> 接口在 SQLite 数据库中记录一条熔断项（Runtime Circuit）：</p>
      <ul>
        <li><strong>熔断状态判定</strong>：熔断记录包含当前时间、冷却截止时间（<code>retry_at</code>）、失败次数以及具体的错误信息。</li>
        <li><strong>冷却与自愈恢复</strong>：在熔断冷却期间，网关调用 <code>get_active_circuits</code> 会检索出该条路由正处于熔断冷却中。路由大脑在进行逻辑角色的物化时，将自动排除该冷却中的路由，并触发警告提示。当冷却时间（<code>retry_at</code>）到期后，熔断记录自然失效，后续请求将重新被允许执行探测或调用，从而实现了物理路由的自愈冷却闭环。</li>
      </ul>

      <h3>3. 双轴健康投影模型</h3>
      <p>为了全面且正交地评估物理路由状态，网关提出了双轴健康度投影模型。可用性（Availability）轴与能力（Capability）轴完全正交独立，互不混淆：</p>
      <ul>
        <li><strong>第一轴：可用性投影（六态 UI 状态）</strong><br/>
          系统将底层的物理端点状态、物理路由状态、凭证存在性以及当前熔断信息融合成六态 UI 状态：
          <ol>
            <li><code>ready</code>（绿色）：端点与路由状态均经真实探测验证通过，表示当前完全可用。</li>
            <li><code>historical_ready</code>（蓝色）：端点已验证，且证据库中存在该路由历史探测成功的记录，但当前缺乏实时的在线探测状态，常出现在导入端点配置后的初始阶段。</li>
            <li><code>untested</code>（灰色）：配置完整且无熔断，但从未进行过探测。</li>
            <li><code>failed</code>（红色，带原因码）：表明存在需要人工修复的故障。原因包括：配置缺口（如缺失密钥或模型 ID，原因码为 <code>missing_config</code>）与测试失败（如网络不可达，原因码为 <code>endpoint_unreachable</code> 或 <code>model_failed</code>）。</li>
            <li><code>cooling_down</code>（灰色，带倒计时）：路由存在未过期的熔断记录，处于冷却恢复中。</li>
            <li><code>off</code>（灰色，不可选）：端点或路由被用户主动停用，或由于模型被服务商弃用而被系统归入停用区。</li>
          </ol>
          可用性投影判定时，遵循明确的优先级次序：<code>off > failed > cooling_down > ready > historical_ready > untested</code>。
        </li>
        <li><strong>第二轴：能力投影（四态能力状态）</strong><br/>
          独立于可用性投影，主要用于评估系统对该路由特性的了解程度。系统根据路由在工具调用、长文本支持、结构化解析等能力测试中的覆盖率，将能力投影为：<code>unknown</code>（未知）、<code>callable_only</code>（仅可调用）、<code>partial</code>（部分支持）、<code>known</code>（完全支持）。
        </li>
      </ul>

      <h3>4. 路由算法与 Fallback 动态降级链</h3>
      <p>路由大脑负责将用户的逻辑角色意图平铺为具体的、可执行 of 物理调用链：</p>
      <ol>
        <li>逻辑角色（Role）由一个或多个物理路由组成。路由大脑首先从注册表中检索出角色关联的候选路由列表。</li>
        <li>读取当前活动路由的可用性投影，自动过滤掉所有处于 <code>off</code> 或 <code>failed</code> 状态的故障路径。</li>
        <li>检测处于 <code>cooling_down</code> 状态的路由。如果路由处于冷却中，路由大脑会将该路由排除在当前的物理调用链之外，并生成 <code>RouteWarning</code> 诊断信息。</li>
        <li>提取候选路径对应的凭证引用，通过 <code>CredentialResolver</code> 进行运行时密钥准备。</li>
        <li>根据匹配度与优先级，将剩余可用的路由排列为一条有序物理调用链。当主路线发生故障时，网关将按照此调用链顺序进行动态降级调用。</li>
      </ol>

      <h3>5. 用户工作流</h3>
      <p>用户在配置与验证模型路由时的生命周期工作流如下：</p>
      <ol>
        <li><strong>凭证录入</strong>: 用户配置模型端点。系统自动生成导入草稿，但不修改当前运行配置。此时，相关路由状态投影为 <code>untested</code> 灰色状态。</li>
        <li><strong>端点验证</strong>：用户发起端点测试。网关通过最小接口请求验证端点的可达性并获取模型列表。测试成功后，若证据库中存有该路由历史探测成功的记录，路由状态自动转换为蓝色 <code>historical_ready</code> 状态。</li>
        <li><strong>深度路由探测</strong>：用户对特定路由发起真实探测。网关触发一次模型会话调用。若调用成功，证据库记录成功证据，路由状态正式升为绿色 <code>ready</code>。</li>
        <li><strong>熔断降级与恢复</strong>：若网关在执行过程中遇到网络抖动或超限限流等临时错误，系统将写入熔断项。路由可用性瞬间投影为带有倒计时标志的 <code>cooling_down</code>。路由大脑检测到状态变更后，自动将后续请求路由至 Fallback 调用链中的下一条备选路径。冷却截止后，路由熔断解除，可用状态重新评估恢复。</li>
      </ol>
      
      <div class="lesson-alert alert-amber"><b>【界面占位符】：此处在产品界面呈现凭证导入与草稿应用工作流。用户可直观对比本地已配置凭证与导入草稿的差异，当检测到并发写入冲突时，界面将弹出冲突提示并允许用户选择保留本地配置或应用草稿数据。</b></div>
      <div class="lesson-alert alert-amber"><b>【界面占位符】：此处在 LLM 角色配置页面展示物理路由的降级顺序。界面通过动态拓扑图展示当前逻辑角色绑定的物理路由链，处于熔断冷却（cooling_down）的节点在图上显示为灰色，并伴有自愈冷却倒计时，同时以虚线箭头指向 Fallback 备用降级路线。</b></div>

      <div data-demo="demo-seam"></div>

      <h3>6. 工程契约</h3>
      <p>网关公共内核提供以下标准的数据结构与计算接口：</p>
      
      <p><strong>状态投影函数定义：</strong></p>
      <pre><code>def project_route_state(
      *,
      route_id: str,
      endpoint_status: str,
      route_status: str,
      credential_available: bool,
      circuit_retry_at: datetime | None = None,
      draft_history: bool = False,
  ) -> ProviderModelStateProjection:
      pass</code></pre>

      <p><strong>状态投影输出结构（ProviderModelStateProjection）：</strong></p>
      <pre><code>class ProviderModelStateProjection(BaseModel):
      route_id: str
      ui_state: Literal["ready", "historical_ready", "untested", "failed", "cooling_down", "off"]
      reason_code: Literal["missing_config", "endpoint_unreachable", "model_failed"] | None = None
      retry_at: datetime | None = None
      ui_detail: str | None = None
      evidence_refs: list[str] = Field(default_factory=list)</code></pre>

      <p><strong>熔断存储服务接口契约（SqliteLlmHealthStore）：</strong></p>
      <pre><code>class SqliteLlmHealthStore:
      def open_circuit(
          self,
          scope: str,
          scope_id: str,
          opened_at: datetime,
          retry_at: datetime,
          ttl_seconds: int,
          reason_code: str,
          failure_count: int,
          message: str
      ) -> None:
          pass

      def get_active_circuits(
          self,
          route_id: str,
          endpoint_id: str,
          rate_limit_bucket: str
      ) -> list[RuntimeCircuit]:
          pass</code></pre>

      <p><strong>证据库记录结构（EvidenceRecord）：</strong></p>
      <pre><code>class EvidenceRecord(BaseModel):
      evidence_id: str
      evidence_type: EvidenceRecordType
      trust_state: EvidenceTrustState
      observed_at: str | None = None
      attempted_at: str | None = None
      scope: dict[str, str] = Field(default_factory=dict)
      endpoint_id: str | None = None
      route_id: str | None = None
      model_id: str | None = None
      probe_status: str | None = None
      reason: str | None = None
      probe_attempts: list[dict[str, Any]] = Field(default_factory=list)
      successful_probe: dict[str, Any] | None = None
      failed_probe: dict[str, Any] | None = None</code></pre>

      <p><strong>物理路由解析结构（ResolvedRoute）：</strong></p>
      <pre><code>class ResolvedRoute(BaseModel):
      role_name: str
      route_id: str
      endpoint_id: str
      protocol: Protocol
      base_url: str
      credential_ref: str
      credential_fingerprint: str
      timeout_seconds: int = 120
      provider_model_id: str
      canonical_id: str
      capabilities: dict[str, CapabilityValue] = Field(default_factory=dict)
      snapshot_version: SnapshotVersion | None = None</code></pre>

      <p><strong>角色物化接口契约（materialize_role）：</strong></p>
      <pre><code>def materialize_role(
      *,
      role: str,
      routes: list[ResolvedRoute],
      projections: dict[str, ProviderModelStateProjection],
  ) -> MaterializedRole:
      pass</code></pre>

      <h3>7. 错误和边界</h3>
      <ul>
        <li><strong>并发防护设计（指纹一致性）</strong>：在端点测试开始时生成一个唯一指纹，在测试结果写入数据库前校验指纹一致性。如果不一致（说明在此期间凭证或地址被修改），系统主动丢弃该次测试结果，以防旧数据覆盖新配置。</li>
        <li><strong>探测期与运行期的地址规范不一致</strong>：网关与引擎进行第三方网络调用时，必须执行统一的端点 URL 规范化（如去重多余的 <code>/v1</code> 路径），避免探测链路能连通但实际运行时发生 404 错误。</li>
        <li><strong>版本不一致降级机制</strong>：当加载的角色路由快照版本（<code>snapshot_version</code>）与系统注册表版本不一致时，路由解析器会主动剥离过期的模型能力，降低该路由的就绪状态，防止过期接口导致执行失败。</li>
        <li><strong>API 密钥页面的连接状态收口</strong>：为确保状态的绝对真实，第三方或自定义服务的可用性状态完全由后端的六态健康投影驱动，前端禁止本地维护副本。当探测失败时，网关强制回写降级状态，以防前端展示与后端执行状态发生不一致。</li>
      </ul>
    `
  },

  engine_runtime: {
    tag: '第二卷 · GraphAgent Harness 架构与机制手册',
    title: '2.4 Engine 运行时：LangGraph 串联与事件流协议',
    text: `
      <p>在 GraphAgent Harness 的执行架构中，Engine 运行时是整个执行链路的动力核心。它负责将静态的 skill 编译快照解析为可执行的 LangGraph 拓扑，并通过一套精细的共享黑板机制与高可靠的事件流协议，保障图节点间的数据隔离与运行状态的增量同步。</p>

      <h3>一、 拓扑解析与不可变 Artifact 载入</h3>
      <p>Engine 运行时在执行任何图逻辑前，首先将输入的不可变 <code>ArtifactRef</code> 进行解析，将其组装成底层的 LangGraph 拓扑结构。整个装配与解析流程遵循严格的确定性规约：</p>
      <ul>
        <li><strong>内容指纹（Content Hash）</strong>：在编译阶段，Engine 递归遍历 skill 源码目录中的所有源文件（自动剔除隐藏文件、临时缓存及编译二进制），按照 POSIX 相对路径进行字典序排序。对所有路径名与文件二进制内容进行 SHA-256 哈希计算，最终生成以 <code>sha256:</code> 为前缀的唯一 <code>content_hash</code>。此哈希是该产物版本在成品库中的唯一内容寻址凭证。</li>
        <li><strong>执行指纹（Execution Fingerprint）</strong>：为了避免因表现层微调（如在 Studio 中调整画布节点的布局坐标、颜色、备注等 UI 元数据）而导致执行端重新构建或缓存失效，Engine 在计算 <code>execution_fingerprint</code> 时，会解析 <code>GRAPH.md</code> 的前言元数据，提取并剥离 <code>metadata.ui</code> 配置，仅对逻辑执行相关的拓扑定义和节点配置进行哈希。这保证了表现层修改不影响执行层的不可变签名。</li>
        <li><strong>存储分区隔离</strong>：编译生成的产物分为两类存储分区。调试与本地测试期间生成的产物被归入临时区（<code>ephemeral</code>），不需要写入生产成品库；而通过发布流水线发布的产品级成品则被写入生产区（<code>product</code>），并分配唯一的版本号（<code>version</code>），运行时仅从相应的存储区中读取对应版本的 <code>CompiledArtifactManifest</code>。</li>
        <li><strong>LangGraph 装配</strong>：Engine 接收到 <code>ArtifactRef</code> 后，通过内部的解包机制读取对应的 <code>GRAPH.md</code> 拓扑声明，自动将声明中的各个 Phase 实例化为 LangGraph 的 <code>StateGraph</code> 节点。节点间的跳转条件则被编译为控制流的条件边（Conditional Edges），从而完成运行时图拓扑的装配。</li>
      </ul>

      <h3>二、 共享黑板架构（Blackboard Context）</h3>
      <p>为了打破传统工作流中节点间点对点传递参数导致的网状紧耦合，Engine 运行时引入了共享黑板机制。各节点不再感知其前驱或后继节点的具体存在，而是通过统一的数据切片与合流规约与黑板进行交互。</p>

      <h4>1. 三区隔离设计</h4>
      <p>黑板的数据容器被严格划分为三个不同的功能分区：</p>
      <ul>
        <li><strong>只读输入区（inputs）</strong>：在图初始化时，传入的原始数据通过漏斗（Funnel）校验后存入该区域。此区域在整个图运行期间对于所有节点皆为只读。在状态融合阶段，任何试图对该区域进行二次修改或覆写的操作都将直接被引擎拒绝。</li>
        <li><strong>节点产出区（phase_outputs）</strong>：以 <code>phase_id</code> 为键的嵌套字典结构。每个节点在执行完毕后，只能将自身的结构化输出写入对应的 <code>phase_id</code> 槽位。该槽位具有强独占性，每个节点仅允许写入一次，不支持任何形式的覆盖或二次覆写，从而在执行期杜绝了由于节点并发冲突导致的脏数据写入。</li>
        <li><strong>草稿计算区（scratch）</strong>：供运行期插件、工具或临时节点进行中间计算时使用的演算沙箱。同样采用键值对存储，并且遵循单次写入的独占规约，防止多插件协同过程中的键名冲突。</li>
      </ul>

      <h4>2. Reducer 状态融合机制</h4>
      <p>黑板状态 <code>BlackboardState</code> 的 <code>data</code> 字段通过特定的 Reducer 函数 <code>blackboard_data_merge</code> 进行状态更新与冲突检测。在每次节点状态提交时，Reducer 执行如下判定逻辑：</p>
      <pre><code># 状态融合冲突判定规则示意
if right_data["inputs"] and merged["inputs"] != right_data["inputs"]:
    raise GraphAgentFatalError("data.inputs is read-only after initialization")

for phase_id in right_data["phase_outputs"]:
    if phase_id in merged["phase_outputs"]:
        raise GraphAgentFatalError(f"phase_outputs[{phase_id}] written more than once")

for key in right_data["scratch"]:
    if key in merged["scratch"]:
        raise GraphAgentFatalError(f"scratch key={key} written more than once")</code></pre>
      <p>如果发生上述任意一处重写冲突，运行时将抛出错误码为 <code>[F-v3-runtime-state-mapping-failed]</code> 的 <code>GraphAgentFatalError</code> 致命异常，并立刻中止当前的执行流程，确保状态机不会进入非预期的漂移状态。</p>

      <h4>3. 数据切片与合流工作流</h4>
      <p>在单个节点的执行生命周期中，黑板数据的流动经历两个阶段：</p>
      <ol>
        <li><strong>切片（Slice）</strong>：在节点启动前，状态映射器（<code>StateMapper</code>）读取该节点对应的 <code>PhaseIOSchema.inputs</code> 字段声明，将只读输入区（inputs）与上游节点已归档的产出区（phase_outputs）进行过滤切片，为节点构建局部的只读输入视图，杜绝节点越权读取黑板上的其他无关数据。</li>
        <li><strong>合流（Merge）</strong>：节点顺利执行结束后，其输出数据被组装为字典，以该节点 ID 为键，通过 <code>blackboard_data_merge</code> Reducer 提交合并至全局黑板的 <code>phase_outputs</code> 区域。</li>
      </ol>

      <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现拓扑图的运行期动效。用户可以直观地在画布上看到数据流从 inputs 区域经过各个节点的输入 Schema 切片过滤，最后合并写入 phase_outputs 的完整 Reducer 轨迹图，点击任意连线即可查看实时的 JSON 数据快照。</b></div>

      <h3>三、 事件流协议与容错控制</h3>
      <p>Engine 运行时通过 WebSocket 通道向外推送图运行期间的微观执行轨迹。为了应对不稳定的网络环境，设计了一套基于自增序列与双端游标的事件流协议。</p>

      <div data-demo="demo-loop"></div>

      <h4>1. 事件信封（EventEnvelope）结构</h4>
      <p>所有的运行事件统一封装在 <code>EventEnvelope</code> 中，其核心报文结构如下：</p>
      <table border="1" cellpadding="5" cellspacing="0" style="border-collapse:collapse; width:100%;">
        <thead>
          <tr style="background-color:#f2f2f2;">
            <th>字段名称</th>
            <th>类型</th>
            <th>说明</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><code>stream_id</code></td>
            <td>String</td>
            <td>流唯一标识，用于标识一次运行会话</td>
          </tr>
          <tr>
            <td><code>seq</code></td>
            <td>Integer</td>
            <td>单调递增的事件序号，从 1 开始，用于检测网络丢包与乱序</td>
          </tr>
          <tr>
            <td><code>run_id</code></td>
            <td>String</td>
            <td>当前执行图运行的唯一 UUID</td>
          </tr>
          <tr>
            <td><code>event_type</code></td>
            <td>String</td>
            <td>事件类型名称（如 <code>phase_started</code>, <code>BlackboardReduceEvent</code>）</td>
          </tr>
          <tr>
            <td><code>payload</code></td>
            <td>Dict</td>
            <td>事件的具体数据载荷</td>
          </tr>
          <tr>
            <td><code>cursor</code></td>
            <td>String</td>
            <td>格式为 <code>stream_id:seq</code> 的游标字符串，用作续传寻址凭证</td>
          </tr>
          <tr>
            <td><code>timestamp</code></td>
            <td>DateTime</td>
            <td>事件生成的 UTC 时间戳</td>
          </tr>
        </tbody>
      </table>

      <h4>2. 事件流缓冲器（EventStreamBuffer）容错机制</h4>
      <p>服务端与客户端之间通过 <code>EventStreamBuffer</code> 建立容错传输通道，实现以下四种核心容错算法：</p>
      <ul>
        <li><strong>幂等去重</strong>：接收端在接收到新事件时，校验其 <code>seq</code>。若该 <code>seq</code> 已存在于缓冲器或已被淘汰（已进入 <code>evicted_seqs</code> 集合），则直接予以丢弃，避免网络重试导致的重复消费。</li>
        <li><strong>乱序重组</strong>：如果由于网络多径传输导致事件未按序到达（例如先收到 <code>seq=2</code>，后收到 <code>seq=1</code>），缓冲器会自动根据 <code>seq</code> 的升序进行重新排列，以确保向上层交付保序的事件队列。</li>
        <li><strong>空洞检测与恢复（Gap Detection）</strong>：若缓冲器中写入的事件 <code>seq</code> 产生了不连续的跳跃（如从当前最大 <code>seq=1</code> 直接跳跃至 <code>seq=3</code>），系统会立即抛出 <code>StreamCursorGapError</code>（错误码为 <code>stream.cursor_gap</code>）。消费端捕获该异常后，会发起续传请求，携带上一次成功消费的 <code>cursor</code>，触发服务端调用 <code>resume(cursor=...)</code> 重新推送从该游标位置开始的所有增量事件。</li>
        <li><strong>背压控制与淘汰机制（Backpressure &amp; Eviction）</strong>：
          缓冲器具有最大容量 <code>capacity</code> 限制：
          <ol>
            <li>当缓冲容量塞满且有新事件写入时，系统将最旧的事件从内存中移除并归入已淘汰集合中。若客户端此时尝试以已淘汰的游标进行 <code>resume</code> 续传，系统将抛出 <code>StreamCursorExpiredError</code>（错误码为 <code>stream.cursor_expired</code>），客户端接收到该错误后会自动转为发起一次全量同步。</li>
            <li>在极限背压场景下（如 <code>capacity</code> 被设置为 1），任何在缓冲区被占满时的写入操作将直接触发 <code>StreamBackpressureError</code>（错误码为 <code>stream.backpressure</code>），对执行流进行即时截断与反向压制。</li>
          </ol>
        </li>
      </ul>

      <div class="lesson-alert alert-amber"><b>【界面占位符】：在 Studio 的运行面板中，这里呈现事件流的实时监控折线图与连接状态指示器。当发生网络抖动导致 Gap 触发时，界面会亮起黄色同步灯，显示“正在通过游标恢复增量事件”，整个恢复过程在后台自动完成，对用户完全无感。</b></div>
    `
  },

  hitl_resume_protocol: {
    tag: '第二卷 · GraphAgent Harness 架构与机制手册',
    title: '2.5 HitL 与 Resume 中断恢复协议',
    text: `
      <p>在复杂智能体协作流的执行过程中，流程的连续性与可控性是保证系统稳定运行的核心。本章节定义了系统在遇到需要人工干预、执行失败或数据变更时，进行状态挂起、安全恢复以及下游链式失效的行为规范。</p>

      <h3>一、 产品概念</h3>
      <p>为了在长程智能体计算中实现精细化的调试与交互，系统建立了基于快照的中断恢复机制：</p>
      <ul>
        <li><strong>异步中断与人工干预（Human-in-the-Loop, HitL）：</strong>在智能体执行特定敏感工具调用、需要人类进行逻辑确认，或进入预设的等待节点时，引擎会挂起当前的执行线程。这种挂起是非阻塞的，引擎在释放执行资源的同时，完整保留当前计算环境的状态快照，以等待外部信号唤醒。</li>
        <li><strong>状态续跑（Resume）：</strong>当执行挂起或因异常中断后，用户可以通过向系统发送恢复指令，使得智能体在中断的特定节点处继续执行。恢复执行时，系统不需要重新跑完该节点上游的全部拓扑路径，而是直接读取该节点的历史存盘快照，并支持在恢复时注入外部输入数据或覆盖已有黑板上下文。</li>
        <li><strong>状态屏障（Fencing Token）：</strong>为防止因网络延迟、多客户端并发调用或重复点击恢复按钮导致的状态脑裂和数据重复写入，引擎引入了单调递增的屏障凭证机制。每次状态挂起或更新快照时，系统都会生成并绑定一个唯一的屏障凭证，后续任何恢复指令必须携带此凭证，否则引擎拒绝接收状态更新。</li>
        <li><strong>状态脏检查（Dirty Check）与失效追踪：</strong>在调试过程中，如果用户修改了某个节点的设计配置（例如提示词、执行脚本或输出定义），或者手动篡改了某一步骤的中间上下文，这会导致原本已存盘的历史快照与新的流定义产生不一致。脏检查机制用于自动评估这种不一致对图拓扑下游的影响，并精准废弃受波及节点的快照，从而确保续跑数据的完整与安全。</li>
      </ul>

      <h3>二、 系统行为</h3>
      <h4>1. 执行挂起与快照归档</h4>
      <p>当执行器运行到配置了人工干预属性的节点，或者智能体内层循环触发了中断原语时，引擎启动挂起时序：</p>
      <ul>
        <li><strong>状态存盘：</strong>引擎调用快照管理器，以当前的运行实例标识和对应的命名空间（例如内层对话命名空间 <code>agent:writing_phase</code>）为键值，将当前的黑板数据和消息通道历史写入持久化介质。</li>
        <li><strong>租约锁定：</strong>引擎在数据库中获取该运行实例的独占租约锁，防止其他异步工作流同时修改该实例的状态。</li>
        <li><strong>令牌递增：</strong>系统将该运行实例的屏障版本号递增，并将最新的屏障凭证与本次快照进行强绑定。</li>
        <li><strong>事件抛出：</strong>引擎释放执行线程，同时向事件总线广播挂起事件，向外公开当前的中断上下文、Checkpoint 标识及所需的屏障凭证。</li>
      </ul>

      <h4>2. 恢复时序与数据注入</h4>
      <p>当外部系统发起恢复请求时，引擎通过以下逻辑判断是否允许恢复：</p>
      <ul>
        <li><strong>凭证校验：</strong>引擎提取请求中的屏障凭证，与数据库中该运行实例存储的最新令牌进行比对。若请求中的凭证小于当前存储的令牌，引擎立即拒绝执行。</li>
        <li><strong>状态回装与注入：</strong>若凭证校验通过，引擎载入对应的快照，将恢复请求中携带的外部输入（如人工输入的文本或工具返回消息）注入到消息历史的末尾，或者将指定的修改参数合并入黑板数据中。</li>
        <li><strong>释放租约与重新调度：</strong>引擎释放该实例的租约锁定，将图状态机恢复至中断节点，并重新加入执行队列开始向后推演。</li>
      </ul>

      <h4>3. 脏检查与失效判定算法</h4>
      <p>为了保证拓扑图在被修改后不会以错误的历史数据续跑，系统按照以下算法自动标记失效状态：</p>
      <ul>
        <li><strong>变更检测：</strong>一旦检测到节点 <code>A</code> 的图定义、输入输出协议发生变更，或用户手动覆盖了节点 <code>A</code> 产生的数据，系统将节点 <code>A</code> 归入置脏初始集合 <code>D = {A}</code>。</li>
        <li><strong>依赖传递：</strong>从集合 <code>D</code> 中的节点出发，沿着有向无环图的边进行向下遍历。对于任意一条边 <code>(u, v)</code>，如果起点 <code>u</code> 属于集合 <code>D</code>，则将终点 <code>v</code> 自动加入集合 <code>D</code>。</li>
        <li><strong>快照失效：</strong>遍历结束后，系统更新运行实例在数据库中的状态，将所有属于集合 <code>D</code> 的节点的历史快照标记为无效，并将它们的安全凭证版本强制设为过期。</li>
      </ul>
      <p>例如，在拓扑路径 <code>A -&gt; B -&gt; C</code> 中，若修改了节点 <code>A</code> 的配置，引擎会将 <code>B</code> 和 <code>C</code> 的快照标记为失效。如果此时存在一条独立路径 <code>D -&gt; E</code>，则 <code>D</code> 和 <code>E</code> 的快照依然有效，系统保留它们直接续跑的能力。</p>

      <h3>三、 用户工作流</h3>
      <h4>1. 人工干预与回复注入</h4>
      <p>当智能体在生成内容或调用敏感工具时暂停，系统引导用户通过以下工作流完成干预：</p>
      <ul>
        <li>用户在画布中观察到处于挂起状态的节点，节点呈现特定的等待指示灯。</li>
        <li>系统自动在节点上方呈现专用的交互区，展示智能体当前的提问或需要确认的参数。</li>
        <li>用户在交互区中填写反馈意见或修改参数，点击发送。</li>
        <li>系统将输入内容组装为标准的消息格式，并携带当前的屏障凭证发送至引擎，流程自动恢复并继续向下运行。</li>
      </ul>
      <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现悬浮于故障或挂起节点上方的工作栏（Debug Bar），包含输入文本域、确认注入并续跑按钮。用户可在文本域中键入给智能体的反馈，或补充缺失的运行时参数，交互操作直接针对节点进行。</b></div>

      <h4>2. 上下文篡改与精准续跑</h4>
      <p>在调试失败的运行实例时，用户可以通过手动干预数据来验证修复方案：</p>
      <ul>
        <li>用户点击拓扑图上的连接线或数据节点，打开历史黑板数据的查看面板。</li>
        <li>用户将只读查看器切换至编辑模式，直接修改 JSON 数据中的键值，并保存修改。</li>
        <li>系统执行脏检查算法，将该节点下游的所有可达节点的续跑操作予以限制，并在界面上将这些下游节点的恢复按钮进行置灰。</li>
        <li>用户点击被篡改节点上的恢复按钮，引擎载入修改后的黑板数据作为起始状态，仅重新运行该节点及其下游，而其他未受波及的分支则保持原有执行结果。</li>
      </ul>
      <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现双击拓扑边上的数据锚点时，弹出的全局黑板编辑器。该编辑器由只读状态切换至可编辑状态，允许用户手写修改 JSON 格式的上下文数据。编辑器底部提供保存并重新评估依赖项的交互项，保存后受影响的下游路径将发生置灰的视觉变化。</b></div>

      <h3>四、 工程契约</h3>
      <h4>1. 运行实例挂起事件数据格式</h4>
      <pre><code>{
  "event_type": "run_suspended",
  "run_id": "run_77263",
  "checkpoint_id": "chk_99201",
  "checkpoint_ns": "agent:draft_phase",
  "fencing_token": 14,
  "payload": {
    "interrupted_node": "writer_agent",
    "prompt": "请输入下一章节的核心大纲：",
    "require_input": true
  }
}</code></pre>

      <h4>2. 恢复运行接口契约</h4>
      <ul>
        <li><strong>请求路径：</strong><code>POST /api/v1/runs/{run_id}/resume</code></li>
        <li><strong>请求内容：</strong></li>
      </ul>
      <pre><code>{
  "checkpoint_id": "chk_99201",
  "checkpoint_ns": "agent:draft_phase",
  "fencing_token": 14,
  "context_overrides": {
    "data": {
      "outline": "第一章：起航"
    }
  },
  "human_response": {
    "role": "tool",
    "content": "已确认",
    "tool_call_id": "call_9912"
  }
}</code></pre>

      <h4>3. 置脏失效标记数据结构</h4>
      <pre><code>{
  "checkpoint_id": "chk_99202",
  "node_id": "editor_agent",
  "run_id": "run_77263",
  "is_valid": false,
  "taint_reason": "上游节点 writer_agent 的输出被手动修改",
  "invalidated_at": "2026-06-15T17:32:21Z"
}</code></pre>

      <h3>五、 错误和边界</h3>
      <h4>1. 屏障冲突防御</h4>
      <p>如果两个不同的管理终端或重复的网络请求尝试对同一个挂起状态发起恢复，且其中一个请求已经成功执行，则数据库中的屏障版本号已发生递增。后续携带旧凭证的请求到达时，引擎会校验失败，返回状态冲突错误响应，并拒绝修改状态，从而避免双重恢复导致的数据混乱。</p>

      <h4>2. 并发租约冲突</h4>
      <p>当某一运行实例的恢复逻辑正在被执行引擎加载并占用时，该运行实例对应的数据库记录将被租约锁锁定。此时如果有其他客户端尝试篡改或恢复该实例，接口将返回资源锁定错误，指示当前实例正处于独占操作中，必须等待当前锁释放或超时。</p>

      <h4>3. 脏状态阻断</h4>
      <p>若用户绕过界面控制，尝试通过直接调用接口从一个已被标记为 <code>is_valid: false</code> 的快照恢复流程，引擎在加载快照时会校验其有效性字段。一旦发现该快照已被上游变更置脏，将拒绝反序列化并抛出状态校验异常，避免智能体在前后矛盾的数据上下文中运行。</p>

      <h4>4. 运行时状态隔离与污染防护</h4>
      <p>在写入快照时，系统强制要求进入快照的所有数据必须是可序列化的 JSON 结构或标准的消息序列。引擎的执行中间件、数据库连接句柄、已编译的物理执行图对象等运行时状态，严禁写入快照中。如果反序列化过程中检测到包含不安全的系统类实例，系统会立即将该快照置为失效，并强制回退到上一个有安全记录的超级步骤起点，以防内存对象污染导致运行时崩溃。</p>
    `
  },

  stable_boundary: {
    tag: '第二卷 · GraphAgent Harness 架构与机制手册',
    title: '2.6 三模块稳定边界与错误纪律',
    text: `
      <h3>一、 产品概念：三模块稳定协作边界</h3>
      <p>系统的运行由 Engine（执行引擎）、Gateway（管理网关）与 Studio（可视化工作台）三个核心模块共同支撑。为了保证在跨进程协作与分布式网络环境下的系统健壮性，系统确立了严密且契约化的稳定边界。其核心原则在于：任何协作模块之间的交互故障，均不可作为静默的系统背景噪声而被忽略，必须通过结构化、机器可读的错误承载体进行透明传递，使得上层应用能够据此实施精确的控制流分发与诊断恢复。</p>

      <h3>二、 系统行为：绝对禁止静默降级</h3>
      <p>系统在设计与执行上严格执行<strong>绝对禁止静默降级</strong>的纪律。当任意模块在处理执行原语、读取配置、校验凭证或进行路由分发时发生异常，系统坚决不执行任何无感知的兜底或静默退避。系统行为规范如下：</p>
      <ul>
        <li><strong>配置与并发冲突：</strong>当 Gateway 写入配置时，必须对 <code>etag</code> 进行一致性校验。若检测到版本冲突，Gateway 立即中止写入并抛出并发冲突错误，拒绝静默覆盖。</li>
        <li><strong>路由与降级规避：</strong>若路由链解析为空，或者 fallback 决策链已耗尽且判定为放弃（<code>give_up</code>），Gateway 必须将其作为显式错误抛出，绝不静默使用空路由或执行默认路由绕过。</li>
        <li><strong>运行产物与完整性验证：</strong>Engine 在通过 ArtifactRef 执行或预测产物时，若成品库中缺失对应的编译产物，系统将直接返回未找到产物的硬失败，坚决不进行后台静默重编，防止未经审核的变更代码进入生产环境。</li>
        <li><strong>并发租约与脑裂防护：</strong>在多实例并发运行状态下，所有的写入操作均需校验 <code>fencing_token</code>。当租约被抢占或心跳超时，Engine 直接拒绝旧租约的写入请求，通过硬失败退回以防止双写导致的数据脑裂。</li>
        <li><strong>原子发布与垃圾回收：</strong>Studio 触发的发布流水线若在中间步骤发生失败，发布管道将自动启动物理补偿垃圾回收（GC）逻辑以清理残留的临时发布物料，决不保留不完整的半成品发布项。</li>
      </ul>

      <h3>三、 用户工作流：结构化诊断与异常响应</h3>
      <p>当底层模块由于违反边界而产生硬失败时，用户在 Studio 中可以通过直观的界面反馈获取详细的故障信息，并根据系统给出的明确路径进行干预：</p>
      <ol>
        <li>当运行或编译失败时，Studio 错误抽屉会自动弹出，显示本次运行的诊断信息。</li>
        <li>如果系统发生网络波动等传输族错误（错误载体中的 <code>retryable</code> 属性为真），界面会呈现高亮的“立即重试”按钮，支持用户一键发起幂等重试。</li>
        <li>如果发生凭证失效或配置 Etag 冲突等非重试错误（<code>retryable</code> 属性为假），界面会高亮提示具体冲突的字段或密钥，并提供跳转配置面板的链接，引导用户输入新凭证或同步最新配置。</li>
        <li>错误诊断面板会准确解析并呈现错误码、人类可读的错误说明以及 JSON 安全的 context 上下文数据，帮助用户精确定位问题所在的图节点或编译源文件。</li>
      </ol>
      <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在 Studio 的运行日志面板呈现诊断抽屉。诊断抽屉会根据 ResponseEnvelope 的 error_payload 渲染错误码、错误大类、以及 details 中的 context 上下文数据，并根据 retryable 属性显示或隐藏“立即重试”按钮。</b></div>

      <h3>四、 工程契约：统一机器返回包装与大类划分</h3>
      <p>三模块之间的网络和跨进程交互统一使用 <code>ResponseEnvelope</code> 信封承载。其 JSON 契约格式定义如下：</p>
      <pre><code>{
  "schema_version": "string",
  "ok": "boolean",
  "data": "any | null",
  "error_code": "string | null",
  "error_payload": {
    "error_code": "string",
    "message": "string",
    "details": {
      "context": "dict",
      "additional_info": "any"
    },
    "retryable": "boolean"
  }
}</code></pre>

      <p>所有的错误均归入全局的五大故障大类，并拥有专属的 <code>error_code</code>：</p>
      <table border="1" cellpadding="5" cellspacing="0" style="border-collapse: collapse; width: 100%;">
        <thead>
          <tr style="background-color: #f2f2f2;">
            <th>错误大类</th>
            <th>典型场景</th>
            <th>代表性错误码（<code>error_code</code>）</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><strong>完整性（Integrity）</strong></td>
            <td>Hash 校验失败、按 Hash 取不到编译包、指纹漂移</td>
            <td><code>integrity.hash_mismatch</code></td>
          </tr>
          <tr>
            <td><strong>并发（Concurrency）</strong></td>
            <td>Config Etag 冲突、Seal 封存后再写、Lease 租约抢占</td>
            <td><code>concurrency.lease_preempted</code></td>
          </tr>
          <tr>
            <td><strong>传输（Transmission）</strong></td>
            <td>HTTP 超时、接口拒连、序列化失败、事件流断线或 Gap</td>
            <td><code>stream.cursor_gap</code></td>
          </tr>
          <tr>
            <td><strong>凭证（Credential）</strong></td>
            <td>外部密钥库访问失败、密钥失效/过期</td>
            <td><code>credential.vault_access_failed</code></td>
          </tr>
          <tr>
            <td><strong>资源与终态（Resource/Terminal）</strong></td>
            <td>路由链空缺、Fallback 耗尽放弃</td>
            <td><code>resource.route_exhausted</code></td>
          </tr>
        </tbody>
      </table>

      <p>为了保证数据在网络边界上的可传输性，任何非 JSON 原生对象在构造 <code>error_payload.details</code> 时必须进行强制的 JSON 安全规范化转换：</p>
      <ul>
        <li>路径对象（例如 <code>Path</code>）自动规范化为 <code>str</code> 字符串。</li>
        <li>集合对象（例如 <code>set</code>）自动规范化为排序后的列表（<code>list</code>）。</li>
        <li>嵌套的 Pydantic 模型自动转换为嵌套 <code>dict</code> 字典。</li>
        <li>未处理的异常对象自动转换为 <code>TypeName: message</code> 格式。</li>
      </ul>

      <h3>五、 错误和边界：传输故障与有界快照</h3>
      <p>系统在面对网络异常与诊断数据过载等边缘场景时，遵循如下边界控制规则：</p>
      <ul>
        <li><strong>传输恢复边界：</strong>对于普通的网络抖动，系统支持基于幂等键进行重试；但是对于事件流发生 Cursor 失效或 Seq 序号断裂的情况，系统拒绝静默修复，将直接抛出 <code>StreamCursorGapError</code> 或 <code>StreamCursorExpiredError</code> 强制阻断流程。</li>
        <li><strong>诊断列表截断策略：</strong>Engine 在执行结果中返回 <code>diagnostics</code> 诊断快照列表。为了防范诊断条数过多撑爆内存，系统会在诊断数量超过 <code>diagnostics_limit</code> 限制时自动执行截断，并标记 <code>diagnostics_truncated: true</code>。但在截断发生时，<code>diagnostic_counts</code> 仍会精确统计全部错误（包括已截断项）的数量，以便上层消费。</li>
        <li><strong>延迟实现的分布式错误：</strong>针对多节点协作下的时钟漂移、物理网络分区、以及跨节点配额限制，系统在当前阶段仅保留对应的接口错误定义，不做自动协商恢复。一旦检测到此类错误，系统直接通过接口向上抛出硬失败，保留故障现场。</li>
      </ul>
    `
  },

  custom_logic: {
    tag: '第三卷 · 二次开发与扩展指南',
    title: '3.1 自定义 Logic 节点开发',
    text: `
      <h3>产品概念</h3>
      <p>自定义确定性 Logic 节点用于处理无需大语言模型介入的结构化数据转换与业务逻辑判断。在 Skill 工作流中，当数据流转面临格式不一致、需要过滤特定字段、执行数学计算，或跨数据源进行合并时，开发人员通过自定义 Logic 节点实现高可靠、零随机性的计算规则。这类节点在执行上具有极低的延迟，并对相同的输入保证产生完全一致的输出。</p>

      <h3>系统行为</h3>
      <p>Studio 会持续扫描工作空间中的各 Phase 目录。在检测到 <code>LOGIC.md</code> 配置文件后，画布组件会自动解析其 Front Matter 中定义的输入输出参数，并在图形化画布上构建对应的 Logic 节点引脚与动作列表。在编译阶段，编译器会对 Python 的动作方法名与签名进行静态验证，确保动作的声明与具体的 Python 代码实现文件保持一致。在运行时阶段，系统将启动独立的隔离沙盒环境加载 Python 脚本，以防止用户代码运行时的副作用影响主进程的稳定性。</p>

      <h3>用户工作流</h3>
      <p>开发人员创建和连接自定义 Logic 节点分为以下三个步骤：</p>
      <ol>
        <li><strong>创建配置定义</strong>：在特定的 Phase 目录下创建 <code>LOGIC.md</code> 文件。在文件顶部的 Front Matter 中声明 <code>actions</code> 动作列表，并利用 JSON Schema 描述 <code>inputs</code> 与 <code>outputs</code> 的属性字段与必填项。</li>
        <li><strong>编写业务逻辑</strong>：在该 Phase 目录的同级 <code>actions/</code> 目录下创建与动作同名的 Python 文件（例如 <code>data_transformer.py</code>），在其中继承 <code>BaseLogicNode</code> 类，并重写 <code>execute</code> 方法实现核心转换逻辑。</li>
        <li><strong>画布配置连线</strong>：打开 Studio 界面，系统会自动渲染更新后的节点引脚。开发人员在画布上将上游输出引脚与 Logic 节点的输入引脚连线，再将 Logic 节点的输出引脚连接至下游输入引脚。</li>
      </ol>
      <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现画布配置连线界面。画布中展示自定义 Logic 节点，节点左侧为 inputs 对应的输入引脚，右侧为 outputs 对应的输出引脚，节点正中显示关联的 Action 动作名称。开发人员可以通过拖拽，将上游节点的输出引脚连接到 Logic 节点的输入引脚，或将 Logic 节点的输出引脚连接至下游节点的输入引脚。</b></div>

      <h3>代码规范</h3>
      <p>自定义 Logic 节点由 <code>LOGIC.md</code> 配置文件和对应的 Python 脚本共同构成其物理载体。以下为具体的代码规范范例：</p>
      
      <p>1. <code>LOGIC.md</code> 配置文件示例：</p>
      <pre><code>---
io:
  inputs:
    type: object
    required: [raw_text, min_length]
    properties:
      raw_text:
        type: string
      min_length:
        type: integer
  outputs:
    type: object
    required: [processed_text, is_valid]
    properties:
      processed_text:
        type: string
      is_valid:
        type: boolean
---
&lt;action&gt;data_transformer&lt;/action&gt;</code></pre>

      <p>2. <code>actions/data_transformer.py</code> 脚本示例：</p>
      <pre><code>from sdk.logic import BaseLogicNode

class DataTransformer(BaseLogicNode):
    def execute(self, context: dict) -> dict:
        raw_text = context.get("raw_text", "")
        min_length = context.get("min_length", 0)
        
        # 执行数据清洗与逻辑校验
        cleaned_text = raw_text.strip()
        is_valid = len(cleaned_text) &gt;= min_length
        
        # 必须显式返回字典，其属性结构须严格匹配 LOGIC.md 中 outputs 声明 of Schema
        return {
            "processed_text": cleaned_text,
            "is_valid": is_valid
        }</code></pre>

      <h3>工程契约</h3>
      <p>自定义 Logic 节点在运行时遵循严格的工程隔离契约，以确保数据流的确定性与执行安全性：</p>
      <ul>
        <li><strong>输入 Schema 白名单限制</strong>：当运行时环境调用 Action 时，传入 <code>execute</code> 方法的 <code>context</code> 字典仅包含在 <code>LOGIC.md</code> 的 <code>inputs</code> 中声明的字段。即使上游输出了多余的数据，或者黑板中包含其他上下文参数，该节点也无法感知或读取，从而实现了输入数据的严格隔离。</li>
        <li><strong>修改限制契约</strong>：任何试图通过 <code>context.set()</code>、<code>context.update()</code> 或 <code>context["key"] = value</code> 直接修改传入参数对象的行为都将失效。系统仅以 <code>execute</code> 方法显式返回的字典作为节点的输出数据。</li>
        <li><strong>输出白名单硬失败契约</strong>：动作执行完毕后，运行时会对返回值进行严格校验，一旦违反输出契约，系统会直接触发硬失败：
          <ol>
            <li>如果 <code>execute</code> 的返回值不是字典类型（例如返回了列表、字符串或 None），系统抛出错误码 <code>[F-v3-logic-action-return-invalid]</code> 并终止执行。</li>
            <li>如果返回的字典中包含了未在 <code>LOGIC.md</code> 的 <code>outputs</code> 属性中声明的键名，系统抛出错误码 <code>[F-v3-logic-output-field-undeclared]</code> 并终止执行。</li>
          </ol>
        </li>
      </ul>
    `
  },

  custom_tool: {
    tag: '第三卷 · 二次开发与扩展指南',
    title: '3.2 自定义 Tool 与 Action 编写',
    text: `
      <p>在设计和扩展智能体系统时，深刻理解自定义工具（Tool）与外层执行节点（Action）的区别，是构建高质量业务逻辑的基础。本节从概念界限、系统行为、用户工作流、代码规范及工程契约五个维度，详述自定义 Tool 的开发范式。</p>

      <h3>1. 概念界限</h3>
      <p>在系统运行架构中，外层 Action 与内层 Tool 处于不同的执行层级，拥有完全隔离的物理注册表和调用逻辑：</p>
      <ul>
        <li><strong>外层 Action（逻辑节点执行单元）</strong>：属于 <code>LOGIC</code> 阶段（Phase）的原子执行函数。它们在工作流画布中由引擎确定性地顺序调用。Action 扮演的是纯函数角色，接收只读的输入数据字典，运算后返回更新字典并直接写入系统状态黑板。大语言模型（LLM）无法自主选择执行或忽略 Action。</li>
        <li><strong>内层 Tool（智能体辅助组件）</strong>：属于 <code>AGENT</code> 阶段（Phase）的辅助逻辑组件。它们被绑定在大语言模型的 ReAct 循环上下文中，由大语言模型根据当前步骤的推理状况自主决定是否触发、何时调用以及传入何种参数。</li>
        <li><strong>物理隔离</strong>：外层的 Action 注册表（<code>ActionRegistry</code>）与内层的 Tool 注册表（<code>ToolRegistry</code>）在物理上相互独立，底层数据不互通，不进行自动转换。</li>
      </ul>

      <h3>2. System 行为</h3>
      <p>智能体引擎在处理自定义工具时，遵循明确的生命周期阶段：</p>
      <ul>
        <li><strong>静态编译阶段</strong>：编译器在解析 <code>SKILL.md</code> 文件时，读取其 YAML frontmatter 中的 <code>tools</code> 列表，同时扫描智能体正文 XML 文本节点中的 <code>@tool:工具名</code> 提及标记。</li>
        <li><strong>本地文件检索</strong>：编译器在当前 Phase 目录同级的 <code>tools/</code> 文件夹中检索与声明名称对应的 Python 文件。例如声明了 <code>custom_analysis</code>，编译器将加载 <code>tools/custom_analysis.py</code> 脚本中的函数。</li>
        <li><strong>动态注册与绑定</strong>：引擎提取脚本中带有 <code>@agent_tool</code> 装饰器的函数，结合系统内置工具（例如用于读取声明的 <code>builtin.read_artifact</code> 等），将它们统一转换为具备结构化参数模式（Schema）的工具列表，并在初始化智能体时传递给底层执行器。</li>
      </ul>

      <h3>3. 用户工作流</h3>
      <p>用户在二次开发中引入自定义工具，需完成以下开发步骤：</p>
      <ol>
        <li><strong>创建物理目录</strong>：在当前的 <code>SKILL.md</code> 同级目录下新建名为 <code>tools/</code> 的子目录。</li>
        <li><strong>编写工具函数</strong>：在 <code>tools/</code> 目录内新建 Python 文件（例如 <code>data_fetcher.py</code>），编写包含 <code>@agent_tool</code> 装饰器的具体函数，并利用 Python 类型提示（Type Hints）声明参数与返回类型，同时在 Docstring 中写入详尽的参数描述。</li>
        <li><strong>在 frontmatter 中声明</strong>：打开 Phase 所在的 <code>SKILL.md</code>，在 YAML frontmatter 的 <code>tools</code> 列表中添加工具文件名（如 <code>data_fetcher</code>）。</li>
        <li><strong>在正文步骤中提及</strong>：在 <code>SKILL.md</code> 的 <code>&lt;steps&gt;</code> 块中，使用 <code>@tool:data_fetcher</code> 语法允许标记该步骤将使用此工具，以通过编译期的可达性静态检查。</li>
      </ol>
      <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现当前 Phase 的工具绑定列表与实时调试面板，用户可以在画布侧边栏查看 @tool 提及状态，并直接对自定义 Tool 进行单步触发与 Mock 输出配置。</b></div>

      <h3>4. 代码规范</h3>
      <p>自定义工具的编写必须符合严格的类型标注与描述文档规范。以下是编写自定义工具的标准代码范例：</p>
      <pre><code>from graph_agent.core.decorators import agent_tool

@agent_tool
def check_inventory_status(item_id: str, warehouse_code: str) -> dict:
    """
    查询指定仓库中特定商品的实时库存状态与可用量。

    参数:
        item_id: 商品的唯一识别码，例如 "SKU-99081"
        warehouse_code: 仓库区域代码，例如 "WH-EAST-01"
    """
    # 此处为具体的业务执行逻辑
    inventory_data = {
        "sku": item_id,
        "warehouse": warehouse_code,
        "available_stock": 150,
        "status": "in_stock"
    }
    return inventory_data</code></pre>

      <h3>5. 工程契约</h3>
      <p>为了保障引擎运行与工具执行的可靠性，系统制定了以下工程契约：</p>
      <ul>
        <li><strong>参数 Schema 自动推导</strong>：引擎通过反射解析函数的 Python 类型提示和 Docstring 描述，自动构建出符合 Pydantic 与 OpenAPI 规范的参数校验 Schema，大语言模型依据此 Schema 生成精确的 JSON 调用参数。</li>
        <li><strong>异常安全阻断（ToolError 转化机制）</strong>：当工具函数在执行过程中因网络或计算错误抛出普通 Python 异常时，引擎内置的异常处理中间件会截获该异常，并将其转换为 <code>ToolMessage(status="error")</code> 返回给智能体循环。这能向大语言模型提供具体的报错摘要和异常类型，使其可以在循环中尝试修正参数并重试，而不会导致整个 Phase 执行崩溃。</li>
        <li><strong>控制流异常豁免</strong>：对于特殊的控制流异常（例如用于流式中断或人工介入信号的系统级异常），中间件不会拦截，而是允许其直接向外层抛出，以便触发图执行的挂起和交互状态。</li>
        <li><strong>最大迭代次数限制</strong>：若大语言模型因参数错误导致工具调用持续抛出异常，执行流程在达到 <code>SKILL.md</code> frontmatter 中配置的 <code>max_iterations</code> 铺平阈值后，会强制终止智能体循环并向调用端抛出执行超时错误。</li>
      </ul>
    `
  },

  custom_middleware: {
    tag: '第三卷 · 二次开发与扩展指南',
    title: '3.3 自定义 Engine Middleware (中间件)',
    text: `
      <p>在构建复杂的智能体应用时，开发者通常需要处理内容安全审查、Token 消耗统计、调用链路追踪等与具体业务逻辑解耦的系统级需求。通过自定义 Engine Middleware（中间件），开发者能够以非侵入式的方式，对引擎的核心执行流程进行全局拦截和增强。</p>

      <h4>一、 产品概念</h4>
      <p>Engine Middleware 是引擎执行链路中的全局拦截器。它将敏感词审查、Token 统计等横切关注点与 Skill 的核心业务逻辑解耦。中间件链采用链式调用机制，每个中间件都可以访问当前工作流的状态，并在执行前后施加特定的逻辑控制，保障了 Skill 业务代码的纯净性与可维护性。</p>

      <h4>二、 系统行为</h4>
      <p>引擎在装配期按照配置的拓扑顺序排定并初始化中间件链。在运行时，引擎的内层循环会在特定的生命周期节点自动触发中间件的钩子方法：</p>
      <ul>
        <li><strong>before_node_execute</strong>：在每个图节点（Node）开始执行前触发。</li>
        <li><strong>after_node_execute</strong>：在每个图节点（Node）执行完毕并输出结果后触发。</li>
        <li><strong>before_model</strong>：在向大语言模型（LLM）发起调用请求前触发，此时可用于审查输入提示词或动态注入上下文。</li>
        <li><strong>after_model</strong>：在模型（LLM）返回响应后、引擎解析输出前触发。</li>
        <li><strong>wrap_tool_call</strong>：在工具（Tool）被调用时进行包装，能够全面控制工具的输入参数与执行结果。</li>
      </ul>

      <h4>三、 用户工作流</h4>
      <p>自定义中间件的配置与使用遵循以下工作流程：</p>
      <ol>
        <li><strong>定义类结构</strong>：开发者继承系统基类 <code>GraphMiddleware</code>，实现所需的生命周期方法。</li>
        <li><strong>编写逻辑</strong>：在相应的生命周期钩子中编写拦截逻辑（如在 <code>before_model</code> 中检测输入是否包含违禁词）。</li>
        <li><strong>注入引擎</strong>：在引擎实例初始化时，将自定义的中间件对象加入到配置数组中，引擎在装配期会自动排定链式关系。</li>
      </ol>
      <div class="lesson-alert alert-amber"><b>【界面占位符】：这里在产品界面呈现中间件链的配置与管理面板。用户可以在面板中查看当前已挂载的中间件列表，通过拖拽调整其执行顺序，并为特定的中间件配置动态参数。</b></div>

      <h4>四、 工程契约与代码规范</h4>
      <p>所有自定义中间件必须继承 <code>GraphMiddleware</code> 并严格遵循方法签名契约。以下是一个实现内容安全审查与工具调用审计的中间件代码范例：</p>

  <pre><code>from typing import Any, Callable, Sequence
  from graph_agent.middleware import GraphMiddleware
  from graph_agent.core.exceptions import GraphExecutionError

  class AuditAndSecurityMiddleware(GraphMiddleware):
      def __init__(self, blocked_words: Sequence[str] | None = None) -> None:
          super().__init__()
          self._blocked_words = blocked_words or []

      def before_node_execute(self, node_name: str, state: Any) -> None:
          # 节点执行前的状态审计
          pass

      def after_node_execute(self, node_name: str, state: Any, output: Any) -> None:
          # 节点执行后的状态变更记录
          pass

      def before_model(self, phase_name: str, messages: list[Any]) -> None:
          # 调用模型前的内容安全审查
          for msg in messages:
              content = getattr(msg, "content", "")
              if isinstance(content, str):
                  for word in self._blocked_words:
                      if word in content:
                          raise GraphExecutionError(
                              message=f"Surfaced content violated security policy due to blocked word: {word}"
                          )

      def after_model(self, phase_name: str, response: Any) -> None:
          # 模型输出审计
          pass

      def wrap_tool_call(
          self,
          request: Any,
          handler: Callable[[Any], Any]
      ) -> Any:
          # 包装工具调用以记录执行耗时和捕获工具内部错误
          try:
              result = handler(request)
              return result
          except Exception as e:
              # 将工具层的未捕获异常包装为规范的执行错误
              raise GraphExecutionError(
                  message=f"Tool call failed during middleware interception: {str(e)}"
              ) from e
  </code></pre>

      <h4>五、 错误和边界</h4>
      <p>中间件作为系统级防护网，其异常处理遵循严密的硬失败契约：</p>
      <ul>
        <li><strong>硬失败中断</strong>：中间件内抛出的任何 <code>GraphExecutionError</code>（或其继承的子类）都会被引擎捕获，直接作为系统硬失败向外抛出。引擎将立即中断当前工作流的执行，防止错误数据扩散或违规内容输出。</li>
        <li><strong>未捕获异常</strong>：非 <code>GraphExecutionError</code> 的普通 Python 异常会被引擎自动包装为 <code>GraphExecutionError</code>，并触发硬失败流程。</li>
        <li><strong>装配期异常</strong>：若在装配期检测到中间件链的配置存在循环依赖或缺失必要依赖，系统将在启动前抛出 <code>LoaderError</code> 并拒绝启动。</li>
      </ul>
    `
  }
});
