---
doc: design-units-index
status: drafted（活注册表、不入哈希锁；PM 已审 decomposition/owner 2026-06-05：25→22 单元、四层 owner 改回 ③b；22/22 owned-lock=locked（12 integration-locked、10 external floating-draft）、63 档已 FROZEN 2026-06-05；锁态由 _design-unit-lock-snapshot.json 快照测试强制；新增单元/改 spans 须 owner 评审并同步快照）
aligns_with: ../../development/design-doc-standards/02-audit-standard.md（R8）· 01_workflows/INDEX.md（轴① 走查索引，不同物）
---

# Studio MVP1 设计单元索引（轴③ · R8 枢纽）

> **这是设计单元 INDEX**（横切单元 × 模块切面 × owner × binds_code × 锁），**不是** `01_workflows/INDEX.md`（那是轴① workflow 走查索引）。
> **职责**：只记**映射**，不重述实现 / 决策（实现 SSOT 在能力模块就近，决策原话就近 + workflow 留底）。
> **去重铁律**：每个**切面**只有一个 owner 模块写实现；别的模块写同切面 = 重复违规（扫本表「切面 × owner」即照出）。
> **锁（owner-scoped 三态，见标准 §1.6）**：`owned-lock`（studio 自有切面锁住?）· `external-binding`（外部 `(引)` 切面绑定态:none/floating-draft/pinned-draft/frozen-pinned/stale）· `integration-lock`（派生:owned-lock=locked 且 external∈{none,frozen-pinned} 才端到端锁）。**当前:22/22 `owned-lock=locked`;12 个纯 studio 单元 `integration-lock=locked`;10 个含 engine/gateway `(引)` 的单元 `external-binding=floating-draft`、`integration-lock=unverified`**（外部契约还在 drafted,不背书端到端）。
> **FROZEN(2026-06-05)**：63 档语义审计全 PASS(全量脚本 + 逐档 + codex 双向对抗 + M4 复核)后已逐档 `FROZEN`,哈希锁底账见 [`_audited-ready-hashes.json`](./_audited-ready-hashes.json)、由 `test_doc_hash_lock.py` 强制。**改这 63 档须 owner 批准并走 exemption 或同步底账**(否则哈希锁拦截)。本 INDEX 为活注册表、不入哈希锁。
> **引擎拥有的切面**：studio 单元若依赖 engine 拥有的契约（子图 path 解析 / golden 落点 / skill 语法 / 错误码 / resolver 协议 / checkpoint），owner 标 `engine:<module>` 且 **只引用 `docs/engine/mvp1/` SSOT、不在 studio 复制**。
> **⚠️ 标记**：`binds_code` 里带 ⚠️ 的 = 已验真的 code↔design drift（现状 vs 目标冲突），按 decision-3 忠实落进对应 baseline + 警告。本表先收口，逐档 retrofit 时把 ⚠️ 同步到各 baseline 的「测试锚点」差异表。
> **状态**：spans / owner 已逐档 retrofit + 审计复核（R8 双射 22=22、owner 唯一性 PASS;owner 切面单一标 `(owner)` 可机器核验），无 † 待复核项。

## 单元表

| 单元 | 源 workflow / atom action | spans（切面 → 模块 · 角色;角色∈owner/消费/引/落点） | binds_code 主符号（⚠️=已验真 drift;✅=drift 已修复并附代码证据） | owned-lock | external-binding | integration-lock |
|---|---|---|---|---|---|---|
| `subgraph-path-inline-drilldown` | 02_authoring D7/G2/T5/T6 | path 解析→`engine:02-resolver`(引) · 语法→`engine:skill-syntax`(引) · 落点→`engine:physical-layout`(引) · inline 展开/下钻/面包屑→`canvas`(owner) · 新建子图/默认落点 UI→`graph-authoring`(owner) · 未解析导入→`assets`(owner) · 写盘→`native-fs`(引) | `canvas-authoring.ts:defaultPhaseMarkdown` ⚠️写 `mode: subgraph` 旧格式(违 D7) · `SubgraphInline.tsx` ⚠️假数据 · `AssetsPanel.tsx` ⚠️读 `sub_skill_ref` 旧形态 | locked | floating-draft | unverified |
| `compile-stage-gate` | 03_compile(owner=compile-lint，PM 已决) | gate 规则→`compile-lint`(owner) · 触发 UI→`center-action-bar`(消费) · gate 影响→`predict`(消费; 不重写) · gate 影响→`run-execution`(消费; 不重写) · 报错呈现→`compile-lint`(消费; drawer 见 compile-lint-structured-error) · 状态源→`state-engine`(消费) | `Workspace.tsx:onPredict/onRun` ✅已接真处理器 handlePredict/handleRun(Workspace.tsx:2091 / 2153,2026-07) · `CompileErrorPanel` ⚠️底部浮层 vs drawer 决策(DEF-010) | locked | none | locked |
| `predict-execution` | 04_run-and-verify | predict 模式机制→`predict`(owner) · 入口 UI→`center-action-bar`(消费) · 干跑 mock→`engine:06-seam/01-models`(引) | `Workspace.tsx:onPredict` ✅已接 `postPredictRun`(Workspace.tsx:2115,2026-07) | locked | floating-draft | unverified |
| `run-execution-node-status` | 04_run-and-verify | run 机制 + 批量/循环触发与展示→`run-execution`(owner) · 循环原语→`engine:04-run-outer/02-iterate`(引) · 节点灯/边 UI→`canvas`(消费) · 事件到节点态投影→`state-engine`(owner) · 事件源→`engine:02-observability`(引) | `Workspace.tsx:onRun` ✅已接 handleRun→startRun(2165) · `GraphCanvas:statusByNodeId` ✅真跑态 deriveNodeStatuses(runStream.events)(665 / 2608) · `build-nodes.ts:buildNodes` ✅消费真 statusByNodeId(270 / 375) · WS bridge ✅useRunStream(583) | locked | floating-draft | unverified |
| `golden-per-agent-node` | 04_run-and-verify（golden-eval 段） | studio golden 编辑/diff→`golden-eval`(owner) · 落点→`engine:physical-layout`(引; .workspace/golden) · eval→`engine:05-run-inner/06-golden-eval`(引) · run 播种→`run-execution`(消费) · copilot bar→`copilot-assist`(消费) | `golden_diff.py:set_golden_baseline_for_run` ✅已落 per-node golden cases(node_id 参数 + cases/{id}.json;golden_diff.py:58-79 / 186-208,2026-07) | locked | floating-draft | unverified |
| `compile-lint-structured-error` | 03_compile(DEF-010) | drawer 呈现→`compile-lint`(owner) · 底部布局与 trace 协调→`timeline`(消费) | `Workspace.tsx:CompileErrorPanel` ⚠️底部浮层/toast vs drawer | locked | none | locked |
| `phase-field-whitelist` | 02_authoring | 字段白名单/Properties→`phase-editing`(owner) · 字段权威→`engine:skill-syntax`(引) | `phase-frontmatter.ts:parsePhaseForm` ⚠️读写 V2.x `mode/system_prompt/exit_contract/python_callable` 旧格式 · `PropertiesPanel.tsx` ⚠️stale phase form + edge JSON dump | locked | floating-draft | unverified |
| `node-properties-role-test` | 00_settings-ux-spec §2.7（#11） | role 测试 / 状态投影机制→`studio-settings`(owner，复用 §2.5 测试 + §2.4 role-fit) · 节点 Properties UI(role 旁 Test 键 + 状态)→`phase-editing`(落点) · 节点 Properties UI→`properties`(落点) | 后端 `llm.py:POST /api/llm/roles/{name}/test`(live) · 前端 Properties role 行 Test+状态 ✅已建(PropertiesPanel.tsx:696 runRoleTest + roleTestStatusBadge,复用 settings role-test job,2026-07) | locked | none | locked |
| `conflict-overwrite-resolution` | 02_authoring | 冲突呈现→`conflict-overwrite`(owner) · 触发面→`canvas`(消费) · 触发面→`editor`(消费) · 触发面→`file-editing`(消费) | `canvas-authoring.ts:handleSaveConflict`(与顺序覆盖 overlay 两条 UX 未统一) | locked | none | locked |
| `debug-resume-checkpoint` | 05_debugging 场景C | 节点级 resume UI→`debug-resume`(owner) · checkpoint/resume→`engine:04-run-outer/03-checkpoint`(引) | `runs.py:resume_run` ✅已实现(非 501;runs.py:259-313 → engine.py:1036,2026-07) | locked | floating-draft | unverified |
| `trace-dot-blackboard` | 04_run-and-verify | dot/黑板语义→`trace-observability`(owner) · dot 渲染→`canvas`(消费) · inspector→`properties`(消费) · inspector→`timeline`(消费) · 只读消费→`editor`(消费) · 只读消费→`debug-resume`(消费) · 事件源→`engine:02-observability`(引) | `ContextEdge.tsx:getMockEdgeContext` ✅假黑板已删,dot=edgeContextFromEvents 真派生 + staticEdgeInference 静态推断(GraphCanvas.tsx:1429-1434) · `TracePanel`/`useRunStream` ✅已挂载(Workspace.tsx:583 / Panels.tsx:237) | locked | floating-draft | unverified |
| `native-rust-writer` | 全局 D12 | Studio 自有写入唯一写者→`native-fs`(owner) · 本地写→`file-editing`(消费) · 本地写→`editor`(消费) · 本地写→`publish`(消费) · 本地写→`skill-workspace`(消费) · Copilot SDK Write/Edit 例外→`copilot-assist`(消费) | `client.ts:writeSkillFile` ⚠️走 FastAPI 非 Rust · publish 本地 Package/Export 的 Rust writer 未落地，当前成品 artifact bytes 仍由后端 `EngineAdapter.compile/_zip_directory` 产生 · `tauri.ts:open_in_cursor/terminal/codex` ⚠️D3 死代码 | locked | none | locked |
| `workspace-open-folder-mru` | 01_workspace | 工作区/MRU/Remove→`skill-workspace`(owner) · 落盘→`native-fs`(引) · Home UI→`welcome`(消费) | `skills.py:import_existing` ⚠️GRAPH/SKILL 门禁(违 D11/D2) · `skills.py:list_skill_summaries` ⚠️注册表聚合 · `WelcomePage.tsx` ⚠️读 `/skills` 注册表 | locked | none | locked |
| `io-panel-artifacts-test-inputs` | 02_authoring/04 | i/o 面板→`input`(owner) · 切面消费→`phase-editing`(消费) · 切面消费→`predict`(消费) · 切面消费→`run-execution`(消费) | `InputPanel.tsx:inputFiles` ⚠️假 `input/schema.json` · `test_inputs.py:create/delete_test_input` ✅已实现(204;test_inputs.py:112 / 152,2026-07) | locked | none | locked |
| `publish-artifact-autocommit` | 06_eval | 发布/ProductArtifactStore release→`publish`(owner; 发布≠git push,autocommit=本地存档) · 远端 Artifact Registry→`publish`(owner; 后置 sync) · 落盘→`native-fs`(引) · 快照→`local-history`(落点) | `skills.py:publish_skill` · `publish_pipeline.py:ProductArtifactPublisher` · `product_store_local.py:LocalProductArtifactStore` · Package/Export Rust writer 未落地 ⚠️ | locked | none | locked |
| `local-history-snapshot` | 05_debugging | **快照列表/显示**→`local-history`(owner,只显示) · **快照写机制**→`publish-artifact-autocommit`(引,非本单元) · 快照写机制→`native-fs`(引,非本单元) | `HistoryPanel.tsx` ⚠️仅 git snapshot，run detail/batch 未接 | locked | none | locked |
| `settings-six-state-provider-health` | 00_settings(`00_settings-ux-spec.md:255` 锁 6 态) | 6 态标准投影(内核)→`graph-agent-gateway`(引; ③b) · 6 态消费边界(投影 staging,应下沉 ③b)→`gateway`(owner; ③a) · HTTP 壳→`llm-copilot-http-api`(owner; ③a) · role-fit 消费 + UI 渲染→`studio-settings`(消费; ③a) · role-fit 消费 + UI 渲染→`settings`(消费; ③a) | `LlmRolesTab.tsx:roleTestStates`/`CopilotTab.tsx:routeStatusOverrides` ⚠️前端易失真相 · `llm.py:_role_test_jobs` ⚠️内存 job store · ⚠️`llm_state_projection.py` 6态投影该下沉 ③b | locked | floating-draft | unverified |
| `model-group-role-materialization` | 00_settings §2 | materialize 编排 + model group/endpoint 标准化(内核)→`graph-agent-gateway`(引; ③b) · materialize 消费边界(应下沉 ③b)→`gateway`(owner; ③a) · HTTP 壳→`llm-copilot-http-api`(owner; ③a) · UI 编辑角色/绑定 + 交 ③b 物化→`studio-settings`(消费; ③a) · UI 编辑→`settings`(消费; ③a) | `llm.py:router` ⚠️巨型路由(HTTP glue+probe+materialize+Probe Catalog 混,materialize/catalog 内核该下沉/收敛 ③b) · `llm_probe_catalog.py:sync_remote_probe_catalog` 仍需远端源注入边界 | locked | floating-draft | unverified |
| `copilot-sdk-test-parity` | 00_settings §3 | copilot 测试=真实 SDK 调用→`copilot-assist`(owner; ③a 拥有 SDK 运行时,测试须同路) · copilot_chat route 解析→`graph-agent-gateway`(引; ③b) · route 消费边界→`gateway`(owner; ③a) · HTTP 壳→`llm-copilot-http-api`(owner; ③a) | `llm.py:_probe_copilot_sdk_tool_call` ⚠️走 `AsyncAnthropic`，真实 chat 走 `ClaudeSDKClient`，测试路径不等价 | locked | floating-draft | unverified |
| `copilot-session-persistence` | 03/copilot | session 持久化(D8)→`copilot-assist`(owner) · 消息渲染(含 ThinkingBlock 翻译/@mention)→`copilot-assist`(owner) · Copilot SDK Write/Edit 自写→`copilot-assist`(owner; D12 例外) · session 落盘→`native-rust-writer`(引) · UI→`copilot`(owner; region) | `copilotStore.ts:reset` ⚠️内存态，未满足 D8 持久化 · `copilot.py:_translate_sdk_message` ⚠️未翻 ThinkingBlock · Settings Copilot ⚠️仍依赖 `mock-copilot-data.ts` | locked | none | locked |
| `shell-runtime-gate` | 00_shell | 外壳即时渲染/sidecar(D10)→`shell-layout`(owner; 含 settings overlay + copilot slot) | `App.tsx:RuntimeGate` ⚠️全屏 gate(违 D10 eager sidecar) | locked | none | locked |
| `i18n-error-code-ui-copy` | 04_platform/i18n(DEF-015) | i18n 架构(前端单权威)→`i18n`(owner; 错误码由各 region 经 t(key) 消费) · 收编错误码→`studio-settings`(消费) | `llm-error-messages.ts:composeTestErrorMessage` ⚠️仅英文 catalog，react-i18next 骨架未落 | locked | none | locked |

## 重复 owner 风险（待 retrofit 时按本表「切面 × owner」消解）
- **stage gate**：`compile-lint` / `center-action-bar` / `predict` / `run-execution` / `state-engine` 都描述 Compile→Predict→Run 链 → owner=`compile-lint`(gate 规则)，其余消费、不重写。
- **6 态 / materialize(内核归 ③b)**：**6 态标准投影 + materialize 编排 + endpoint 标准化 = ③b `graph-agent-gateway`**(公共内核,studio 只引不复制);`studio-settings`、`settings`(③a)只做 UI 编辑 + 消费投影;`gateway` platform = ③a 消费/适配边界,HTTP 壳 = `llm-copilot-http-api`(③a)。**(2026-06-05 改回四层反转:内核归 ③b,不挂 studio-settings)**
- **trace dot / 节点态**：`trace-observability`(语义) / `state-engine`(事件→态) / `canvas`(渲染) 各占一切面，勿混。
- **D12 本地写**：Studio 自有写入唯一 owner=`native-fs`，所有 Studio 写者引它；Copilot SDK Write/Edit 是 MVP1 明确例外，归 `copilot-assist`。
- **golden**：`golden-eval`(studio 编辑/diff) vs `engine:physical-layout`(落点) vs `engine:06-golden-eval`(eval) 切清。

## 真空 / 债
- 当前所有单元 `binds_code` 多为 ⚠️ drift（现状 ≠ 目标）；按 decision-3 忠实落进各 baseline + 警告，**不改代码**。**(2026-07 对账更新:compile-stage-gate / predict-execution / run-execution-node-status / golden-per-agent-node / debug-resume-checkpoint / trace-dot-blackboard / node-properties-role-test / io-panel test_inputs 等 spine + run + trace + golden + resume drift 已由 6-7 月 PR 修复,本表 binds_code 已逐条标 ✅ 并附代码 file:line;对应 baseline 的旧 ⚠️ 待后续批次同步。)**
- **(锁语义修正 2026-06-05)** 单元锁改 owner-scoped 三态(见标准 §1.6):22/22 `owned-lock=locked`;12 个纯 studio `integration-lock=locked`;10 个含 engine/gateway `(引)` 的 `external-binding=floating-draft`、`integration-lock=unverified`(外部契约还在 drafted)。锁态机器强制由 `_design-unit-lock-snapshot.json` 快照测试提供(codex 建中);外部 pin 台账推迟至 engine/gateway 稳定(见 `docs/design/studio-mvp1-lock-semantics-decision.md`)。
- owner spans 已审计复核（无 † 待复核项）；后续改 `locked` 单元的 owner / spans 须同步 `_design-unit-lock-snapshot.json` 快照(或走 exemption)。
