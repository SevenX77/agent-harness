总裁决：rework

[P0] 结论：R2-P0-2 仍未真实落实，门②以另一组项目替换原始 12 类，“十二项全部闭合”不成立。依据：原 12 类唯一逐项清单见 `domain-reports/ac2a0cf93205b9ba4_v1.md:298-319`，包括 `core.manifest`、`RunSession`、`result_contracts`、`run_layout`、`topology_projection`、`graph_serializer`、`core.result`、`SkillLoader`、`make_event_envelope`、`compile_skill`、`ArtifactRef`、`LLMProviderError`。v3 `inventory-synthesis-draft.md:136-151` 仅较准确覆盖其中四项，遗漏 manifest、RunSession、topology_projection、core.result、ArtifactRef、LLMProviderError 等，却以零消费的 `evaluate_golden_baseline`、`compute_diff`、`generate_heuristic_stub`，Studio 重造的 checkpoint validity、无具体符号的 resume/HITL 及不同对象的 event union 补足数量。第 12 项还混淆两仓：主仓 Studio 真实消费 graph_serializer（G2 `domain-reports/aad870f45bd77060f_v1.md:89-94`），零消费的是新仓同名 seam（`:123-132,171`）；“接线或冻结”也允许画布断路过门。建议：以原 12 类为固定基线，逐项映射至明确 Port、Studio 重写或删除；每项给出具体符号、唯一 owner、正向消费断言和内部 import 归零证据，新增问题另列，不得替换原项凑数。

[P0] 结论：R2-P0-1 仍未闭合，§4 所称“契约/实现解耦”不能解除 ModelResolver 的实现硬依赖，当前顺序仍可能在网关实现未闭合时进入删直连和 host-native/handoff 路径。依据：v3 `:128-134` 一面要求每步完成后才进入下一步，一面在 M9/S4 声明完成 Port 对接和 gateway 权威实现，却允许 M4/M6/M8 继续加固且不阻塞步 2。G3 `domain-reports/ac08659a6d5b556a3_v1.md:239` 明列 M9 依赖 M4/M6/M8；这些依赖会改变边界语义与形状：M6 把 resolve/handoff 裸字符串收成枚举（`:236`），M8 收口 role materialize（`:238`），M4 则会改变当前 `ResolvedRouteChain` 所携 URL 计划语义。上位 §4.5、§5.9 要求的是可成为 embedded 第一执行路径的 gateway Port 对接，不是接口草案定稿；§5.3 同时规定 engine 只依赖稳定 Port，gateway 是其权威 adapter 实现。建议：允许 Port 草案先行，但设置不可绕过的硬边：`M4/M5/M6/M8 → M9 可运行实现及正向 e2e → 主链步3`；Port 契约由 engine/执行侧拥有，gateway 仅拥有 adapter 实现。

[P1] 结论：§4 供给链的串行拓扑与最新 G3 订正不符。依据：v3 `:134` 写成 `M3→M4→M5→M6→M8→M7`，并仅保留 M4 的 `INV-1..3`；G3 最新订正 `domain-reports/ac08659a6d5b556a3_v3.md:84-91` 已将 M4 从“M3 之后”提升至与 M0 并列的最高档。原依赖只支持 M7、M8 均依赖 M6，不支持 `M8→M7`，且最新订正为 M4 给出四条验收断言。建议：重画为 M4 处于最高档、`M5→M6`、M6 之后 M7/M8 分叉、M8 再硬连 M9，并恢复四条 M4 验收断言。

[P1] 结论：§1 的 J1–J7 旅程主体及失败传播与域报告一致，但汇总依赖边存在一处漏边和一处归属漂移。依据：v3 `:21` 与 G4 `domain-reports/aeed340fbc0847f26_v1.md:117` 均明确 embedded 执行经 ModelResolver 进入域 6，但 `:27` 仅列 `域3→域2→域6`，传递链不能替代 J3 运行时的直接 `域3→域6`。v3 `:27` 又列 `域10→域12`，而 `:63-64` 已将域 12 收窄为 sidecar 生命周期/liveness，并规定 B8 独立、不得预并入域 12；J5 `:23` 和 G7 `domain-reports/a66fac8a014fefd6b_v1.md:97` 实际需要写闸、native-fs、checkpoint 等平台能力。除这两处汇总边问题外，J1–J7 步骤和失败传播均获 G1–G7 支持，不存在其他冲突。建议：补充 `域3→域6` 直接边；将域 10 的平台依赖拆至 B8 以及经资格审确定的 native-fs、checkpoint、CLI owner，不再以域 12 兜底。

[P2] 结论：接受 R2-P1-1“部分成立”的收窄；矩阵拟归属并未锁定 owner，DAG 可以作为暂定排期拓扑保留，但其可执行边界还应明示。依据：v3 `:35` 将 L3 和矩阵标为待资格审、拟归属，资格不通过即回炉且不得先实施；`:175` 又要求立项时逐个补资格审。这一全局门足以避免把 DAG 误作最终 owner 裁定。建议：补充声明 DAG 为 provisional；任一候选资格审失败即视相关节点和边失效，必须重新生成 DAG，不得沿旧图实施。

无发现/接受项：R2-P0-3 已真实落实，v3 `:160` 仅保存脱敏结构快照和不可逆摘要，并禁止复制含 `api_key` 的 `llm_credentials` 原件；R2-P1-2 已落实，MoirAI 归域 10、run 终局归域 3，文本解码与 frontmatter 解析分行；R2-P1-3 已落实，B8 保持独立候选且不预并域 12；R2-P1-4 的系统旅程主体已落实，仅存在上述新发现的依赖边问题；R2-P1-5 已落实，联合 catalog 的 owner、命名空间、投影闭集与 T8 同批锁定；R2-P1-7 已落实，批 B 具备原生 v1 语料范围、owner 和验收，并与门⑤区分。接受 R2-P1-1 按上述边界收窄。接受 R2-P1-6 收窄：G5 `domain-reports/ac68cf30433471aa0_v1.md:130-139,254` 明文支持 engine baseline 中的 compile 非 fatal，Studio compile-fatal 属于 drift；v3 `:185` 已保留该语义，真正待裁的是 predict/golden-eval，并已交 E-T5 完整 Effect 推导，无法推出唯一解时上交裁决。
