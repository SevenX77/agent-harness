## 增补（后台扫描结果并入）

后台那条更宽的 `&lt;phase&gt;` owner 扫描（覆盖 `apps/studio`，我先前的 Grep 替代只覆盖了 engine 包）跑完了。三点增量，其中一条要**更正**我报告里的一处隐含判断。

### 增补 1｜旧格式 writer 有 3 个 tag 生成点（域一②B 加强）

不只是一个正则。writer 侧还自建 `&lt;phase&gt;` 文本**生成**：

- `core/graph_serializer.py:104` — `f"&lt;phase {' '.join(attrs)}&gt;{phase.id}&lt;/phase&gt;"`
- `core/graph_serializer.py:106` — `f"&lt;phase&gt;{phase.id}&lt;/phase&gt;"`（无属性分支）
- `core/graph_serializer.py:152` — `f"&lt;phase&gt;{phase}&lt;/phase&gt;"`（另一函数，另一形状）

即：**1 个读正则（`loader.py:1837`）+ 1 个写正则（`:156`）+ 3 个生成点**，五处各自持有「`&lt;phase&gt;` 标签长什么样」的知识，无共享常量。这条把域一 F-T9（若裁决保留 XML 载体则必须消灭 writer 影子 parser）的工作量从「合并两个正则」抬到「统一读/写/生成五处」。

### 增补 2｜更正：Tauri 下的 vendored engine 副本不是源 owner，但是**更严重的东西**

扫描命中 3 份 `apps/studio/tauri/{,target/debug/,target/release/}vendor/site-packages/graph_agent/core/{loader,graph_serializer}.py`。我核了：**全部 gitignore、未 tracked**（`apps/studio/tauri/.gitignore:3 /target/`、`:7 /vendor/site-packages/`；`git ls-files` 空）。所以它们**不**计入我报告里的 parser/模板 owner 计数 —— 那部分计数不变。

但它们暴露了一条我漏掉、且对本次交接影响更大的事实：

- `apps/studio/backend/scripts/build_vendor.py:4-16, 86` — vendored sidecar 由 uv workspace closure 驱动，**钉死本地 `packages/graph-agent` / `graph-agent-gateway`**
- `AGENTS.md:78` — 桌面 app **永远** import `vendor/site-packages` 冻结快照，**dev 构建也一样**
- `apps/studio/tauri/tauri.conf.json:8`（`beforeBuildCommand`）会跑 `build_vendor.py`；但 `:7`（`beforeDevCommand`）**只跑 `ensure_vendor.js`，不跑 `build_vendor.py`**
- `AGENTS.md:101` 明写「"only re-run build_vendor.py when dependencies change" caveat is **INCOMPLETE**」；`.ah/VERIFY.md:45` 把重跑列为**手工**验证步骤（AGENTS.md 工作流第 7 条）
- **无任何自动 staleness 门禁**

结论：桌面 app 跑的从来不是仓里的 engine 源码，而是构建期快照。因此「冻结 `packages/graph-agent`、由 `graph_skill_runtime` 接管 engine」这件事**在 `build_vendor.py` 重新指向之前对交付物零效果**，而且是**静默零效果** —— 装出来的 app 会继续用旧格式 reader（`&lt;phase&gt;` XML、89 码）和旧 `llm_role` 行为，编译期没有任何信号。这正是我报告里两个域所有跨仓分歧能在「切换已完成」宣告之后继续存在于产品里的物理机制。

**新增工单**

| ID | 优先 | 内容 |
|---|---|---|
| **X-T1** | **P0（与 D-T1 同级，交接前置）** | `build_vendor.py` 的 workspace 依赖从 `packages/graph-agent` 重指向 `graph_skill_runtime`，并加一道 staleness 门禁：vendored snapshot 的 engine 版本/哈希与当前 engine owner 不符即构建失败。否则 F-T2/F-T3/D-T1 全部只在仓里成立、在装出来的 app 里不成立 | `apps/studio/backend/scripts/build_vendor.py:4-16,86`；`apps/studio/tauri/tauri.conf.json:7,8`；`AGENTS.md:78,92,101` |

### 增补 3｜一条线索查完，非缺陷（记录以免重复挖）

扫描命中主仓 `core/error_registry.py:153` 的 remediation 文案 `'修正 `&lt;phase ... output&gt;` 标记'`。我核了新仓对应行：新仓 registry **无任何** `&lt;phase&gt;` / `&lt;node&gt;` / `GRAPH.md` 残留（`grep` 计数 0），相应 remediation 已改写为 `LOGIC.md`/`SUBGRAPH.md`/`AGENT.md` 口径（`src/graph_skill_runtime/core/error_registry.py:179-180`）。主仓那句在 v0.3 格式下是**正确**文案。→ 两仓都无缺陷，remediation 文案迁移是干净的。这也是新仓格式收敛质量的一个正面证据点（与域二④「doc_link 未迁」形成对比：**语义文案迁了，文档指针没迁**）。

---

其余结论不变。域二 D-T1（`DEFAULT_LLM_ROLE` 违反 2026-08-31 用户裁决）仍是唯一「新 engine 已违反刚生效裁决」的项；现在它旁边多了 X-T1 —— 两者合起来才构成完整的交接门：**一个保证新 engine 行为正确，一个保证桌面 app 真的用上新 engine。**
