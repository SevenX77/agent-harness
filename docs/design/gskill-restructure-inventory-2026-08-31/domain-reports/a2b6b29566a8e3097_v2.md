## 报告修正(基于 background 搜索完成 + 决议文档已定位)

### 修正 1 · 决议文档存在,我原报告的"附注 3"错了

`docs/design/gskill-restructure-decision-2026-08-31.md` **确实存在** —— 在 worktree 里,不在 main:

- `.worktrees/docs-restructure-decision/docs/design/gskill-restructure-decision-2026-08-31.md`(37008 bytes)
- 分支 `docs/restructure-decision` @ `44cf1ffa`;main @ `d034e5f1`(`git worktree list`)

我原先只查了主工作树的 `docs/design/`,漏了 `.worktrees/`。**结论不变**(brief 为准),但该文 §8③ 逐字给出了本域应然,与 brief 完全同源:

&gt; 「边界 fail-fast:非法输入在**膜上**就拒绝;错误走**统一信封**(含跨域请求所需的 CORS 头);原因码入册 —— 门禁 + 原因码注册表」

即 ③A2/③B 全部落在该文的**封闭验收集**内,不是我自选的口径。

### 修正 2 · 契约生成基建:确认不存在(背景搜索完成)

全仓 `openapi` 命中仅三类,**无一是生成链**:vendored FastAPI 自身源码(`tauri/target/debug/vendor/site-packages/fastapi/openapi/*`)· cargo fingerprint JSON · `test_api.py`(两处断言)。另有同一 worktree 的镜像副本。⇒ 工单 T12–T14 全部成立。

### 修正 3 · 我的 ⑦ 有方法缺口(对照决议 §7.2)

决议要求的四件事,我做了两件:

| 要求 | 我做了吗 |
|---|---|
| split-first | ✅ |
| 八项治理义务 | ✅ 逐候选过了 |
| 三类正向合并证据 | ✅ |
| **三向反事实资格判据**(删掉/并进/拆更细,各是否毁掉行为区分) | ❌ **未做** |
| **膜与原子性分开各审** | ❌ 只审了膜(①1.3),未单独审原子性 |

⇒ ⑦ 的 7 个模块判定应视为**未过完整资格检验**,需补这两道。

### 修正 4 · 死码结论按 §8⑦「先查裁决」纪律复核 —— 两条改判

决议 §8⑦ 要求删死码前用 `git log -S` 区分「主动下线」与「忘了接线」。我原报告跳过了这步。补做结果:

| 项 | 引入 | `git log -S` 判定 | 对工单的影响 |
|---|---|---|---|
| **`get_sidecar_stderr`** | `59fc9c24 feat(studio-tauri): add Rust sidecar runtime manager` | `git log -S "get_sidecar_stderr" --all -- apps/studio/frontend` = **空**。任何提交、任何时刻都**从未**在 FE 出现过 | **忘了接线**,非主动下线 ⇒ **T5(接线)确认成立,绝不可当死码删**。50 行崩溃栈从第一天起就没有出口 |
| **`utils/tauriIpc.ts`** | `f5f35c92 feat(studio-tauri): verify frontend IPC bridge`(bring-up 冒烟) | 调用点死于 `5dda2eb5 feat(studio-frontend-v2): T1.1 add hash router` —— 该提交删掉整个 907 行 `App.tsx`,commit message **只讲 hash router**,未提 IPC 验证 | **改判:「被重写顺手带走」,非「主动下线」**。仍建议删(其职责已被 88 处 `isTauriRuntime` 与全部可用 Tauri 命令覆盖),但删除理由要写成这个,不能写"当初主动下线"。附:`verifyTauriWindowIpc` 若被重新接线会**改窗口标题**为 `"Skill Studio - Tauri Mode"` —— 这是删优于接的实证理由 |
| **`RuntimeSidecarStatus.sidecar`** | 随 runtime.ts | `git log -S ".sidecar ===" --all` 与 `-S "sidecar === 'degraded'" --all` 均**空** | **生来即死**,从未被任何组件消费过 ⇒ T7 删除成立,理由是"从未接线",非"已下线" |

### 修正 5 · 按 §3-8「覆盖不到的格子必须显式标注」补声明

决议 §8 是 11 条封闭验收集。本报告对本域的覆盖情况:

| 条 | 覆盖 |
|---|---|
| ① 概念唯一 owner | ✅ 见 ④ |
| ② 膜完整 | ⚠️ 部分 —— 列了膜(①1.3),但**未做**"依赖声明 vs 实际 import 双向机械核验" |
| ③ 边界 fail-fast(信封 + CORS + 码入册) | ✅ 本域主线,见 ③A2/③B |
| ④ 非法状态不可表示 | ⚠️ 仅零星触及(`SupervisedSidecar` 判别式联合 ✅;`AutoRestartOutcome` 在 lib.rs:1320 被压成 `Result&lt;_,String&gt;` ❌)。**未系统扫** |
| ⑤ SSOT 读取(禁 mount/focus/轮询取数) | ❌ **未覆盖** —— 属数据读取域,本次未扫 |
| ⑥ 契约零手抄 | ✅ 见 ③D / 附属工单 |
| ⑦ 无平行实现无死码 | ✅ 见 ④⑤,且已补 §8⑦ 裁决检验(修正 4) |
| ⑧ 纯逻辑与 IO 分层 | ⚠️ 部分 —— 指出 `RuntimeGate.tsx:117-132` 把调度/记账/呈现揽在组件内(⑦M5),**未系统扫后端路由层** |
| ⑨ 业务逻辑 TDD + job 归属 | ❌ **未覆盖** —— 只核了门禁**存在性**(CORS 仅测预检、预算无跨层对账、one-error-exit 正则漏 4 处),未核 job 归属与打包环境证据 |
| ⑩ 完成语义三级 | ❌ **未覆盖** |
| ⑪ 界面状态真实 + 失败零副作用 | ⚠️ 部分 —— ③A2 正是⑪的双重违反(界面状态假 + 失败有副作用:杀掉健康进程、丢在途工作),但**未做真机点验断言** |

**未覆盖原因统一说明**:⑤⑨⑩ 超出本域(运行时底座)边界或需真机/CI 执行证据,本次为只读静态盘点,不具备执行条件。

### 不变的部分

①–⑥、⑧ 的全部断言与坐标不受影响。特别是最重的两条:

- **③A2**(兜底 500 无 CORS → 杀健康 sidecar)7 环链条全部在代码中验实,且修复陷阱(`add_exception_handler(Exception,…)` 落在 `ServerErrorMiddleware`、在 CORS 外侧、**不解决问题**)仍是 T1 的关键约束。
- **③A1**(`setup()` 同步阻塞上界 90s)代码事实确定;按 §2.4 claim-non-seed,`RuntimeGate.tsx:33` 注释与 `shell-layout/mvp1-alignment.md:27` 的"已核实"**均不构成证据** —— 二者只核了 React 层,而窗口不可交互的那段时间根本没到 React。真机首帧采样仍待做。
