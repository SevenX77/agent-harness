# 交付台账(Delivery Ledger)

> 本文件是本仓**当前活动工作的唯一可变状态载体**:在做什么、到哪一步、被什么挡住、过哪道门算完。
> 新会话接手推进工作,**先读本文件**,不靠考古会话交接件。
> 维护规则:每合并一个在册 PR,同 PR(或紧随其后的台账 PR)更新对应行状态;状态翻转只增写不删史(旧状态留在 git 历史)。
> 状态词汇:`待开工` / `进行中(分支名)` / `✅ 已合并(#PR)` / `阻塞(原因)` / `已搁置(重启条件)`。

## 当前冲刺:Copilot 闭环(决议 2026-07-30)

### 决议(用户裁决,2026-07-30)

1. **闭环重定义**:产品闭环 = 用户通过 Studio copilot(MoirAI)面板**对话式走完 MVP1 七节旅程**(00 settings → 01 init → 02 authoring → 03 compile → 04 run-and-verify → 05 debugging → 06 eval/publish,旅程定义见 `docs/studio/mvp1/01_workflows/INDEX.md`)。copilot 是闭环主角,不是辅助。
2. **推进方式 = 接线冲刺**:2026-07-30 三路代码勘察(引擎 / Studio 前后端 / copilot 工具面)证实:缺口无一例外是"后端能力已存在但未暴露成 copilot 工具"或"组件写完未挂载"。因此本冲刺只做接线,**不做 ontology 改造、不做模块化重构、不做文档系统工程**(三者对闭环零贡献,明确后置)。
3. 本台账是冲刺的唯一状态源;`HANDOFF-operator-*.md` 交接件自本决议起只写"读台账 + 本次增量",不再承载队列。

### 验收判据(因果验证,冲刺完成的唯一标准)

一段**脚本化真实对话**在 copilot 面板全程跑通,除审批卡外不碰鼠标,每步有盘上证据(skill 目录 / run 目录 / golden 文件 / publish 产物):

1. "帮我建一个〈X〉skill" → `create_skill` 建出 UI 可见的 skill;
2. 配置/确认模型角色(既有工具);
3. 编译(`compile_skill`)→ 如有诊断,copilot 修复后复译通过;
4. predict 试飞(`predict_skill`);
5. 真跑(`run_skill`,经审批卡放行);
6. 读结果与 trace(`get_run_detail`),向用户复述模型实际产出;
7. 按用户反馈修改 skill → 复译 → 复跑;
8. 将满意的 run 定为 golden 基准(golden 工具);
9. 发布(`publish_skill`)。

### 关键设计决定

1. **真实 run 允许 agent 触发**,交互走与 LLM 配置写工具相同的阻塞式审批卡(`can_use_tool` 挂起,机制见 `apps/studio/backend/app/services/copilot.py:606-625`)。此决定推翻 `copilot_tools.py:158` 工具描述中"真实运行只能由用户在 UI 触发"的禁令。
2. **新工具全部是既有后端端点的薄包装**,照 `copilot_tools.py` 既有 16 工具的模式实现;业务逻辑留在 routers/services 层,工具层不长逻辑。
3. **引擎并联缺陷两步走**:先在编译期对并联拓扑发正式 `[F-v3-*]` 诊断(堵住"编译绿灯→运行炸框架原生错"的谎),后做真修(执行态状态写入迁 delta 语义 + 接上已有的 `blackboard_data_merge` 合并器),真修合并时撤除编译拦截。
4. **明确后置项**(重启条件 = 用户重新裁决):~~CLI 路 MCP 暴露(N5)~~(已被 2026-07-31 裁决提为最高优先,见下);新建向导/模板 UI(copilot 成为新建主路后降级);gateway 状态投影 7 条毛刺(不阻断闭环);文档系统工程(类型学 / docs 门禁 / AGENTS.md 瘦身)。

### 第二轮裁决(用户裁决,2026-07-31,首次真机测试后)

用户在面板里真跑了一轮 demo-loop(session 证据:`~/.claude/projects/D--coding-skills-demo-loop/54db2ab2….jsonl`,编译诊断 4→2→1 收敛后卡死在 `STUDIO_RUNTIME_INPUT_MISSING`),据此裁决:

1. **CLI 路(N5)提为最高优先**:"先修 Open in CLI,因为 cli 是成熟的 agent,不需要调试这些细节"。面板细节修复全部排在 N5 之后。
2. 真机测试坐实的面板缺口(N5 之后按序清账,登记为 P-系列):
   - P-1 **运行输入缺工具**:copilot 无法供给/绑定 test input,`STUDIO_RUNTIME_INPUT_MISSING` 是对话闭环的死墙;应能在无真实输入文件时自行 mock 一份 test input。
   - P-2 **AskUserQuestion 黑洞**:模型提问但面板不渲染、SDK 空答继续(session 记录 24-25 实证);用户从未收到提问。处置方向:面板会话禁用该工具,让模型用正文提问收尾。
   - P-3 **Bash 只读白名单**:`ls` 等无害只读命令免审批(代码强制解析:单命令、无管道/重定向/连接符才放行),不靠提示词。
   - P-4 **审批卡状态服务端权威化**:决议状态只存组件本地 state,重渲染即复活可点(`tool-approval-card.tsx:49-73`);后端 resolve 时应发 resolved 事件,前端从事件流投影。
   - P-5 **编译诊断结构化渲染**:is_error 工具结果(编译诊断集)不该渲染成系统故障样的红色 JSON 堆;中断后 CLI 聚合错误回声("SDK returned an error: …McpToolCallError×N")一并评估降噪。
   - P-6 **copilot 面板宽度自适应**:随窗口宽度伸缩(clamp + 可拖拽),窗口宽裕时面板加宽。✅ 已修(2026-08-02):宽度真相改为画布宿主宽度的比例(默认 0.275,历史默认 352px@1280 不变),拖拽把手回写比例,ResizeObserver 跟随窗口重算,仍夹在 280-720px;纯函数 `rightPanelWidthPx`/`rightPanelRatioFromPx` 带单测,复用规则已入 FRONTEND_UI_SPEC §4。
3. ~~**新讨论项(设计探讨,未裁决实施)**:把 Open in CLI 的终端内嵌进 copilot 面板区域,启动 CLI 时以终端界面替代对话界面("CLI 即 copilot");依赖 N5 工具面先就位。~~ → **PM 2026-08-02 裁决实施**(三条修正:宽度沿用现状不放宽、保留 header CLI 控制按钮作为唯一关 CLI 出口、tmux 滚动必须可用)。设计与验收判据落在 `docs/studio/mvp1/03_regions/copilot/ah-orchestration-design.md` §10;实现见下「阶段 3」行。

#### N5 · Open in CLI 工具面(当前最高优先)

| # | 项 | 状态 | 关键坐标 |
|---|---|---|---|
| N5-1 | 设计(operator 自决 2026-07-31,免上抛,以效果为裁) | ✅ 已定 | 决定:①出口=同一批工具对象再建 Server,官方 `StreamableHTTPSessionManager` 挂 sidecar `/mcp`,复用全局 Bearer 中间件,sidecar 保持只绑 127.0.0.1(本机 WSL mirrored 网络实测 localhost 直通 HTTP 200);②审批=A 案,交互式 Open in CLI 摘 bypass 旗标,用 CLI 原生审批当闸,读档经 allowedTools 预放行;③CLI 首版不暴露 delete_llm_endpoint/delete_llm_route(级联删凭据);④claude 用 `--mcp-config`(装机 2.1.199 实证支持 http+headers),token 走 `.mcp.json` 的 `${STUDIO_API_TOKEN}` env 展开不落明文;⑤codex 0.142.5 原生 `--url`+`--bearer-token-env-var`,无需桥。**不确定项(以效果为裁)**:U1 非 mirrored 网络的机器 localhost 不通→改为 lib.rs 注入宿主 IP;U2 codex 摘 bypass 对 ah 编队流的影响→先只动 claude,codex 看效果;U3 FastAPI BaseHTTPMiddleware 与 SSE 流的兼容→TestClient 已过,真机 CLI 长会话再验 |
| N5-2 | 实现:sidecar `/mcp` streamable HTTP 出口(工具面=面板 27−2) | ✅ 随本行同 PR 合入 | `app/services/cli_mcp_surface.py` + `main.py` lifespan 内建 manager/`app.state` 转发挂载;4 测试(工具差集/内存会话协议/401/initialize 200+session-id);`mcp>=1.29` 补为 backend 直接依赖(uv.lock 变更→合并后根 uv sync + vendor 重建) |
| N5-3 | 实现:lib.rs 注册 studio MCP(claude `--mcp-config` + 摘 bypass + allowedTools 读档;codex config.toml `[mcp_servers.studio]` url+bearer_token_env_var)+ 资产回写 | ✅ 随本行同 PR 合入 | payload 注入 `STUDIO_MCP_URL`/`STUDIO_API_TOKEN`(sidecar 未起则整段省略、会话照常启动);5 个新 Rust 测试绿(本机既有 4 个无关失败,干净树复现过);cli.md 新增「Studio Tool Surface」节、KB-13 撤销「CLI 无工具」警示 |

#### 阶段 4 · copilot 工具面的能力对等与状态对等(2026-08-03 用户批准)

决议正本:`docs/design/2026-08-03-copilot-state-parity-and-tool-surface-decision.md`
(两条原则、6 条关键设计决定、PR 拆分、8 条验收判据、边界)。事实基础全部来自
exp-b-round3 北极星实测,证据在 `D:/coding/skills/_copilot-lab/rounds/exp-b-round3/`。

| # | 项 | 状态 | 关键坐标 |
|---|---|---|---|
| P4-A | 后端三关领域事件(compile/predict/run 在 service 层发 `skill_gate`) | ✅ 随本行同 PR 合入 | 新模块 `app/services/gate_events.py`(载荷构造 + async/from-thread 两个发布入口,广播失败只记日志不影响闸门);发布点:`compile_skill_for_studio` 包一层薄壳分派 pass/fail、`predictor.dispatch_predict_job` 用**同一个投影函数** `export_predict_diagnostics(result).status` 判定通过与否(与前端判定同源)、`run_manager.start_run` 发 started 且 `_finalize_terminal_run` 的 `finally` 发终态;6 测试含一条**结构断言**钉住"router 不得自行发布、三个 service 必须发布"——若发布点漂回 router,MCP 路径就不发事件,而只走 HTTP 的行为测试看不出来 |
| P4-B | 前端闸门归约器重构 + 订阅 | ✅ 随本行同 PR 合入 | 决议 D4;新模块 `apps/studio/frontend/src/components/studio/gate-state.ts`(纯函数 `projectGateEvent` + 去重键 `gateEventKey` + 载荷解析),`Workspace` 里唯一施加点 `applyGateEvent`——**点击处理器与事件流都走它**,两条路径不可能分叉;去重键让本地投影与随后到达的广播只生效一次(抽屉不弹两回),作用域限定当前打开的skill(别的 skill 只更新 stage、不夺走视图);失败事件带上**同一份聚合诊断**,接收方不再自算一份(diagnostics SSOT);10 条前端测试含一条双路径投影一致性断言 |
| P4-C | run 可观测性:`query_run_trace` + `wait_for_run` | ✅ 随本行同 PR 合入 | 决议 D6;新模块 `app/services/run_trace_query.py` 为纯投影(逐 phase 汇总循环轮数 / llm 与 tool 调用数 / finish_task 提交与驳回次数 / 驳回原因 top-N,外加按 phase·事件类型·seq 切片并投影成小体积事件——prompt 与完整 context 不进工具结果);判定规则取自 exp-b-round3 真实 trace:`finish_task` 的 result 等于 `PHASE_COMPLETE` 为接受、非空且不等于它为驳回、空串两者都不算(不猜)。`wait_for_run` 挂在 `run_manager` 既有 `subscribers` 队列上,run 结束该队列收到 None,等待因此由事件驱动、零轮询;已结束的 run 立即返回终态。两个工具都进面板与 CLI 的**只读预放行清单**。7 条纯函数测试 + Rust 171 绿 |
| P4-D | `set_output_artifacts` 工具 | ✅ 随本行同 PR 合入 | I/O 面板 "Configure output artifacts" 的后端等价物,调用**同一个** service `update_artifacts_payload`;产物 schema 从路由内联提到 `app/models/runtime_config.py`,面板与工具共用一份校验(两个写入方各带一套校验正是它们悄悄漂开的方式,已用一条结构断言钉住路由不得再自带 schema)。整份清单全量替换,空清单即取消全部声明;形状不合法在边界拒绝并回具体 pydantic 错误。它是**写工具**,不进只读预放行清单,照常走审批。4 条测试 |
| P4-E | CLI 会话注入 `skill_id` + `workspace_root` | ✅ 随本行同 PR 合入 | 修 F3/F13(两次独立复现)。id **从 skill_index 反查**(`native_fs::registered_skill_id_for_root`)而不是由打开方传入——会话必须绑在**已登记**的那个 skill 上,而由调用方给 id 正是第一次就出错的那个输入;身份随 master 首条 prompt 下发(`master_prompt`),claude 与 codex 两条命令都带。未登记的工作区照旧发裸 prompt,不编造 id。Rust 新增 4 测(共 171 绿) |
| P4-E2 | **回归修复:身份注入把 ah.toml 写坏,Open in CLI 起不来** | ✅ 随本行同 PR 合入 | P4-E 注入的身份文本含换行,而 TOML 基本字符串不允许字面换行——生成的瞬态 ah.toml 在第 7 行断开,`ah start` 以 `invalid basic string` 退出码 3 失败,**CLI 会话完全打不开**。真机第一次点开就撞到;P4-E 的 Rust 测试只断言 `cmd.contains(<skill id>)`,从不校验生成物能否解析,所以那条测试一直是绿的。修复=`toml_string` 转义换行/回车/制表符(序列化器本就该做,不是给这一处打补丁);新增两测:转义单测 + **把生成的瞬态配置真的 parse 一遍**(claude 与 codex 两条),后者正是原本缺的那道门。红态实证:去掉修复后两测红(171 passed / 2 failed),修复后 173 全绿 |
| P4-E3 | **封存的 predict 无人能问:补状态账 + 把 trace 纳入 seal** | ✅ 随本行同 PR 合入 | 真机 exp-b-round4 第一轮实测暴露:会话想查"review 在 predict 里收到了什么", 无工具可用, 遂用 Bash 手扒 `.workspace/runs/predict-*`, 又被 Claude Code 持久 shell 的 cwd 绊倒(第一条命令 `cd` 进 run 目录后, 第二条的相对路径就落到了 run 目录之内), 把 `No such file or directory` 误判成"目录被扫了"并一路追着幻觉跑到 scratchpad。**产品侧真因有两条, 都是 predict 的产物目录不如 run 完整**:(1) predict 一结束就被 `finish_transient_predict_run` 从注册表摘掉(`run_manager.py`), 而目录里没有 `run_metadata.json`, 于是 `_metadata_for` 两头落空, `get_run_detail`/`query_run_trace` 一律 RESUME_CHECKPOINT_NOT_FOUND;(2) 封存只把 `result.json` 写进 manifest, 而读取一律经 manifest, 所以引擎写在同一目录里的 `trace.jsonl`/`final_state.json` 对所有 reader 不可见(实测该目录 8 个文件, manifest 只认 1 个)。修法都落在生产端, 不在读取端打特例:predict 封存时补写状态账(格式归 run_manager, 新增 `record_predict_outcome`; status 取 `export_predict_diagnostics` 同一投影, 与 gate 广播共用一次判定, 不另立裁判), 并把 `trace.jsonl` + `final_state.json` 一起纳入 seal。同时拆掉诱因:`predict_skill` 的描述与 `detail_hint` 不再指向 `.workspace/runs/<run_id>/`, 改为指向 `query_run_trace`。TDD 三测(状态账存在/失败判定与 gate 同源/跑完的 predict 能被 query_run_trace 读出 trace 与 final_context)先红后绿 |
| P4-E6 | **跑起来只能等:补暂停/恢复/终止三态** | ✅ 随本行同 PR 合入 | PM 反馈"run 跑的时候至少该能停", 追问"不是有 checkpoint 吗, 为什么无法表达暂停"——**追问是对的, 我第一版把它做成了单一终态 `cancelled`, 漏了中间那层**。证据:`runner.py:426` `cleanup_checkpoints_on_finish: bool = True`, 即**只有自然跑完才清 checkpoint**;被中途终止的 run 走不到清理, `checkpoints.db` 完整留着(round4 真实 run 留了 11MB), 而 `runner.py:503` `resume_skill(skill_path, workspace_dir, run_id, ...)` 正是从它接着跑。所以"终止进程"≠"这次 run 结束了", 中间隔着"可恢复"。更正后的模型:**暂停**(`POST /{run_id}/pause`, status `paused`, 只停 worker、不做终态封存)与**终止**(`POST /{run_id}/stop`, status `cancelled`, 走终态封存)是两个动作;终止从 running 与 paused 两处都可达。状态词表 `RunStatus` 与 gate 词表各补 `paused`/`stopped`, "状态→广播"仍走显式映射表。工具条按 PM 指定的三态:running → 单个 **Pause**;paused → **Resume + Stop 两个按钮并列**(一个暂停的 run 有两个去向, 不由另一个隐含);其余照旧 Compile/Predict/Run。Resume 复用既有 `handleResume`(engine resume 通路早已存在)。测试:后端 4 条(暂停落 paused / 重复暂停 409 / **paused 仍可终止** / running 可直接终止)+ 前端 3 条工具条形态, 先红后绿;backend 1549 全绿, 前端 1910 全绿 |
| P4-E4 | **Open folder 跳去"文档":Windows 资源管理器不认正斜杠** | ✅ 随本行同 PR 合入 | I/O 面板 input file 行的文件夹按钮打开的是"文档"而非目标目录。链条:`IoConfigDialog.tsx:484` 拼完路径做 `.replaceAll("\\", "/")` → `lib.rs` `existing_path` 原样保留分隔符 → `Command::new("explorer").arg(target)`。**实证**:同一目录分别用反斜杠与正斜杠调 explorer, 前者窗口标题 `import_files`, 后者 `文档` —— 资源管理器对正斜杠路径不报错、静默打开默认目录。修法落在唯一出口:新增 `file_manager_argument`, Windows 下把分隔符归一成 `\` 再交给 explorer(任何调用方都可能传正斜杠, 在出口治比在每个调用点治对)。**红态踩坑并已修正**:首版断言比较两个 `PathBuf`, 而 Windows 上 `/` 与 `\` 都是路径分隔符、两者判等, 于是不修也绿(与 P4-E2 的 `cmd.contains` 同一类空断言)。改为比较真正交给进程的参数字符串后红态成立, 修复后 174 全绿 |
| P4-E5 | **跑完动画不停 / 工具栏横跳 / Trace 面板不跟 copilot** | ✅ 随本行同 PR 合入 | PM 真机反馈三条, 落点都在"用错判据"。**(a) 动画**:`ContextEdge` 的流动动画绑 `hasTraceData`, 而那是"这条边有数据可点开看", 一旦跑过就永久为真, 于是 run 结束后画布仍在流。改为绑"上下文此刻正在过这条边"=目标 phase 正在执行;判据 `runningPhaseOf` 抽进 `node-status.ts`, Workspace 的 trace 高亮与画布的边动画共用一条规则, 不各推一遍。有数据但未在流的边保留静态 accent 描边。**(b) 工具栏**:`center-action-bar` 按左右安全区取中点, 而两侧面板都是**浮层**(不挤占画布), 面板一开合就横移(实测 239px)。#567 曾因按窗口居中被拖宽的 copilot 面板压住——两次反馈是同一规则的两个特例:**默认钉在画布正中, 仅当浮层真会盖住时才让开**, 用 `clamp()` 表达, bar 自身宽度由 `useLayoutEffect` 实测发布成 CSS 变量(标签会随 stage 变, 不能写死)。**(c) Trace 面板**:人点 Run 走 `setActivePanel("timeline")`, 而 copilot 路径的 `follow-run` 只 `setRunId` —— 工具栏跟了、抽屉跟了、面板漏了(P4 自身缺口)。新增 `open-trace` 效果由 `projectGateEvent` 统一产出;predict 也补了 started 广播(predict 同样流事件), 点击路径删掉自己那份 `setActivePanel`, 两条路径同一实现。测试:边流动 2 条 + 面板效果 3 条 + 工具栏 clamp 1 条, 先红后绿;前端 1912 全绿, backend 1545 全绿 |
| P4-E7 | **产出契约是 skill 的固有一端:引擎导出字段全集 + MoirAI 能判断能配置** | ✅ 随本行同 PR 合入 | PM 定性:"产出 artifacts 是常态, 就和最初要输入 input 一样", 但"也有可能这个 skill 确实不需要, 这就需要 moirai 对 skill 本身的理解"。round4 实测正是这个形状——run 报 success、artifacts/ 空目录, 因为 `runtime_config.artifacts` 默认 `[]` 且只能人去 I/O 面板手点(`InputPanel.tsx` 的 "No artifacts configured yet." 就是默认态), **全程没有任何一处提出过"这个 skill 该产出什么"这个问题**。根因:"跑到出口时黑板上有哪些字段"这条推导只存在于前端 `lib/io-config.ts` 的 `blackboardAtOutput`/`reconcileOutputFields`——那是**图的契约事实**却被 UI 私有了, 所以后端、工具、任何检查都无从判断(这正是 P4-F 当时明确挂起的另一半)。落点在引擎(编译单出口本就在解析每个 phase 的 io schema):新增 `core/blackboard_contract.py`, 纯函数投影 `blackboard_fields_at_output`(根 io.inputs ∪ 各 phase io.outputs, 拓扑序后者覆盖同名, 每字段带 produced_by/type/是否被 io.outputs 声明)与 `undeclared_output_names`(声明了却无人产出 = 图honour不了的契约)。adapter 加 `get_output_contract`, copilot 加只读工具 `get_skill_output_contract`, 返回声明的输出 / 出口黑板全集 / 当前落盘声明 / **declared_outputs_not_landed**(声明了输出却一个字段都不落盘= 这次 run 什么可读的东西都没留下)。工具描述把这件事写成判断题而非配置项:多数 skill 需要落盘, 确有 skill 只在黑板里传给同图下游——那要**判断为不需要**, 而不是漏掉。测试:引擎 5 条(输入打底/后写覆盖先写/声明标记/缺产出被报/自洽时无遗漏)+ 工具 3 条, 先红后绿。engine 1523 全绿、backend 1548 全绿、mypy --strict 干净。**注意**:两套 pytest 必须分开跑(AGENTS.md 本就分两条)——混在一条命令里跑会互相干扰出 5 条假红 |
| P4-E8 | **零产物成功要说话:wait_for_run 在成功时报 artifacts_landed + 契约提醒** | ✅ 随本行同 PR 合入 | round4(干净阴性)与 round5(被会话死亡污染, 不作数但同形)连续两轮:run success、artifacts/ 空、全程无人问"这个 skill 该产出什么"。P4-E7 的 get_skill_output_contract 是**被动**工具, 只帮已经决定要问的 agent;缺的是把问题递到 agent 眼前的**主动触发点**。落点=wait_for_run 的成功返回(agent 读到成功的那一刻):附 `artifacts_landed`(真实落盘文件表), 为空时附 `output_contract_reminder`, 点名 get_skill_output_contract/set_output_artifacts 并保留"确实不需要落盘就说出理由"的出口(PM 定性:产出是常态但非必然, 判断归 MoirAI)。失败的 run 不提醒——该谈的是失败本身, 混在一起会把它埋掉。TDD 3 条先红后绿, backend 1558 全绿。**附带记录两条独立缺陷**:(a) round5 会话在 run 结束前 48s 被 systemd 外部 stop (anchor unit 干净 Stopped、Studio app 日志零 kill/close、发起者未查明)→ 提 ah 仓 issue;(b) run 刚结束时 frontend GET run detail 吃到 409 run_not_sealed(sealing 竞态窗口)→ 待修 |
| P4-E9 | **终态出口共享产出契约提醒:get_run_detail 也要说话** | ✅ 已合并(#625);**真机实证(exp-B R7, 2026-08-07)**:会话用 get_run_detail 读终态, `output_contract_reminder` 送达(transcript 1 次)→ 会话读 KB-14、调 get_skill_output_contract、判断"配 artifact 在许可内但改变对外交付"→ 上报请示 → operator 应答后 set_output_artifacts+重跑 → `segmentation_result_latest_20260807_114439.json` 真实落盘, 会话按 KB-14 因果链自验 declared⇒run⇒listed⇒read。R6 三层缺口全闭合 | exp-B round6 实测:P4-E8 把提醒挂在 `wait_for_run`, 但会话全程用 `get_run_detail` 轮询终态、从未调 `wait_for_run`——提醒一次都没送达。终态是同一个事实, agent 从哪条工具读到它, 提醒就在哪条工具出现:提醒文案提成共享常量, `get_run_detail` 在 success+零产物时同样附 `output_contract_reminder`(failed 等其余状态不附, 语义同 P4-E8)。决议文档 `.kiro/specs/studio-moirai-agent-system/decision-2026-08-07-run-terminal-output-contract-and-cli-read-tier.md` D-1 |
| P4-E10 | **CLI 免审批名单对齐 cli.md 读/探测档(第三次漂移)** | ✅ 已合并(#625);Rust 测试双向锁死(读/探测必在名单、写/执行绝不入);R7 真机跑在 ah 沙箱 bypass(IS_SANDBOX)下, 名单未经受审批模式实测——正常审批模式的实测留给下一次面板/审批轮 | 权威规则 cli.md:23「Read and probe tools are pre-allowed」, 但 lib.rs `CLAUDE_STUDIO_ALLOWED_TOOLS` 手抄名单连续两波工具上新未跟:纯读 `get_skill_output_contract`(#585)与 KB-13:50 归类为 probe 的 `run_role_test`/`test_llm_endpoint`/`test_llm_endpoint_models`/`probe_llm_route` 均漏。本次补齐 5 项;写/执行类照旧走审批。**观察项**:名单是 Rust 侧手抄清单, 已第三次漂移, 暂以「新工具 PR 必核 cli.md 档位并同步名单」为纪律, 不引入跨语言生成链路。决议 D-2 |
| P4-E11 | **pause_run/stop_run MCP 工具(能力对等)** | ✅ 已合并(#625);**真机实证(exp-B R7)**:会话自诊首个 run "已注定在 segment 空转烧 token" 后主动调 stop_run 止损(run 终态 cancelled), 随后修根因、重跑成功——R6 的 "No cancel tool is exposed" 缺口闭合且被正确使用 | round6 会话原话 "No cancel tool is exposed on this CLI surface — I'll let the doomed run expire"(A[223]):人有 Pause/Stop 按钮(P4-E6), copilot 没有。补两个工具直调 `run_manager.pause_run`/`stop_run`(与 HTTP 路由同链, gate 广播照发→状态对等免费);非法状态由 run_manager 409 语义落 is_error, 工具层不重复防御。执行档、不进免审批名单(与 run/resume 同待遇)。cli.md 执行类枚举 + KB-13 工具地图同步。决议 D-3 |
| P4-E12 | **golden case 内容可自定义改写(write_golden_case)** | ✅ 已合并(#628);**真实验证(2026-08-07)**:经真实 sidecar `/mcp` 面(MCP 握手, 39 工具在列)对 exp-b-round7 调 write_golden_case ×2(review+segment), 把修正版 golden(run3 13 段种子 + 25 行退出句归 C 段→C 22-25 + 59 行省略号并入 B 49-59, 共 11 段, A=1/B=9/C=1, 复原度精确)写入既有 baseline 替换违反 P0 的 12 段版;get_golden_content 读回 + 磁盘核验 `origin=authored`/`authored_at` 落盘、baseline.json 血统未动、setup.json 仍为 run 直升(无 origin 键) | 用户裁决 2026-08-07:「golden应该是可以自定义改的,不是只有run的结果才可以当作golden」。设计源 golden-eval mvp1-alignment.md F6 本就写着「golden 本身随时可写……Run 的真实输出可作 golden 默认种子……在其上编辑」,但实现唯一写路 `set_golden_baseline_for_run` 永远取封存 run 的 node_outputs——代码落后设计。补服务层 `write_golden_case_content`(404/409 locked/422 case_not_found/422 空对象四条边界;只重写 case 文件并标 `origin=authored`+`authored_at`,run 血统与兄弟节点 case 不动;**locked 首次从声明变实效**)+ 写档工具 `write_golden_case`(审批, cli.md:23 "golden writes" 已覆盖;不加 HTTP 路由——UI 编辑入口按 F3 仍归 I/O 数据流方向, 无调用方的链路不留)。凭空建 baseline/追加新节点 case 显式不做, 归 I/O 方向裁决。决议文档 `.kiro/specs/studio-moirai-agent-system/decision-2026-08-07-golden-case-authoring.md`。TDD:服务 6 条+工具 4 条先红后绿 |
| P4-E13 | **连按两次 Compile 按钮卡死:闸门幂等用错了身份** | ✅ 已合并(#676) | PM 真机复现(2026-08-09, exp-b-round7):按 Compile 成功、高亮移到 Predict, **再按一次 Compile 就永久变灰**, 打开文件跑实时 lint 也解不开。根因在 P4-B 自己:去重键 `gateEventKey` 取 `runId ?? contentHash`, 而**产物哈希是产物的身份, 不是一次发生的身份**——源码没改, 第二次编译产出同一个哈希, 第二条 pass 事件与第一条同键, 在 `applyGateEvent` 第 686 行被丢弃, 而丢弃发生在 `updateStage` **之前**, 于是点击处理器乐观置的 `compiling` 再没有任何事件能推走(`center-action-bar.tsx:50` 的唯一禁用条件)。同一缺陷另有两个实例:fail 无哈希故 subject 恒为空串, **连续两次编译失败第二次必被吞**(错误抽屉也不弹, 哪怕缺陷集完全不同);同一个 run 暂停两次同键。修法不是换个更好的键(event_id 方案已评估并放弃, 见决议 §4.1), 而是拆开两件事:**状态无条件写入**(D1, 给出"不可能卡在非终态"的构造性保证), **副作用按投影相同性折叠**(D2, 比较的是"界面要变成什么样", 含 `follow-run` 的 run_id 等载荷——按状态迁移比较会漏掉同状态不同载荷的情形, run_manager 无单 run 互斥守卫故该情形可达)。新模块 `gate-effect-fold.ts` 承担折叠判定, `gateEventKey` 与 `appliedGateOutcomes` 账本整体删除;后端 `compile_skill_for_studio` 补发 `started` 与 predict/run 对称(D4), 使 copilot 连续两次同结果编译之间有分隔。**同步修正 2026-08-03 决议 D4 的幂等条款**(原文即缺陷源)。决议正本 `docs/design/2026-08-09-gate-outcome-idempotency-decision.md`。TDD:折叠单元 6 条 + 双路径折叠 1 条 + Workspace 回归 1 条(红态实证 `expected 'compiling' to be 'compile-pass'`, 正是 PM 看到的那个卡死态)+ 后端 2 条改写先红后绿 |
| P4-F | engine 编译期校验未知声明(`target` 枚举) | ✅ 随本行同 PR 合入 | 决议 D5;实测 `target: __probe_invalid__` 编译 0 缺陷且透传进 manifest,而运行期只认 `{file, artifact}`(`runner.py:1952`)。落点在 engine loader 的 `_validate_inline_io_schema`(io.outputs 是引擎契约、compile 是单出口),诊断文案直接列出合法取值;运行期那句写死的字面量换成共享常量 `DECLARED_OUTPUT_TARGETS`,校验与执行读同一份枚举。**沿用既有码 `[F-v3-graph-io-schema-invalid]`,不新增码**——码表有 97 码冻结(R4.3/design §6.5,五道锁),未知 target 本就属于 inline io schema 不合法,具体字段与合法取值由 message + field_path 承载。4 测。**遗留**:决议里 F 的另一半(`runtime_config.artifacts` 的 fields 必须是黑板真实字段)本 PR **未做**——字段全集推导目前只有前端一份,后端要校验须先统一那份推导(归 P4-I `get_workspace_config`),复制一份会重犯两个实现的错。现状:写错 target 编译报缺陷,写错 fields 仍只在 run 完表现为空目录 |
| P4-G | `get_skill_overview(skill_id)`:manifest 摘要 + phase 列表 + 每 phase 的 io 字段名与类型 + validator 有无 + llm_role | ✅ 已合并(#603);真机:经 sidecar `/mcp` 真端点 MCP 握手+`tools/call`,27 工具在列,exp-a-round3 返回 3 phase 结构投影(字段名+类型+required),断言全过 | 实现:走 `_load_compiled`+`_graph_topology` 同一条编译路径,不带 detail 副作用(git init/watch/读正文);投影=graph 摘要+graph_io/每 phase 的字段名+类型+required(非整份 schema 转储)+mode/validator/llm_role/depends_on/is_graph_output/subgraph path。有界锁死:正文零泄漏有 marker 断言;编译不过 fail-fast 指向 compile_skill(诊断 SSOT 不复述)。三面注册:工具表+免审批清单+CLI claude allowlist;两个工具面契约锁按新工具更新。5 新测,backend 1563 全绿,Rust 196 绿 |
| P4-H | `read_skill_file(skill_id, path, range?)` | 待开工 | 经 skill 索引解析、限定 skill 目录内,替代会话裸用 Bash/Read 摸文件 |
| P4-I | `get_workspace_config(skill_id)` + `list_run_artifacts` / `read_run_artifact` | 待开工 | runtime_config 的结构化投影(输入绑定 / test inputs / artifacts 声明 / llm 覆盖);现状 `get_run_detail` 的 artifacts 字段永远是空数组 |
| P4-J | 知识资产:新增 KB「产物与落盘」;KB-09 补 `query_run_trace` 诊断套路;KB-13 随工具面更新;修 KB-08 矛盾 | 待开工 | KB-08 第 22 行 "users cannot override this behavior manually" 与第 27 行 P1 档 "manual mock overrides are supplied ... in the test panel or copilot callbacks" 互斥 |
| P4-K | P2 写工具收口(`write_skill_file`/`bind_test_input`) | 待排期 | 现状:CLI 会话直接 Write 磁盘,绕过「Rust native-fs 是 skill 文件唯一写者」 |
| P4-L | copilot 能读需要登录才看得到的网页(借用户自己的 Chrome) | 🚧 本期 | **用户裁决(2026-08-12):不用 Playwright,走 OpenCLI + Browser Bridge**,signal-ops 已在用同一方案。机制:一个 Chrome 扩展 + 本地 daemon,agent 借**用户已经登录好的那个 Chrome**;**登录只有人能做**,这里不索取密码、不读导出的 cookie。**边界用封闭词表钉死**:`app/services/web_access.py` 的 `READ_VERBS` 只有 `open`/`extract`,OpenCLI 那些会「以用户身份行动」的动词(`click`/`fill`/`type`/`eval` 等 12 个)进 `WRITE_VERBS` 并逐个测不可达。**副作用可枚举 = 它能进免审批名单**——不然读一页文档弹一次审批卡(Bash 是执行类,被 `_execution_requires_approval_hook` 强制每次弹卡),没法用。**两条 live 实测改了实现**:①`opencli doctor` 在扩展没连时 **exit code 也是 0**,退出码不能当就绪信号,必须读输出;②npm 在 Windows 上装的是 `opencli.CMD`,`CreateProcess` 不解析无扩展名的名字——真跑一次直接 `FileNotFoundError: [WinError 2]`,而所有 fake-runner 单测全绿。修法是 `shutil.which` 先解析(它认 PATHEXT),不用 `shell=True`(那会把模型给的 URL 放上命令行)。**桥没就绪返回的是「请用户做什么」,不是空页面**——防的是 agent 把「没人开 Chrome」报成「这家厂商没有文档」。**顺手锁了一条一直没人守的不变量**:copilot 免审批名单与 Open-in-CLI 的 `CLAUDE_STUDIO_ALLOWED_TOOLS`(lib.rs)此前靠手工维持一致,加第 21 个工具时补了跨语言 parity 测试。真机实证:经真实 opencli 读到 DeepSeek 官方定价页 2256 字 markdown。配套 skill `app/agents/skills/web-research/SKILL.md` 写清分块续读、桥断了怎么跟人说、以及**登录态页面第三方复核不了,只能落 `agent_draft` 不能冒充 `provider_doc`** |
| P4-L1 | 新用户的机器上怎么有 opencli | ⏳ 待办 | 承 P4-L。用户问的正是这条:**opencli 得内化成 app 的能力或依赖**,不能让人手装。查实分三层:**CLI+daemon 能内化**(纯 Node,`bin: dist/src/main.js`、`engines: node>=20`、8 个依赖、无原生模块),照 Python sidecar 那条路 vendor 进 `vendor/node/<triple>/` + `vendor/opencli/`,`tauri.conf.json` 的 `resources: ["vendor/**/*"]` **已经覆盖,打包配置都不用改**;**扩展文件的分发已经是自动的**(CLI 自己按 `~/.opencli/update-check.json` 下载解包);**但把扩展装进 Chrome 本质不能自动,也不该**——它 manifest 里**没有 `key`、没有 `update_url`**(不在应用商店),权限是 `debugger` + `cookies` + `<all_urls>`(能挂调试器、能读所有站点 cookie)。所以正确形态是**引导安装不是静默安装**:Settings 加一个 Web Access 项(三态由 `opencli doctor` 判),扩展随包发,界面给一步引导并**明写这个扩展能读你所有网站的 cookie**,让用户知情同意 |
| P4-L2 | 前端依赖没有任何审计门禁 | ✅ 随本行同 PR 合入 | **两档阈值,不是一档**:`npm audit --omit=dev --audit-level=low`(会到用户手上的东西,零容忍)+ `npm audit --audit-level=high`(dev 工具链只拦 high/critical)。**依据是实测的退出码**:生产依赖 `--omit=dev` 任意等级 → exit 0;全量 high+ → exit 0;全量 moderate+ → **exit 1**(两条都来自 devDependency `shadcn` 的传递依赖:`@hono/node-server` moderate + `body-parser` low)。所以一档严格的门禁**上线即红**,而长期红的门禁等于没有门禁——这正是 SonarCloud 的病。此前 npm 侧一个门禁都没有,Dependabot #85 是 **GitHub 替我们看见的** |
| P4-L4 | `cross-platform-smoke` 从「在跑」变成「必过」 | ✅ 随本行同 PR 合入 | **它是唯一在 Linux 以外跑测试套件的 job,而这台主开发机就是 Windows。** 之前它只是咨询性的,后果有两次实证:①#743 修的 WinError 5 是纯 Windows 缺陷,而这个 job 在它之前一直红着没人管;②**#749 在这个 job 还没跑完时就自动合并了**——我当时还写下「如果是真回归 CI 的 Windows job 会拦住」,这句话对一个拦不住任何东西的检查项**不成立**。必过项 5 → 7(加 windows-latest / macos-latest 两个)。改动前的分支保护已备份,`strict` / `enforce_admins` / `approvals` 三项逐一复核未变 |
| P4-L5 | Sonar:把生成产物移出扫描范围 | ✅ 随本行同 PR 合入 | **先量再排除**:`docs/**/*.html` 四个文件全是生成物或参考件(N6 手册由 `build_template_slice.py` 从 tpl-*.json 生成、12D 修复框架有自己的生成器与校验器、mvp0 那个是 UI 效果图),不发布也不执行。它们是本项目最大的「缺陷」来源:手册 32 条、修复框架 26 条,而且**全项目 31 个 BUG 里有 16 个在生成的文档里**(主要是重复 element id——那说的是生成器的事,不是产品的事)。手工去改输出等于改输出而不改程序;留着计数则真正的 15 个 BUG 被噪声挡住。**生成器自身的源码(同在 docs/ 下的 build_template_slice.py 等)仍留在分析范围内**,只有它们的输出被排除。**2026-08-12 作废**:这四个 HTML 连同生成器已随「下线」裁决整体删除(P4-L7),`docs/**/*.html` 排除项因此从 `.sonarcloud.properties` 撤回——为不存在的文件留排除项,就是下一处需要被解释、被遗忘的死配置 |
| P4-L6 | Sonar 本身设为必过门禁 | ⏳ 待办 | **故意没有在本 PR 做。** 2026-08-12 实测:1535 条未解决(CODE_SMELL 1411 / VULNERABILITY 93 / BUG 31,其中 **9 条 BLOCKER**),**一条都没有人分诊过**。这个状态下设成必过 = 每个 PR 上来就红,然后它会被关掉或被绕过。顺序必须是:先分诊(真的修、不适用的显式压掉),再提升为门禁。已知的真问题样本:`io_scan.py:269-276` 的 `POST /scan` 接受请求体里的任意绝对路径直接读并返回内容样本,**零 confine**,而同仓 `copilot_tools.py:95` 和 `:225` 早就把 `resolve()` + `is_relative_to()` 写对了 |
| P4-L7 | N6 前端实施手册 + 12D 修复框架整体下线 | ✅ 随本行同 PR 合入 | **用户裁决「下线」(2026-08-12)**,起于「`docs/**/*.html` 这个归档吧,没什么用」。删的是一整套生态、不是一张网页:295 个切片与截图 + 生成出的 `index.html` + 生成器 `build_template_slice.py` + 模板 + 两份撰写方法论 + 12D 框架 HTML 与它的校验脚本 + mvp0 效果图 + 一条只做子串断言的 nav 测试 + `temp/` 下 13 个当年误入库的 N2 切片,合计 315 个文件。**理由是成本收益不对等**:手册按规则原文本来就只是「MVP1 设计文档的派生视图,不是设计真相本身」,却给每个前端任务压上「回写切片 + 重生成 + 截图入库」的义务;它的状态标签是手维护的,规则里早就写着「用代码核对再信」——所以撤掉它没有拿走任何可信的东西。**不另存 `_archive/`**:git 历史就是归档。规则侧同步改完:`AGENTS.md` 三条标准文档条目 → 一条「退役」说明;`apps/studio/frontend/CLAUDE.md` 的端到端 SOP 由 8 段收成 7 段(原 Phase 6「回写手册切片状态」整条消失,7/8 顺次前移),Phase 2 的四级级联收成两条岔路(设计源里有 / 设计源里没有);`FRONTEND_HANDOFF_PROMPT.md`、`ah-orchestration-design.md` §9.10 必读清单、`RUN_AND_SCREENSHOT.md`(§4 手册伺服整节删除,原 §5 的 CDP 点验升为 §4,4 处引用同步改)一律改指 MVP1 设计源 |
| P4-L3 | 同一份工具名单在仓里有四份副本 | 📌 记档 | 做 P4-L 时被自己的改动量量出来的:加**一个**工具要改**四处**——`copilot._DECLARATIVE_ALLOWED_TOOLS`、`lib.rs` 的 `CLAUDE_STUDIO_ALLOWED_TOOLS`、`test_copilot_assembly.py` 里硬编码的期望列表、`test_copilot_config_tools.py` 里硬编码的期望集合。四处保持一致**全靠测试挨个把人拦下来**,而其中「两份免审批名单彼此一致」这一条**在 P4-L 之前根本没有任何东西守着**(那条 parity 测试是 P4-L 补的)。违反仓规「同一业务规则/常量/schema 只允许一个权威定义」。正确形态:名单只有一份权威定义,其余三处从它派生(Rust 侧可在构建期生成或由测试对读)。**没有夹在 P4-L 里做**——一任务一 PR,这是独立重构 |

#### 阶段 3 · CLI 终端内嵌 copilot 面板(「CLI 即 copilot」,2026-08-02)

| # | 项 | 状态 | 关键坐标 |
|---|---|---|---|
| T3-1 | Rust PTY 宿主 + 输出走 Tauri channel;外部终端窗口路径整条删除 | ✅ 随本行同 PR 合入 | 新模块 `apps/studio/tauri/src/cli_terminal.rs`(portable-pty + channel + 会话 owner 去重);删 `spawn_terminal_with_launcher`/`focus_existing_windows_terminal`/`windows_cmd_start_powershell_args`/`spawn_linux_terminal`/两个 .ps1 生成器/窗口标题;PTY 字节链有实测单测(`pty_delivers_the_child_process_output`,含 ConPTY 光标查询应答) |
| T3-2 | 面板内终端视图:面板持有会话、组件只渲染;xterm 依赖升到 `@xterm/*` | ✅ 随本行同 PR 合入 | `cli-terminal-session.ts`(会话工厂 + 可重放输出历史)+ `cli-terminal-view.tsx`(纯渲染器,懒加载);删孤儿 `TerminalPanel.tsx` 与 `TerminalSession`/`TerminalStatus` 类型 |
| T3-3 | tmux 鼠标滚动(D6) | ✅ 随本行同 PR 合入 | 启动/attach 脚本 attach 前按**会话工作目录**发现 ah 的 tmux socket 并 `set-option -g mouse on`(不复制 ah 的 socket 哈希);真机实测 `mouse on` 已生效、滚轮上下滚动可逆 |
| T3-5 | PM 反馈两处修正:动作栏压面板 + 收起面板丢终端 | ✅ 随本行同 PR 合入 | ①动作栏改为在画布安全区之间居中(`center-action-bar.tsx`,与 minimap 同一组 safe-area 变量);②CLI 会话所有权从 copilot 面板上移到 `Workspace`——面板收起会被卸载,会话存那里必然随之消失且泄漏终端客户端;设计源 §10 D3 同步修正 |
| T3-6 | **CLI 会话拿不到 Studio 工具面(N5 实际失效)** | ✅ 随本行同 PR 合入 | 根因:master 由 ahd 派生,ahd **不继承** launcher shell 的 export(实测 daemon 与其下所有进程 STUDIO_* 变量为 0),`--mcp-config` 整段被 `${STUDIO_MCP_URL:-}` 守卫静默丢弃,会话起来后 `/mcp` 报 No MCP servers configured;修复=端点与 token 烤进 master `cmd` 串本身(ahd 原样执行),与 `build_ah_bash_script` 的同类推理一致。真机实证:修前 `No MCP servers configured` → 修后 `studio · ✔ connected · 25 tools` |
| T3-7 | **run 结束后 `get_run_detail` 永久报 `running`** | ✅ 随本行同 PR 合入 | 现象(2026-08-03 exp-b-round3 run `2026-08-03T07-01-08_ff0be8c9`,独立 MCP 会话复现):同一份响应里 `status: running` + `metrics: null`,而它自己的 `event_type_counts` 已含 `run_ended: 1`,盘上 `sealed`/`result.json` 早已写完;同一 run 的 HTTP `/runs` 列表却报 `success`。根因:列表读盘(`list_runs` glob `run_metadata.json`),`get_run_detail` 走 `_metadata_for` → 内存 `RunRecord`,而 `record.metadata = metadata` 是 `_finalize_terminal_run` 的**最后一句**、排在两次存储 await 之后,任一次抛错就永远停在 `running`。修复=该赋值移进 `finally`。**保持记录为状态 owner(不改成读盘)**:盘上 `run_metadata.json` 在 `latest/` 同步**之前**就写成终态,以盘为准等于提前宣布完工——实测会让 `test_api`/`test_skill_git_p0` 的 `latest/run_metadata.json` 断言按 1/3 概率竞态失败 |
| T3-8 | **CLI 在跑,面板却渲染 `Open in CLI`(状态生产者被订阅者杀掉)** | ✅ 已合并(#586);真机点验 6/6 通过 | 现场取证(2026-08-03 21:37,exp-b-round4):`ah events` 自报 `runtime_state:"active"`(master pid 938 + 3 worker 全 IDLE),而 Tauri 进程下**一个 `ah events` 子进程都不剩**,面板于是停在挂载初值 `inactive` 渲染 Open。根因是所有权错误:MoirAI 面板条件挂载(`Workspace.tsx:2663`),折叠即卸载 → dispose 发 `unwatch_code_assistant_status` → Rust 无条件杀掉**共享**的 `ah events` 流并清空快照缓存(`lib.rs:1675-1703`,无引用计数);它与新挂载发的 `watch` 之间没有顺序保证,后落地即让活着的订阅者永远收不到状态,且无兜底重连。叠加第二处:"没有快照帧"被投影成 `inactive` 这句断言(`unwrap_or(AssistantStatus::Inactive)`)。修复=删掉整条订阅者驱动的 teardown(生产者归 `CodeAssistantRuntimeState`,`watch` 幂等且顺带收敛到单工作区)+ 新增 `unknown` 相位表示"尚未观测"(面板 hands-off)+ 流启动时 `status --json` 播种。决议落盘 `.kiro/specs/studio-ah-state-contract-v1/decision-2026-08-03-status-stream-ownership.md` |
| T3-9 | **回归:Close 之后 Open 控件永久禁用** | ✅ 已合并(#587);真机复验:Close 后约 1 秒回到可点的 `Open in CLI`,观察者 PID 19860→88993 证明流确实重建 | T3-8 的真机点验抓到:`ah stop` 杀掉 ahd 后,该 config 的 `ah events` 子进程**不退出**、只是永远不再发帧(实测存活 3 分 08 秒、`events-exited-respawning` 计数 0)。T3-8 在 Close 里清空快照缓存,指望重生的流填回来——重生根本不发生,于是投影永久停在 `unknown`,Open 控件禁用点不动,连重开 CLI 的入口都没了。修复=删掉"只清缓存"这个动作,Close 确认消失后改为**重开观察流**(`restart_status_streams_for_workspace`),重建的 `ah events` 立刻发 `daemon_absent` → 投影 `inactive` → Open 恢复可点。一般规则:一条流绑定的是某个 daemon 实例而非 config,Studio 自己改变 daemon 存亡就必须重开观察者。决议 D-C7 已改写(原"可接受的短暂窗口"依据被实测推翻) |
| T3-10 | **`lingering` 判据误报:没有 tmux server 也说"CLI running"** | ✅ 已合并(#589) | 真机取证(2026-08-04,本机 WSL,`ah 1.7.0`):Studio 管的 6 份 config 里 **4 份**处于 `runtime_state:"inactive"` + `ahd_alive:true` + `tmux_server_alive:false` + `ahd_has_inventory:false`,全部 agent `KILLED`/`tmux_alive:false`——2026-08-02 决议 D-A1 的 `ahd_alive` 判据把它们全报成 `lingering`,面板给出一个 attach 不到任何东西、也无事可关的 Close 控件。D-A1 的推理是单向蕴含:ah 只在 ahd 退出时回收 tmux,但 ahd 活着**不蕴含** tmux server 存在(机器重启把 tmux 带走、ahd 又被拉起就是这个形状)。修正=面板呈现改判 `tmux_server_alive`(死窗格活在 server 里,server 没了就什么都没剩下);`/exit` 留死窗格的原始场景 server 仍在,判定不变。**只改呈现**,启动前清残留那条道继续用 `ahd_alive`(D-A4 宁可多清)。逐字捕获帧冻结为 fixture `SNAPSHOT_AHD_ALIVE_TMUX_GONE`;D-A1 已加修正块 |
| T3-11 | **CLI 控件的 hands-off 窗口改成进行态(loading),不再像"功能不可用"** | ✅ 已合并(#590) | T3-8 引入 `unknown` 相位后,状态未观测的那一两秒头部渲染的是一个**外观与不可用无异**的禁用 `Open in CLI`,使用者分不清"正在查"和"这功能挂了",第一反应是反复点。改为带 `Loader2` spinner 的进行态控件 + 说明在等什么的文案(`Checking…` / `Starting…`,更具体的事实优先)+ `aria-busy`。渲染优先级 = 有真实可执行动作 > 进行态 > 空闲入口,所以"一个在跑、另一个正在启动"时头部仍给 Attach/Close,不被 spinner 顶掉。可复用规则已回写 `FRONTEND_UI_SPEC.md` §2.7a「进行态 vs 不可用」|
| T3-12 | **残留 = 还有运行时:attach 打开死窗格,Close 才销毁** | ✅ 已合并(#591);⚠️ 真机未观测到,可达性依赖 [ah#53](https://github.com/SevenX77/ah/issues/53)(`tmux_server_alive` 在残留存在时报 false) | PM 裁决 2026-08-04(取代 D-A3):有残留就当作还在 running,attach 上去看那块 `remain-on-exit` 死窗格,必须 Close 清干净才能再 Open(最后一条本来就是现状)。推翻 D-A3 的理由:那块窗格正是 ah **有意**留下供事后取证的,而使用者想看的就是它——CLI 怎么退的、最后报了什么;原方案等于保留一份证据再让自己的 UI 拒绝展示。更糟的是它的落地方式:"不给 attach"被实现成"attach 即销毁"(走 `CleanupStale` 清掉整个运行时再报错),一次**观察**动作毁掉**观察对象**。落地=attach 独立判据 `reconcile_snapshot_attach`(与 Open 那条道分开),残留→`AttachResidue` 直接 attach 不清理,`degraded` 仍先清(坏状态没有可读窗格);菜单项标 `(exited)`,否则冻住的窗格会被读成卡死;「面板说有运行时」与「attach 有落点」共用谓词 `runtime_has_unreaped_residue` 并有测试锁死一致 |
| T3-13 | **bearer token 移出进程表:MCP 端点与 token 改走 `[master.env]`** | ✅ 已合并(#592) | ahd 不继承启动 shell 的环境(T3-6),所以这两个值此前只能烤进 master 的**命令串**——而命令串在进程表里全机可读,`ps` 一眼就能看到 bearer token(2026-08-04 实测可见)。ah 1.8.2 给了 master 席位自己的 env 通道(ah#37,由本仓提的 issue 促成),改走 `[master.env]`:启动脚本读法不变(`${STUDIO_MCP_URL:-}`),值不再进 argv。**同时把 `AH_VERSION_MIN` 从 1.4.0 抬到 1.8.2**——旧版 ah 会静默忽略未知配置段,不报错只是工具面消失,这种无声降级必须挡在启动前;launcher 脚本里那句硬编码的 `# requires ah >= 1.3.4` 注释也改成从 `AH_VERSION_MIN` 派生,免得再漂移。版本 fixture 换成本机实测的 1.8.2(门槛)/1.8.1(实际因缺 `[master.env]` 起不来的那一版) |
| T3-14 | **launcher 版本门禁把合规版本挡在门外(四份拷贝各自腐烂)** | ✅ 已合并(#593);真机复验:Open in CLI 3 秒起会话,token 在进程表零命中,`/proc/<pid>/environ` 两个变量都在,sidecar 收到已鉴权的 `/mcp` 流量 | T3-13 把门槛抬到 1.8.2 后真机复现:装着 1.8.2 的机器被自己的门禁拒绝,报 `ah 1.8.2 is installed; Studio requires ah >= 1.8.2`。根因不是门槛写错——是那段 shell 门禁被复制成**四份**,已经各自腐烂:一份的第三分支写成 `[ "$min_minor" -eq "$min_minor" ]`(拿一个从未赋值的 shell 变量跟自己比,恒假),让「主次版本相等、只看 patch」这条路彻底不通;另一份混进 `[ "$----" = "$----" ]`(恒真);剩两份是干净的。门槛还是 1.4.0 时看不出来(装的都是 1.7/1.8,minor 更大,走前一条分支就过了)。修复=四份合成**一份** `ah_version_gate_script()`,并把三分支比较换成一次数值比较(没有分支就没地方藏恒真恒假条件);非数字版本串(ah 缺失时的 "unknown")落进 case 拒绝分支,不让 `$(( ))` 炸出来。测试改成**行为验证**:把生成的片段喂给真 `sh` 跑八个版本档(门槛/上下邻居/非数字),断言放行结果 |
| T3-15 | **恒假的 state_dir 前置门把 bootstrap 整条路杀死** | ✅ 已合并(#598);真机复验:切走切回后 `bootstrap-seeded runtime_state=Active` 首次出现 | 补测轮观测:运行时活着、status 退 0、两条道身份字段一致,`bootstrap-seeded` 仍恒 0。根因:`verify_snapshot_identity` 要求 ah 的 `state_dir` 位于工作区下或含 Studio 的 workspace hash——而它是 ah 用自己的内部算法从 **config 路径**派生的 hash(实测 `/root/.local/state/ah/9cc1e7dd`),两个条件恒假 ⇒ 恒 Err ⇒ bootstrap 播种、Close 的 kill 升级、Open 冷窗口全部拿不到快照。第一性原理:验证=比较预期与观察,请求方无法独立形成预期的字段不能当验证条件。修复=身份锚只取 session `path`+`project_id`(请求方可预测、可跨 Windows↔WSL 规范化),`state_dir` 降为诊断(与 `config_path` 同级);空 sessions=无证据而非反证,接受。测试改以逐字捕获快照为准(旧的 SchemaDerived 身份 fixture 按错误预期构造 state_dir,让实现和测试共享同一假设互相印证了七周)。决议 `decision-2026-08-05-identity-anchor-and-tmux-server-alive.md`;Req 2.7/design.md 已加修订块 |
| T3-16 | **Resume:关闭清场后显式恢复上次对话(PM 裁决 2026-08-05)** | ✅ 已合并(#600+#601);真机 F-6/F-7 全过:软链 `sandbox/.claude/projects → /root/.claude/projects` 建对(getent 派生,#601 修掉死守卫+空目录坑);暗号对话落宿主(slug `-mnt-d-coding-skills-exp-a-round3` 与设计预判一致)、活过 Close;Resume 1 秒起、argv 带 `--continue`、恢复的终端里搜到上次暗号 ×2;从未开过会话的 demo-loop 点 Resume → 打印回落说明、无 `--continue` 全新启动 | 两项裁决落盘 `decision-2026-08-05-close-cleans-resume-restores.md`:R1=退出全量清扫是产品预期(此前"另案待裁"关闭);R2=恢复对话走显式 Resume。机制:ah 给 claude master 设 `CLAUDE_CONFIG_DIR=<沙箱>/.claude`(home_layout.rs:1813),`ah stop` 删 master 沙箱(ahd.rs:278) ⇒ 对话记录活不过关闭。修复=包装脚本把 `$HOME/.claude/projects` 软链到宿主 WSL home(与凭据同一手法,新开与恢复都做);`Open in CLI` 下拉加 "Resume Claude code",同一条启动流程,执行尾分叉:对话目录非空→`--continue`(不带初始 prompt),空→打说明回落全新启动(安全降级)。仅 claude,codex 命令不暴露该参数(类型层面)。worker 不共享目录=天然隔离,resume 不会捞到 worker 的对话 |
| T3-17 | **`STUDIO_AH_HOST_HOME` 整体退役(#596 遗留死代码清理)** | ✅ 已合并(#604) | 证据:payload 三处 `export` 在 payload 内零读取者,而 master 由 ahd 从零拼环境 spawn 收不到它(2026-08-05 实测 /proc 环境无此变量);`.claude.json` 是 ah 材料化拷的真文件、`.credentials.json` 不存在而登录正常(#596 shared_credentials_dir)、codex auth.json 由 ah 3a9c130 共享。处置:三 export+三软链段删除;claude/codex 的 binary 回退**用途仍活**(daemon PATH 无保证)→ 重锚 getent `host_home`;测试升级为「生成物零 STUDIO_AH_HOST_HOME」终局锁。Rust 196 绿 |
| T3-18 | **copilot 默认模型 = DeepSeek V4 Flash(PM 裁决 2026-08-06)** | ✅ 已合并(#607);真机:冷加载角色 chip 即 DeepSeek V4 Flash,DeepSeek 面板答题 10s 且 DOM 现 `studio:get_skill_overview completed` | 默认写死在前端家族阶梯 `pickDefaultCopilotGroupIds`,flash 插排位第一;设计源 00_settings-ux-spec.md §3.2 + 手册三张 copilot 切片同步。官方 DeepSeek /models 实测 = v4-flash + v4-pro,双双 verified |
| T3-19 | **内置 copilot 角色 = 固定模型;v4 pro 退役(PM 裁决 2026-08-06)** | ✅ 已合并(#608);真机:掏空 PUT 422/换模型 PUT 422/删固定角色 409 全在真端点复验;设置页两张内置卡零模型移除钮,V4 Pro 卡消失 | 真机点验抓到的缺陷:固定角色模型可被 UI 移除 → copilot_deepseek_v4_pro 成空壳。修在两层:前端藏移除钮+拖拽只认推荐组;后端 `fixed_copilot_model_change_error` 双 PUT 入口边界校验。固定配置 v4_pro→v4_flash;数据清理后终态=opus_4_8+deepseek_v4_flash 两只固定角色 |
| T3-20 | **Resume 跟随上次打开的 CLI + codex resume(D-G,2026-08-06)** | ✅ 已合并(#609);真机 G-5:codex 开→暗号→Close→菜单现 "Resume Codex"→恢复终端见暗号+codex 自报 resuming+argv 含 `resume --last`;无记录工作区无 Resume 项 | owner=Tauri 启动记录 `code_assistant_last_opened.json`;codex sessions 软链回 host home(与 claude projects 同构);`codex resume` 默认按 cwd 过滤(0.142.5 实证)。决议文档 D-G1..G3 修订块 |
| T3-21 | **codex_apps 空段拒启修复** | ✅ 已合并(#611);真机:修复后 Codex TUI 正常起,codex 内置 codex_apps MCP 自行启动 | 真机点验抓到:旧脚本在 codex_apps 段缺失时凭空创建仅含 startup_timeout_sec 的空段,codex >=0.142 校验 `invalid transport` 拒启 master 秒死。删凭空创建分支,只留已有段的补丁路径(RED→GREEN,201 Rust 测试) |
| T3-22 | **Settings「CLI」区(安装/鉴权/一键装/分角色配置)** | ✅ 已合并(#613 状态面板 + #614 安装/配置);真机:七行依赖全 ok(截图)、安装钮全 ok 时隐藏、effort 下拉=CLI 档位、autosave 落 /api/settings、claude master argv 现 `--effort low`、clotho 覆盖入 ah.toml `env = { ANTHROPIC_MODEL = ... }` | 设计已写回 00_settings-ux-spec.md §3.9(含两处修订:一键安装拉真控制台因 OAuth 交互;codex worker/worker 级 effort 无 env 证据不注入)。提案 #610 存档。期间 GitHub 大面积故障拖了合并数小时(外因) |
| T3-23 | **P1 数据面读工具 PR-H/PR-I(read_skill_file / get_workspace_config / list_run_artifacts / read_run_artifact)** | ✅ 已合并(#616+#617+#618);真机:/mcp 端点 34 工具在面,整读/切片/逃逸拒/投影/空枚举全过 | 全部有界(D6):400 行/48KB 截断如实标注,路径限定 skill 目录与 artifacts 目录内。真机直测抓出两个既有缺陷并修复:① query_run_trace/wait_for_run/set_output_artifacts 定义了却没进注册表,两个面全是死工具(#618 补注册+「定义即注册」防漂移测试);② (type, None) 可选参数约定从未生效(SDK 无 tuple 分支落 string 兜底、dict 名单一律全必填),四工具改完整 JSON schema 透传(#617 错机制、#618 纠正) |
| T3-24 | **PR-J 知识资产** | ✅ 已合并(#616) | 新增 KB-14 产物与落盘(两条声明路径 target∈{file,artifact}+runtime_config.artifacts、<stem>_latest_<ts> 命名、fingerprint 参与、验证链);KB-09 补 query_run_trace 有界诊断套路;KB-13 工具地图补齐现役读写工具;KB-08 P0-P2 矛盾修复;hub 注册 |
| T3-25 | **P2 写工具收口 PR-K(write_skill_file / bind_test_input)** | ✅ 已合并(#623);真机:/mcp 36 工具在面,两工具 required 列表正确(expected_hash 可选生效) | write_skill_file 走 update_skill_file 同一 service(路径白名单/冲突哈希/写入登记);bind_test_input 落 .workspace/import_files(运行时工作区数据,与 I/O 面板 import 同链同命名规则),refresh 物化绑定。两工具进写审批登记表、绝不进免审批清单(测试锁);KB-13 工具地图同步。CLI 会话自此没有裸 Write 绕写边界的理由 |
| T3-4 | mvp0 遗留后端终端栈删除(`routers/terminal.py` / `terminal_manager.py` / `models/terminal.py` / ws 路由 / `ptyprocess` 依赖) | ✅ 已合并(#620);真机:重建 vendor 重启后 POST /api/skills/*/terminal 404 | ptyprocess 从 backend 与 engine 的 studio extra、根 mypy override、uv.lock 全链清零;test_security.py 整文件(纯终端安全测试)与请求审计台账两行随删;backend 1561/engine 1523/gateway 全绿 |

#### 事故修复 · copilot 权限模型"未知工具默认放行"漏洞(exp-B,2026-08-01)

**事故**:无头实验 exp-B(证据 `D:\coding\skills\_copilot-lab\rounds\exp-b-round1\events.jsonl`)中,copilot 的 Write 被写白名单拒绝后,模型改用 Windows CLI 自带的 PowerShell 工具把 `runtime_config.json` 和 import 文件直接写进无权目录 —— 16 次调用全部放行,零审批卡。

**根因**:`copilot.py` 的 `can_use_tool` 只显式处理 Write/Edit、Bash、MCP 写三类,末行对一切未知工具默认 `PermissionResultAllow`(黑名单模型);PreToolUse 的写边界 hook(`_WRITE_CLASS_TOOLS`)与 Bash 强制-ask hook 也都不覆盖 PowerShell,硬边界同时失效。

**决议(操作指令,2026-08-01)**:权限模型改为"已知语义白名单"三档 —— ①声明式免审批名单(`_DECLARATIVE_ALLOWED_TOOLS`,本次补入 TodoWrite/Skill 两个已知声明式工具)直放,该名单同时喂 `allowed_tools` 预放行与 `can_use_tool` 直放两层(单一事实源);②名单之外的一切 —— 执行类(`_EXECUTION_CLASS_TOOLS` = Bash/PowerShell,PreToolUse ask-hook matcher 与审批档共用这份定义)、写类、MCP 写、未知工具 —— 一律 `_hold_for_tool_approval` 挂起审批;③默认放行档删除,不存在 fall-through Allow。`Task`(三女神 subagent)无使用实证,留在默认审批档,未来凭证据显式分类。

| # | 项 | 状态 | 关键坐标 |
|---|---|---|---|
| SEC-1 | can_use_tool 三档白名单 + PowerShell/未知工具默认审批 | ✅ 随本行同 PR 合入 | 回归测试 `tests/services/test_copilot_guardrails.py`「exp-B 事故回归」节(PowerShell/未知工具必须挂起、免审批名单分类互斥);验收 = 全 CI 门禁绿 |

**与 P-3 的合并语义**(P-3 是反方向的放宽,后续处理时以此为准):只读命令白名单免审批、其余执行类一律审批、未知工具默认审批。

### 冲刺清单

#### 第一波 · copilot 旅程工具(关键路径)

| # | 项 | 状态 | 关键坐标 |
|---|---|---|---|
| W1-1 | `create_skill` 工具(包 `POST /api/skills`,走索引落库) | ✅ 已合并(#526) | 8 测试 + 完整 backend 套件绿;顺带修服务层缺陷:失败创建现在回滚目录,skill_id 不再被半成品毒死 |
| W1-2 | `run_skill` + `get_run_detail` 工具 + 真跑审批放行 | ✅ 随本行同 PR 合入 | 真跑走审批卡(推翻旧"只能 UI 触发"禁令);get_run_detail 有界投影(事件只给计数+错误摘录,final_context 4000 字符截断);9 测试 |
| W1-3 | golden 工具组(list / read / set / delete) | ✅ 随本行同 PR 合入 | 四工具:读免审批、写走审批卡;写直调 golden_diff 服务层(HTTP 层 browser-fallback 护栏是防浏览器绕 Rust 的边界,copilot 后端写=DEF-027 同族的已接受写路径);plan 端点不包(其写计划无人执行);10 测试 |
| W1-4 | `resume_run` + resume 有效性工具 | ✅ 随本行同 PR 合入(与 W1-5 同 PR) | get_resume_validity 免审批;resume_run 走审批卡,支持 checkpoint/节点区间/human_input/context_overrides;9 测试(与 W1-5 合计) |
| W1-5 | `publish_skill` / `fork_skill` 工具 | ✅ 随本行同 PR 合入(与 W1-4 同 PR) | 两工具都走审批卡;publish 直调路由函数并显式供给同组依赖(不复制发布管线);fork 走服务层 |
| W1-6 | 资产纠偏:KB-13 工具清单与"Rust 唯一写者"两处失实 | ✅ 随本行同 PR 合入 | §2 重写为 27 工具两审批档真相 + CLI 表面"无工具"诚实声明;§3 改为三条写路径(Rust D12 / Write-Edit 直写例外 / MCP 审批写) |

#### 第二波 · 引擎(run 路径的诚实与补全)

| # | 项 | 状态 | 关键坐标 |
|---|---|---|---|
| W2-1 | 并联拓扑编译期诊断(暂不支持并联执行,`[F-v3-*]` 码 + 指位) | ✅ 被真修吸收(#804) | "暂不支持并联"的临时拦截不再需要(并联已可执行);编译期保护改为其残余真问题:无依赖关系的两 phase 声明写同一 output 字段 → `[F-v3-parallel-write-conflict]`(错误码全表 98→99) |
| W2-2 | 并联真修:执行态写入迁 delta + reducer 通道 | ✅ 已合并(#804) | 实现取 WorkflowState reducer 方案而非接 BlackboardState 的 `blackboard_data_merge`(该状态类未投用;决议文档 `.kiro/specs/decision-2026-08-15-engine-parallel-fanout-state-channels.md`):phase 节点回 delta,`data`/`flow` 通道 `merge_business_channel`/`merge_flow_channel` 折叠,`phase_outputs` 按 key 合并;invoke 入口包装形 `{"inputs": …}` 由 `coerce_business_data` 单一权威展平。**遗留**:`recursion_limit` 接线(`core/runner.py` invoke 点)未随本 PR,单独排期 |
| W2-3 | iterate.over 契约统一:裸业务字段名(KB 契约)——运行时黑板解析 + 编译期 over 来源校验(`[F-v3-iterate-over-not-list]` 编译相)+ 状态路径形删除 | ✅ 已合并(#806) | 决议 `.kiro/specs/decision-2026-08-15-engine-iterate-over-business-field.md`;根因链:KB-06 教裸名/真实 skill 全裸名/引擎只解析 `data.*`/编译零校验/Studio 吞 fatal 成 crashed/`RUN_REQUIRES_PREDICT` 连坐锁死 run。顺带修掉 `BusinessData.get` 走 `model_dump()` 不认别名导致路径静默取空 |
| W2-4 | predict 占位 mock 下的相位校验器降级(不再当致命,记 `validator_downgraded`) | ✅ 已合并(#809) | 决议 `.kiro/specs/decision-2026-08-15-predict-stub-validator-downgrade.md`。**只在这一轮用的是启发式占位 mock 时**降级;有 golden case 的相位照常严校——predict 仍要能发现真问题。降级记录挂持久 `_MOCK_SOURCE_LOG`(trace stamper 在 `on_phase_end` 就 pop 掉 cache,而校验器在其后才跑) |
| W2-5 | predict 桩拿得到子图内相位的 output schema | ✅ 已合并(#811) | 决议 `.kiro/specs/decision-2026-08-15-predict-nested-phase-schema.md`。根因:runner 只遍历**根 skill** 的 `compiled.nodes` 收集 schema,子图是装配期另起一次 `compile_skill` 编出来的,其相位从不进这份名单 → 桩退化成 `{"value": "<mock_unknown>"}` → 跑满 `max_iterations` 抛 `[F-v3-agent-exit-control-failed]`。schema 改挂相位自己的 `PredictGatewayChatModel` 实例(第一版按相位名建注册表,两个子图里同名 `review` 互相串味,生产 trace 实证) |
| W2-6 | `depends_on` 多前驱 = 汇合闸(LangGraph 列表形边),不是 N 条触发线 | ✅ 已合并(#812) | 决议 `.kiro/specs/decision-2026-08-15-engine-multi-dep-join-waits-for-all.md`。普通边是触发线不是栅栏(`langgraph/graph/state.py:915-920`:单起点等一个、起点列表等全部)。只有前驱**同超步结束**时两种写法才等价——常见菱形扇出扇入正好满足,所以长期未暴露。深度不齐则汇合相位抢跑;必填字段碰巧齐了则更坏:**静默多跑 N 次**,run 路径多烧 N 次真 token 且后一次覆盖前一次 |
| W2-7 | loop 相位的"提供集"=`accumulate.var`(编译期对齐运行期) | ✅ 已合并(#813) | 决议 `.kiro/specs/decision-2026-08-15-engine-loop-accumulator-dataflow.md`。运行期 `final_payload = {accumulate.var: acc}`(与设计源 `docs/engine/mvp1/02-mechanism/04-run-outer/02-iterate/baseline.md:48` 一致),而 `io.outputs` 是**每一轮**的契约;编译期却把 `io.outputs` 当提供集 → 作者无解:写累积器名则每轮输出校验必挂,不写则下游消费累积器编译必挂。顺带收紧另一半:下游消费 loop 的**每轮**输出字段,从前编译放行、运行读不到,现在编译期直接拦 |

| W2-8 | predict 端点把引擎交给工作线程,不在事件循环上跑 | ✅ 已合并(#818) | 决议 `.kiro/specs/decision-2026-08-15-predict-off-the-event-loop.md`。端点是 `async def` 却直接调同步的 `dispatch_predict_job`,引擎批处理路径内部的 `asyncio.run()`(`core/graph_assembler.py` `_run_batch_iterate_payload` 与 subagent 批处理路径)在事件循环线程上必然抛 RuntimeError → **任何用 `iterate.mode=batch` 的 skill,从界面点 Predict 一定失败**;同函数在 `services/copilot_tools.py:543` 已包 `asyncio.to_thread`,所以 copilot 路径一直能 predict、界面按钮一直不能 |
| W2-9 | 引擎归不了类的异常不再冒充 LLM provider 失败(`engine.unexpected_error`) | ✅ 已合并(#819) | 决议 `.kiro/specs/decision-2026-08-15-engine-unclassified-error-code.md`。`runner.py` 两处 `except Exception` 包的是**整张图的一次执行**,兜底码却是 `llm.provider_invoke_failed`;而真 provider 异常全都自带 code(`LLMProviderError.__init__` 把 `error_code` 列为必填构造参数、`LLMProviderMissingError` 类级写死),兜底分支执行本身就证明"它不是 provider 异常"——这个默认值每次生效都是错的。同函数三行外的 `_safe_provider_error_message` 用的正是相反判据。不动 `adapters/engine.py:392`:那处包的是 `model.stream(...)` 内部,默认诚实 |

| W2-10 | 一个 batch 条目 / loop 轮次是一次独立调用,有自己的迭代身份 | ✅ 已合并(#822) | 决议 `.kiro/specs/decision-2026-08-15-engine-batch-item-isolation.md`。相位级 batch 的 `_phase_batch_runner` 只设 branch index(纯事件标签)不设 `active_outer_ns`,而它的图级孪生 `_graph_batch_runner` 两个都设;`_skill_node` 又把 `checkpoint_ns` 写死成 `agent:{phase_id}`、`thread_id` 是整次 run 的常量 → 每个条目递给 agent 图的 key **完全相同**,checkpointer 让条目 2 恢复条目 1 的整段对话,`ExitControlMiddleware` 让条目 2 继承条目 1 用剩的预算。相位级 loop 同一漏点一并修。中间件身份走自定义键 `agent_invocation_id`(实测 LangGraph 递给 hook 的 `checkpoint_ns` 是**每次 hook 调用**的新 uuid,`_skill_node` 写的那个到不了) |
| W2-11 | 校验器强制的 `business_data_md` Markdown 形状,契约里必须明说 | ✅ 已合并(#825) | 决议 `.kiro/specs/decision-2026-08-15-business-data-md-shape-in-exit-contract.md`。`parse_md` 的规则是「一个 `## ` 标题 = 一个完整输出对象」,但提示词只说 "business_data_md 遵循 output_schema **列业务字段**" 再甩一份原始 JSON Schema,工具参数描述是 "Final structured/unstructured business markdown output." ——**两处都没提 `##`**(实测:该次 run 的 `prompt_captured` 全文搜不到任何 `##` 示范)。引擎里有个函数专门说这件事(`tools/dynamic_schema.py:253` `render_dynamic_schema_output_format`),**`src/` 下零调用点**;两种写法经 `core/schema_engine.py:139` 汇成同一份 JSON Schema,所以**没有任何相位被告知过这个形状** |

| W2-12 | 引擎自己定过性的致命错误,出口不得压回「不明」 | ✅ 已合并(#826) | 决议 `.kiro/specs/decision-2026-08-15-fatal-error-payload-survives-to-the-caller.md`。**这条修的是 W2-9 自己留下的洞**:`_artifact_error_result` 读 `exc.error_code` / `exc.details`,那是 `LLMProviderError` 的形状;引擎自己的 `GraphAgentFatalError` 把分类放在 `error_payload`(`core/exceptions.py:128-160` 由 `make_error_payload` 建,带 `[F-v3-*]` 码 + `phase_id` + `field_path`),两个 `getattr` 全部落空 → 一个**已经分好类、已经定位到相位和字段**的错误,到调用方手里只剩 `engine.unexpected_error` + `exception_type`。真机实证:`raw_settings_markdown is a required property` 报出来时既没有相位名,也没说是`io.outputs` 三道校验(finish_task / 相位输出 / validator 返回值)里的哪一道——而三道的修法相反 |

| W2-13 | 编译缓存键必须覆盖编译真正读过的文件 | ✅ 已合并(#829) | 决议 `.kiro/specs/decision-2026-08-15-compile-cache-key-covers-every-input.md`。`core/cache.py` 的 `_collect_skill_files` 只走 `root/GRAPH.md` + `root/phases/**/*.md`;而带嵌套子图的 skill 把绝大多数相位放在 `root/subgraph/**`,每个 validator / logic action 都是 `.py` → 实测 lab skill **133 个 `.md` 只进了 5 个,27 个 `.py` 一个没进**。因果证据:破坏一个 subgraph 阶段后编译报 `issues=0`;**内容一字不改**,只把根 `GRAPH.md` 的 mtime 推后 1 秒,同一次破坏立刻报出 7 条诊断——唯一变量是缓存键。取舍按 ccache(depfile 记全部实际打开的头文件)与 Bazel(显式声明输入 + 沙箱强制)的同一条不对称:键多收一个文件只多一次重编译,少收一个文件是**错误答案且无任何提示**。点目录是唯一排除项,理由是实证而非整洁——`.workspace/` 每跑一次 run 就重写,收进键等于缓存不存在。遗留(明写不装作已解决):从 skill 根之外链接进来的 subgraph 仍不在键内,要覆盖它得让 loader 上报真实读取集合 |
| W2-14 | 编译诊断必须指名它真正所在的那个文件 | ✅ 已合并(#830) | 决议 `.kiro/specs/decision-2026-08-15-compile-diagnostics-name-the-file-they-are-in.md`。`_payload_source_path` 不做相对化,做的是**截断**——`GRAPH.md` 直接返回字面量 `"GRAPH.md"`,其余从**最后一个** `phases`/`io` 段截起。一层 skill 上截出来的串碰巧等于相对路径,所以一直没暴露;lab skill 实测:41 个 md 压成 32 个串,**只有 5 个**在根目录下找得到同名文件,**8 个不同的 `GRAPH.md` 全渲染成 `"GRAPH.md"`**,`phases/review/SKILL.md` 同时指两个真实文件。端到端:弄坏 `subgraph/story-analysis/GRAPH.md:19`,引擎报 `source_path='GRAPH.md' line=19` → 抽屉显示 `GRAPH.md:19`、Monaco marker 落在**根图第 19 行那段正常内容**上。另一处安静的丢失:`lintErrorsForFile` 按 `candidate === target || endsWith('/'+target)` 挂行内 marker,打开嵌套子图相位文件时两条都不成立 → **那些文件从来没有过行内 marker**。修法归位到唯一知道根的边界 `SkillLoader.compile_skill`(深处只带完整路径,出口统一改写一次;嵌套天然成立:子图出口→父图接回绝对→父图出口),照编译器诊断通行做法(GCC/Clang 记真实文件、按 cwd 渲染,不把头文件的错算到包含它的文件上);拒绝其 include-stack 展示,因为 Studio 呈现面只有一个 `source_path` 字段。根相位路径一字不变、引擎既有测试零改动 = 只改错的那一类的证据 |
| W2-15 | `actions/*.py` 里可以写普通私有 helper——只有被声明的名字才是 action | ✅ 已合并(#834) | 决议 `.kiro/specs/decision-2026-08-16-actions-module-private-helpers.md`。`loader.py:1419-1420` 把 `_module_functions` 扫出的**每一个**模块级函数都跑 `_validate_action_signature` 并注册成 `ActionDef`,而 `_module_functions`(`:1494-1499`)连 `_` 开头的私有函数一起收 → 编译 lab skill 实际报出 `action '_addressable_units' must accept exactly one inputs parameter` / `'_entity_ids'` 同款,而这两个名字**既不在** `LOGIC.md` 的 `actions:` 清单里、**也从未**被派发。作者已被迫绕道:`assemble_batch.py:49` / `prepare_batch.py:25` / `build_scene_stream.py:9` 的 helper 全部嵌进 action 函数体内,`prepare_chapter.py:78` 的 `_resolve_chapter(inputs)` 能过**纯属它恰好只有一个叫 `inputs` 的参数**。举证三问:①派发唯一入口 `graph_assembler.py:1560,1566-1567` 只遍历 `phase_ast.actions`,且 `loader.py:2802-2809` 强制 frontmatter 清单与 body `<action>` 逐项同序 → 未声明函数**没有任何可达路径**;②格式真相源 `docs/engine/skill-spec/00-FORMAT-GROUND-TRUTH.md` §3 把 `actions` 定义为「action 名注册表」、签名规则主语是 `<action_name>`,查遍 physical-layout:25 / compile-rules CR2:388 / LE1-3:391 / skill-syntax 全文**没有一句**说 actions 模块是只放 action 的扁平命名空间 → 现状是漂移不是 as-designed;③错误码 `[F-v3-logic-action-entrypoint-missing]` 注册表定义是「action 无 `run()`」(compile-rules:288),拿「缺失」的码去报「签名不对」方向相反。修法:注册表以声明为准——只有出现在 `actions:` 里的函数才校验+注册,purity 扫描**位置不动**保持文件级(compile-rules:79 主语是「action/tool Python 文件」,helper 一样能不纯);并补上对称的另一半——声明了却找不到同名函数 = 编译期 FATAL(修复前实测 `DID NOT RAISE`,只在运行期抛 `KeyError`),照抄同一份 loader 里 `validator: true` → 检查顶层 `validate` 函数的既有形状(`:315-338`)。借 Python `__all__`/`_` 前缀与 pytest 收集规则的「只对被选中的对象施加该角色的规则」,**拒绝** pytest 的按前缀隐式选中(会造出第二个真相源;判据只有「名字在不在声明清单里」)。新诊断当场抓到存量漂移:`test_graph_io_target_declaration.py` 的 fixture 声明 `actions: [draft]` 却写 `def run(inputs)`(mvp0 约定,`mvp0/skill-spec/03-logic-md-spec.md:94`),一跑必 `KeyError`,按「设计为准」改 fixture。真实 skill 因果闭环:同一份 `text-segmentation` 副本补回模块级 `_entity_ids`,旧 loader 报原文错误、新 loader `COMPILE OK` |

| W2-16 | 子 skill 里的诊断不认领根图上的任何节点 | ✅ 已合并(#833) | 决议 `.kiro/specs/decision-2026-08-16-a-child-skills-diagnostic-claims-no-root-node.md`。**这条是 W2-14 明写留下的那个洞**(#830 决议「已知未处理(另立)」)。`PHASE_FILE_RE = /(?:^|\/)phases\/([A-Za-z0-9_-]+)\//` 在路径**任意位置**匹配,#830 把 `source_path` 变成真正的根相对路径之后,嵌套相位第一次以完整形状 `subgraph/<child>/phases/<id>/<file>.md` 到达前端,而这条正则照样在中间匹配上,得出相位 id 后把**另一个 skill 的相位**的诊断挂到根图同名节点。用 #830 的引擎 fixture 实测两次(2026-08-16):往 `subgraph/first/phases/review/SKILL.md` 加未知字段 → `[F-v3-agent-schema-unknown-field] | 'subgraph/first/phases/review/SKILL.md'`,被读成根节点 `review`;让 `subgraph/first/GRAPH.md` 声明产不出的输出 → `[F-v3-graph-io-schema-invalid] | 'subgraph/first/GRAPH.md' | 'io.outputs.required'`,**子图的输出块被 `boundaryNodeIdFromField` 读成根图全局 Output 节点**。修法是把「路径从根往外读」立成唯一规则并只存一份(新模块 `diagnostic-paths.ts`;此前 `field-compile-errors.ts` 与 `node-compile-errors.ts` 各存一份逐字相同的正则,本次缺陷正是两份拷贝同时错):相位归属锚到 `^phases/<id>/`,`field_path` 只在诊断本身位于根 `GRAPH.md` 或无文件时才允许认领根节点,`lintErrorsForFile` 从「后缀包含」改成整串相等。顺带删掉后者的「绝对沙箱路径泄漏」容忍——后端 `_relocate_lint_files_to_skill_root`(skills.py:480)无条件剥前缀、`_relative_compile_path`(skills.py:2296)永不输出绝对路径、#830 后 `source_path` 本就是根相对,该泄漏已不可能;而保留它现在**有害**:孙 skill 的文件天然以子 skill 文件的整条路径结尾,打开后者会把前者的诊断一并划红。子图诊断本次**不挂任何根节点**(设计源未规定,三条理由见决议 D1:路径里没有任何东西能说出承载它的根相位名 `alpha`——子 skill 目录叫 `first`;引擎自己已在 `phases/alpha/SUBGRAPH.md` 上报 `[F-v3-agent-subgraph-invalid]`,画布不哑;抽屉拿的是全量列表,信息不丢) |
| W2-17 | baseline 文档不得把已停用的错误码写成活着的编译闸 | ✅ 已合并(#835) | 决议 `.kiro/specs/decision-2026-08-16-baseline-docs-must-not-cite-dead-error-codes.md`。`02-mechanism/04-run-outer/01-graph-exec/baseline.md:54/86` 断言"子图 outputs 仍严校(编译期)…不一致报 `[F-v3-subgraph-io-mismatch]`,错误信息标明 `outputs do not match`"——两句全假。机械核验:`grep -rn "outputs do not match" packages/graph-agent/src/` **零匹配**;该码在引擎源码里只剩 `error_registry.py:95` 一条 registry 条目。这道闸 2026-06-20 已由 `cad7dbc0`(PM 授权)从 `loader.py:_validate_subgraph_io_contracts` 删除,commit 原文写明"retained in the registry (no longer emitted) to preserve the round28 registry↔owner bijection"。**代码没漂,漂的是文档**:设计源 `01-contract/02-skill-syntax/mvp1-alignment.md:78` 本来就写「父图和子图 IO 不需要字段全集一一相等」,`:126` 更把「父子图 IO 1:1 强绑定」列进 §4 drift 清单。边界并未消失,只是挪到运行期 `runtime/state_mapper.py:318` 按声明 schema 过滤,越界记 `[F-v3-runtime-state-mapping-failed]`。连带订正同段三个失效行号(`loader.py:528`/`:211`/`:363` → `:996`/`:397`/`graph_assembler.py:1589`),并标注码表 `03-compile-rules/mvp1-alignment.md:307/308`——两码来历不同,措辞分开:mismatch 是「不再发出」,schema-incompatible 经 `git log -S` 全史核验**从未有过发出点**(只有 `c32575fa` 建表那一条)。不只改文字:新增机械门禁 `test_baseline_doc_error_code_liveness.py`,断言"baseline 里引用的每个 `[F-v3-*]` 都必须在引擎源码有发出点,否则该行须自带 `无发出点` 标记"(`error_registry.py` 显式排除,否则门禁恒真);用同行标记而非豁免名单,因为豁免清单会和它守护的正文以同样方式腐烂。**门禁的边界也如实写进决议**:它只认"引用了错误码"的句子,同一条假话写成纯散文就抓不到——实证是 push 后靠人工通读又揪出四处不带码的漏网(同文件 `:4` frontmatter `status` 的「outputs 仍严校」、`:11`「现状一句话」的「outputs 仍严格相等」、`01-contract/02-skill-syntax/baseline.md:31` 差异表现状列、`01-graph-exec/mvp1-alignment.md:28` 的「**outputs 保留**相等校验(下游契约)」),已一并订正;最后一处属设计与设计打架,以 skill-syntax §3.4/§4 为准。不把门禁扩成匹配「严校/1:1」这类词,是因为那会退化成按措辞定罪的子串断言,本仓已因此下线过整套手册门禁。该门禁当场抓出第三例:`01-contract/03-compile-rules/baseline.md:42` 把无发出点的 `[F-v3-mention-unused-registry-entry]` 与有发出点的 `[F-v3-reference-reader-failed]` 并列成两个会报的 WARN,同句还写着码表"96 个"而实测 `len(ERROR_REGISTRY)==99`,一并订正。**明写没做的**:全仓另有 13 个"只有 registry 条目、无发出点"的码,逐个判定该补发出点还是该退役需各自的证据,留作后续任务——未经核实批量标注它们,正是本条要修的那种错误。mvp0(7 处)与 `docs/studio/_reorg/`(3 处)不动:前者是已定稿里程碑的历史记录,后者成对记着"改前状态 + 待办变更项",都不是现状描述 |
| W2-18 | 一个相位开启它自己的对话,不接着上一个相位往下说 | 🚧 PR #838 待合并 | 决议 `.kiro/specs/decision-2026-08-16-a-phase-opens-its-own-conversation.md`(用户 2026-08-16 裁决「要改」)。**这不是新规矩,是一条只执行了一半的旧裁决**:九轮定稿 Round 8 第 3 条用户原话「phase 间默认强隔离(`messages = []` by-design)+ 按需挖掘机制(context_access opt-in)」,现行迁移决议 `docs/design/2026-08-15-legacy-cognitive-features-migration-decision.md:184` 还加了锁;而 live 代码自己引用这条裁决——`_cognitive_framework_tools` docstring 原文 「(Round 8: strong isolation by default)」——**只接了后半句 opt-in,默认隔离从来没接线**。漂移由两条与本议题无关的提交带进来(`353fbb8a` 2026-05-31 / `3edd12d0` 2026-06-07),仓内无任何决议记录。真跑现场证据(`2026-08-15T12-40-22_bb6e358a`):`continuity` 开场 61 条消息 **60 条属于别的相位**,其中一条 human 消息原文「工具 `finish_task` 在 phase `foreshadow` 中连续重复执行了 3 次」——关于**另一个相位**的死循环诊断被当成给它的指令;`foreshadow` 与 `prop` 继承 `entity_and_characters` 的整段已完成对话后照抄它的输出字段,被 finish_task 以 `Extra inputs are not permitted` 驳回。**说准确**:相位并没有瞎跑——作者写的系统提示词自己插值 `{字段}`,实测 continuity 的系统提示词里确实带着它要检查的事件 ID/角色/道具;丢掉的是引擎那份结构化输入 JSON 块(42 次相位执行只有 38 次`runtime_input_injected`,continuity 与 settings 一次都没有)。修在 `build_phase_input` 一处而不是 `graph_assembler.py:2194`:`_skill_node` 收到的就是这份切片,改 mapper 会让 `orig_msg_count` 基准跟着归零;**只改 assembler 会让未知工具校验 + token 记账 + `completed_tool_call` 事件三件事静默失效而全套测试仍绿**。middleware 逐槽核对:RuntimeInput / ExecutionControl / LoopDetection 三槽净修复,零槽失效;Compaction 的参数注释自报出处是同一份九轮定稿(`compaction.py:51-55`),所以压缩本来就是相位内机制,隔离之后它才第一次跑在设计尺度上。写回不动 → trace/checkpoint/HITL 全不受影响,既有测试零改动(1510 passed)。遗留明写:相位**内**被 nudge 后自己也拿不到输入块(判据是「有没有 HumanMessage」这个代理指标,另立);`flow.working_memory` 是第二条跨相位通道,待裁决 |

> **W2-3 ~ W2-9 是一条因果链,不是七个独立缺陷。** 它们是在用 MoirAI 修真实复杂 skill
> (`story-deconstruction`:子图嵌两层 + 六路并行 + loop 累积 + 图级 batch)的过程中
> 逐个撞出来的——前一个不修,后一个根本暴露不出来。W2-1~W2-7 修完后,该 skill 的 predict
> 在补丁副本上跑通全链(42 次相位执行,`success = True`,产出 `story_framework`);
> W2-8 是把这个成果送到界面上的最后一环(真机复验:`predict-2026-08-15T10-19-17_aaac658c`
> `status: success`,2.4 秒)。
> **但"predict 绿"的分量要说清楚**:同一次 predict 里 **16 条相位校验器被静默降级**
> (10 个不同相位名),因为占位数据满足不了语义校验器。准确的说法是"图的形状、连线与
> logic 节点的执行链路通了",不是"这个 skill 验过了"。
> **首次真跑(DeepSeek V4 Flash,2 章样例)失败**,`.workspace/runs/2026-08-15T10-19-55_df555c19`,
> 197 秒 7 相位后 `[F-v3-agent-exit-control-failed] Phase 'segment' failed: nudge budget
> exhausted (counts={'planning': 1, 'selfcheck': 0, 'standard': 1, 'total': 2}, max_nudges=1)
> after 8 iteration(s)`。trace 显示 batch 的两个条目**共用同一份 agent 循环预算**
> (迭代计数器 1→8 之后条目 2 从 **9** 接着数),且条目 2 的输出是条目 1 的内容
> (输入 `chapter_number=2` + 第 2 章原文,输出却是第 1 章剧情)——**这一条不报错、出错数据**。
> 根因即 W2-10。
> **第二次真跑(W2-10 合并 + 重建 vendor 之后)**:`.workspace/runs/2026-08-15T11-35-15_55b58e42`。
> `segmentation` 子图两章全部跑完,**串台消失**——第 1 章提交「驱车前往野狼谷、搭帐篷、
> 取出铜镜」,第 2 章提交「篝火旁铜镜看到异次元宫殿、次日清晨下山」,各自对上各自的原文。
> 流程推进到 `event_timeline` 子图,233.967 秒后崩在相位 `aggregate`,根因即 W2-11
> (模型把一个字段写成一个 `##` 块,`parse_md` 读成三个各自缺字段的对象,连打 5 次回票)。
> **第三次真跑跑通了**:`.workspace/runs/2026-08-15T12-40-22_bb6e358a`,`status: success`,
> **647.75 秒 / 42 次相位执行 / 77 次 LLM 调用**,产出 `story_framework`
> (6 个实体、2 条统一事件流、1 个场景、6 条伏笔闭合、2 条高潮排名)。
> 四种被点名的机制真跑覆盖:subgraph(4 顶层 + 2 层嵌套)、并行节点(六路维度全跑完)、
> loop(`stitch` 两轮累积)、batch(两章隔离正确)。**`llm compare` 这个 skill 不用**
> (全仓 grep 只命中 golden 知识库里一句无关散文),所以这次实验对它**零覆盖**。
> 产物里 `character_ranking` 为空,由 skill 自己的质量校验记成一条
> `quality_findings: "L1: Empty character ranking"`——行为对(记录而不打死整条链),
> 内容缺口归 skill。
>
> **skill 侧同批修掉的 7 处同类缺陷**:`outputs.required` 列的必须是**模型要提交**的字段,
> 不是校验器**产出**的字段。用「required 里的字段,validator 是否从提交里读过」做机械审计,
> 在同一个 skill 里查出 7 个相位(`aggregate` / `stitch` / `retroactive` /
> `entity_and_characters` / `event-extraction:review` / `settings` / `global_analysis`)。
> **其中 2 处第一版改错了**:`io.outputs` 这一份 schema 被**三道**校验共用——
> `finish_task` 校验模型提交、`runtime/state_mapper.py:255` 校验相位节点原始输出、
> `:389` 校验 validator 返回值。validator 一旦**重命名**字段(提交 `raw_settings_markdown`
> → 输出 `chapter_event_timeline`),**任何非空 `required` 都不可能同时满足两边**;
> 作者只剩「required 清空」(等于关掉强制)或「validator 把提交原样回传」两条路。
> 这是引擎表达能力的缺口,**编译期完全查不出来**,只在跑到那个相位时才炸。未开单。
>
> **另一处已定位未开单的诊断缺陷**:`AgentLoopIterationMiddleware._iteration`
> 是装配期建的实例属性,并行分支 / 批处理条目 / loop 轮次共用它。实测一个跑在两个并行分支上
> 的相位,trace 里迭代号一路数到 21,而引擎报的是「after 10 iteration(s)」——两个数都对,
> 但读者无法对账,也分不出哪条属于哪个分支(`sub_run_id` / `group_key` / `checkpoint_ns`
> 在这些事件上全是 `null`)。不阻塞跑通,属诊断能力缺陷。
> **踩坑记录**:桌面 app 的 sidecar 永远从冻结 vendor 快照导入引擎,这五个 PR 合并期间
> 未重建 vendor,导致 MoirAI 一直在对落后 3 个 PR 的旧引擎做推理并报"32 errors",
> 而同一时刻当前源码离线编译是 `COMPILE OK`。评估 agent 表现前必须先按 AGENTS.md
> Workflow Pipeline 第 7 条重建 vendor,否则整段观察作废。

#### 第三波 · 人看的面(闭环"看懂"半边)

| # | 项 | 状态 | 关键坐标 |
|---|---|---|---|
| W3-1 | LLM 原始回复查看:PromptInspector 加 Response tab | 待开工 | 数据已齐 `packages/graph-agent/src/graph_agent/core/callback_bridge.py:308-345`;组件 `apps/studio/frontend/src/components/PromptInspector.tsx:61-77` |
| W3-2 | 批量 run 挂线(client 函数 + 挂载点;组件/类型/报告模板已就绪) | 待开工 | 后端 `routers/runs.py:92/316`;孤儿组件 `components/history/BatchSummary.tsx` |
| W3-3 | fork 按钮 + delete skill 接线(消灭 7 处死胡同文案) | 待开工 | 后端 `routers/skills.py:953/985`;文案位 `components/studio/Workspace.tsx:1220` 等 |

## 并行工作线:Trace 流式化(决议 2026-08-09)

> 与上面「Copilot 闭环」冲刺**并行**,不共享它的验收判据。本线要解决的是同一件事的另一半:
> copilot 闭环让 agent 能把旅程走完,本线让人在旅程进行**当中**看得见正在发生什么 ——
> Trace 面板此前只是运行结束后的账本。

### 决议正本

- `docs/design/2026-08-09-trace-ia-and-streaming-overhaul-decision.md` ——
  Trace 信息架构 + 步骤流式呈现 + run 目录布局(D1-D14)。
- `docs/design/2026-08-09-streaming-tracing-architecture-decision.md` ——
  流式 tracing 架构(§1 事实 B1-B11、§2 决策 D1-D8、§3 验收判据 1-13、§6 实施切分 S1-S5)。
  它**取代**前一份 §4「明确不做」的第一条(LLM 输出逐 token 流式);前一份其余条目全部有效。
  **交付闸门(用户裁决 2026-08-09)**:S1+S2 完成后由实施者自评,无问题直接推进 S3-S5,不再逐段审批。
  已按实测两次修订本身:#673 把范围从三模块扩到四模块并作废「不触碰 gateway」那条(S1
  自评实测:`GatewayChatModel` 只实现 `_generate`,LangChain 据此判定它不可流式,
  `stream()` 退化成一次阻塞调用 —— 决议自己的验收判据 1 因此不成立);#678 立 D10 与
  SO 判据(gateway 的路由决策进 trace)。

### 第一阶段 · Trace 信息架构与呈现(已收口)

| # | 项 | 状态 | 关键坐标 |
|---|---|---|---|
| T1 | Trace 面板信息架构改造:单一 Trace 区域、可读 run id、焦点滚动而非隐藏、搜索与筛选、rehearsal 抽屉、进行中步骤、终态收敛、报告独立行、最新 run 用徽章而非拷贝 `latest/`、区域自称统一、predict 失败也留账、流地址与 API 同源、predict 收尾与 run 一致 | ✅ 已合并(#653-#667,15 个 PR) | 决议 D1-D14;面 `apps/studio/frontend/src/components/studio/trace/` |

### 第三阶段 · 运行时配置是偏好(2026-08-10 立项)

决议:`docs/design/2026-08-10-runtime-settings-are-preferences-decision.md`
(配置=偏好不是命令;参数被拒 ≠ 路由不可用;五态封闭枚举;探针带真实设置提前判定;
report.md `## Routes` 汇总;顺手拆 `call/chat_model.py` 的四件事)。

| 步 | 在做什么 | 状态 | 关键设计决定 |
|---|---|---|---|
| P0 | 决议落盘 | ✅ 已合并 | 立项证据是两条实测:推理被我们自己丢了几个月无人吭声(#691 已修);一个参数写错导致「所有 provider 都失败」——**最硬的硬关伪装成路由故障** |
| P1 | 参数被拒 ≠ 路由不可用:摘掉被拒项、记 warning、继续用这条路重发 | ✅ 已合并(#693);**真机验收(2026-08-10)** | 排第一位因为它是当前唯一会真正打死调用的硬关;路由健康与参数可接受性是两个维度,异常分类必须分得开。**判据不解析报错措辞**——异常分类里 `scope == "request"`(provider 读懂了这一条请求并拒绝,即 400/413/422)才发问,同一条路由摘掉偏好层重发一次:能答就说明问题在参数;容量/凭据/路由类失败不触发,不多花一次调用。新增路由决策 `retried_without_rejected_settings`(gateway/engine/契约文档/KB-12/前端五处同改)。顺带落 D9 第一刀:设置合成独立成 `call/settings.py`,`CallSettings` 把「调用本身」与「偏好」分层持有,`without_preferences()` 让摘除成为一次取值。**实测倒逼的第二处更正**:工厂 `_runtime_kwargs` 原本会从路由设置里补回调用方没传的项,导致摘掉的 `top_p` 在重发时原样回来、被同一条 400 再拒一次——已删除该回读,工厂只做映射不做决定(详见决议 D2 的 2026-08-10 补记)。**验收判据 1 已过**:vendor 重建后在真机 app 跑 exp-b-round7 全程,角色 analyst 的 temperature 设为 3.0(DeepSeek 合法区间 [0,2]),run 状态 `success`、三节点全 Success;`trace.jsonl` 内 8 次 LLM 调用每次都是 400 → `retried_without_rejected_settings` → `answered`,provider 原话`Invalid temperature value, the valid range of temperature is [0, 2]`,reason 尾注`retrying without temperature, reasoning`;trace 条目呈 warning 色(`bg-warning/10 text-warning`)、抽屉顶部「8 route issues」。对照组:`main` 同条件 `failed_terminal`→`AllProvidersFailedError`,一次都跑不完。**顺带坐实 P2 的必要性**:每次调用都要重新挨一次拒绝再摘,没有任何地方记住「这条路由拒收此项」——这正是 P2 探针要在开跑前定下的事 |
| P2 | 探针带上本次真实设置,开跑前定 `rejected` | ✅ 已合并(#695);**真机验收(2026-08-10)** | 探的位置早就在了(每次调用前一个 1-token 探针),问错了问题——写死的最小请求不带用户任何设置。**判据是「探针发的那条请求与正式调用同源」而不是「代码里传了参数」**:`client_manager` 里手搓的两份探针请求(openai/anthropic)已删除,探针改为 `CallSettings.as_cheap_question()` + 同一个注入进来的 `RouteChatModelFactory`——同设置、同构造器、预算换成 1、摘掉 tools(工具不是设置)。副产物:探针协议覆盖从 2 种变成工厂支持的全部;`probe_timeout_seconds` 经工厂新增的 `timeout_seconds` 覆盖参数继续生效(不能让省时间的问题自己耗掉一次正式调用的时长)。追问有门槛:异常分类 `scope != request` 直接收工,`scope == request` 才摘偏好再问,能答再逐项点名(一次只带一项);全接受时永远只有一问。路由决策 `retried_without_rejected_settings` 随之改名 `dropped_rejected_settings`——判定移到调用之前,「retried」不再成立(五处镜像同改)。**被拒项精确到项**:探针点名后只摘被点名的那一项,其余刚被接受的设置继续送出去(实测末次调用仍带 `temperature:1.4` + `reasoning_effort:low`,只少了 `top_p`);只有走事后失败路、没人点名时才整层摘掉。协议映射表从三段 if 链改成 `_PROVIDER_KEYS` 单一映射并对外暴露 `provider_request_keys()`——它就是造请求用的那张表,不是描述它的第二张;探针据此跳过「这条协议的请求体里根本没有位置表达它」的项(openai 系的 `reasoning` 布尔就是),不为它白花一次问。`unsupported` 作为一个**要被报出去的状态**仍待 P3 的设置事件落地。**验收判据 2 已过**(请求体从 httpx 层抓,不接受上游断言):healthy 组探针体 = `{max_completion_tokens:1, temperature:1.4, top_p:0.4}`,与正式调用同设置仅预算不同;bad 组(top_p=5.0)六条请求依次为 全设置探针(400)→ 摘偏好探针(ok)→ temperature 单项(ok)→ reasoning_effort 单项(ok)→ top_p 单项(400)→ 正式调用(带 temperature+reasoning_effort、无 top_p,答 221)。**真机 app 验收**:vendor 重建后跑 exp-b-round7(role temperature=3.0),run `success`、三节点 Success、23 次调用全部 `dropped_rejected_settings`→`answered`,trace reason 一律以 `probe:` 开头(证明判定发生在调用之前)且一律只写 `running without temperature`(证明只摘被点名项);trace 条目 warning 色,抽屉顶「23 route issues」 |
| P4 | `report.md` 新增 `## Routes` 汇总 | ✅ 已合并(#699);**真机验收(2026-08-10)** | 逐次细节留在 trace,报告只做收敛后的总结;不做运行结束弹窗(否则真正的 warning 被噪音淹掉) |
| P5 | 前端 trace 条目逐项显示「你要的 → 实际的」 | ✅ 已合并(#698,随 P3 一起);数据面已真机验收,UI 逐项点验待补 | 复用现成的 `eventSeverity(event)`,不新造一套颜色规则 |

#### 第三阶段续 · 偏好贴合路由(2026-08-10 立项,PM 四问触发)

决议:`docs/design/2026-08-10-preferences-fit-the-route-decision.md`
(值超范围先贴合、贴不了才谈拒收;边界按"变不变"分两类来源;新增第六态 `adjusted`;
effort 进 UI、top_p 不进)。前决议管「被拒之后怎么办」,本决议管「别递一个它接不住的值」。

| 步 | 在做什么 | 状态 | 关键设计决定 |
|---|---|---|---|
| A1+A2+B1 | 边界与贴合(网关)+ 写入归一(后端)+ effort 请求形状 | ✅ 已合并(#697);**真机验收(2026-08-10)** | 新增 `registry/bounds.py`:边界是路由的事实,**文档常量**(温度量程、top_p 上限、协议 effort 词表)进 provider-doc 表,**随模型变的枚举**(effort 到底卖哪几档)靠探;实测优先于文档。`initial_budget` 补上 `budget_cap` 约束——原本上限只管翻倍升级、不管起始值,用户填 999999 原样出门吃 400。温度两层各有唯一 owner:authored 0..2 量程由 Studio 写入边界归一(`RoleIntent` 归一而非 422——拒绝保存与让调用失败是同一个错误),provider 量程由网关按"百分比 × 该路由天花板"换算,天花板优先取路由能力值。贴合过的项在 `actual_runtime_settings` 里带上 `asked`,不静默改值。**B1 的意外发现**:effort 不需要嵌套路径机制——`ChatAnthropic(effort=...)` 实测渲染成 `output_config.effort`,`ChatGoogleGenerativeAI` 叫 `thinking_level`,两个 adapter 的 Literal 类型与官方文档完全一致,映射只是平铺两行。此前 anthropic 映射表里**根本没有 effort**,给 Claude 路由设 effort 一个字节都发不出去 |
| B2 | effort 探测:候选档位逐个试,写进路由 capabilities | ✅ 已合并(#703);**真机验收(2026-08-10)**:主 app 重建 vendor 后对 `deepseek-official:deepseek-v4-pro` 打强制探测,3.5 秒返回,七档全部收下并写成 `probed_verified` 能力;为排除"参数没上路"这一同样能解释 7/7 的读法,同路径抓请求体 + 故意问 `banana`——请求体确是 `{"model":"deepseek-v4-pro","max_tokens":1,"reasoning_effort":"high"}`,`banana` 被 400 并回吐完整枚举 `none/minimal/low/medium/high/xhigh/max`,与测量结果一字不差,**因此这次测量修正了 B4 表里 DeepSeek 那一行的文档值**(决议已订正)。前端 reload 后 analyst 角色的档位下拉从五档变八档(多出 None/Minimal),证明测量确实经 model group 投影到了控件 | 各家枚举实测不同(anthropic `low/medium/high/xhigh/max`、gemini `minimal/low/medium/high`、deepseek v4-pro `low/high/max` 且 `medium→high`/`xhigh→max` 服务端折叠、openai 按型号从 `minimal/low/medium/high` 到 `none/…/max`),文档给不出统一答案,只能探。今天路由上那个 `reasoning.effort=low` 是我们自己能力探测的副产品被提升成默认值(`metadata.source=official_test`),用户看不见也改不了(B3 已给它一个能看能改的入口)。**编排落在 studio、词表与记录留在网关**——多模态探测就是这个范式(`probe_route_multimodal` 在 studio 编排、调网关探测原语),effort 照抄:`settings_bounds.effort_probe_candidates` 答「问哪几档」、`capabilities.measured_effort_capability` 答「测出来的怎么记」,扇出走 studio 自己的 `_probe_route_generation_atom`(顺带拿到探测中事件与 disabled 检查)。三条实现期决定已写回决议 D-A:①候选按协议词表设限、②只问自称会思考的路由(不推理的模型每一档都是付费拒绝)、③限流/网络/超时这类与档位无关的失败**整轮作废**,不写能力——把「没测出来」写成「它不支持」会删掉这条路由真正卖的档位 |
| A3 | 修:只因被贴合才被知道的设置拿不到判定 | ✅ 已合并(#700);**真机验收(2026-08-10)** | 真机跑合并后的代码抓到的:越界 `top_p` 确实按 1.0 出门了,而设置事件对它只字未提——贴合正确且不可见,正是本决议要消灭的那种沉默。根因:报告表每项一条记录,只有自带记录的项才带 provenance;仅因被移动才被知道的项没有,于是被「这是不是用户选的」那道过滤当成没人选而丢掉。贴合记录现在带上路由结算它时的 provenance 与实际送出的值。顺带删掉 `AUTHORED_SOURCES` 里的 `runtime_settings`——它抄自另一个字段的来源字符串,resolver 根本不会在运行时设置上盖这个章,是个永远匹配不到的成员;现在改由测试对着封闭枚举核对,不再对着我的记忆。**教训**:单测全绿是因为每个用例都亲手喂了一条带 source 的记录,真机跑一遍才抓到 |
| B3 | effort 成为角色级设置 + 控件进 LLM 角色面板 | ✅ 已合并(#702);**worktree 实测(2026-08-10)** | effort 是角色选一次、各路由各自贴合的设置:`RoleIntent.reasoning_effort` 存所选档位,`role_materialization._apply_reasoning_effort` 在物化时用 `settings_bounds.effort_bounds/fit` 贴合成该路由卖的最近一档(就低不就高),所以面板读回的值就是会发出去的值。**档位通道改了**:决议初稿写的是 `RuntimeSettingDescriptor.allowed_values`,实际走 `ProviderModelOption.capabilities['reasoning_effort']`——两条都是同一份 registry 真相的按路由投影,而后者已经铺到角色卡片(旁边 max output tokens 控件正是从它读上限),前者今天前端没有任何消费者;同一批数据取短路径,D-F 已按此订正。协议文档词表在**读时**补(`_provider_route_ui_capabilities`),不写盘——「这个协议能拼出哪些名字」是随时可答的常量,落盘只会变陈旧;实测档位存在时原样保留。空态给禁用 + 说明,不给"去测"入口:B2 未落地前现有路由测试并不测 effort,指过去是空承诺。top_p 明确不进 UI(与 temperature 同向作用,各家文档均建议二选一)。**验证教训**:单测把 `@/components/ui/select` mock 成原生 `<select>`,而原生 select 接受空 value,于是「`SelectItem value=""` 会被 Radix 拒绝并炸成错误边界」全绿放行,起真环境打开那一屏才看见「LLM Roles failed to render」——mock 掉设计系统组件的用例必须再去真环境看一眼(已写进 FRONTEND_UI_SPEC §2.8) |

#### 第四阶段 · 网关按域成树,测试只有一套方言(2026-08-10 立项,PM 三问触发)

决议:`docs/design/2026-08-10-gateway-module-tree-and-probing-decision.md`
(测试能力分 T0 静态校验 / T1 可达性 / T2 能力测绘 / T3 行为测试四层;方言唯一实现、生产与探测共用;
网关按七个域成树、域外禁止深导入)。用户授权:一期一 PR,自测通过即自动推进下一期。

| 步 | 在做什么 | 状态 | 关键设计决定 |
|---|---|---|---|
| P0 | 决议落盘 | 🚧 进行中 | 立项证据:①"effort 怎么进请求体"这一句规则在仓里有 8 处赋值、分布在 5 个函数(生产 3 + 探测 5),生产走官方 SDK、探测手搓 httpx——**探测通过不等于生产能跑**;②`provider_probe.py` 全文无 `tools` 字样,四个 Test 按钮没有一个能回答"这条路由能不能进 ReAct 循环",而这正是引擎唯一真正依赖的能力;③外部对网关的 import 共 531 条、绝大多数深入内部文件(`registry.schema` 一处被 import 146 次),内部结构因此变成了公共契约 |
| P1a | registry 域归位 + 域级契约门禁 | ✅ 已合并(#706) | 保留 `registry` 这个名字(AGENTS.md 与 MVP1 设计源的既定术语),改的是它的范围:解析/探测/调用迁出,只留真相。七个模块搬进域内(`storage_contracts`→`config_store`、`registry/storage`→`fingerprint`、`canonical`+`route_identity`合并为`identity`、`import_draft_store`→`catalog`、`state_projection`→`projection`、`settings_bounds`→`bounds`、`credential_resolver`),`probe_catalog` 别名壳删除。域级契约 = `registry/__init__` 的 93 个导出,域外只许 `from graph_agent_gateway.registry import X`——78 个文件的深导入已改写,新增 `test_nobody_outside_the_registry_domain_imports_its_files` 扫全仓守住,尚未归位的六个模块列在 `_AWAITING_REHOME` 并标了各自的期数(只许减不许加)。**实施中三处订正已写回决议 D5**:两个 storage 是两件事不合并;删别名后保留 catalog 命名、legacy `ImportDraft*` 名整体消失;发现 `materialize_role` 同名两物(设计源 `01-design.md:102` 只定义一个),本期不裁决、留给 P1b |
| P1b | resolve/ + role/ 两域归位 + `materialize_role` 归一 | ✅ 已合并(#707) | 六个模块进 `resolve/`(resolver/lint/profile_selector/error_classification/handoff/fallback),`role_materialization` 进 `role/`。**同名两物查清后不是重复实现,是一个契约被拆成两半**:设计命名的 `MaterializedRole` 带着"空链必须说明原因"的不变量却只有测试在用,而 studio 生产调的那个 `error_code` 零命中——角色全部路由挂掉时网关交回一个沉默的空列表,调用方只有真去跑才撞上 `resource.no_available_route`。裁决:保留生产那个的输入输出(它算得更全),把设计的名字与不变量搬上去(Pydantic + `model_validator`,空链必带错误码);registry 里的重复三件套与从无调用者的第二个 `MaterializeRoleRequest` 删除,两条旧契约测试用存活 API 重写。`error_payload` 不保留——细节的唯一 owner 是 `materialization_report`,错误码只是对整个角色的判决。域级门禁同步推广到三个域并加了两条规则:测试可以点名文件(私有名不在任何契约里、patch 也必须拿到模块对象),生产代码不行;`_AWAITING_REHOME` 从六项缩到两项 |
| P1b-UI | 角色跑不了时前端说出原因 | ⏳ 待办 | 数据已到位(`materialization_report.error_code`,前端类型已加);怎么显示(角色卡状态点?测试按钮旁?)是独立的 UI 设计题,不夹在重构 PR 里 |
| P1c | call/ 域归位 + 根部只剩共用词汇 | ✅ 已合并(#708) | 十三个模块进 `call/`(clients/dispatch/chat_model/factory/profiles/models/predict/settings/outcome/pre_call_probe/resolver/protocol/tracing),`exceptions.py`→`errors.py`。**`observe/` 这个域取消**:`tracing` 要拿 `SettingOutcome` 造事件、`outcome` 依赖 `call_settings`、而 `chat_model` 又要调 tracing 发事件——三条连起来是 call ⇄ observe 的包级循环。根因是划分标准错了,`observe/` 圈的不是一组共同不变量而是"看起来都跟观测有关",按名字相似分家正是 D4 要避免的。改法:tracing 与 outcome 跟着 call 走,`events.py`/`errors.py` 留根部当**跨域共用词汇**(纯 DTO / 异常,不依赖任何域)。同期把写死旧布局的三个测试改成断言新树(`test_package_skeleton` 现在断言四个域的 `__init__` 都在、且都能独立导入)|
| P1d | probing/ 域 | ✅ 并入 P2c-3 完成 | 只剩 `registry/provider_probe.py` + `probe_contracts.py` 两个模块待搬;它们的归位与 dialect 抽取是同一件事,分开做等于搬两次 |
| P1z | 全仓文档旧路径一次性回写 | ✅ 已合并(#715) | 56 个文件、622 行改写,4 个 audited 哈希同 PR 重钉。扫描规则不是"全文替换",是**两条各自成立的判据**:①凡写全 `graph_agent_gateway/<模块>.py` 或 `graph_agent_gateway.<模块>` 的,按新树改写;②只写裸 `<模块>.py` 的,仅当这个名字在全仓唯一时才改(16 个名字通过校验)。**中途踩过一次**:先做的版本对裸名一律替换,把 `docs/studio/mvp1/04_platform/i18n.md` 里指 studio 自己 `app/core/exceptions.py` 的那句改坏了——`exceptions`/`storage_contracts` 这类名字三个模块各有一份。**三类文档明确排除**:`docs/pr-reports/` 与 `docs/superpowers/`(历史记录)、决议文档本身(它就是那张新旧对照表)、以及自称归档的 MVP0 快照(`docs/graph-agent-gateway/mvp0/` 与 `docs/engine/graph-agent-gateway/`,全文 60 处提 `llm_config.py` 这种早就不存在的文件——只修其中碰巧还在的那一半,读者反而分不清哪个指针是活的)。还有一个是被门禁挡回来的:`docs/engine/mvp0/public-api-contract.md` 在引擎的**契约哈希锁**冻结名单里(`test_contract_hash_lock.py:11`),改它要在 exemption 里填 `pm_approval`,不是同 PR 重钉哈希能了事的——已回退,冻结的契约就该冻着。顺带清掉 6 处指向**已删文件**的悬空引用(不是这次搬家造成的):`temperature.py`(#697 删)→ `registry/bounds.py`、`probe_catalog.py`(#706 删的别名壳)→ `registry/catalog.py`、`registry/probe_contracts.py`(#713 删的转发壳)→ `ProbeResult` 现由 `registry/__init__.py:170` 导出;最后一处连句子的说法都是错的("经 probe_contracts 重导出"),按实情改写 |
| P2a | 录基线:每个官方方法发出去的那条请求 | ✅ 已合并(#709) | 88 条(11 方法 × 4 种设置 × 有图/无图)经 `MockTransport` 回放,url/鉴权头/请求体逐字段钉死。**它不是测试覆盖率,是这次重构的判据**:方言抽取要么基线一行不动,要么就得让 diff 显出来说清为什么 |
| P2b | `dialect/` 域落地,官方方法探测(A3)切过去 | ✅ 已合并(#710) | 十个官方方法的方言收进四个模块(anthropic / openai 三种 / google),按 wire family 分家、per-method 差异做成构造参数(密钥放哪、内容要不要块、预算字段叫什么、怎么说思考多少)。`dialect/` 是**叶子域,不依赖网关里任何东西**——base_url 由调用方先过 catalog transform 再交进来,反向 import 会做成包级循环(同订正 6)。11 分支 if 链换成一张表,查不到就在发请求之前拒绝。88 条基线一行未动。顺带记下三处:anthropic 的 transform 探测侧原来没应用(新写法对所有基线用例同解)、openrouter 原来会先发一个错方言的请求再拒绝、A2 因为还在按 backend 名猜 wire 所以暂留两份重复规则(见决议订正 8-11)|
| P2c-1 | 录 A1/A2 基线:主机名猜测的后果实录 | ✅ 已合并(#711) | 100 条(4 协议 × 4 主机 × endpoint 名),把「猜」的结果钉死后再动它。**录出来的不是偶发猜错,是主机名压过用户声明的协议**:`anthropic_compatible` / `google_genai` 的 endpoint 只要 base_url 落在 `api.deepseek.com`,探测就发 OpenAI chat 请求;`openai_compatible` 的 endpoint 只要**名字**里含 deepseek,预算字段就从 `max_completion_tokens` 变 `max_tokens`。顺带记下:`test_provider_endpoint`/`test_provider_route` 这两个公开函数以 `test_` 开头,谁 import 谁就被 pytest 当测试收集——P2c-2 随搬迁改名为 `probe_*`,与 `probe_official_call_method` 一致 |
| P2c-2 | 探测把「哪一家」和「怎么说话」拆成两个问题 | ✅ 已合并(#712) | 原来 `endpoint_probe_backend` 一个答案身兼二职,于是主机名能推翻用户声明的协议。现在 `probe_wire_backend(protocol)` 决定请求长什么样(依据:**生产就按协议分派**,`dispatch.py:189`),`endpoint_probe_backend` 只回答厂商身份(studio 的官方方法菜单按它取 `probe_candidates.json`),且只认 `deepseek.com` 域 + DeepSeek 真发布过的那两个协议面,**不再看 endpoint 的名字**。100 条基线 37 条变化,三类:协议不再被主机名推翻、名字不再决定菜单与字段、OpenAI 兼容面预算字段改 `max_tokens`(`_call_openai_compatible`(dispatch.py:239)对所有 openai 兼容路由都发这个;探测原本替 OpenAI 造了个生产从不发的 `max_completion_tokens`、却替 DeepSeek 发对了)——改齐后 **DeepSeek 的 openai 兼容 endpoint 一条都没变**。**中途踩过一次**:先做的版本把两问题合并成「一律按协议」,studio 全量才炸出官方菜单被悄悄删掉;教训是删概念前先问它在替谁回答问题。同期拆开 `provider_probe_backend_for_method` 混在一起的两个报错 |
| P2c-3 | `probing/` 域落地 + 抽 `judge`,`_AWAITING_REHOME` 清空 | ✅ 已合并(#713) | 六域成树至此完整(registry / resolve / role / call / dialect / probing)。域内三块:`wire.py` 问哪个问题、`judge.py` 答案是什么意思、`results.py` 报回去什么。`probe_contracts.py` 不搬直接删——7 行转发壳、全仓零 import,与 P1a 删的 `probe_catalog.py` 同物种。两个公开入口 `test_provider_*` 改名 `probe_provider_*`:以 `test_` 开头导致谁 import 谁被 pytest 当用例收集。边界门禁当场又抓到 `wire.py` 的深导入,改走包入口时发现 `ProviderKind`/`ProviderProbeBackend` 根本不在 registry 的 `__all__` 里(全靠深导入才能用),已补进契约。判据:两份基线(88+100)一行未动 |
| P3 | ~~生产 dispatch 切到同一组 dialect~~ | ❌ 前提不成立,已作废(决议订正 21) | 立项时以为生产在手搓 HTTP,所以"切到同一组 dialect"才有意义。实测(P3a/#714)是**生产全程走官方 LangChain 模型**,手搓那条路对任何合法路由都不可达——没有东西可切。这条目标改由两件事承接:P3a 把真实调用发的请求钉成契约(已合并),P3c 把那条不可达的路删掉(本期)。"探测 = 生产"这条保证改由 P3b 给:让探针走生产同一个工厂,而不是让生产走探针的方言 |
| P3a | 钉住真实调用发的请求 + 更正 P2c-2 的预算字段 | ✅ 已合并(#714) | **P2c-2 判断错了并已更正**:我引 `dispatch.py:239` 断言生产发 `max_tokens`,但那条路**生产走不到**——`RouteChatModelFactory.build` 对四个协议各有分支返回原生 LangChain 模型,`GenericRouteChatModel` 只在兜底里构造,而 protocol 是那四个值的 Literal。实测真实对象的 `_get_request_payload`:openai/ark 一律 `max_completion_tokens`+`reasoning_effort`(deepseek 路由的 `PatchedChatDeepSeek` 也是),anthropic 是 `max_tokens`+`output_config.effort` 且无 `thinking` 块,google 是 `max_output_tokens`+`thinking_level`。新增 `test_production_wire_contract.py` 把这张表钉死;探针改回 `max_completion_tokens`(顺带修掉比我更早的 deepseek 错)。教训:**「生产怎么做」必须实测那个真会跑的对象**——`dispatch.py` 名字齐、测试齐、1026 行,唯独没人调 |
| P3b-1 | 判据读的是一个答案,不是谁去取的 | ✅ 已合并(#719) | **开工前实测推翻了 D8 的前提,决议已订正。** D8 说探针走生产工厂后失败会变成 SDK 异常、不再是 `httpx.Response`,所以判据要换成 `classify_exception`,**因此外溢到 UI**。核了三家签名:`openai.APIStatusError(message, *, response: httpx.Response, body)`、`anthropic.APIStatusError` 逐字相同、`google.genai.errors.APIError(code, response_json, response, ...)`——**异常里就揣着响应**。而判据要的只有状态码和 body(它靠读 body 才能把 400 分成"模型不存在"和"欠费"、才能认出"这 URL 根本不说这个协议"),连不上和超时那两种没有响应,对应的 `network_error` / `timeout` 本来就在词表里。**所以 `ProviderProbeStatus` 九个值一个不动,前端不改**——用户授权了动 UI,但实测表明不动就能做对,授权是允许不是理由。本期只做一件事:判据的入参从 `httpx.Response` 换成 `ProviderAnswer`(状态码 + body 原文),整个 `judge.py` 实际只碰 `.status_code` / `.json()` / `.text` 三样;`wire.py` 里加一个 `_answer_from()` 作为**响应变成答案的唯一入口**。判据:88 + 100 两份基线**文件字节未变**,566 passed |
| P3b-2 | 路由探针改走生产工厂 | ✅ 已合并(#720) | A2 不再自己拼 HTTP:改用 `RouteChatModelFactory` 造出**生产同一个模型**,问它一句 `ping`、`max_tokens=1`,把三家 SDK 的异常归一成 P3b-1 的 `ProviderAnswer`。工厂按范式当参数传入(`factory=`,同 `call/pre_call_probe.py::build_probe_model`),不给工厂开新口子。**Google 那个前置事实已核实,而且它是真坑**:`langchain_google_genai` 不原样抛 `APIError`,而是包成 `ChatGoogleGenerativeAIError`,原件挂在 `__cause__` 上;不多走这一跳,Google 的每一次失败都会塌成 `error`,400/404/401 全分不出来。所以 `answer_from_failed_call` 顺着 `exc → __cause__ → __context__` 找第一个带响应的。**那 100 条 endpoint 基线砍成 20 条,路由那 80 条删掉而不是重录**:它问的「路由探针往线上放了什么」已经不再由本包回答——请求现在由 SDK 生成,而生产发什么已经在 `test_production_wire_contract.py` 里从模型对象上**直接读出来**钉死了。重录等于把同一个事实用更绕的方式录第二遍,代价还是往三个第三方对象的私有属性打补丁(anthropic 连 `http_client` 都不收,得换掉它持有的 `_client`)。那 80 条覆盖的三样各有去处:请求体→生产契约表;base_url→工厂与 registry 的 base_url 测试;请求路径与鉴权头→**网关里已经没有代码决定它们了**,断言它们等于在测 SDK。判据:endpoint 那 20 条字节未变;gateway 488 passed / 1 xfailed(566 − 80 + 2,逐条对得上);ruff、`mypy --strict` 绿;studio `mypy` 干净 + 1670 passed |
| P3c | 删掉任何合法路由都走不到的那条调用路径 | ✅ 已合并(#718) | **裁决已下(用户 2026-08-11:「没用的就删」),决议 D9 已记。** 不可达是坐实的:`ResolvedRoute` 是 `extra="forbid"` 的 Pydantic 模型、`protocol` 是四值 Literal(`registry/schema.py:29`、`:522`),工厂对四个值各有分支直接 return——唯一构造出兜底的办法是`model_construct` 跳过校验,那正是原测试干的事。**净删约 2400 行**:`call/dispatch.py`(1026)、`call/models.py`(301)、工厂兜底、`LLMClientManager` 里只有 dispatch 用的 206 行、以及 754 行只测这条死路的测试。**兜底没有换成 `raise`,换成了 `assert_never`**——把 `str(route.protocol)` 改回 `route.protocol` 后类型系统能证明四条分支穷尽,将来加第五个协议却不加分支是**类型检查失败**,不是运行时才炸。剩下的类改名 `LLMCircuitAndUsageLedger`:它只剩熔断 TTL 与用量计数,不再管理任何 client。**测试逐条判过,不是整个文件删**:活规则只被死路径测过的三处改打真实路径——熔断 TTL(改名即可)、凭据解析(改打 `call/factory.py:309` 那份活的,并补上 vault 失败的错误码断言)、以及 `_PROVIDER_KEYS` 里 `top_p`/`seed`/`stop`/`stop_sequences` 的映射(改成从工厂造出的真实模型读 payload,进 `test_production_wire_contract.py`)。其余(anthropic thinking 块、dispatch 温度重映射、`openai_responses` 走 Responses API)测的是生产根本不发的请求,随死代码一起删。**顺带照出一个产品级事实**:`call/settings.py` 一直往调用参数里塞 `call_method_id` / `request_mapper_id`,而唯一读它们的就是那个死模型——工厂造出的四种原生模型都不看,实测 payload 里也确实没有。也就是说**一条路由验证过哪个官方方法,对生产真正发出去的请求没有任何影响**:profile 写着 `openai_responses` 的路由,照样按 chat/completions 发。这两个参数本期随之删掉(写了没人读),但「生产该不该照着已验证的方法走」是 P4 的题,不在本期解 |
| P4 | `questions`/`runner` 落地,四入口改成选题;effort 测量归位;删伪造证据分支 | ✅ 已合并(#721 + #722 + #723) | **开工实测把题面收窄了两处,决议已记(D5 的 P4 开工补记)**:① `runner.py` 没单独建——今天只有一种执行形状,`ask_each` 就是它的全部;② 「四入口改成选题」没做——做完 P4c 才看清今天**只有一组题**(effort),为一个元素造选择器正是 KISS/YAGNI 挡的那种扩展点,等 P5 的工具调用带来第二组题再立。实际落地 = P4b(#721,删伪造证据分支)+ P4c(#722,`probing/questions.py`)+ P4d(#723,effort 测量归位)。本行原状态停在 ⏳,是三个子步合并后没有回填 | T2 不拆成"跑通一次"与"全量测绘"两个能力,而是同一能力的深度参数——判据/方言/写入口三者相同,拆开必然长出第二份实现 |
| P4a | 厂商身份归位 registry,顺手对齐一条主机规则 | ✅ 已合并(#717) | 「这条 endpoint 是哪一家的」不发任何请求,是对已配置对象的识别,所以不归 `probing/`(决议订正 18)。真动手才看清**它和 `probe_wire_backend` 共用一副身体**:厂商那问的兜底分支就是线路那问,而线路那问整个函数体只有两次 registry 查表——只搬一个,要么把同一条规则在 registry 再写一遍,要么在 `probing` 留个一行转发壳(本系列已删过两次的物种)。所以两个一起搬,并按已有的 `provider_backend_for_method` 起同族名:`provider_backend_for_protocol` / `provider_backend_for_endpoint`;名字里的 "probe" 去掉——它们不发请求。**搬迁当场抓到一个真分岔**:同一个「主机是不是它」的问题,`probing` 和 `registry` 各有一份不同的 `_url_hostname`,而 `base_url` 是无校验自由文本。实测 `api.qnaigc.com/v1`(少写协议头)厂商规则认得出、方法菜单规则认不出,同一台主机两个答案。统一到宽的那份(容忍缺协议头与末尾点),方向选它是因为反过来会悄悄把用户的端点从菜单里删掉。判据:两份基线(88 + 100)一行未动。遗留:`base_url` 该不该在边界拒收没有协议头的字符串,是产品行为改动,另议 |
| P4b | 探测就是去问:删掉不发请求就写实测证据的分支 | ✅ 已合并(#721) | `POST /routes/{id}/probe` 原来有两条路:带 `force` 才真去问,不带就**把请求体里写的 capabilities 直接标成 `source="probed_verified"`、把路由置为 `verified`**——调用方说自己行,注册表就记下"实测过了"。核了全部调用方,**没有一个想要那条路**:前端 `AvailableModelsSidebar.tsx:152`、copilot 工具 `copilot_tools.py:1930` 恒传 `force: true`。所以删的不是一个分支,是一个**能凭空制造实测证据的入口**。`force` 参数随之消失(只剩一种行为,不需要开关)、`RouteProbeRequest` 随之消失(它两个字段只被删掉的分支读)、`_capability_key` 随之消失。**两处旧测试是这个缺陷的成文形式**,断言的正是"请求体里写什么就变成 probed_verified":一处整条删,另一处只删探测前奏、保留它真正在测的 profile-apply 冲突规则。新测试反过来钉死:`test_route_probe_evidence.py` 断言这个接口没有"不发请求"的形态,且请求体里塞 capabilities 也不会变成能力记录。判据:studio 全套 + 前端四门禁绿 |
| P4c | 一条路由值得被问哪些问题,归网关 | ✅ 已合并(#722) | 以 effort 为样本量出的分工错位:**该问哪些档位**(`registry/bounds.py:187` `effort_probe_candidates`)和**答案怎么记**(`registry/capabilities.py:205` `measured_effort_capability`)本来就在网关,唯独中间那段——**怎么问、什么算问出来了**——落在 studio 的 router 里(`llm.py` `_accepted_effort_levels`:并发发 N 次、`rate_limited`/`network_error` 这类 status 一出现整批作废、只有 `ok` 的档位才算数)。那是判据不是 HTTP 编排,第二个入口想问同一个问题就得抄一遍。本期把它搬进 `probing/questions.py`:`Question`(问什么值 + 带哪份 runtime_settings)、`effort_questions(protocol)`、`accepted_effort_levels(answers)`(整批作废规则)、`ask_each(questions, ask)`(并发问 + **把每个答案和它回答的那个问题钉在一起**)。**`ask_each` 收一个 asker 而不是自己去调探针**,因为 studio 每次问都要套自己的两件事:端点被禁用要拒、要给前端发 probe active 事件;网关自己发请求会把这两件事要么重写一遍要么悄悄丢掉。`runner.py` 没单独建——今天只有一种执行形状,两个薄模块不如一个(KISS/YAGNI),P5 接工具调用时再看。判据:网关 500 passed / 1 xfailed;studio 全套绿;两份基线未动 |
| P4d | 测试模型也测 effort:一条路由被验证就被测,不看是谁按的按钮 | ✅ 已合并(#723) | **修的是一个漏,不是搬家。** effort 只有一处在测(`_probed_route_capabilities`,唯一调用点在 `_force_probe_route`),而那个接口在前端**只有一个触发点**——挂在「已弃用模型」上的 Re-probe 图标按钮(`AvailableModelsSidebar.tsx`)。正常路径加模型走的是「测试模型」(`POST /endpoints/{id}/models/test`),那条路写能力用 `_third_party_probe_capabilities`:只抄 list-models 字段或退回硬编码默认值,**一次 effort 都不问**。所以按正常流程加进来的模型,effort 档位永远测不到。本期把规则改成一句话——**一次真实生成把路由验成 verified 的那一刻,就把只有测量能定的东西测掉**——写成 `_measure_what_only_asking_settles`,第三方模型测试与强制路由探测两条路都改走它;`_force_probe_route` 里那份内联的也拆掉,规则只剩一处。不思考的模型仍然一次都不问(每档都会被拒,每次拒绝都要付钱)。**official 那条分支刻意不接**:见下一行。判据:studio 全套绿 |
| P4d-注 | official 端点的 effort 仍然没被测 | 📌 记档 | 给 official 分支也接上 `_measure_what_only_asking_settles` 时,被一条既有规则当场拦下:`test_official_manual_model_test_uses_profile_probe_and_persists_attempts` 断言 official 的手动模型测试**绝不能走通用生成探针**(`raise AssertionError("official manual tests should use official profile probes")`),而 effort 那组问题正是骑在通用生成请求上的。查了 official 自己的候选表(`app/data/probe_candidates_dynamic.json` / `probe_candidates.json`):它们确实带 effort,但**只带 `low` / `minimal` 两个值**,而且是用来区分调用方式(profile)的,不是枚举 effort 阶梯。所以 official 路由的 effort **今天没有任何地方在测**,而且不能靠本期这条路补——要把同一组问题交给 `probe_official_call_method` 去问,那是**第二种执行形状**,正是 P4c 里说「等第二个用例出现再立」的那个形状。归 P5 一并做 |
| P4e | 能力的 source 说的是这一项能力从哪来,不是这条路由考没考过 | ✅ 已合并(#724) | **同一个物种,退了一层。** P4b 删的是「请求体里写什么就算实测」;这次是「这条路由通过过一次生成,就把它身上**每一项**能力都盖成实测」。实点三处都写成 `"probed_verified" if <route.status == "verified"> else "api_list"`(`llm.py` 两处 upsert + 一处 official 文档值)。后果不只是标签难看:`resolve/lint.py:74` 对 error 级要求专门检查「这项能力是不是 `manual`/`probed_verified`」,就是为了拦住「角色悄悄依赖一个没人验证过的声称」——把 list 抄来的声称盖成实测,角色就直接走过去了。**前端那条规则本来就是对的、只是一直落空**:`api/llm.ts` 里 `routeAcceptsImageVerified` 要求 `source === 'probed_verified'` 才算「认图」,注释白纸黑字写着「provider_doc/api_list 只是 catalog 声称,不是实测判据」;后端一直把 list 声称的 `input_modalities` 盖成实测,于是那道判据从来没真正生效过。改法:**provenance 由知道它的那一层返回**——`_third_party_probe_capabilities` 现在同时返回值和来源(list 给了就是 `api_list`,退回生成默认值才是 `probed_verified`),两个 upsert 与 `EndpointGenerationVerification` 一路带着它走;official 的文档值(带着 doc URL 的那些)恒为 `provider_doc`。顺带修掉乙类:`_profile_supports_reasoning` 原来把 `capability + profile_id + request_mapper_id` 拼成一条字符串搜 "thinking"/"reasoning" 子串来判定会不会思考,却盖着实测的章;候选表里 `capability` 本来就是显式声明的三值之一(`text_chat`/`thinking`/`reasoning`),改成读它。这一项还是 effort 要不要花 N 次请求去问的门,猜错两个方向都要付代价。判据:studio 全套绿 |
| P6 | 权威文档与使用手册跟上这棵树 | ✅ 已合并(#726/#727/#729/#734/#739);**判据重测(2026-08-12)**:21 篇自称当前/目标的文档里活的死路径已清零 | **先量再做**:把 `docs/graph-agent-gateway/` 下 42 篇 md 点名的模块路径逐个去真实目录里找,**62 个里有 14 个哪里都不存在**。按「只扫自称描述当前系统的文档」分开(baseline/mvp0/_impl 那 18 篇是历史记录,不扫),剩 **17 篇自称当前/目标的**里有死路径,而且全是 `mvp1-alignment.md` 这一档——按 AGENTS.md 那正是 source of truth。死路径两类:**搬了家**(`registry/resolver.py` 被 15 篇点名,现已拆成 `resolve/resolver.py` + `call/resolver.py`;`error_classification`/`lint`/`profile_selector` 进 `resolve/`;`canonical`/`storage` 的符号进 `registry/identity.py`/`fingerprint.py`)和 **被删了**(`call/dispatch.py`、`call/models.py` P3c 删,`registry/probe_contracts.py` P1a 删)。第二类不是路径写错,是**那几节描述的结构整个不存在了**,所以 P6 是逐篇判、不是 sed 替换。另:`packages/graph-agent-gateway/README.md` 和整个 doc 树里**没有一份「怎么用这个 SDK」的手册**(注入存储 / 解析角色 / 发一次调用 / 探一条路由),用户目标点名要的就是它,一并做。**这里原先写着「10 篇 alignment 在哈希锁里,同 PR 重钉」——这句是错的**:`docs/graph-agent-gateway/mvp1/_audited-ready-hashes.json` 的 `count` 是 **0**,`_meta.note` 白纸黑字写着 "Gateway MVP1 docs are still drafted. Add SHA-256 entries here only when files are owner-approved"。gateway 这批文档一条哈希都没锁,改它们不需要重钉。(engine 那边才有冻结名单,别把两个模块的门禁记混。) |
| P6a | 使用手册落地 + README 不再说假话 | ✅ 已合并(#726) | 用户目标点名的「使用手册」。新增 `docs/graph-agent-gateway/USAGE.md`:宿主要自己提供什么(`RegistrySnapshot` + `CredentialProviderProtocol`,以及为什么后者拆成 describe/get 两个方法)、六个域各管什么、四条常做路径各几行代码(角色→路由链 / 路由链→一次调用 / 探端点 / 探路由)。**手册里每个例子都真跑过**:写完拿一个脚本把文档里的 import、构造与签名逐条执行,当场抓到两处错——`ResolvedRole` 带的是 `SkippedRoute` 不是 `RouteSkipDiagnostic`;网关的 `ProviderEndpoint` **没有 `display_name`**(那是 Studio 在自己那边继承出子类加的 UI 字段)。两处都已订正,后者还成了手册里演示边界的例子。README 两节改掉:§4 从「编排→调用」三句话换成六个域的表(并保留那条贯穿线),§7「对外 API 现状」原文说「顶层 `__init__` 目前只导出 `GatewayChatModel` / `ModelResolver` / 异常」——实点是顶层 34 个、六个域各 99/18/4/34/18/19,整段是假的,已重写。判据:文档内相对链接全部可解析 |
| P6b | 文档里的路径指向活代码 | ✅ 已合并(#727) | **搬家的那批已清**:`registry/error_classification.py`/`lint.py`/`profile_selector.py`→`resolve/`、`canonical.py`→`identity.py`、`storage.py`→`fingerprint.py`、`registry/resolver.py` 拆成 `resolve/resolver.py`+`call/resolver.py`,死引用 159→55。**这一轮又挖出路径扫描漏掉的一类:符号级死名**——扫描只比对模块路径,不比对类名方法名,于是 `LLMClientManager`(P2 改名 `LLMCircuitAndUsageLedger`)在三篇 alignment 里活了 23 处、`_invoke_with_token_escalation` 11 处、`probe_provider` 7 处,全是 source-of-truth 档。已改:07 全篇(binds_code + 探活/截断/copilot 各段)、06、05、08、mvp1/README、`LLM_MODEL_CONFIGURATION_FLOW.md`、两篇 community catalog 文档(其中一篇的"当前实现参考"写的是 `/Users/sevenx/...` 某台机器的绝对路径,一并改成仓内相对路径)。**没改的两类**:① 07/ledger 里那些故意保留的旧名(讲"它被什么取代"必须点名旧的);② `call/dispatch.py`/`call/models.py`/`ordinary_chat`/`GenericRouteChatModel` 共 117 处,**被下面 P6-阻 挡住** |
| P6-阻 | **P3c 删掉的"普通 chat 面"是设计明文要求的,我判错了** | ✅ 已裁决(2026-08-11):恢复 | 我在 P3c 说那条路"对任何合法路由都不可达",据此删了 `call/dispatch.py` + `call/models.py`(#718)。**对 `GenericRouteChatModel` 成立**(工厂每个协议都有分支,轮不到它);**对 `dispatch_ordinary_chat` 不成立**——`git show 8362d3b5^:.../call/__init__.py` 显示它是 `call` 域的**公开导出**(第 21 行 import、第 84 行进 `__all__`),签名直接吃 `(route, messages, ...)`,根本不经工厂。仓内没有调用方**正是公开 SDK 入口的常态**,不是死代码的证据。设计明文:`docs/graph-agent-gateway/mvp1/09-inv-invocation-runtime/mvp1-alignment.md:43-53` 记 PM 2026-06-04 决策"gateway 必须格式中立,对外三张脸",第二张就是"**普通 chat 面**:gateway 用自己的「普通 chat 内核」(`ordinary_chat.dispatch_ordinary_chat`)调,返回**非-LangChain** 结果,给不想依赖 LangChain 的消费方",PM 原话"现在市面上没有用 chat 协议的 app 了？全部都用 chatX 了？";同篇还写死了测试关键点"① 普通 chat 面返回非-LangChain 结果"。全仓查过没有更晚的决策废止它。**用户 2026-08-11 那句"没用的就删"是在我给的错误前提上作的裁决**,所以我不拿它当已授权。两条路:(a) 按设计恢复第二张脸(不是 revert #718——`GenericRouteChatModel` 确实该死;是在现有 `dialect` 域上重建一个公开的非-LangChain 入口 + 补那条测试);(b) 用户确认三张脸的决策已退役,那就把 09 改写成"两张脸"并说明原因。**裁决:走 (a) 恢复。** 用户原话"当然要咯，我的那句话现在问你还是成立啊"——即 2026-06-04 那句反诘今天依然成立,不用 LangChain 的消费方实打实存在,所以这张脸不是过时,是被我判错删的。**恢复的形状不是照旧还原**(不 revert #718、不把 1026 行自研序列化拉回来、改吃整条 fallback 链而非单条路由),关键设计决定与验收判据落在决议文档 D10;那 117 处文档随恢复后的真实形状一起改写,另起一条 P6e |
| P6e | 两张脸定型后,文档跟着改完 | ✅ 已合并(#729) | P6b 卡住的那 117 处。**不是把死名换成活名,是几节设计整个要重讲**:10 那节写的是"有官方 ChatX 就用官方,没有就 `GenericRouteChatModel` 兜底"——今天兜底**在类型上不可能存在**:`Protocol` 是封闭四值,`build` 四个分支齐全并以 `assert_never(protocol)` 收尾,注释白纸黑字"Not a fallback... that fails the type check rather than quietly building something else"。加第五个协议忘写分支是编译期红,不是悄悄走兜底。顺带回答了原设计留的"真非标 provider 怎么办":不再包一层 best-effort 交给 engine——**说不准能不能用的东西,不该长成一个能被当成可用的对象**,走不通就该在 03 判为不可用。**`GenericRouteChatModel` 那 5 条序列化规则没跟着删**:它们讲的是 wire 本身的要求,不随实现作废,改写成一张"今天谁满足它"的表——规则 1(带 tool_calls 的空-content 消息不能丢)**仍是我们的责任**(网关原样传 `BaseMessage`,`test_chatx_invocation_runtime.py` 那条测试就是守它的),2/4/5 归官方 ChatX。用处是排障别找错地方。11 那条"把 thinking 规则从 provider 分支提炼出来"的待办**其实早已完成,只是落点变了**:住 `dialect/reasoning.py`(调用方说一次"想多久",各 dialect 各自翻译),不是当初设想的 profile/init kwargs。**顺手修了一类不同的伤**:09/10 各有一节(#392 写入的 temperature 换算)在 git 里就是乱码,中文全变 `?`,共 9 行整段不可读——按 `registry/bounds.py` 的 `AUTHORED_TEMPERATURE_MAX`/`_PROTOCOL_TEMPERATURE_MAX`/`temperature_ceiling` 和两条测试断言**重新写出来**(authored 0..2,到 Anthropic 上限 1.0 所以 1.5→0.75,到 OpenAI 上限 2.0 所以 1.5 不变,`None` 一路保持 `None`),不是猜丢掉的字。**改法纪律**:凡提到已删结构的句子一律保留"原文说过什么"再说"今天是什么",不假装那段设计从来不存在。判据:doc lock 5 passed |
| P6f | 13 那篇设计从头到尾在讲一个**已经不存在**的事件 | ✅ 已合并(#739) | P6e 清完 117 处死路径之后剩下的最后一块,而且它不是路径问题:`13-x-tracing-events-exceptions/mvp1-alignment.md` 通篇以 `LLMFallbackEvent` 为主角——标题、Owns、接口契约表、F1/F3 两节、gaps、覆盖表、代码索引全在描述它。**可这个事件在全仓一个字都不剩**:`grep -rn "llm_fallback\|LLMFallbackEvent" packages/graph-agent-gateway/src packages/graph-agent/src apps/studio/frontend/src` = **0 命中**。**不是我判它该删,是决议早就判了**:`docs/design/2026-08-09-streaming-tracing-architecture-decision.md` D10-b 原文「`llm_fallback` 是这一族里已经实现的那一种取值。依「No backward compatibility」,新事件**取代**它:同一个变更里删掉 `llm_fallback` 的两处定义与前端对它的专门分支,不留双读、不留别名」——代码执行了,文档没跟上,于是权威设计与实现整整两个月互相说着两件事。**重写不是换名字**:今天是**一种**事件九种取值(`skipped_circuit_open`/`probe_failed`/`dropped_rejected_settings`/`retried_same_route`/`escalated_budget`/`fell_back`/`failed_terminal`/`answered`/`exhausted`),回退只是其中一种;「作废已流出的内容」是决策上的布尔字段而不是第八种取值(D10-b 明写「作废不是一种决策,是决策的后果」);字段从 `from_provider`/`to_provider`/`context` 字典换成扁平的路由身份 + `provider_status_code` + `next_route_id`;原文 gaps 里那条「字段名保留、留待未来 schema 升级再统一」——**那次升级就是 D10**。另补一整节 F4:`LLMCallSettingsEvent`(2026-08-10 偏好决议的产物,原文里完全没有这回事)。**旧文不装作没写过**:文末「原文与今天的差别」逐条留底七处说法及其今天的下场(同 P6e 纪律)。**顺带把同一批还在点名死符号的权威条目改活**:`mvp1/README.md` 的 13 行与 09 行(`call/dispatch.py`/`models.py:GenericRouteChatModel`,#718 已删)、`DESIGN_UNITS_INDEX.md` 四行 binds_code(`exceptions.py`/`probe_catalog.py`/`call/clients.py:probe_provider`/`_invoke_with_token_escalation` 均已不存在)、包 `README.md` §G 与 §F(`exceptions.py`、`registry/error_classification.py`)。判据:gateway doc lock 5 passed、gateway 全套 502 passed / 1 xfailed、重新量一遍权威文档点名的 906 处路径后 13-x 归零(其余剩项均已带「(已删)」「已不存在」标注或是逐字引用原文) |
| P6c | `legacy_v3_endpoint_id` 已无人可用 | ✅ 已合并(#730) | P6a 做手册时点公共 API 顺带查实:`registry/endpoints.py:174` 的 `legacy_v3_endpoint_id` 入参是一个 **v3 provider 字典**,而 #725 删掉 v3 迁移之后,全系统再没有任何地方产生这种字典;全仓引用只剩它自己的定义、`__all__`、registry 包入口的 re-export,以及一个只测它自己的测试。连同私有的 `_legacy_product_endpoint_id` 与 `_OFFICIAL_LEGACY_ENDPOINT_IDS`(需先确认后者无他用)一起删。**注**:同批做的公共 API 死名扫描出过假阳性(`AnthropicMessages` 被判死,实际在 `dialect/methods.py` 里被实例化,只是那行以引号开头被我的过滤规则跳过了),所以这一条是**逐个核过的单条结论**,不是扫描结果。**动手时又多删了一个**:`_url_path` 在删掉 `_legacy_product_endpoint_id` 之后只剩定义、零调用——ruff 不报私有函数未使用,是手工核过才发现的(同处的 `_url_hostname`/`_host_matches` 有别的用户,留)。**纯删除没有新行为可测,不写"断言这个名字不存在"式的仪式测试**;判据是全套仍绿、且唯一随之消失的测试就是测它自己那条(`test_registry_endpoints.py` 的 `test_legacy_v3_endpoint_id_helper_preserves_known_migration_ids`)。gateway 497 + studio 1671 绿,ruff/mypy --strict 绿 |
| P6-恢复 | 第二张脸回来了:`chat_plainly` | ✅ 已合并(#728) | 用户裁决走 (a) 之后的落地。**不是 revert #718**:`GenericRouteChatModel` 与 `call/dispatch.py` 那 1026 行自研序列化不回来(D10-1)——P3 的教训是一次调用两条线路会各说各话,把自研序列化拉回来等于把病灶请回来。**改成按 09 的意图落而不是按字面落**(D10-2):09 原文说第二张脸有自己的「普通 chat 内核」、`GenericRouteChatModel` 是它的 LangChain 包装;那句话的意图是**两张脸共用一个内核**,写成 `dispatch.py` 只因为当时那就是内核。今天内核是 `RouteChatModelFactory` + `GatewayChatModel` 的编排,所以新入口复用它、只在边界转成朴素 dataclass。**主次因此反过来**:原设计是 generic 内核为主、官方 ChatX 为辅,今天每个协议都有官方分支,generic 兜底路由不到。**三处刻意的取舍**:① 吃整条 `ResolvedRole` 而不是旧签名那一条 `ResolvedRoute`——只吃一条的话它比第三张脸(handoff)只多做一次序列化,而且不用 LangChain 的 app 就此丢掉 fallback,等于没装网关(D10-4);② 旧签名 19 个形参不重建,设置早由 role 物化好(D10-5);③ 只给同步版,编排一路到底同步(没有 `_agenerate`),异步版只会是 `asyncio.to_thread` 的包装,那是消费方一行的事(D10-6)。**测试按 09 白纸黑字那两条关键点写**:逐字段查 `PlainAnswer` 没有一个 LangChain 类型;首选路由 401 时照样走到下一条。**手册例子逐字跑过**,第一遍跑出 `text` 是整条 message 的 repr、`usage` 全 0——是我脚本里的 fake 不忠实(`stream()` 该吐 `AIMessageChunk` 不是整条 `AIMessage`),照 `tests/stream_fakes.py` 的真形状改完全对。09 全篇按实情改写(它原先还在指 `dispatch.py`/`ordinary_chat`/`LLMClientManager`/`_invoke_with_token_escalation`)。判据:gateway 498 + studio 1671 + engine 1581 全绿,ruff/mypy --strict 绿 |
| P6d | 07 的探问段按新语义重写(记档时写的是"F8",**实际落点是 F3**) | ✅ 已合并(#734) | P6b 只把死路径改活,没重写设计。07 通篇按"probe = 这条 route 活着吗,失败就 mark_down"写;今天 `call/pre_call_probe.py:8-11` 明说 "A refusal here is not a route that is down. It is a question with a follow-up"——问的是"收不收这些设置",被拒是去掉偏好重问,不写熔断,而且探针**刻意复用生产同一个 `RouteChatModelFactory`**(原设计写的是"用 client manager 的轻量 SDK client,不走 ChatX",正好相反)。取代它的决策是 `docs/design/2026-08-10-runtime-settings-are-preferences-decision.md`。**动手第一件事是纠正落点**:这条记档和文档里那 5 处指针都写着"见 F8 ⚠",但 ⚠ 从来就在 **F3**,F8 讲的是另一件事(多条 route 的批量探测编排)。指错了段会让下一个人去改无关的章节,已连同正文一起改对。**重写内容**:F3 改题为「前置探问(正式请求构造器 + 1 token 预算 + base_url 归一化消费)」,按决议 D1/D2/D5 正面写清五件事——问什么(带本次真实设置问"收不收")、用什么问(`CallSettings.as_cheap_question()` + 调用方注入的同一个 builder,判据是**成品 payload 同源**而非"代码里传了参数")、拒绝意味着什么(不写熔断/不换路/不判全体失败)、追问的阶梯(先看 `classify_exception(...).scope`,非 `request` 直接收工;是 `request` 才整层摘掉重问,能答再**一次一项**点名,且只问 `provider_request_keys` 里带得动的项;常规情况永远只有一问)、以及它看不见什么(`ignored` 归答案收口时刻)。**测试点整段作废重写**:旧的两条("probe 失败 → mark_down + fallback event + 换路"、"probe 不可 fallback → 抛 `AllProvidersFailedError`")按新语义都不成立,换成五条覆盖上面五种走向。**旧文不删,收进本节末尾「原文与今天的差别」**(P6e 定下的纪律:凡提到已删结构的句子,先说原文说过什么,再说今天是什么)。顺带把模块头三处跟着改对:标题的"探活"、Tier 里"`client_manager` probe/熔断/usage 已在包内"(probe 早搬出该类,`call/clients.py:17-126` 全类无 probe)、Owns 里的"1-token probe"。判据:文内 12 处代码坐标逐条按当前源码核过(`chat_model.py:166/309-320/318`、`settings.py:133-144`、`pre_call_probe.py:8-11/14-16/63-81/100-105/109-113/118-124`、`clients.py:17-126`),纠正了记档时抄错的 `pre_call_probe.py:63-83`;gateway 那份 `_audited-ready-hashes.json` 的 `count` 是 0,改这批文档不需要重钉哈希 |
| P7 | 两个「留着别 404」的空转端点 | ✅ 已合并(#736) | `/api/llm/catalog/sync` 与 `/api/llm/catalog/repository/ensure` 是 disabled no-op,注释写明留着的理由是 "kept only so the existing UI button does not 404" / "so any older client that still calls it gets a clean, networkless reply"。**动手前先去核这两条理由,两条都不成立**:① 前端全仓只调 `/llm/catalog/sync-verified` 一条,这两个端点**一个调用方都没有**——所谓「还在按的按钮」不存在(记档时我照抄了注释的说法,这里更正);② 「让老客户端有个干净回复」正是 AGENTS.md 明令禁止的向后兼容。剩下提到它们的全在 `.kiro/specs/` 的历史规格里,那是记录不是消费方。所以正解是纯删除,没有 UI 要改。**连带删掉三条只断言它们空转回复的测试**(`test_llm_registry_api.py` 两条、`test_community_catalog_boundary.py` 一条),并把 `docs/development/STUDIO_REQUEST_AUDIT.md` 里的 4 行条目清掉——那份台账有 CI 门禁盯着(`test_studio_request_audit_ledger.py` 的 stale 断言当场就红了),它给这两条写的裁决词是「Explicit catalog repository setup command」「Explicit remote catalog sync command」,描述的是它们**十年前**的样子,而不是今天这个空壳。**纯删除不写仪式测试**(同 P6c/P13 纪律),判据是 studio 全套绿 + 全仓零引用。**查到但没动**:`/catalog/share` 与 `/catalog/contribute` 同样没有前端调用方,但它们不是空壳(前者真导出证据,后者是社区上传路径),且挂在一份标注 Implemented 的规格上——「功能做了没接 UI」和「已废弃留壳」是两回事,另记 P14,不夹带 |
| P8 | 探测自己把请求发坏了,不算模型的错 | ✅ 已合并(#731) | **真机点验挖出来的,离线测试看不见。** 多模态探测发的是一张 **1×1 PNG**,Ark 直接 400:"Image dimensions are too small. Minimum allowed dimension: 14 pixels. Current dimensions: width = 1, height = 1"(实测 2026-08-11,ark-official × doubao-seed-2-0-pro-260215)。代码把这次拒绝记成 `invalid_model` / `probe-failed`——**等于给这个模型盖了「不认图」的章,而它其实认图**:换成 16×16 之后同一条路由同一个 provider 直接 `probe-verified` / `ok`。三处一起修,服务同一句话「没能问出答案,就不许替模型作答」:① 探测图 1×1 → 16×16(它是我们自己的 payload,不该成为探测失败的原因);② 判决器加第三个同族分支——`judge.py` 里本来就有两个先例(余额不足的 400 不算模型错、协议不匹配的 4xx 不算模型错,注释原文"conflates ... and poisons every consumer downstream"),图 payload 被拒是同一族,归 `error` 不归 `invalid_model`;③ **根因在证据层**:`verified = result.status == "ok"`,其余一律写 `probe-failed`——余额不足、断网、超时全被记成「这个模型不行」,而且这份证据是要往社区分享的。effort 那边早学会了三分法(`INCONCLUSIVE_PROBE_STATUSES`),证据这边没学会。把那份词表从网关私有升为公开概念、经 `app/core/adapters/gateway` 透给 studio(不许 studio 另造一份同义词表),不确定就不写证据。**线路基线同 commit 重录**(`probe_wire_baseline.json`,36 行全是图数据、其余字节一个没动——重录脚本踩过两个坑:`sort_keys` 会把整份文件重排、Windows `write_text` 会写 CRLF,两者都让 diff 失去评审价值)。判据:gateway 499 + studio 1679 绿;**真机复验闭环**——同一条路由修前 probe-failed、修后 probe-verified |
| P9 | 多模态实测结论落在没人读的地方 | ✅ 随本行同 PR 合入(与 P12 同一改动) | 承 P8 的真机发现。多模态探测成功后写的是 **evidence** 里的 `input_modalities: [text, image]`(`_successful_multimodal_probe_capabilities`),但 `route_effective_capabilities`(`app/services/llm_route_capabilities.py:9`)只读 `route.capabilities` + verified profiles,**不读 evidence**。于是 route 的 `input_modalities.source` 永远停在 `provider_doc`,而前端 `routeAcceptsImageVerified`(`apps/studio/frontend/src/api/llm.ts:1671-1675`)读的正是 `capabilities.input_modalities`、要求 `source === 'probed_verified'` 且值里含 `image` 才算认图(**记档时我写的是 `vision.source`,是错的**——`vision` 确实停在 `api_list`,但前端根本不读它,读的是 `input_modalities`)——**这条判据现在不可能为真**,哪怕图真的探通了(实测:ark 那条路由探测返回 probe-verified/ok,`vision` 仍是 api_list)。这跟 P4e 是同一条 provenance 链的另一个断点:P4e 堵住了「catalog 声称冒充实测」,这里是「实测到不了能力位」。**更正后这条的形状比记档时小得多**:`verified_profile_route_capabilities`(`app/services/llm_route_capabilities.py:21-53`)本来就把 ready profile 的 `input_modalities` 写成 `source="probed_verified"`,而且这份合并结果确实会落进 route.capabilities(实测那条 ark 路由的 `verified_methods` 就是 `probed_verified`,它只可能来自 profile)。缺的不是「证据升级能力」这套新机制,而是**多模态探测没有产出 profile**:`probe_route_multimodal`(`routers/llm.py:1456-1498`)对结果只做一件事——`_merge_probe_evidence_into_route`,既不碰 capabilities 也不建 profile;而同一个仓里 `llm.py:4022` 早就有「探通了就建一条 ready profile」的现成写法。所以正面那一半是接上既有通道,不是发明新机制;真正要裁的是负面那一半(见 P12)。不夹带进 P8 |
| P12 | 模型答「我只吃文本」,被记成「这个模型是无效的」 | ✅ 随本行同 PR 合入(与 P9 同一改动) | **P11 修好启动器之后,在主 app 上做合并后逐项点验时当场撞出来的**,是 P9 的孪生缺口。点 Settings → Copilot → DeepSeek V4 Flash 组第 2 条 route(Ark Official)的多模态测试按钮,实测 2026-08-11T19:00:42Z:Ark 返回 HTTP 400 InvalidParameter,原文 **"Model only support text input"**。这是一句**关于模态的、确定的回答**——模型在,文本能用(该 route 的 `status` 至今是 `verified`),它只是不吃图。可 `judge.py` 把它落到 `probe_status: invalid_model`,证据层随即写下 `trust_state: probe-failed`,而这份证据是要往社区分享的;UI 的 evidence 计数当场从 40 verified/44 failed 变成 **41/45**(+1 verified 是同一轮 doubao 手动探测,+1 failed 就是这次)。**不是 #731 造成的**:#731 加的 `_rejects_our_payload` 词表全是图**形状**被拒(尺寸/格式/过大),讲的是「我们自己的 payload 坏了」;而这一句讲的是模型自己的能力,落不进那个分支,走到 `model_not_found_status` = `invalid_model` —— 改动前后同一条路径。**根子跟 P9 是一件事的两面**:多模态探测现在**两个方向都推不动** `vision`——问出「认图」时结论只进 evidence、进不了能力位(P9),问出「不认图」时干脆被记成模型无效(本条)。所以本条不单独开修:`ProviderProbeStatus` 是封闭词表,今天没有一个成员表示「模型明确回答不支持这种输入」,要不要加成员、以及测出来的正负结论怎么落到 `vision`(source 能不能到 `probed_verified`),正是 P9 那句「证据升级能力,还是探测直接写能力」要裁的同一个设计问题。**先裁 P9,再一起改**;裁之前不许随手把它映射成 `error`——那是把「一个明确的回答」谎报成「没问出答案」,方向相反的同一种不诚实。**裁决已下(D11,2026-08-12)并同 PR 落地**:词表加一个成员 `capability_unsupported`(答了、确定、答案是不),判据落在 gateway 的 judge 与 `_rejects_our_payload` 并列;答案→能力归 `registry/capabilities.py` 的 `measured_image_input`,正负两答都写成 `probed_verified`(正答 `[text, image]` + `vision=True`,负答 `[text]` + `vision=False`),studio 的 `_successful_multimodal_probe_capabilities` 随之删除;负答的 evidence 是 `probe-verified` 而不是 `probe-failed`——trust_state 说的是这条记录怎么立的,不是它答的是哪一面。**顺带修的两处**:① 这份封闭词表在 studio 被手抄了两遍(`EndpointModelTestResult.status` / `ModelProbeResult.status`),已改成直接引用网关的 `ProviderProbeStatus`;② `_route_is_language_capable` 把「没有 output 模态记录」读作「不产文本」,而本次改动第一次让实测模态落进 route.capabilities——已改为只让**有记录的**那几张模态表投票。**真机实证(2026-08-12,本树私有 sidecar :8788,Settings → Copilot → DeepSeek V4 Flash 第 1 条 route)**:点多模态按钮,Ark 仍答 HTTP 400 InvalidParameter「Model only support text input」,盘上记录从 `probe-failed/invalid_model` 变成 `probe-verified/capability_unsupported`,`vision` 变成 `probed_verified` 的 `false`,且经 `GET /api/llm/registry` 交回前端时仍是 `probed_verified`。**这次真机把第一版设计推翻了一处**:第一版让正负两答都改 `input_modalities`,盘上确实写成 `probed_verified`,但读投影 `_merge_profile_capabilities` 对模态清单取并集且保留 base 元数据,交回前端时又变回 `provider_doc`。改那条并集规则是错的——文本生成探测写下的模态清单同样带 `probed_verified`,但它只证明文本能用、根本没问过图,拿它覆盖文档会删掉文档已知的 image/pdf。定稿改为**不对称落点**:两答都写 `vision`,只有正答写 `input_modalities`(清单是下界),前端判据改读 `vision`。详见决议 D11 |
| P13 | 注入形参还叫「客户端管理器」,而它一个 client 都不管了 | ✅ 已合并(#735) | 执行 `docs/design/2026-08-10-gateway-module-tree-and-probing-decision.md` 里早已写下的那句「剩下的东西不再管理任何 client,名字必须跟着改」。类在 #718 已改名 `LLMCircuitAndUsageLedger`,**注入形参 `client_manager` 还留着旧名**,而 `chat_plainly` 那边早就叫 `ledger`(`call/plain.py:65`)——同一个东西两个名字,读代码的人得先猜它们是不是同一件事。改成 `ledger` 一名到底:`GatewayChatModel.ledger`、`ModelResolver.ledger`,`_default_client_manager`→`_default_ledger`,`_manager(...)`→`_ledger_or_default(...)`(旧写法是 `manager = _manager(ledger)`,一行里两个名字指同一个东西)。**动手前把决议里「改名不外溢」那句独立核过一遍**:`GatewayChatModel(` 的构造点只在网关自己的 src 与 tests,studio 与 engine 各 0 命中,所以这是纯网关内部改名,不是公开契约变更。顺手改掉一条日志字段 `phase=llm_client_manager`→`phase=llm_circuit_ledger`(全仓只有旧 kiro spec 提过它,无消费方)。**纯改名不写仪式测试**(同 P6c 纪律),判据是既有覆盖全绿 + 全仓零残留:gateway 499 + studio 1679 绿,ruff/mypy --strict 绿,`git grep client_manager -- packages/graph-agent-gateway` 为空。**顺带把三条 README 死路径改活**:它们点名的 `client_manager.py`/`gateway_chat_model.py` 早已是 `call/clients.py`/`call/chat_model.py`,`registry/probe_contracts.py` 已随 #713 删除(七行 re-export,`ProbeResult` 本来就定义在 `registry/schema.py`);其中「探活 + 瞬时重试 + 截断升级重试(✅ client_manager.py)」一行是三件事挤在一个死文件名下,拆成三处各自的家 |
| P14 | 两个真干活、却没有任何界面调得到的 catalog 端点 | 📌 记档 | 做 P7 时顺手核出来的。`/api/llm/catalog/share`(把本地 probe-verified 证据导出成可分享的清单)与 `/api/llm/catalog/contribute`(社区上传)都有实现、有后端测试,**但前端一处都不调**(全仓只有 `/llm/catalog/sync-verified` 有前端调用方)。它们跟 P7 删掉的那两个空壳不是一回事:那两个是 return 一个 `status: disabled` 就完事的壳,这两个真干活,而且 `.kiro/specs/community-probe-catalog-service-phase2a/` 把它们标成 Implemented。所以要判的是**哪一种**:是「能力做完了但 UI 还没接」(那就补入口),还是「这条路已经不做了」(那就跟 P7 一样删干净)。**没有证据前不猜**——两种都会导致相反的动作 |
| P15 | 存 settings 得等没人在读——一条只在 Windows 上炸的写入缺陷 | ✅ 已合并(#743);**因果证据**:该 PR 自己的 `cross-platform-smoke (windows-latest)` 绿 | **PR #739(纯文档改动)把 `cross-platform-smoke (windows-latest)` 跑红了**,`PermissionError: [WinError 5] Access is denied` 出在 `.llm_credentials.json.<rand>.tmp` 改名盖到 `llm_credentials.json` 那一步;ubuntu/macos 全绿,本地全绿。该 job **不在 5 道必过门禁里**,所以它可以一直烂着没人管——这正是它值得单独记一条的原因。**先坐实是谁的错再动手**:在本机写出确定性复现(持有一个读句柄再存)→ 同一个 `[WinError 5]`、同一对路径,**是我们写入方的缺陷,不是 runner 环境干扰**。**根因是两半都没做对,而仓里早有做对的那一份**:`app/core/adapters/atomic_file.py` 的模块注释开头就写着「`os.replace` 调的是 `MoveFileExW`,目标只要有任何句柄打开就 `ACCESS_DENIED`——光共享 delete 还不够」,它自己用的是 `FileRenameInfoEx` + POSIX 语义改名,读侧用 `FILE_SHARE_DELETE`。而 settings 那批文件**两半都没用**:五个写入方各自手抄了一遍「写临时文件再改名」(`llm_credentials.py` / `llm_roles.py` / `llm_role_test_results.py` / `runtime_truth_init.py` / `metadata_local.py`),四个读取方用裸 `read_text`。**两半都是承重的**,错误码会告诉你缺哪一半:旧发布器 = WinError 5,发布器修好但读侧还是裸 open = **WinError 32**(共享冲突),两边都改才通——这一步是实测出来的,不是推出来的。**顺带抓到 CRLF**:五份手抄没有一份传 `newline="\n"`,于是 Windows 上存出来的 credentials 是 CRLF,违反 `CROSS_PLATFORM.md` 的「文本一律 UTF-8 + LF」——同一份文档的字节随主机而变。**权限没有退化**:`mkstemp` 建的临时文件本来就是 0600,改名把 mode 带过去,既有测试 `test_llm_v4_backend_contract.py:311-312` 断言文件 0600 / 目录 0700 照旧绿;并把这条保证写进发布器的 docstring + 一条 POSIX 断言测试(**保证归发布器,不再靠每个调用方记得 chmod**)。**改了一条既有测试的打点**:`test_save_credentials_atomic_write_preserves_file_on_replace_error` 原来 monkeypatch `os.replace`,而 Windows 上改名走的是 `SetFileInformationByHandle`——再打那儿就是**拦不到、测试反而空绿**,改打发布器自己的改名接缝,断言不变。**加一道防复发门禁**:`test_one_publisher.py` 用 AST 扫 `app/` 下所有 `os.replace`,只放行 `atomic_file.py`;已验证它对修改前的代码会命中 `llm_credentials.py:461`,正是出事那一行。**查到但没动**:网关 `registry/catalog.py:90` 也是裸 `os.replace`,但它属于另一个模块(不能 import studio 的 adapter),且 `ProbeCatalogStore` 在 studio 后端**一处都没被构造**,不在活路径上,记档。判据:三条复现测试先红后绿、studio 全套绿、ruff + mypy 绿、真正的因果证据是**这个 PR 自己的 `cross-platform-smoke (windows-latest)`** |
| P10 | 「收下了」不等于「照做了」 | ✅ 已合并(#738) | effort 测量把 provider **接受**某档记成 `supported: true`,而一个根本不校验该参数的 provider 会全盘接受——两种 provider 发回来的接受清单**一模一样**,光看清单永远分不开。同一个仓里 `call/outcome.py` 对这件事的分寸严得多,开头就写 「``sent`` is the honest answer for the many settings whose effect nothing in the response can confirm: reporting them as applied would be a claim nobody checked」。**修法是加一个对照组**:每批多问一档**根本不存在**的(`EFFORT_CONTROL_LEVEL`),让两种情况的答案第一次长得不一样;对照档由 `effort_questions()` 自己带出去、不交给调用方记着加,判读落在既有的 `None`(「这批什么也没定」)上,**schema、能力位、前端一处都不用改**。先写红测试再改:gateway 3 条 + studio 集成 1 条,另有 8 条既有测试因批次多一档跟着改期望。**论据更正(2026-08-11,同日)**:当时拿 deepseek-v4-pro 七档全收当作「不校验」的例子,**这个例子引错了**——`docs/design/2026-08-10-preferences-fit-the-route-decision.md:90-107` 的抓包实测显示它**确实校验**(`effort='banana'` → HTTP 400 `unknown variant \`banana\`, expected one of none, minimal, low, medium, high, xhigh, max`),回吐的七名枚举与探针刚问过的七档完全一致,所以那 7/7 是一次真测量。机制不受影响(对照档在 DeepSeek 上会被 400,测量照常成立),错的只是用来论证它的例子;`probing/questions.py` 的 docstring 与两条测试的说明已同步改掉。判据:gateway 502 passed / 1 xfailed、studio 1677 passed / 4 skipped、ruff + mypy --strict 全绿 |
| P11 | 启动器点名了一个网关早已不存在的模块,桌面版因此起不来 | ✅ 已合并(#732) | **P8 之后重启桌面版才暴露的**,离线测试与 CI 都看不见。`scripts/studio-dev.ps1` 起 Tauri,`beforeDevCommand` 先跑 `apps/studio/tauri/scripts/ensure_vendor.js`;它拿 vendored 解释器执行 `import graph_agent, graph_agent_gateway, graph_agent_gateway.probe_catalog`,而 `probe_catalog.py` 早在 **#706「registry 域自己作答」** 里就并进了 registry 域(`git log --diff-filter=D` 实证)。于是这道门**永远不可能满足**:检查失败 → 重建 vendor → 再查还是失败 → 抛 `Python vendor closure rebuilt, but required imports still fail`(`ensure_vendor.js:159`),`beforeDevCommand` 非零退出,应用一次也起不来。之前没发作,只是因为那扇窗从 #706 合并前就一直开着没关过。**修法不是把死名换成 `probing`**——那还是同一个病:一份**手抄的 SDK 模块树**放在 JS 里,和 Python 那棵树各自演化,下次重构照样烂掉。真正的权威检查就在同一个文件里:`localPackageSourcesAreVendored` 把两个包的**每一个 `.py`** 与 vendored 副本逐字节 sha256 比对,比点名一个子模块强得多且不会过期。所以点名清单直接从 `LOCAL_PACKAGE_SOURCES` 派生,只剩两个包根,内部怎么重组都与它无关。**新增的守卫测试**逐条把清单里的名字解析回 `packages/*/src` 的真实文件——先写、先红(原文报 `graph_agent_gateway.probe_catalog does not exist under ...`)、再改。顺带修掉一处不可移植:`ensureVendor skips rebuild` 那条测试原先要求**下载来的 vendored runtime** 存在,在任何新检出或 worktree 里必然失败,改成用 `uv sync` 之后一定在的工作区 `.venv`。判据:`node --test ensure_vendor.test.js` 8 条全绿(改前 8 条里 2 条红);vendored 解释器实跑 `import graph_agent, graph_agent_gateway` 退出码 0 |
| P11-注 | 这批启动器测试**哪里都没在跑**,现在跑了 | ✅ 已合并(#737) | `apps/studio/tauri/scripts/*.test.js` 五个文件 24 条测试,不属于任何 npm 包,`.github/workflows/ci.yml` 里也没有 `node --test` 步骤——所以 P11 那条 drift 从 #706 起就没有任何门禁会发现,只能等人重启应用才炸。同一份 ci.yml 里 `cargo test (tauri lib)` 那步的注释早记过同一类教训:"These broke silently on Windows for weeks ... precisely because no CI job ran them"。**接进 CI 前必须先让它们自足**,而只有一条不自足:`ensureVendor skips rebuild` 要求真有一个 Python 解释器(先是要下载来的 vendored runtime,#732 改成工作区 `.venv`,两者在纯前端 CI 机上都不存在)。**修法沿用同文件已有的接缝**——`rebuildVendor` 早就把 `spawn` 做成可注入的,把同一个接缝给 `canImportVendoredPackages` 即可,不是为测试新发明机制。**顺带补上一条从来没测过的分支**:导入失败 → 重建 → 再查通过,断言恰好三次 spawn(查、建、再查)且**只有第一次失败**——否则「重建让它通过」和「它本来就会通过」分不开。落点选 `frontend-gates`(**必过门禁**,已有 Node 20,无需 npm 装包也无需 Python),放在 `npm ci` 之前快速失败。**踩到一个坑**:`node --test <目录>` 在 Node 24 下被当成模块路径去 require,直接 MODULE_NOT_FOUND;改成 `working-directory` 进那个目录再跑裸 `node --test`,与版本无关。判据:本地 24 条全绿(改前也是绿的,但其中一条依赖本机 `.venv`);真正的因果证据是**这个 PR 自己的 CI**——`frontend-gates` 跑在没有 `.venv`、没有 vendored runtime 的全新 ubuntu 检出上,它绿就是自足性被执行证明,不是我读代码读出来的 |
| P5 | T3:ToolCall(L1) → ReactLoop(L2),copilot CLI 收编为第三种执行器 | ⏳ 待办 | T3 必须走生产同一条调用路径(网关 dispatch + `bind_tools`),不得手搓 HTTP,否则测的不是生产会跑的东西 |
| P5-注 | T3 的工具循环覆盖要从零建 | 📌 记档 | 全仓唯一一处 ReAct 工具循环测试 `test_generic_chat_model_runs_langchain_create_agent_tool_loop` 挂在 P3c 要删的死模型上。删它不是损失(被测对象生产不跑),但 **P5 不能以为工具循环已经测过**,必须在 `RouteChatModelFactory` 造出的真实模型上重新建 |
| P16 | predict 的实时 WS 可达性没有任何测试覆盖 | 📌 记档 | 清理遗留分支时查实。废弃分支 `feat/engine-predict-run-unification`(2026-07-12,tip `45dbf3c5`)带着一条 241 行的 `test_predict_realtime_ws.py`,**main 上没有对应物**——`test_predict_e2e.py` / `test_predict_skill_integration.py` / `test_predictor_service.py` 都不测这件事。那条分支的代码已不可用(`predictor.py` 之后被 #659/#665/#667 连着重写三轮,正是 predict 往 run 路径归并),**但它问的问题仍然没有答案**:predict 的实时事件面到底可不可达,今天靠的是没人验过。分支已删,tip 记在此行,90 天内可恢复取那份测试当起点。 |
| P17 | MVP1 baseline 与代码的对账要按今天的文档重做 | 📌 记档 | 同上。废弃分支 `docs/mvp1-status-writeback`(2026-07-12,tip `10820180`)做的是 batch-2 的 baseline drift 对账 + trace F1/F5 + golden 条目,**活儿本身仍然成立**;但它顺带重钉了 `docs/studio/mvp1/_audited-ready-hashes.json`,而那批文档一个月来改过多轮,**那份哈希今天是错的**——合进去要么撞哈希锁、要么把错的钉死。所以处置是**按今天的文档重做一遍对账**,不是复活那份 diff。 |
| P18 | 一把坏 key 被读成「这个模型不存在」,而且运行时不再回退 | ✅ 随本行同 PR 合入 | **做 P9/P12 的合并后真机点验时,顺着同一条状态词表撞出来的第三个同species缺口**,实测 2026-08-12:Google 用 HTTP 400 `INVALID_ARGUMENT` / `"API key not valid. Please pass a valid API key."` / `details[0].reason = API_KEY_INVALID` 报 key 无效,而同日 DeepSeek 用 HTTP 401 报同一件事。`probing/judge.py` 只在 401 那一支判 `invalid_key`,Google 的 400 于是落到 `model_not_found_status` —— 路由探测记成 `invalid_model`(「这个模型不存在」),端点探测记成 `error`。**两层损害**:① key 是端点级的,判错名字就绕过了 studio 的结构性短路(`_STRUCTURAL_PROBE_STATUSES`),批量探测逐个模型烧请求并逐条写下「此模型无效」;② `invalid_model` 是确定答案(不在 `INCONCLUSIVE_PROBE_STATUSES` 里),于是一批连门都没进的能力探测会被读成「这条路由就卖这几档」。**同一个洞在运行时更贵**:`resolve/error_classification.py` 有「400 且像账单」的例外却没有凭据那一支,所以 Google 的 400 走 `fail_request` —— fallback 链第一站就死,后面几条备用 route 一条不试,这与本模块 06 号设计单元自己写的铁律「401/402/403 → fallback(credential scope)」直接打架。修法见决议 D12:账户级条件的词表独立成叶子模块 `graph_agent_gateway/account_conditions.py`(合并时修掉一处已发生的漂移——`purchase credits` 只在运行时那份、`quota exceeded` 只在 judge 那份),judge 与运行时判据读同一份;judge 的四张标记表抽出 `_error_text` 并读进 Google 的 `details[].reason`;顺手纠正 D11 落地留下的 `_model_probe_failure_message` —— 一条 `trust_state=probe-verified` 的记录不该同时写着「probe **failed**」,改名 `_model_probe_outcome_message`,动词由「问出答案了吗」决定。**前端零改动是核对过的结论**:studio 早把 `invalid_key` 当结构性失败、端点提示语早写「Invalid API key」。 |
| P19 | key 已死的端点在界面上仍然是绿的 | ✅ 随本行同 PR 合入 | **做 P18 的合并后真机点验时当场看见的**:点完 Gemini 的「测试」,盘上 `last_test_message` 已经是 `Invalid API key (INVALID_ARGUMENT).`、端点连同它**全部 50 条 route** 都被停用(这是 P18 让 R-E2 第一次对 Google 生效——那条规则由 `result.status == "invalid_key"` 触发,而 Google 的 key 错误从来没被判成 invalid_key,所以一把死掉的 key 此前会留着 50 条路由等角色选用),**而端点 chip 还是绿色**。根因:`apps/studio/frontend/src/api/llm.ts` 的 `routeIsUsable` 五个条件里没有一个读 `route.status === 'disabled'`,其中 `verified_profiles.some(p => p.status === 'ready')` 在那 50 条停用路由里**恰好有一条**仍成立(key 有效时铸下的),于是它「证明」了端点可用。**一条陈旧证据顶掉了一个明确的当前禁令**;同页面里路由 chip 却是灰的(`ProviderCard.tsx:1013`),同一份事实两个界面说法相反。修法见决议 D13:`routeIsUsable` 先问禁令(`disabled` 直接出局)再看证据;`failed` 保持在证据之下不动——它是**探测结论**,可能只针对某个模型、可能是抖动,现有设计允许 ready profile 压过它是对的。规则同步进 `FRONTEND_UI_SPEC.md` §2.7b。**两个 PR 合起来才闭环**:P18 让后端说出「Invalid API key」,本行让这句话走到 chip 上(`endpointTestStatus` 正是靠 `message.includes('invalid api key')` 判出 `invalid_key` 的)。 |
| P20 | P6 的「死路径清零」是一次手工测量,量完就开始烂 | ✅ 随本行同 PR 合入 | **接着 P18 往上游查文档同步时点出来的**。P6 的验收判据是「网关 MVP1 设计源里指向已不存在文件的引用清零(机械可核)」,当时确实清零了——但那次是**人拿脚本扫一遍**,不是门禁,所以域一搬承诺就静悄悄坏掉:重新扫 `packages/graph-agent-gateway/README.md`(P6 判据点名的那份),**8 处指向不存在或已搬家的文件**——`registry/probe_contracts.py` 被 #713 删了却还标着 ✅、`registry/storage.py` 的两个符号已搬去 `registry/fingerprint.py`、`registry/resolver.py`/`registry/lint.py` 已搬去 `resolve/`、`probe_catalog.py` 那条 🔻 其实早就下沉成 `registry/catalog.py`;README 内部还自相矛盾(§B 说 probe_contracts 在,§D 的括号说它已删)。**根因不是这 8 处写错了,是没有任何东西会 import 一份文档**。所以修法是把测量变成门禁:新增 `packages/graph-agent-gateway/tests/test_gateway_docs_name_real_files.py`,扫 README + USAGE.md 里每一个 `` `path.py` `` 与 `` `path.py:symbol` ``,前者必须解析到真实文件、后者那个符号必须真在那个文件里(只查文件名会漏掉「lint.py 还在、只是换了个域」这一类;门禁上线时就靠这一条抓出 7 处只写 basename 的studio 路径,已补成 `services/xxx.py`)。门禁只管这两份**本包自己交付**的文档;`mvp1/` 那些带 baseline/历史约定的设计单元不进,历史记录本来就该点名已经消失的文件——决议文档同理,它的 D3/D4 三处失准改用「订正」段落维护(六个域不是七个、`probing/` 那四项是职责不是文件名、唯一写入口在 `registry/capabilities.py` 不在 `probing/evidence.py`)。 |
| P21 | Windows 上的跨进程文件锁会「等到一半放弃」 | ✅ 随本行同 PR 合入 | **被 P19 那个 PR 的 `cross-platform-smoke (windows-latest)` 拦下来才发现的**——那次红跟 P19 的改动毫无关系(P19 只动前端 TS),红的是 `test_runtime_state_store_multiprocess_first_acquire_allows_only_one_owner`:八个抢租约的进程里,七个应该都拿到 `state.lease_conflict`,结果集合是 `{None, 'state.lease_conflict'}`——有一个报的错**根本没有 error_code**,也就是说它抛的不是那个类型化异常。本机实测坐实了机制:`apps/studio/backend/app/core/adapters/runtime_state_store_local.py` 的 Windows 分支用 `msvcrt.locking(LK_LOCK)`,而它**不是「等到属于你为止」**——它重试十次、每次间隔一秒,然后抛 `OSError [Errno 36] Resource deadlock avoided`(Windows 11 上实测 9.1 秒放弃);其他平台的 `fcntl.flock(LOCK_EX)` 则是持有者拿多久就等多久。**同一个 `_run_file_lock`,两个平台两种含义**:只要持有方的写入慢过 ~9 秒,Windows 上等待方的正常等待就变成一个裸 OSError,穿透了本模块所有调用方都据以编写的 `StudioAdapterError` 契约。修法:Windows 改用 `LK_NBLCK`(占用时**立刻**抛 `PermissionError`,实测 0.000s)自己轮询等待,等待时长因此由我们决定、且无上界,与 flock 对齐;非 `PermissionError` 的 OSError 一律不重试,直接抛——那是这个句柄自己的问题,不是争用。新增测试 `test_the_run_file_lock_waits_however_long_the_holder_takes`:持锁 12 秒(**长于那个内部上限**),断言等待方既没提前放弃、最终也拿到了锁——会放弃的锁过不了这一条。 |
| P22 | 两个 SDK 谁依赖谁,清单上写的和代码里做的是反的 | ✅ 随本行同 PR 合入 | **顺着"网关接口是否清晰"做机械审计撞出来的**:先证实宿主对网关的 6 个 import 全部落在域入口、零深入(自写脚本 AST 扫 `apps/studio/backend/app`),再反向扫网关源码时发现唯一一条越界——`packages/graph-agent-gateway/src/graph_agent_gateway/errors.py:8` 的 `from graph_agent import ModelProviderError`。**第一轮我判错了**:以为这条继承是漂移,写了测试要把它拆掉,结果被引擎包里的 `test_gateway_errors_inherit_from_model_provider_family` 拦下——查设计源坐实它是**契约**:`docs/engine/public-api-contract.md:205` 把「`graph_agent_gateway.errors.GatewayError` 及其叶子是 `isinstance(..., ModelProviderError)`」写成 `ModelProviderError` 的**后置条件**,同文件 :25 与 `docs/engine/mvp0/error-handling/logic-explained.md:79` 同口径——公开异常目录只有五个家族,网关失败归在 provider 那一族,宿主 catch 家族而不是一串叶子。**继承是对的,错的是它周围三件事**:① 网关**从不声明**这条依赖(`pyproject.toml` 里没有 `graph-agent`),而是用 `try/except Exception` 兜底成 `RuntimeError`——那条退路一旦触发,上面那条后置条件就在某些安装里**静默失效**,同一个 `except ModelProviderError` 在一个环境里对、另一个环境里空捞(同 P21 一个病:同一个名字两种语义);② **引擎反过来声明了 `graph-agent-gateway`**,而引擎自己的 `test_engine_source_has_no_gateway_concrete_imports` 禁止引擎源码 import 网关——一条谁都不许走的边,却让"装了引擎就白得网关";③ 于是 **studio backend 直接 import 网关却从不声明它**,靠②那份顺带。修法是让每条边都**既被声明、又被使用**:网关声明 `graph-agent` 并删掉两处 try/except(缺依赖就是装坏了,应当当场报错),引擎摘掉那条不许用的声明,后端补上自己真在用的;`uv.lock` 的净变化正好是这一条边的搬迁(2 增 2 删)。**门禁三条,都改成陈述规则而不是罗列拼法**:原来那张「文件→禁止字符串」三行表,恰恰是被 `errors.py` 换个拼法(公开门面 `graph_agent` 而非 `graph_agent.core.exceptions`)绕过去的,现在改成 AST 扫全树的「只许走引擎公开门面、不许进子模块」;`test_gateway_errors_do_not_inherit_engine_execution_error` 保留,另加一条断言继承**无条件**成立(errors.py 模块级不得再有 `try`);manifest 两条各表一面。判据:三个 pytest 套件全绿(网关 521 / 引擎 1581 / 后端 1717)、ruff + 三份 mypy 全绿、pip-audit 0 CVE。**顺带记档,不在本 PR 修**:`packages/graph-agent/pyproject.toml` 还有一个 `studio` extra 带着 `fastapi`/`uvicorn`/`watchdog`——纯 SDK 的清单里点名宿主的 web 框架,是同一族的味道,但那是引擎→宿主的另一条边,单独一个 PR 处置。 |
| P23 | 被拒的 key 旁边还挂着一个绿勾 | ✅ 随本行同 PR 合入 | **P19 合并后做真机点验时,在同一张卡片上撞见的**:`Settings → API Keys → Gemini Official`,端点 chip 已按 P19 变红、提示语「状态：API key 无效 … 错误码：INVALID_ARGUMENT」,头部徽章也写着 「Invalid API key」,**而 `API Key` 标签旁的勾仍是绿的**,鼠标悬上去写「API Key 已被模型列表端点接受」。两处都由同一份 `matchedResult` 渲染,却各判各的——**两道口子叠在一起**:① **顺序错**:`ProviderCard.tsx:1948` 先问「有没有模型列表」再问「是不是被拒了」,而模型列表**是故意在测试失败后继续展示的**(同文件 `:302` 的 `canShowDiscoveredModels`,为的是让用户失败时也看得见上次结果)——于是「手里还留着上次取回的模型」被当成了「这把 key 现在好使」,陈旧产物替它的来源作了证;② **比错了东西**:那条判「被拒」的分支比对的是字面量 `matchedErrorCode === "invalid_api_key"`,而 `endpointErrorCode`(`api/llm.ts:1454`)**优先返回后端存的结构化码**,Google 报的是 `INVALID_ARGUMENT`——所以那一支对 Google 永远不成立,就算顺序对了也不会变红。设计判据在 `docs/studio/mvp1/01_workflows/00_settings-ux-spec.md`:`:50`「**状态 = 最近观察的投影**」,`:132`「**绿=好；蓝=以前好**」——被拒的 key 顶多是「以前好」,不该是绿。修法:判据改成先读**分类后的** `last_test_status === "invalid_key"`(分类只有一个出口 `endpointTestStatus`,组件不再自己复制一份不完整的规则),再退到模型列表。先写红测试三条(被拒+仍有模型 → 必须 invalid;换一种厂商拼法 `unauthorized` → 同样 invalid;最近一次是 ok → 仍然 valid),头两条实测红(渲染出的 HTML 里 `data-api-key-status="valid"` 与徽章 `Invalid API key` 同框),改后 101 条全绿。规则回写 `FRONTEND_UI_SPEC.md` §2.7b,作为 P19 那条「禁令压过证据」的两条推论:**产物不能替来源作证**、**判据只读分类状态不比厂商原始码**。 |
| P24 | 「还有哪些能力没下沉」这份清单,自己就是过期的 | ✅ 随本行同 PR 合入 | **Stop hook 指出「修了六个具体缺陷 ≠ 网关成熟」之后,先去量、再去判**。量的结果分两半。**耦合是好的,有数为证**:六个域的依赖图**无环**,`registry` 扇出 0 / 扇入 4(底层),`probing` 扇出 3 / 扇入 0(顶层),`dialect` 扇出 0,跨域 import 语句总共约 33 条摊在 49 个文件 1.1 万行上——分层清晰、方向一致,不是「谁都能伸手够谁」的形状。**但「离目标还差多少」这个问题,答案写在本包 README §7,而它是错的**:那条「待下沉」列了九项,逐条对代码核实后发现**四项早已下沉**——endpoint 标准化拆分在 `registry/endpoints.py:standardize_endpoint_candidates`、list-models 解析在 `probing/judge.py:model_ids`、6 态投影在 `registry/projection.py:project_route_state`(studio 侧 `llm_state_projection.py` 只剩 28 行纯委托)、materialize 编排核心在 `role/materialization.py:materialize_role`(studio 侧 45 行纯委托)。真正还在 studio 的是五项:能力合并(`llm_route_capabilities.py` 76 行)、model group 分组(152 行)、identity(三个文件 566 行)、notable(37 行)、熔断持久化(`llm_health_store.py` 216 行 sqlite)。**这就是 P20 那个病复发在更高一层**:P20 修的是「文档点名了不存在的文件」,这次是「文档点名的文件都在,但它对状态的**断言**过期了」——#763 那道门禁只验路径存在,不验状态声明为真。**修法不是再写一遍清单,是让清单没法再烂**:把它改成一张**点名文件**的表,每一行写清这项能力今天真正住在哪个文件——已下沉的点网关文件,待下沉的点 studio 文件。于是 #763 已有的那道门自动成了这张表的棘轮:谁把一项挪下去、删掉 studio 那侧的文件,门禁立刻红。**这条棘轮是实测过的**,不是设想:写这张表时我把 materialize 的文件名猜成 `role/materialize.py`,门禁当场报红(真名是 `materialization.py`);表写完后又故意把 notable 那行改成一个不存在的文件名,门禁同样报红——证明它确实在查 studio 那一侧,不是只查网关自己那半边。 |
| P25 | 「能力合并」有两份实现,而且两份不一样 | ✅ 随本行同 PR 合入 | **P24 那张表列出五项待下沉,挑第一项动手时才发现它根本不是「搬家」,是「合并」**:studio 的 `llm_route_capabilities.py` 有一份公开实现,而网关 `role/materialization.py` 里**另有一份私有拷贝**(`_route_effective_capabilities` / `_verified_profile_route_capabilities` / `_profile_supports_reasoning`)。同一条业务规则两处定义,违反 SSOT;更糟的是**两份行为不同**——判「这个档案算不算会思考」时,studio 那份只读候选**声明的 capability**(`profile.capability in {thinking, reasoning}`),并在 docstring 里写明理由:这个结论要盖 `probed_verified` 章,而且它决定 studio 要不要为每档 effort 各花一次请求,「猜错一个方向费钱、另一个方向漏测」;网关那份却把 `capability` + `profile_id` + `request_mapper_id` **拼成一串做子串搜索**(`"thinking" in haystack`)——正是前者明令禁止的做法。于是一个 `capability=text_chat` 但 mapper 名叫 `openai_thinking_chat` 的档案,在两边会得到相反的判定。**修法是合成一份、放在能力真相的所有者那里**:`registry/capabilities.py` 新增 `verified_profile_capabilities`(只有 `ready` 档案算数)与 `route_effective_capabilities`(实测压过声称),两处旧实现全部删除,studio 经 `app/core/adapters/gateway.py` 消费,`llm_route_capabilities.py` 整个删掉。**合并时按强的那份收敛**,弱的子串搜索一并消失,新测试专门钉住这条(`test_a_profile_that_asked_the_model_to_think_and_got_a_yes_proves_thinking` 里那个 `named_but_not_asked` 用例)。**过程中撞到一处真实约束**:`test_role_effort_intent.py` 手搓 dict 当路由、直接调私有 `_apply_intent`,类型化函数进去就 `AttributeError`——查清那是**测试便利**而非生产形态(生产与其余测试传的都是 `ProviderRoute`),所以改测试去构造真对象,而不是把新公开 API 退回 duck-typing。判据:网关 525 条 + 引擎 1581 条 + 后端 1718 条全绿,ruff/mypy --strict 全绿。**P24 那道棘轮当场兑现了两次**:删掉 studio 文件后,门禁不但逼我改 §7 那张表,还抓出 §3.C 里我没想起来的第二处引用——正是「让清单没法再烂」要防的东西。 |
| P26 | 「同一个模型」在网关里本来就有一份答案,studio 那份是第二个层级不是第二份实现 | ✅ 随本行同 PR 合入 | **接着 P24 那张表往下走,这一项是 model group 分组。先核边界、不照单搬**:README §2 把「渲染状态颜色与**分组折叠**」明写为应用加工,§3.B 又把「按同类分组」列为待下沉的网关能力,读起来像打架。逐字读下来不打架——§2 那条的标题是「**UI 交互 / 录入 / 展示**」、动词是「渲染」,说的是界面上那个折叠动作;§3.B 说的是「把同一模型的多个变体 / 快照 / 渠道折叠成一个用户可见的模型组,**让应用查找 / 选择更方便**」,是算出组交给应用。这正是 §2「一条贯穿的缝」那句原话:「gateway 产出结构化的数据与能力;应用负责把它喂进来 + 渲染出去 + 用出去」。**算组归网关,折叠归界面**。**真正的意外在动手时**:网关 `registry/` 下**已经有** `identity.py`,它的 docstring 自称管「A model's identity: the ids it is stored under, **and the group it belongs to**」——那 studio 这份算什么,第二套分组?核下来不是重复,是**两个层级**:`canonical_id` 是**执行身份**,必须和 route_id 后缀逐字节一致(它自己的 docstring 记着当年拆开导致两个函数要跨文件对齐的教训),所以 `claude-opus-4-1-20250805` 和 `claude-opus-4-1` 在那边是两个组;studio 那份是**人可见的粗分组**,刻意把日期快照折掉好让选择器只显示一行。两者都合法,但**必须写明不可互替**,否则下一个人会把它们合并——所以新模块 `registry/model_naming.py` 的 docstring 与新测试 `test_the_display_group_folds_what_the_execution_identity_keeps_apart` 各钉一遍。**搬迁本身还挖出一条真边界**:`project_model_identity` 拿 `endpoint.display_name` 当推断厂商的线索之一,而 `display_name` 是 **studio 独有字段**——它声明在 studio 的 `ProviderEndpoint` 子类上,`_gateway_endpoint()` 转网关类型时明确 exclude 掉。照搬等于让网关假设一个只有 Studio 才有的字段,那这份能力就只服务于长得像 Studio 的宿主。改成显式参数 `provider_label` 由宿主喂进来,网关的 `ProviderEndpoint` 一个字段都不用加。**合并两个文件时又抓到一处同名不同义**:两边各有一个 `_has_model_version_context`,identity 那份查「前面有没有品牌词」、groups 那份查「有没有版本号 token」——分居两个文件时谁也看不出来,合成一个文件就逼它们各自正名(`_tokens_include_model_brand` / `_tokens_include_version_number`)。**新引入的风险当场配了守卫**:`provider_label` 有默认值,漏传不报错、只是品牌推断静默退化,所以 studio 侧留一条 AST 规则测试扫 `app/` 下全部调用点必须带上它。判据:网关 531 条 + 引擎 1581 条 + 后端 1717 条全绿,ruff / mypy --strict 全绿。**P24 那道棘轮第三次兑现**:删掉两个 studio 文件后门禁逼我同时改 §7 表和 §3.B,连我在新注解里随手写的裸文件名 `provider_config.py` 都被当成路径抓了出来。 |
| P27 | 那份「还差哪些没下沉」的清单,连自己漏了什么都不知道 | ✅ 随本行同 PR 合入 | **P24 把清单做成了「不会烂」的(点名的文件必须存在,否则门禁红),但它仍然「可以漏」**——哪些能力该上表,靠的是写表的人记得。这一版去验证这个假设:不再凭记忆,而是**机械扫** `apps/studio/backend/app/services/` 的 64 个模块,用两条判据筛——①属网关域(被 `app/routers/llm.py` 使用,或自身 import 网关);②无宿主依赖(不碰 fastapi / sqlite / 文件路径 / studio 配置;碰了就说明它绑死了 §2「应用加工四件事」之一,得先拆规则与介质)。**扫出九个,其中五个是历来任何一版清单都没点过名的**:`official_capability_sources.py` 291 行(各厂商 models / vision / thinking 官方文档 URL 与取值规则,纯知识)、`community_catalog.py` 281 行(证据外发的脱敏红线——**网关 owns 探测知识库,却不 owns「什么可以离开这台机器」**,白名单构造上传体、私有主机整段丢弃端点身份)、`llm_credentials_evidence.py` 234 行、`model_probe.py` 29 行、`llm_evidence_ids.py` 19 行,合计 854 行。也就是说 P24 那句「九项」本身就是低报的。**扫的过程还发现两处值得记的事实**:其一,`model_probe.py` 不是待下沉而是**已下沉后的残留**——探测本体早在 `probing/`,这里只剩一个宿主侧 DTO 供 router 把网关 `RouteProbeResult` 适配给自己的调用方,属宿主自用形状,应当挂号说明而不是再搬一次;这正是这类盘点的价值——逼人把「为什么留下」写下来一次。其二,`endpoint_probe_priority` 的 docstring 写着「Mirrors ``llm.py:_endpoint_probe_order``」,而**那个函数在全仓源码里已经不存在**(只剩这句话和一条测试注释指着它)。它不是重复实现,而是与网关 `registry/catalog.py:probe_priority` **目标相反的另一种排序**:端点测试要最快见绿所以领头放已验证的,能力发现要探新所以跳过已验证的。把注释改成直接讲清这个对立,不再指向幽灵。**本轮只补清单与事实,没有加新门禁**:让「该入表的都入了表」也变成机械可判,属于新增 CI 规则,按项目规则要先呈方案等用户确认,不能拿我自己上一条发言当裁决开工。判据:文档门禁 7 条绿,ruff / mypy 绿,受影响测试绿。 |
| P28 | 网关 owns 探测知识库,却不 owns「什么可以离开这台机器」 | ✅ 随本行同 PR 合入 | **P27 盘出的五项里,这条错位最值得先修**:探测知识库本体早在网关(`registry/catalog.py` ✅),但决定「一条证据能不能发出去、发出去还剩什么」的那条**隐私红线**一直在 studio。红线本身是纯规则——白名单构造上传体(`extra="forbid"`,让秘密**结构上进不了线**,而不是靠记得去删)、只有 `probe` 且 `probe-verified` 的证据可分享、私有 / 内网 / 带 userinfo 的主机整段丢弃端点身份(**连哈希都不发**,因为可猜名字的哈希不算匿名)。它守护的东西在网关,它自己却不在,这是错位不是分工。**搬之前核了两处疑似重复,结论一正一反**。反的那处:模块自带`normalize_base_url`,而网关已有 `registry/base_url.py:canonicalize_base_url`——核下来**不是重复**,是两件事。后者答「我实际该调哪个 URL」,协议相关(给 DeepSeek 的 anthropic 兼容端点补 `/anthropic`、给 Ark 补 `/api/v3`);前者答「两个人发布同一个端点时该发同一个什么串」,所以小写主机、去默认端口、丢 query。**但两个名字放进同一个包会互相冒充**,喂错一个不会报错,只会得到一个看起来对的错答案——所以搬的时候正名为 `published_base_url`,并在模块开头把这对反差写死。正的那处:红线是纯规则归网关,而 HTTP 上传与同步调度(`community_catalog_upload.py` / `community_catalog_sync.py`)是**介质**,按 §2 第 4 条留在 studio——这层原本就分好了,不是这次拆的,所以只搬纯核心。**测试跟着代码走**:两份纯测试(redaction 138 行 + wire 往返 106 行,共 32 条)整体迁进网关,IO 层测试留 studio。判据:网关 563 条(+33)、后端 1685 条(-32,正好是迁走的那些)、引擎 1581 条全绿,ruff / mypy --strict / pip-audit 全绿。**棘轮第四次兑现**,而且这次抓的是我自己:删掉 studio 文件后门禁逼我改 §7,我在新写的说明里图省事写了两个裸文件名,它又红了一次。 |
| P29 | 网关自己的设计源在指着一堆已经不存在的文件 | ✅ 随本行同 PR 合入 | **目标里「同步相关的权威文档、使用手册」这半句,前几轮一直没碰**:我在改包 README 和台账,却从没核过网关自己的 MVP1 设计源。核下来是坏的。每个设计单元的 frontmatter 有一行 `binds_code:`,列出这个单元负责的源码坐标——**那是现在时的机器可读断言**,不是散文。机械扫一遍:**8 个单元、14 个坐标指向不存在的文件**,其中只有 3 个是我这几轮造成的(`llm_model_identity` / `llm_model_groups` / `llm_route_capabilities`,#771 / #772 下沉走了),其余 `call/dispatch.py`、`call/models.py`(#718 当死代码删)、`copilot_test.py`(#267 退役)、`llm_import_drafts.py`(内核并入 `registry/catalog.py`)**已经错了好几个月**——因为没有任何东西会读一份文档。**修法分两半,分界是「现在时断言 vs 历史叙述」**:`baseline.md` 按其自身定义只描述「当前源码事实」(`docs/graph-agent-gateway/mvp1/README.md:18` 原话「`baseline.md`(现状)+ `mvp1-alignment.md`(目标)」),点名已删文件是**事实错误**,必须改;而 `mvp1-alignment.md` 的正文写「目标 + 它 migrate away 的那个 baseline」,提到已删文件是**历史在做它该做的事**,不能一刀切抹掉。所以:14 条 `binds_code` 全部修正;05 单元(我自己弄坏的那个)正文整体重写——覆盖表三行合并成两行指向 `registry/model_naming.py` 与 `registry/capabilities.py`、§5 八条事实按当前代码重述、归属标注从「现散 ③a 待下沉」改成「已下沉」、代码索引**改引符号不再钉行号**(行号本身就是下一次腐烂的来源);alignment 只动**状态措辞**、推理与被否原文一字不改,并在顶部加一句「怎么读下文里的旧措辞」。**新门禁只钉 binds_code**,并在 docstring 里写明它**不**覆盖什么:另外 6 份 baseline 的**正文**仍在描述 #718 / #267 删掉的代码,那是真实漂移,记在这里而不是粉饰过去;不纳入门禁是因为正文要人去忠实重写,而 `binds_code` 是机器能追责的路径清单。门禁**双向验证过**:绿的时候绿,故意塞一个不存在的路径当场红,还原后又绿。**顺带暴露第三类漂移**:多处行内引用写 `registry/lint.py` / `registry/profile_selector.py` / `registry/resolver.py`,而这三个早已在 `resolve/` 域——05 单元内的已修,全仓范围未扫。 |
| P30 | 手册从没提过这一年往网关里加的公共能力,而且它自己立的规矩正被它自己违反 | ✅ 随本行同 PR 合入 | **目标里「使用手册」这三个字,到 P29 为止一次都没兑现**。`docs/graph-agent-gateway/USAGE.md` 是这个包的使用手册,它开头就立了规矩:「本文所有代码路径与函数签名均取自当前实现;与实现不符即为本文缺陷,以实现为准并修本文」。而 #771 / #772 往网关加的两批公共 API(能力合并、模型命名与分组)手册**一个字没提**——宿主读完手册根本不知道这些能力存在。本轮补两节:**3.8「一堆路由 → 一份人能挑的清单」**,并用一张表钉死那个最容易踩的坑——「同一个模型」有**两个**答案,`registry/identity.py` 的 `canonical_id` 是执行身份(带日期快照与不带日期是**两个**,因为它必须与 route_id 后缀逐字节一致),`project_model_group_identity().key` 是人可见分组(是**一个**,刻意把快照折掉),互相顶替会得到一个看起来对的错答案;**3.9「实测压过声称」**,并写明`verified_profile_capabilities` 两条不肯让步的判据(只有 `ready` 算数、「会不会思考」看声明的 capability 不看名字)。顺手修掉标题「五条常做的路径」——实际有九条。**手册的规矩被当真执行了:我把写进去的每个例子在真实现上跑了一遍**,`display_name` / `section_label` / `group.key` / `release_tokens` / `thinking_protocol.source` 逐值对上,「名字里带 thinking 但声明不是」返回 False、「探过没通过的候选」返回空字典也都实测确认。**跑的过程里跑出一个真缺陷**:`unknown_tokens` 名不副实——`claude-opus-4-1-20250805` 返回 `("opus",)`,而 `opus` 正是推出 family=Claude 的那个词;`_recognized_tokens` 只把 owner 与 family 的**名字**算作认识的,没把**促成这次归类的证据 token** 算进去,于是 opus / sonnet / haiku 这类家族成员词一律落进「不认识」。**没有藏起来也没有顺手改**:手册里如实写明它今天名不副实、给出替代判据(`confidence` 三档,已实测 high/medium/low 各一例),缺陷本身留作独立 PR 修——一个任务一个 PR,文档 PR 不夹带行为改动。 |
| P31 | 一个纯 SDK 的公开入口写着 `Any`,而 `Any` 后面藏着死代码和一处反向依赖 | ✅ 随本行同 PR 合入 | **沿着「接口清晰」往下挖,挖到 `role` 域的公开契约**:`MaterializeRoleRequest` 声明的是 `role: Any` / `credentials: Any` / `health_store: Any | None`,而模块里用一个 「是 dict 就 `.get`、否则 `getattr`」的 `_value()` 摸字段——**487 行里 53 次**,`Any` 出现 31 处。后果不是风格问题:**`mypy --strict` 在这个模块上报 Success,但它什么也没检查**,因为整条路径都是 `Any`。项目把「engine/gateway 走 --strict」当地基,而地基在这里是空的。把它当放大镜扫一遍,`Any` 后面藏着两样真东西。**其一,一段永远跑不到的代码**:`_credential_available` 依次试 `credential_ref` → `secret_handle` → `api_key`,而 **全仓没有任何 endpoint 类型有 `secret_handle` 字段**——它属于 `registry/credential_resolver.py` 的响应对象。在鸭子类型下,「字段不存在」和「字段没设值」读起来完全一样,所以这个分支能安静地待着。**其二,一处反向依赖**:`_route_credential_evidence_refs` 读 `route.evidence`,而网关自己的 `ProviderRoute` **没有这个字段**——它声明在 Studio 的子类上,且 studio 转网关类型时明确 strip 掉。更直白的是那段代码自己的注释:「Keeps the role-materialization read path **in lockstep with the Studio adapter's UI projection**」——一个纯 SDK 的实现里,注释直接写着「为了跟 Studio 适配器的 UI 投影保持同步」。AGENTS.md 对这个方向的措辞是 **forbidden**:studio 专属关注点不许漏进 SDK。**修法按第一性原理,不是把读取包起来**:`EvidenceRecord` 本来就定义在 `registry/schema.py`、探测知识库本来就是本包的,所以**证据本来就该长在网关自己的 route 上**——给网关 `ProviderRoute` 加 `evidence` 字段,studio 子类删掉重复声明,`_gateway_route()` 不再 strip。死分支直接删。**判据**:网关 536 条 + 后端 1717 条 + 引擎全绿,`mypy --strict` 与 `mypy` 双绿。后端 1717 条一条不改地全过,正是「行为没变、只是字段换了个正确的家」的证据。**下一步(独立 PR)**:这两处清掉之后,`MaterializeRoleRequest` 第一次**有可能**被写成真实类型——之前写不了,因为契约依赖一个自己类型上没有的字段。届时删掉 `_value()` 那 53 处,让 `--strict` 在这个边界上重新有意义。 |
| P32 | 网关把「模型挂在谁身上」当成了「模型是谁做的」,而这件事在三层里各写了一遍 | ✅ 随本行同 PR 合入 | **接着 P30 手册里那条已知缺陷往下挖,挖出的不是一个字段坏了,是一个事实被重复实现了。** `project_model_identity` 推断厂商时,把 `endpoint_id`、宿主给端点起的名字、`provider_model_id`、`canonical_id` 拼成一个字符串,谁的品牌词先出现在 if 链里就算谁的。**拿 2026-08-13 的真实注册表(1352 条路由/18 个端点)量**:402 条(29.7%)的厂商是从端点名猜出来的,其中 **320 条自己带着 `vendor/` 前缀,而代码在 tokenize 之前就把前缀丢了**。后果是一个域名里带 `anthropic` 的代理,让它服务的 `xiaomi/mimo-v2.5`、`meituan/longcat-flash-lite`、`amazon/nova-lite-v1`、甚至 `openai/gpt-chat-latest` 统统落进 **anthropic 分区**——而 `section_label` 是**真在 UI 上用的**(`routers/llm.py` → `AvailableModelsSidebar.tsx`),不是死字段。`confidence` 同期是 **1352 条全 `high`**:它读的是 `owner is not None`,而端点兜底几乎总能给出一个 owner,所以这个字段一年来没有承载过任何信息。`unknown_tokens` 是同一个病的第三处:**1118/1352 条(83%)报了「不认识的词」,而排头的 `doubao`(209)、`qwen3`(145)、`opus`(26) 恰恰是推出 owner/family 的那几个词**——`_recognized_tokens` 只把 owner 与 family 的**名字**算作认识,没问那些真正做分类的表。**一条根因**:同一件事(这个 token 是什么)在推断表、分组切分器、unknown 过滤器里各判一次,彼此不通气;版本号更是有三个互相打架的正则,unknown 过滤器挑了最严的那个,连 `claude-fable-5` 的 `5` 都不认。**改法**:(1) 模型自己声明的厂商压过它挂在谁身上,**认不出的厂商也算声明**——`xiaomi/` 的答案是「某个我不认识的厂商」而不是「代理域名里那个词」;(2) `confidence` 报的是**答案的来源**(id 自报 = high / 从端点猜 = medium / 认不出 = low);(3) `unknown_tokens` 改为**复用**分组切分器已经做过的分类,不再自己重猜;(4) 三个版本号正则收敛成一个 `_is_version_number_token`,边界取 1-2 位数字——模块里其它版本规则本来就只认这个宽度。**证据**:14 条新测试先红后绿;网关 550 + studio backend 1717 全绿;拿真实注册表逐条对账 **display_name 改变 0 条、group key 改变 0 条**(证明收紧版本正则没动分组),`section_label` 改 301 条全是「错厂商 → 模型自己声明的厂商」,`confidence` 改 326 条(246 high→low、80 high→medium),报不认识词的路由 1118 → 923、剩下的 `instruct`/`a3b`/`32k`/`4o` 是真词表缺口。另外机械核验了这 301 条**全部传得到 UI**(299 条 studio 自己那条链返回空、让位给网关,2 条一致,0 条被覆盖)。**留下的账(未做,需先呈方案)**:同一张厂商 if 链在仓里还有三份——网关 `registry/model_naming.py:_infer_owner_from_text`(权威)、studio `app/routers/llm.py:_section_label_from_display_name`(且**优先于**网关答案)、前端 `AvailableModelsSidebar.tsx:fallbackModelGroupSection`。按「三次成律」现在正是收敛的时候,但它跨三层、要把 `section_label` 从可选改成必填,属于结构性变更,先呈方案再动。→ **2026-08-13 用户裁决通过,已在 P35 收敛**(决议:`docs/design/2026-08-13-gateway-role-model-and-section-truth-decision.md` 决策二)。 |
| P33 | 网关公开入口上的 `Any`,让它自己定义的类型也被当成「不知道是什么」来读 | ✅ 随本行同 PR 合入 | **P31 说过下一步是给 `MaterializeRoleRequest` 上真类型,这是其中做得成的那一半。** `role/materialization.py` 用一个「是 dict 就 `.get`、否则 `getattr`、都不是就 `None`」的 `_value()` 摸所有输入,共 51 处、落在 10 种对象上。按对象分类之后发现一件事:**其中一大半读的根本就是网关自己定义的类型**——`ProviderRoute`、`ProviderEndpoint`、`EvidenceRecord`、`CapabilityValue`,它们之所以退化成 `Any`,只是因为入口把装着它们的 `credentials` 声明成了 `Any`。**改法**:网关从宿主的注册表只需要两件事——按 id 找 route、按 id 找 endpoint,于是把它写成 `RouteRegistry` Protocol(放 `registry/contracts.py`,与既有的 `CredentialProviderProtocol` 并列;注意 `schema.py` 里 `Protocol` 这个名字已被传输协议占用,放那儿会撞名)。Protocol 而非基类,因为宿主的注册表是它自己的文件:Studio 那份是带 schema_version 与 catalog 同步标记的凭据文件,网关这份是 `RegistrySnapshot`,谁都不该为了被查一次路由而变成对方。**一并删掉的死代码**:`_credential_available` 里 `hasattr(api_key, "get_secret_value")` 的鸭子分支(`api_key` 就是 `SecretStr | None`)、`_metadata_reason_code` 里 `isinstance(metadata, dict)` 的重复防御(两个模型上 `metadata` 都是 `dict`)、`_mapping_get`、以及本模块那份 `_capability_value`(`route_effective_capabilities` 早就返回 `dict[str, CapabilityValue]`,取值直接 `.value` 即可——顺带把「`_capability_value` 有三份实现」这笔账减去一份)。**证据**:7 条新测试先红后绿;`_value(` 由 51 处降到 25 处;网关 575 + studio backend 1685 全绿,`mypy --strict` 与 `mypy` 双通。顺带逼出一处假证据:`test_role_materialization_route_evidence.py` 一直用 `SimpleNamespace` 假冒 route/endpoint,类型收紧后立刻炸在 `rate_limit_bucket` 上——它测的从来不是真的 route,已改成真类型。**剩下的 25 处 `_value(` 不是遗漏,是被一个必须先裁决的问题挡住**:见 P34。 |
| P34 | 网关的 role materialization 读的 role,是 Studio 的 role,不是网关自己的 | ✅ 随本行同 PR 合入(决议:docs/design/2026-08-13-gateway-role-model-and-section-truth-decision.md) | **用户 2026-08-13 裁决通过(「找成熟做法,第一性原理,从底层修复,模块化思维,高内聚低耦合,把单独的功能拆开可复用」),按方案落盘规矩先立决议文档再动工。** 问题:`materialize_role` 读 `role.model_groups` / `role.intent` / `role.model_fallback_enabled`,而网关自己的 `RoleEntry` 一个都没有——三个字段连同 `RoleIntent` / `RoleProviderModel` / `RoleModelGroup` 全在 Studio 的 `llm_config.py`;把网关自己的 `RoleEntry` 喂给网关自己的入口得到空链,网关测试为此手搓假类。**改法(照 #772/#778 的 route/endpoint 先例)**:三个模型 + 三个字段下沉进网关 `registry/schema.py`(`DEFAULT_ROLE_TEMPERATURE` 随字段默认值一起搬,`AUTHORED_TEMPERATURE_MAX` 从 `bounds.py` 挪到 `schema.py` 供钳制校验用——bounds 本来就 import schema,方向不变);Studio 子类只留 `role_kind` 与 `materialization_report`,业务层继续从 `llm_config` 门面拿(**`X as X` 显式再导出**——直接 import SDK 被 `test_productization_import_boundary_red` 棘轮拦下,白名单门面本来就是仓里的法);健康存储走 Port/Adapter:网关 `registry/contracts.py` 新增 `ActiveCircuit` + `HealthStore` Protocol,Studio 的 `SqliteLlmHealthStore`/`RuntimeCircuit` 结构性满足零改动。**随之死掉的**:`_value()` 助手全删(materialization 里 0 处鸭子读取),`_migrate_legacy_provider_preference` 垫片(实测真机 roles 文件旧值 0 次,守着空数据;两条测它的测试改为断言 fail-fast:PUT 422、schema 校验报错),旧测试的 `_Role`/`_RoleIntent`/`_Group`/`_ProviderModel` 假类与 `SimpleNamespace` 夹具全部换真模型。**顺带逼出的真毛病**:Studio `ModelBundle.lint_requirements` 私抄了一份比网关窄的 Literal(改用网关 `LintSeverity`,已导出);`routers/llm.py`/`llm_role_materializer.py` 四处 `cast(RoleEntry/ModelBundle, ...)` 在给 `Any` 打补丁(适配器返回值上真类型后全删,适配器入口改 fail-fast TypeError)。**证据**:新测试文件 6+7 条先红后绿;gateway 595 + studio 1685 + engine 1581 全绿;`mypy --strict`(gateway)/`mypy`(studio)/ruff/pip-audit 全绿。 |
| P35 | 「这个模型归谁」在仓里有四处作答,而且组级归属让猜测能赢声明 | ✅ 随本行同 PR 合入(决议:docs/design/2026-08-13-gateway-role-model-and-section-truth-decision.md 决策二) | **P32 尾注留的账,用户 2026-08-13 裁决通过后按决议二收敛。** 问题:厂商归属 if-链在仓里有四份——网关 `registry/model_naming.py:_infer_owner_from_text`(#779 起权威)、studio 后端 `routers/llm.py:_section_label_from_display_name`(且**压过**网关答案)、前端 `AvailableModelsSidebar.tsx:fallbackModelGroupSection`、外加决议没点名的第四份 `PropertiesPanel.tsx:modelGroupSectionLabel`(同一条链的完整拷贝,验收判据「前端不再含厂商 if-链」覆盖它);且组级归属用等权多数票(`_dominant_section_label`),「模型自己声明的厂商」和「从代理域名猜的」一票对一票——#779 点验的 6 条残留(MiniMax-M1 等)正是两票猜测赢一票声明。**改法(按决议二逐条)**:(1) 网关公开 `owner_source: Literal["model_id","declared_vendor","endpoint_context"]`(内部 `_OwnerReading.source` 本就有,公开到两个投影上,`confidence` 保持由它派生);(2) 新增 `elect_model_group_section(projections) -> str`(`registry/model_naming.py`):按来源分层选举——`model_id` > `declared_vendor` > `endpoint_context`,**只要有人声明,猜测就不投票**;同层多数、平票字典序保证确定性、空输入答 `unknown`;(3) studio 后端两条链整体删除,`_model_group_identity` 改走网关选举(经 adapter 门面 `X as X` 转发,棘轮白名单不破);(4) 前端两份拷贝整体删除,`section_label` 在 API 契约(后端恒非空 + 前端 `llm.ts` 类型)改为**必填**——可选性是给兜底链留的洞,兜底链死了洞一起补上;(5) 手册 USAGE.md §3.8 补 `owner_source` 表与选举函数、mvp1 05 单元 F4 同步。**测试**:网关 8 条新测试先红后绿(含「一条声明胜过多条猜测」的 MiniMax 实测形状复刻);studio 后端 API 级红测试(混合组旧代码答 anthropic、新代码答 minimax)+ seam 文件墓碑扫描(两个死名字不复存在、选举函数在场);前端行为测试(声明分区照抄不重猜)+ 类型收紧冲出 5 个借 `as ModelGroup` 断言绕过 tsc 的假 fixture(全部改为网关真实作答)。**证据**:gateway 603 + studio backend 1687 + engine 1581 全绿;`mypy --strict`(gateway 55 文件)/`mypy`(studio 130 文件)/ruff/前端 eslint+tsc+vitest 2071+build/pip-audit+npm audit(0 CVE)全绿。**拿 2026-08-13 真实注册表(395 组)双投影对账:恰好 1 组改判——`Minimax M1: anthropic → minimax`,零连带**;Trinity Large 两版皆 `arcee-ai`(全路由带声明,旧多数票本就正确);Healer / Hunter / Trinity 裸名组留在 `anthropic`(`endpoint_context`,决议判为诚实答案非缺陷)。**真机 UI 点验已完成(2026-08-13,#783 合并后:vendor 重建 + 预热 pyc + CDP 驱动真窗口)**:① Available Models 搜 minimax → 8 组全部落 MINIMAX 分区,Minimax M1 卡片同时挂 OpenRouter + Qiniu(anthropic 域名代理)两个 provider,不再被投进 anthropic;② 搜 healer → Healer / Healer Alpha 留在 ANTHROPIC 分区(裸名无声明,端点语境=诚实答案);③ 搜 trinity → 同屏分裂:裸名 Trinity(仅 Qiniu)在 ANTHROPIC,带 arcee-ai/ 声明的 Trinity Large / Mini 在 ARCEE-AI——声明压过猜测在真机一屏可见。三张截图留档会话 scratchpad(verify-783-minimax-search / -healer-anthropic / -trinity-split.png);点验后按纪律关闭 9222 调试口并已 curl 复核。 |

### 第二阶段 · 流式 tracing(S1-S5,已全部合并)

| # | 项 | 状态 | 关键坐标 |
|---|---|---|---|
| S1 | `LLMProvider` Port 改流式:`stream()` 成为唯一取答案的方式(`invoke` 删除),`LLMProviderChatModel._generate` 改为消费切片再折叠,Studio adapter 直通 gateway chat model 的流 | ✅ 已合并(#669) | `packages/graph-agent/src/graph_agent/core/llm_provider.py`;`apps/studio/backend/app/core/adapters/engine.py` 的 `_GatewayBackedLLMProvider.stream` |
| S1-a | S1 自评补票:测试里手搓的切片合并规则(后来的覆盖先来的)与真实 `AIMessageChunk`(相加)不符,换成真类型并补回程用例 | ✅ 已合并(#671) | `apps/studio/backend/tests/core/adapters/test_llm_provider_streaming.py`。核对结论:openai 兼容协议的 `stream_usage` 由 gateway `call/profiles.py:20-21` 显式钉住,不依赖 langchain 那个「仅 base_url 为空才自动打开」的默认(`langchain_openai/chat_models/base.py:1144-1163`),所以流式下 token 用量不丢 |
| S2 | 新建 `graph_agent/tracing/` 步骤单出口,收编 B8 的发射点;对外事件契约不变 | ✅ 已合并(#670) | `packages/graph-agent/src/graph_agent/tracing/steps.py`(`StepReporter` / `ToolCallStep`)。同 PR 订正了决议 B8 的事实描述,并把漏列的第四处手搓分发(`core/callback_bridge.py:216-236`,含一个违反 no-backward-compat 的 `except TypeError` 旧签名降级垫片)归给 S3 |
| SG | gateway 也得会流式:`GatewayChatModel` 补 `_stream`,候选/失败策略只保留**一份**实现(`_answer` 生成器),`_stream` 与 `_generate` 都从它取;重试作废已推出的正文用发布出去的`ANSWER_RESTARTED` 标记贯通 gateway → adapter → engine | ✅ 已合并(#674) | `packages/graph-agent-gateway/src/graph_agent_gateway/call/chat_model.py`;engine `LLMProviderChunk.restarts_answer`;adapter `answer_restarts_here(chunk)`。实测:同一次真实调用从 **1 片 / 167 字符 / 全部堆在 4.51s** 变成 **104 片 / 12.12s→12.71s 逐片到达**。语义要点:截断只有在回答结束时才知道,所以重试必然发生在正文已经流出去之后 —— 把两次尝试拼起来得到的是**错的答案**,不只是错的显示 |
| SO | gateway 的动作进 trace(用户 2026-08-09 追加):跳过熔断路由、探活失败、原路重试、抬预算重试、跨路由回退、终止性失败、答成、路由耗尽 —— 八种结局是**同一件事**,收敛成一个`llm_route_decision` 事件加一个封闭 `decision` 枚举,**取代并删除** `llm_fallback`;严重度从事件类型移到 decision(同一类型既报「答成」也报「无路可走」) | ✅ 已合并(#680) | gateway `events.py` / `tracing.py`;engine `callbacks/events.py`;前端 `utils/trace.ts` 的 `routeDecisionDetails` / `eventSeverity`、`TraceStepRow.tsx` 的 `RouteDecisionBlock`。验收:`llm_fallback` 在 gateway src / engine src / 前端 src 三处 grep 为零。`voided_streamed_answer` 挂在造成它的那个决策上 —— 面板正显示着这次决策刚作废的文字,只有它能说出这件事 |
| S3 | 增量帧契约 + 结束帧收敛:LLM 步骤拿到**身份**(引擎铸的 `step_id`,开始帧 / 结束帧 / 每一片增量都带它);新增 `llm_delta` 事件(一个事件 + `channel` 封闭枚举 `text`/`thinking`,外加 `restarts_step`);`llm_call.response_data` 改必填、删 `llm_call.messages` 及前端回退分支;「这类帧进不进永久记录」写成类级 `persisted`,两个 jsonl sink 与队列订阅者都问帧自己 | ✅ 已合并(#684) | engine `callbacks/events.py` / `tracing/steps.py` / `core/llm_provider.py`;`LLMProviderChunk.reasoning` 让推理与正文分道(推理不是回答,折进 content 等于把模型没说过的话交给 agent loop);studio adapter 从 `additional_kwargs.reasoning_content` 取。**为什么 `response_data` 必须必填**:增量帧之所以可丢,正因为它们拼出的那段话另有一份完整落盘;可选字段会让「答案存在」失去依托,显式可丢的流反而成了唯一副本 |
| S4 | 传输分道与背压:增量帧走独立跨进程消息类型 + 独立 WebSocket(`/ws/skills/{skill}/runs/{run}/deltas`),不占 seq、不落盘、不进重放缓冲;`DeltaEnvelope` 没有 `seq` 也没有 `cursor` | ✅ 已合并(#686) | `apps/studio/backend/app/services/run_manager.py` 的 `_DeltaStream`;engine `core/event_contracts.py` 的 `DeltaEnvelope`。**背压不需要计时器**:增量在「还没发出去的队尾」上合并,所以决议要的「时间窗」就是积压本身 —— 跟得上的观众逐片看到原样,只有落后的观众才被合并;超过上限先丢最旧的(实时视图要的是最新,不是最陈旧的)。跨步骤 / 跨 channel / 遇 restart 一律不合并 |
| S5 | 前端在步骤条目内逐字追加:`useRunDeltas` 订阅增量 socket 按 `step_id` 归集,`TraceStepRow` 在**进行中**的那一格里渲染,步骤收口即撤下 | ✅ 已合并(#687) | `apps/studio/frontend/src/hooks/useRunDeltas.ts`;`TraceStepRow` 的 `LiveOutput`。同 PR 把 LLM 两半的配对从「按 phase」改成「按 `step_id`」—— 按 phase 只在「一个 phase 里两次调用从不重叠」时才成立,agent 轮次一次就发好几个,先回来的答案会关掉碰巧还开着的那个 prompt |
| S3-补 | 判据 7 复测不通过后的补齐:`prompt_captured` 的 `template_source` / `variables` **全仓没有生产者**(只有测试构造过),导致 Trace 四段里「模板 / 变量」两段恒空。agent 路径的 system prompt 本就由认知模板产出,把「文本 + 模板标识 + 入参」三件绑成一个值随 phase chat model 下传,由报出调用的那一层写进开始帧 | ✅ 已合并(#689) | engine `core/graph_assembler.py` 的 `_AgentSystemPrompt`、`cognitive/prompt.py` 的 `V030_COGNITIVE_TEMPLATE_ID`、`tracing/steps.py`。**同一个 dict 既填模板也被上报** —— 另写一份「给人看的入参」必然与真正填进去的那份漂移;报告与事实同源才不需要靠纪律维持一致。**真机复测**(运行 `2026-08-09T19-12-24_47d32db5`):Trace 展开步骤后 TEMPLATE=`cognitive/v0.3.0`、VARIABLES=11 键完整入参、RENDERED / RESPONSE 均非空,判据 7 由红转绿 |

**过哪道门算完**:决议 §3 的 13 条验收判据。
**已知需同步的操作**:S1/S2/SG/SO/S3 都改 `packages/graph-agent`(engine)或
`packages/graph-agent-gateway`(gateway)源码,合并后必须按 AGENTS.md「Workflow Pipeline」
第 7 条重建 vendor 快照,否则运行中的桌面 app 仍跑旧 SDK。

### 用户反馈批次 2026-08-12:Settings「CLI」区修订

| # | 项 | 状态 | 处置 |
|---|---|---|---|
| F1 | CLI 区七条实测反馈一次修:依赖行写产品全称(Claude Code)/ codex effort 档位修正为 light-medium-high-xhigh-ultra / provider 与 MoirAI worker 的模型+effort 全部改下拉(worker 新增 effort 覆盖,经 `[agents.X].env` 的 `CLAUDE_CODE_EFFORT_LEVEL` 注入)/ 探测捎带最新版检查(npm×2+GitHub,查不到不标过时)/ 行内「更新」「登录」按钮拉真控制台(`claude update`·`codex update`·`claude auth login`·`codex login`)。另答复反馈第 1 条(修正:最初「从未装过」的说法错了):ah 曾装在 `~/.cargo/bin` 且真机跑通过,8-11/12 的实验性环境重建删掉了 `~/.cargo` 才成了「未安装」;已重装 1.14.3,claude token 过期按「一环境一凭据链」拆掉 7 月的旧软链后由用户重新 OAuth | ✅ 已合并(#747 主体 + #754 登录按钮拆链补缺);主 app 真机 CDP 逐项点验完成(七条全过,截图存档) | 决议 `docs/design/2026-08-12-cli-settings-revision.md`(已批)+ 设计源 `00_settings-ux-spec.md` §3.9 同步修订。已过门:前端四门禁 + cargo test 绿 + 探测脚本真机 bash 实跑(顺手修正探测入口为 `bash -lc`——非登录 shell 无代理镜像,最新版查询在代理机上静默失败)。遗留实测点:worker `ANTHROPIC_MODEL` 吃不吃别名、`CLAUDE_CODE_EFFORT_LEVEL` 生效语义(需 ah 会话 + 用户登录后验);原「随点验截图补手册 CLI 分区切片」一项因 N6 手册整体下线(P4-L7)作废。**点验后追查出并已修的缺口**:「登录」按钮直跑 `claude auth login`,没做 login-doorman 的旧桥拆链(ah #18 一环境一凭据链)——在残留 Windows 凭据软链的机器上会把登录写穿到 Windows 侧;修复=拆链片段提成唯一常量 `CLAUDE_LEGACY_CRED_LINK_CLEANUP_SH`,doorman 与按钮两处共用(TDD 红→绿,cargo 215 绿) |
| F2 | ah 随 app 打包 + 运行期自动布署(答复反馈第 1 条的产品层问题「首次使用者到底要装多少外部应用?ah 不能作为依赖包进来吗?」):钉版 ah 二进制(ah+ahd)经 `ah-vendor.lock.json` + `scripts/ensure_ah_vendor.js` vendor 进 `vendor/ah/`(sha256 校验,dev 链失败仅警告、build 链 `--strict` 失败即停),随 `bundle.resources` 进打包产物;Open in CLI 的 Rust 前置检查失败先无声布署再复查(只缓存成功——旧 OnceLock 连失败一起永久缓存,装好 ah 还得重启 app 的缺陷一并修掉),WSL 启动脚本内嵌同一布署片段兜「运行中环境被抹」的窗口(只升不降,不覆盖更新的开发版);Settings CLI 区 ah 行 missing/outdated 给「部署」钮。两个 WSL 模板的存在性检查+门禁收敛进共用 `wsl_ah_bootstrap_block`,版本数值折算 shell 片段与门禁共用一份定义 | ✅ 已合并(#757)+ 真机点验完成(2026-08-13) | 决议 `docs/design/2026-08-12-ah-vendored-auto-deploy.md`(已批「做」)+ §3.9 修订。已过门:node --test(新增 10 例,含钉版≥AH_VERSION_MIN 跨文件一致性检查)+ cargo test 219 绿(门禁与布署片段均喂真 sh 按判断结果断言)+ 真实 tarball 下载解压幂等实测 + 前端四门禁 + CI 七检查全绿。**真机验收(决议 §4,CDP 驱动真窗口,截图存档)**:移走 WSL 的 `~/.cargo/bin/{ah,ahd}` → 重新检测 ah 行 missing+「部署」钮 → 点击(无控制台、不重启 app)→ 行变 ok 1.14.3;WSL 侧因果证据:`~/.local/bin/{ah,ahd}` 字节数与 vendor 快照一致、可执行、双双报 1.14.3。只升不降与无 vendor 降级两项由喂真 sh 的单测覆盖(1.14.4/2.0.0 不触发拷贝;空片段=历史行为逐字保留)。决议 §4.2 的完整 Open in CLI 端到端(含拉起会话)仍待 claude OAuth(用户侧)后顺带走一遍——布署机制本身与会话前置检查走的是同一条 `try_deploy_vendored_ah`,已被上述点验覆盖 |
| F3 | 登录界面剪贴板快捷键(用户 2026-08-12:「所有登录界面加 c/v 快捷键,c 直接 copy 网页,v 把剪贴板内容复制上去」):登录命令统一经 pty 包装器——https 登录链接一出现即自动复制(零按键,比 c 更进一步),`c` 重复制、`v` 注入剪贴板(粘 OAuth code);孤立按键才算命令,粘贴突发/正常输入原样透传。实测本机进程互通关闭 + Windows Terminal OSC 52 无效 → 剪贴板由 Studio 侧看护线程经 /mnt 交换文件桥接(copy/paste 双向 seq+ack 握手、alive 心跳、done 收尾,生命周期文件驱动免穿 spawn 管线);覆盖设置页「登录」控制台 + 两个 login-doorman。桥/python3 缺失逐级降级为现行为。附带答复:codex 的登录/更新按钮 #747 已有,按"有事才现身"设计(过时→更新,缺失/损坏→登录) | 进行中(feat/login-console-clipboard-keys) | 决议 `docs/design/2026-08-12-login-console-clipboard-keys.md` + §3.9 修订。已过门:cargo test 223 绿(桥协议双向握手/无心跳退场/脚本接线/降级);包装器在 WSL 真 python3 + 真文件握手实测通过(URL 自动复制、v 注入、退出码透传)。已合并 #761;真机点验:URL 自动复制全链实证(clip-io.txt+Get-Clipboard=授权链接),且用户借它完成了 claude OAuth(凭据 20:13 落盘)。**首用暴露 v 失效**:Ink TUI 的终端应答(光标位置报告)流经 stdin 污染「输入行为空」判定,v 永久哑——桥目录无 paste.req 坐实未进命令分支;用户裁决 keep it simple → 判定简化为孤立按键即命令、砍确认横幅仪式(fix/login-console-lone-key,含 ESC 噪声回归钉子,红转绿+WSL 全链驱动复验) |
| F4 | 登录行常驻「登录」+ 显示当前账号(用户 2026-08-12:「登录按钮不要消失,万一用户要换账号呢?」「能否读取登录的账号,显式在 UI 上」):cliRowAction 登录行任何状态都给「登录」;探测脚本第 5 列带账号身份(claude=oauthAccount.emailAddress,codex=id_token JWT email,只读身份字段、token 不出脚本,有测试钉死),行内 muted 文本显示 | ✅ 已合并(#765 主体 + #769 补缺)+ 真机点验完成(2026-08-13) | §3.9 同步修订。#765 曾零 CI:与 #764 在 main 上冲突(mergeStateStatus=DIRTY)——GitHub 对生成不了 merge commit 的 PR 不触发 pull_request 工作流,rebase 后 CI 即跑(教训入册)。首轮点验暴露账号列恒空 → #769 修:探测 grep 模式写死 `":\""` 而真实 `.claude.json` 是 pretty-print(冒号后带空格),三处提取全改 `: *"` + 喂真 sh 的行为测试(fixture 按真实落盘形状,红转绿,cargo 225 绿)。合并后真机点验(CDP 截图 cli-auth-rows-with-accounts.png):两条登录行均 就绪+traveluxi.com@gmail.com+常驻「登录」钮 ✅ |
| F5 | codex 登录改设备码流(用户 2026-08-12 事故报告:「我点 codex 的 sign-in 为什么会影响我桌面端的 codex?」+ 裁决「查 ah/Zeroth 已成功的登录方法,不要自己探索」):裸 `codex login` 在 localhost:1455 等浏览器回调,桌面 Codex app(商店包 OpenAI.Codex)的内核 codex.exe 常驻监听同端口,回调被桌面 app 截胡——WSL 侧死等且开场清掉 WSL 旧 auth.json;interop 关闭故反向不可能。改用 ah/Zeroth 既证配方 `codex login --device-auth`(设备码流:浏览器开固定页+输一次性代码,无本地回调),设置页「登录」+ doorman 两处同改;claude 的授权码粘贴流本无回调,不动 | ✅ 已合并(#770)+ 真机复验(2026-08-13) | 决议 `2026-08-12-login-console-clipboard-keys.md` §6 + §3.9 同步落盘。已过门:三处测试钉住 `--device-auth`(闭表命令 + doorman 接线,红转绿,cargo 224 绿);flag 在 0.147.0 实测接受(help 隐藏),WSL 实跑输出与 Zeroth 采集 markers 逐字吻合。真机:用户已重登 codex(探测行 就绪+邮箱,见 F4 截图);「登录」钮实点验证略过——codex 已在登录态,`codex login` 开场即清 auth.json,实点会打掉用户刚建好的凭据链,接线正确性由钉死测试与 WSL 实跑双证据覆盖 |

### 用户功能批次 2026-08-13:媒体生成设置页(media generation)

| # | 项 | 状态 | 处置 |
|---|---|---|---|
| M1 | 媒体生成模型独立设置页(用户裁决 2026-08-13:类别名 `media_generation`;不进 API Keys 页,原页改名「LLM API-Key」;新页含 RunningHub 凭据、零成本探活 accountStatus(余额展示)、10 条证据锚定模型目录(按模态分组,任务/渠道/价格徽章)、声明式参数 schema 驱动的能力胶囊与默认参数;L3 真实生成测试与调用运行时显式后置为 `media-invocation-runtime` 单元) | ✅ 已合并(#782)+ 真机点验完成(2026-08-13) | 决议 `docs/studio/mvp1/02_capabilities/media-generation/design-decision.md` + gateway 侧 `docs/graph-agent-gateway/mvp1/14-media-generation/design-decision.md`。gateway 新增 `media` 域(schema/catalog/probing,与 LLM registry 类型隔离,role→route 不可见);backend `/api/media/*` 5 端点;前端新 tab。已过门(本地):gateway 15 测试 + mypy --strict + ruff;backend 9 测试 + mypy + 全量 604 gateway 套件;前端四门禁 + worktree Playwright smoke(截图)。已过门(全量):backend 1694 + engine 1581 + gateway 604 全绿(quality-gates 首跑中 test_publish 幂等重试用例因在册 re-run flaky 红一次,重跑即绿)+ pip-audit/npm audit 0 CVE + 新端点登记 STUDIO_REQUEST_AUDIT.md + media_gateway 进 SDK 边界白名单。真机点验(CDP 驱动真窗口,截图存档):tab 改名/新页打开/10 模型分组徽章价格/无效 key 真探活打到 RunningHub 生产返回 code=806 APIKEY_USER_NOT_FOUND 红态呈现/能力胶囊/默认参数下拉仅合法枚举、选 2k 后落盘 %APPDATA%/AgentStudio/media/media_generation.json 因果确认/生成测试按钮不存在(按 D2 后置) |

### 用户功能批次 2026-08-13:tracing 去黑箱(glass-box)

决议正本:`docs/design/2026-08-13-trace-goes-glass-box-decision.md`(#786,用户九条裁决 +
验收判据 1-15)。目标:跑一次 skill,读者能像读程序 print 一样看清每一步在做什么。
实施顺序 E1 → E3 → M1+M2 → M3 → M4 → E2。

| # | 项 | 状态 | 处置 |
|---|---|---|---|
| G-E1 | thinking 落盘:产生答案的推理随 run 保存(`llm_call.response_data.reasoning`) | ✅ 已合并(#787) | engine `core/llm_provider.py` 聚合流式 reasoning 进结束帧;此前 thinking 只活在增量帧里,run 一结束就没了(决议 B2)。vendor 已重建 |
| G-E3 | 机器自述事件:中间件/md2json/validator 发「决定」不发「路过」 | ✅ 已合并(#788 + #790) | 新事件 `FinishTaskVerdictEvent` / `NudgeEvent` / `ToolErrorHandledEvent`(exit 中间件裁决、nudge 注入、工具错误被拦都成为可见步骤);「每步做了什么要像 print 一样写清」的引擎侧供给(决议 D3)。vendor 已重建 |
| G-M1M2 | 折叠原语统一 + LLM 步骤按流程序拆子条目 + Iteration 分层 | ✅ 已合并(#789) | 前端 `components/trace/` 折叠文本原语(5 行/20 行档,编辑器看全文);LLM 步骤内 Template/Variables/Rendered/Thinking/Response 按真实流程顺序排;agent 回合按 Iteration 分组(决议 D1/D2/D4) |
| G-M3 | run 状态投影 SSOT(决议 D7 铁律:run 到终态 ⇒ 任何派生状态不得为 running) | ✅ 已合并(#793) | 新模块 `utils/run-status-projection.ts` 是事件流+run 记录折叠成一切派生状态的唯一出口;stop/pause 响应作为 canonical 快照被捕获;trace 步骤新增 `severed` 态(终态时未闭合步骤显示「never completed」不转圈);B8 两份旧推导(node-status.ts / runOutcomeFromEvents)整体删除;per-consumer 状态→显示对照表入设计源 F6 并有测试锁死 |
| G-E2 | finish_task 保留字成编译诊断:`[F-v3-agent-tool-reserved]`(第 98 码) | ✅ 已合并(#791) | 用户裁决「finish task 这个 tool 还能删掉?这不是搞笑吗」:内置框架工具始终可用,声明它 = 编译错误;`has_finish_task` 旗标删除(出口闸无条件);顺带修出 after_agent `jump_to: end` 无限重入的真缺陷(最小复现 21 次重入,改 return None);UI ToolsField 对诊断行标红;RESERVED_TOOL_NAMES 前端自造规则删除(diagnostics SSOT)。vendor 已重建 |
| G-FLAKE | cross-platform-smoke 抖动:Rust 并行测试竞争进程级环境变量 | ✅ 已合并(#792) | `sidecar.rs` 端口分配改纯函数 + 一行环境变量包装(副作用隔离);测试喂显式输入,不再 set/remove 全局 env |
| G-WELL | 长文本改判:固定高度文本井覆盖 5/20 三态(2026-08-14 用户裁决) | ✅ 随本行同 PR 合入 | 决议 `docs/design/2026-08-14-trace-longtext-text-well-decision.md`:`ui/text-well` 共享原语(copilot ThinkingBlock 与 trace 全部长文本同源消费,D8 通则);G-M1M2 的 folded-text 三态(5 行/20 行/Monaco modal)整体删除;「View full text」改走 `onFileOpen` 虚拟只读文档 = 正常编辑器 overlay;LLMFlow/ToolArguments/ToolCallSubtree/GenericPayload 外层卡片盒撤除(一段文本恰好一层容器);trace-observability 设计源 §4/F9 改判记录 + 哈希重钉 |
| G-M4 | D8 共享样式模块 + D6 选中即范围 + D5 边点=边的步骤 | ✅ 随本行同 PR 合入 | D8:`nodes/node-card.ts` + `nodes/StatusCapsule.tsx` + `edges/edge-style.ts`,SkillNode/GlobalInputOutputNode/buildEdges/ContextEdge 全部消费共享模块(通则入 FRONTEND_UI_SPEC §2.12);单击=选中/双击=面板(边界节点单击不再跳 I/O);D6:`utils/trace-scope.ts` 选中即 trace 范围,面板范围 chip + 一键清除,正面化解 2026-08-09 D2 的「不可见不可关」反对(两次裁决都记入设计源 F3);D5:EdgeContextView 删除,边操作以 trace 行呈现,EdgeTamperSection 只留 tamper 编辑器 + 未跑期静态推断(设计源 F4 改写);`operations` 死管线(EdgeOperation/collectEdgeOperations)连根删除;trace-observability 文档哈希重钉 |

**过哪道门算完**:决议 §3 验收判据 1-15 在真机逐项点验(CDP 驱动真窗口)+ 五列验证报告全绿。
G-M4 合并后为纯前端刷新(vendor 已在 E2 后重建),主仓 `git pull` 即生效。

**真机逐项点验完成(2026-08-14,#794 合并后,CDP 驱动真窗口,18 张截图留档会话 scratchpad)**:
E2 判据 14 由真实数据自然触发(exp-b-round7 review 相真声明了 `finish_task` → 编译诊断
`[F-v3-agent-tool-reserved]` + 节点红徽章 + Properties 工具行红字红三角,按诊断删行后转绿);
E1 判据 1 以盘上成品坐实(真实 run `trace.jsonl` 28/28 `llm_call.response_data.reasoning`
非空,样例 5543 字中文思考;判据 2 的 content=0 形状未自然出现,机制已由 28/28 证明);
E3 判据 3/4 坐实(verdict/nudge/tool_history_repaired/loop_detected 全为完整句子;纯透传
setup 相仅 input_dispatch/phase_start/phase_end 零机器自述;判据 5 的独立 validation 事件
未被自然触发,契约由 #788/#790 引擎测试锁定,finish 路径的校验自述在 verdict 消息中实测);
M1/M2 判据 7/8 三态三截图(收起 5 行 → 展开 20 行 → Monaco 全文)+ 子条目流程序
(PROMPT LOADED → RENDERED PROMPT → THINKING → ANSWER → TOOL CALLS → Runtime settings →
Route used)+ ITERATION 分层(20 轮);M3 判据 9 双向实测(真实 run 到 max_iterations 失败
→ 零 running 残留零动画;真实 run Pause→Stop 取消 → `prompt_captured` 挂 "never completed"
胶囊、结局行 "Run cancelled"、segment 节点按对照表显示 Paused);M4 判据 11/12/13 全态实测
(边点=边操作行+tamper 区、五种范围收窄+chip 一键清除、共享卡片基类 DOM 逐字节相同、
单击=选中/双击=面板)。**带出发现一条(范围外,已挂后台任务 chip)**:run-fail 阶段的
Run 按钮可点但 `handleRun` 静默返回(gate-state.ts:76 × Workspace.tsx handleRun 守卫),
违反 FRONTEND_UI_SPEC §2.7a。点验后 9222 调试口已关闭并 curl 复核。

**两条 ⚠️ 的收尾(2026-08-14 追加)**:判据 2(content=0 的调用思考仅凭 trace.jsonl 可读)已由
特制探针 skill `glassbox-probe` 真跑坐实——单次 llm_call content_len=0、reasoning 232 字、
tool_calls=1(finish_task),UI 展开为 THINKING → TOOL CALLS 无 ANSWER 段,报告行转 ✅。
判据 5 定性为**设计-实现漂移**:意图(校验自述可见)已由 `finish_task_verdict` 消息满足
(真实 run 实测 22 次"passed schema and business validation"/"N problem(s) found"),但判据
点名的 `ValidationPassEvent`/`ValidationFailEvent` 本体是死码——`llm_phase_node.py`
`_prepare_framework_state` 对每个 LLM 相无条件设 `validation_middleware_phase`,
`validation_phase_node.py:48` 因此永远短路(M7 收编的遗留),LOGIC 相则根本没有校验节点
(`graph_builder.py:87` code-only 分支)。按 no-backward-compat 建议删除死路径并把判据 5
改写到 verdict 通道;属引擎契约变更,待用户裁决后另开任务。

**G-WELL(#797)真机点验完成(2026-08-14,CDP 真窗口,`/studio-verify` skill 首用)**:
①长文本井:LLM 步骤展开体 18 个 `data-slot="text-well"`,`max-height:160px` + `overflow:auto`
(实测 scrollHeight 2590 > clientHeight 150,井内滚动),全屏零 `Expand (`、零旧
`folded-text`(截图 verify-well-llm-step.png);②View full text:点击后**编辑器 overlay**
按常态打开(标题 Rendered prompt + json 语言徽章 + Monaco),行为级只读实测——CDP
`Input.insertText` 注入文本未落地且出现 read-only 提示(verify-well-fulltext-editor.png +
typetest 输出);③无盒中盒:generic payload 井的父容器为裸 `mt-2`(无边框、透明底),
LLM 步骤体外层卡片盒已不存在(verify-well-generic-payload.png);④copilot 同源:hello 会话
13 个 Thought 块全部渲染为同一 `text-well`(160px 同款,verify-well-copilot-thought5.png);
⑤流式跟底(autoFollow)未真机跑流式(需真 run),由 `text-well.test.tsx` 单测锁定
(scrollTop 钉底断言)——唯一非真机项,如实标注。#798 同日合并:验证方法论固化为
`.claude/skills/studio-verify`(web 模式验证被裁决禁止),CLAUDE.md 挂载。

**引擎死家族处置(2026-08-15 已裁:先迁移、后整族删;迁移四步 + 整族删共五个 PR 全部合并)**:2026-08-14 定界
调查坐实整个 legacy 执行族(`graph_builder.py`/`phase_executor.py`/`core/phase_nodes/*`
等 14 模块闭包 + 附着孤儿)对活路径零 import;用户 2026-08-15 裁决「先把第三类功能
迁移到现在的活路径上才能整族删」,并确认迁移框架(多数入中间件)。七项冻结功能的
出生档案(引入 commit + 设计动机 + 用户原始决策原文)已考古完成。权威决议文档:
`docs/design/2026-08-15-legacy-cognitive-features-migration-decision.md`(#801)。
实施切分:PR B 认知工具回归活路径(ask_clarification/update_working_memory/
log_ambiguity 挂载 + context_access opt-in,CognitiveFlow 拦截)→ PR C 双闸 nudge
中间件适配(依赖 B)→ PR D compaction 中间件(SummarizationMiddleware+sidecar+
CompactionEvent)→ PR E 整族删(含 ModelResolvedEvent「已被替代不迁移」判定、
ctx 桥 `legacy_context_from_state` 族、零发射点事件类、前端死分支、spec 清单条目)
+ vendor 重建 + `/studio-verify` 真机冒烟。每步过引擎门禁(mypy --strict + 3×pytest)。

**进度(2026-08-15)**:PR A 决议文档 #801 · PR B 认知工具回归 #803 · PR D compaction 中间件 #805 ·
PR C 双闸 nudge #807 · PR E 整族删 #810 —— **五个 PR 全部合并,代码面收官**。
门禁(每条都在对应 worktree 内跑、输出带树路径):ruff 净 · mypy --strict 引擎 112 文件 ·
mypy 后端 133 文件 · 引擎 1472 passed · 后端 1702 passed · gateway 618 passed ·
前端 lint/typecheck 净 + 2111 passed + build ok。零引用证明:对删后 114 个引擎源模块重跑
AST import 图,对已删模块/符号的 import **0 条**。
**收尾件 #823**:PR E 在前端立了一张手抄的引擎事件类型表(`utils/engine-events.ts`,35 条),
用来区分"这个 build 认识的事件"与"没听说过的事件",好让按名字猜(含 `error` 就算失败)
只作用于未来事件、不推翻已知事件的真实含义。手抄件没有闸就会悄悄漂,于是补了
`apps/studio/backend/tests/test_engine_event_catalog_mirror.py`:内省引擎判别联合类型
`CallbackEvent`,与那张表**双向**比对。放在 Studio 后端是因为只有它同时看得见两边
(import 得到 `graph_agent`,又与前端同仓);引擎不该知道有个前端文件,前端没有 Python
可内省。三个变异验伪:删一条 / 加一条幽灵 / 改常量名,均被精确点名。

**真机点验已完成(2026-08-15 13:33–14:0x,报告页
https://claude.ai/code/artifact/49211f28-0991-4833-ae1f-39d68dd37bb4)**。占用等了约 3.7 小时
(09:47 起被 story-deconstruction 实验会话持有,按黑板规矩排队不抢占),13:33 轮到后:
关 app → 重建 vendor(连带补进他人后续合并的 #819/#825/#826)→ 预热 `.pyc` →
带 CDP 调试口重启 → 逐项驱动真窗口 → 验完确认 9222 关闭(`curl` 返回 000)、起无调试口实例、
释放 `cdp-9222`/`main-app`。**九项:八项实测通过,一项仅静态**——
- **① 删后引擎可被 sidecar 导入**:首屏正常、无 Backend unavailable、Recent 可读,本次启动日志
  ImportError/traceback **0 条**;重建后的快照静态验尸:死家族 14 模块 / ctx 桥 / 死事件类均 0 残留。
- **② 编译单出口**:点 Compile 后 2683ms 弹成功 toast(带 sha256 与指纹),后端三次 `POST …/compile`
  全部 200 OK。
- **③ 前端 run-status 投影**:打开 10:19 那条 run,渲染 88 行 / 14 类、**unknown 类型 0 个**;
  `finish_task_verdict` 7 条 = 5×`bg-warning`(rejected)+ 2×常态(accepted),`loop_detected` 2×warning,
  其余各类条数与 trace 文件**逐类相等**(预期值点验前已从 trace 预先算出,是数字对数字)。
- **④ `DeadEndPrunedEvent` 保留——仅静态,未打勾**:发布快照里三节链齐全(生产者
  `middleware/execution_control.py:258` 的 `cb.on_dead_end_pruned(...)`、事件类 `callbacks/events.py:165`、
  `ExecutionControlMiddleware` 位列活链第 3 槽),但**全机所有 run trace 中该事件 0 条**,
  造一个必然 dead-end 的场景再赌 LLM 走到那一步,代价与收益不成比例。

**另有三项在排队期间已于磁盘上坐实**(证据源:同日 10:19 的真实 run
`D:\coding\skills\story-deconstruction-v3-lab\.workspace\runs\2026-08-15T10-19-55_df555c19\`,
跑在 09:39 重建的 vendor 上,该快照内容级实证含五 PR):
- **PR B 认知工具**——`report.md` 的 Tools 表记 `log_ambiguity` ×1 / `update_working_memory` ×3,
  即工具已挂上活路径且被模型真调用;trace 有对应的 `ambiguity_logged` / `working_memory_update`。
- **PR C 双闸 nudge**——trace 3 条 `nudge`,文案唯一出处 `middleware/exit_control.py:204`,
  而该文件 `:35` 从 `nudge_policy` 取策略;失败行带预算计数
  `counts={'planning':1,'selfcheck':0,'standard':1,'total':2}, max_nudges=1`。
- **PR E 后端失败信号重接**——`report.md` 的 Failure 段列 5 条 `segment — finish_task_verdict: …`,
  `review` 的 accepted 不计、`tool_error_handled` 不计。这是因果证据而非时间戳推断:
  `git log -S "finish_task_verdict" -- apps/studio/backend/app/services/run_report.py`
  只有 #810 一个 commit,而 dev 下后端 `.py` 实时加载,故该报告只可能出自改后的代码。

**另有两项没有真机可观测面,只能由单测锁定,报告里如实标注不打勾**:① `context_access`
opt-in 门——引擎事件只记录工具**被调用**、不记录**被挂载**(`prompt_captured` 字段无 tools),
"没挂上"从 trace 看不出来;② compaction 触发条件是上下文超窗 80%,冒烟不可控、全机所有 run 均未触发。

**本条至此收敛**:代码面五 PR + 收尾件 #808/#820/#823/#824 全部合并,门禁全绿,真机点验完成、
报告已交。唯一不打勾的是 `DeadEndPrunedEvent` 的运行时观测(理由见上),它不影响 PR E 的结论
——"该事件有活生产者、不该删"是由代码链证明的,与能否在一次冒烟里触发无关。
PR E 的逐项重核推翻了决议 §5 的一条:
**`DeadEndPrunedEvent` 不删**——它有活生产者(`ExecutionControlMiddleware` 经旧式
`on_dead_end_pruned` 回调发出),决议起草时的清单误判为零发射点,已在决议 §5 落更正;
`ThreadCleanedUpEvent` / `InternalErrorEvent` 的「待定」裁决为删(仅有类定义、零构造点)。
删除的连带面按同一映射重接,保证后端报告与前端 trace 给同一份说法:被删的
`validation_fail` 的活继承者是 `finish_task_verdict`(verdict=rejected,带 `errors`),
硬失败是 `protocol_violation`(带 `violations`);`tool_error_handled` 不算 phase 失败
(引擎已把工具异常吞成模型反馈、运行继续)。

**在册漂移(先于本次删除,未在 PR E 处理,待排期)**:① `spec/source_file_map.yaml` 另有三条
指向不存在文件的条目(`core/harness.py`、`tools/providers.py`、`tools/synthesize_speech.py`);
② 前端 `api/types.ts` 的 `LlmPhase` 仍声明 `max_retries`/`retry_target`/`validator_optional`/
`dead_end_threshold`/`max_nudges`,而活的 `AgentNodeAST`(`core/manifest.py`)这些字段一个都没有
——它们当年长在死侧 `core/types.Phase` 上,活路径从未读过。

在册风险:flow 通道并行写——同一超步两个写 flow 的认知工具并行调用会
InvalidUpdateError(PR #803 实施报告发现,先于该 PR 的引擎级限制);first-principles
修复=给 flow 通道设计合并 reducer,待排期。另录 PR C 实施发现:ToolNode 内工具
Command 带 `goto="model"` 会与 tools→model 常规边双路由,把 agent 循环分叉成两条
并行 model 车道(幻影回合)——PR C 已摘除 update_working_memory / log_ambiguity
两处 goto;`_reject_finish` 与澄清分支的同款 goto 待同根因处置。

### 用户缺陷批次 2026-08-15:copilot 多会话后端路由(会话身份契约)

**缺陷(2026-08-15 真机实测)**:面板 "New chat" 只换前端视图——`newSession()` 纯前端
(`copilotStore.ts`),发送报文无 session 标识(`useCopilot.ts` `buildCopilotSendPayload`),
后端 ws 端点按 skill_id 只存一条 SDK 对话、仅断连 reset(`routers/copilot.py`)。后果:
生成中新建标签发的消息被注入正在跑的会话;流式事件渲染进"当时激活的标签"。

**修复(进行中 fix/copilot-session-routing)**:会话身份显式进契约(设计已落
`copilot-assist` mvp1-alignment COPILOT_ASSIST-5 + copilot region F1 约束):ws 报文必带
`session_id`(缺失 close 4400);SDK client 缓存键加入 session;事件按发起查询的会话 FIFO
归属;关标签走新端点 `POST /copilot/session-close` 结束对应 client;断连整 skill reset 与
单活跃查询不变式不变。过门 = 后端 pytest 全量 + 前端四件套 + 合并后真机点验
(两标签并行互不串 + 关标签后端 client 归零)。

### 用户裁决批次 2026-08-15:MoirAI 两条运行位分写操作规则 + 知识库定位

**裁决**:「Moirai 的规则得是拼合成的,sdk 和 cli 是两种机制,需要两种操作规则」。
决议正本 `.kiro/specs/studio-moirai-agent-system/decision-2026-08-15-per-runtime-dispatch-operating-rules.md`(§9-13 为本批次)。

| # | 项 | 状态 | 处置 |
|---|---|---|---|
| M-1 | 派工语义在两条路上真值相反,规则必须分写 | ✅ 随本行同 PR | `panel.md` 的「派工是只读咨询」由 `_GODDESS_TOOLS` 钉死成立;CLI 侧 Studio 生成的 `[agents.*]` 无 cmd 覆盖 → 女神吃 ah claude provider 默认命令 `claude --dangerously-skip-permissions`,可写可执行零审批(对照:master 显式丢掉该 flag 并有单测锁死)。`cli.md` 补相反的一条:派工是对工作区的动作,包里必须自带边界 |
| M-2 | 规格自相矛盾(R5.4 要 cli.md 提供 `ah ps`,R7.5 禁止复述 ah 命令) | ✅ 随本行同 PR | 用户裁决「人类语义表述错误,你应该能判断出来」→ 按合理意图判读:R7.5 是针对 cli.md 的封闭清单且自带理由,优先;R5.4 括号里那对例子是举例,举了个被禁的例子。`moirai-intro` 去掉两路动词硬编码(真漂移,`design.md` 明写「无表面维度」),`cli.md` **不补** `ah ps`,`requirements.md` R5.4 措辞就地订正。门禁:共享技能里不得出现任何 `ah` 命令;`cli.md` 的 ah 命令集合必须恰好是 `{ask}` |
| M-3 | **被派出去的女神在读陈旧知识库** | ✅ 随本行同 PR | `add_dirs` 是会话级 CLI 参数(SDK `subprocess_cli.py`),`AgentDefinition` **无此字段** —— 挂载给的是**读权限**不是**地址**,女神只能猜。行为实测(派 Clotho 找 `KB-00-hub.md`):修复前她 Glob 三次找到工作区里 CLI 路物化的 `.ah/knowledge/` 副本,该副本少一整篇 `KB-14` 且 `KB-02` 缺 #817 刚补的整节;修复后 Read 直达随包目录。A/B 混杂因素受控(同工作区、陈旧副本原封不动仍在,唯一变量是注入的地址)。修法:`agent_assets.knowledge_location_note()` 生成带解析后绝对路径的运行时事实,拼进面板侧仅有的两个装配产物(主线程 append + 三女神 prompt);写进代码而非 `panel.md`,因为那是随安装位置变化的路径,属资产文件头自己排除的「tool mechanics enforced in code」 |
| M-4 | 分配判据(`roles/moirai.md` 的 "perfectly aligns") | ⏳ 待办 | 维持不动。现象是纯 prompt 设计题连跑 5 次仅 1 次派工,但**没有证据**表明判据措辞是原因——§7 已经有过一次教训(技能挂载漂移看着像根因,实测 1/5 vs 1/5 无差别)。下一步是先测再改:用 `dispatch-rate.sh` 对「scope 契合改为可对照技能映射表机械核对」跑 n≥5 的 A/B。独立任务,不夹带 |
| M-5 | SDK/CLI 对等审计的其余分叉 | 📌 记档 | 三路审计 + 三路对抗核验共 32 条:应有差异 7 / 已登记已知差距(R8.8)3 / 本批次修 3 / 其余留档。留待后续四项见决议 §12:CLI 侧三 worker 拿不到 MCP 却读着声称 studio 工具可用的 `cli.md`;`web-research` 在映射表里无归属;atropos 面板侧派工拿不到 `<judge_context>` 契约;**codex 的沙箱兜底前提不成立**(R8.8 接受该差距的前提是「靠沙盒物理隔离兜底」,而 codex 带 `--dangerously-bypass-approvals-and-sandbox` 关掉了自身沙箱)。对抗核验推翻了审计的四条判定,同样记在 §12 以免下轮重复踩 |

### 用户功能批次 2026-08-15:边升为与节点平级的运行分段(决议已落)

**用户裁决(原话)**:「edge指的是一个node到下一个中间的过程，是需要的，把engine该补的补齐」
「tracing要把edge和node作为平级的运行分段，流中的一个节点」「DeadEndPrunedEvent该怎么处理，
合到正规的线路里去」。第四句授权执行者按仓规自裁细节,不再逐条上问。

**问题定性**:引擎里没有"边执行"这个对象——边的操作寄生在下游节点包装器头部
(`core/graph_assembler.py:1178` 的 `_emit_input_dispatch`、`:922`/`:1077` 的黑板 reduce),
所以 `BlackboardReduceEvent` 的 `from_phase` 恒为 `None`(`:769`,结构上无从填起)、
`input_dispatch` 的来源靠 `state.flow.current_phase` 推断、前端只好按上游 phase 猜边归属
(`utils/trace-scope.ts:23` 注释自陈)。缺失字段是缺失所有者的症状,先补字段是错的修法。

**决议正本**:`docs/design/2026-08-15-edge-as-first-class-run-segment-decision.md`
(目标、设计、八条关键设计决定、四个 PR 切分、九条验收判据)。要点:边段与节点段在事件模型里
形状相同(`edge_start`/`edge_end` 对称于 `phase_start`/`phase_end`);空转移也占一段;
`from_phase_execution_ids` 用复数(扇入本就多上游,单数只能靠"取最近一个"猜);
`phase_execution_id` 与 `AgentLoopIterationEvent.iteration` 是两个概念不合并;
发送端统一到 `_safe_emit_event`,`callbacks/base.py` 的 8 个旧 `on_*` 钩子与
`_dispatch_legacy_event` 翻译层同批删除。

**切分与进度**:

| PR | 内容 | 状态 |
|---|---|---|
| 0 | 发送端统一到 `_safe_emit_event` + 删 8 个旧钩子与翻译层 | ✅ #836 已合并。实测到的两个真缺陷:`execution_control.py` 调 `cb.on_dead_end_pruned` 而引擎装配的 `_EventSinkCallbackAdapter` 只有 `on_event`,AttributeError 被自吞——死胡同剪枝**从未进过任何 trace**(磁盘上每份 trace 该事件计数为 0);同文件的循环上报用未声明的 `on_loop_detected`,同样谁也收不到,删除而非补类型(活所有者是 `LoopDetectionMiddleware`,补了会双报) |
| A | 引擎转移所有者 + `edge_start`/`edge_end` + `edge_transition_id` + `phase_execution_id` | ✅ 随本行同 PR。新模块 `core/edge_transition.py` 拥有转移段;`from_phases` 取自编译期拓扑而非 `flow.current_phase` 推断,`from_phase_execution_ids` 复数取自新的 `flow.phase_execution_ids`(挂进 `_FLOW_DICT_MERGE_FIELDS`,扇出下按 key 合并不互相覆盖)。三个边操作事件的 `from_phase` 单数字段**删除**,前端 `edge-context.ts` / `trace-scope.ts` 同批改读 `from_phases`,`edge_transition` 幽灵分支及其夹具一并删除(原属 PR B 的 D6,因同处一行代码提前落地) |
| B | 前端分段模型:节点段与边段平级呈现、按段标识分组(D5) | ⏳ 待办 |
| C | 后端按段记账(`run_report.py`,D8) | ⏳ 待办 |

合并后按 `.claude/skills/studio-verify` 真机点验交五列报告。

### 环境 blocker(在册)

| # | 项 | 状态 | 处置 |
|---|---|---|---|
| B-1 | `gh` CLI 被失效的 `GITHUB_TOKEN` 环境变量压制(keyring 登录本身有效) | 已绕过(2026-07-30) | 调 gh 一律 `env -u GITHUB_TOKEN gh ...`;根治 = 从系统环境变量删除该变量 |
| B-2 | dependabot 开放 PR 积压(#520-#524,2026-07-24 起) | 部分处理 | 预言应验:mcp/pyasn1 共 6 个 CVE 曾把全部 PR 拦死,已修(#527);npm/cargo/actions 的 #520-#524 仍待审合 |

### 在册搁置项(非本冲刺,重启 = 用户裁决)

- Studio IO 数据流设计回写(用户 2026-07-16 原话"全部搁置,先不动");
- ah 编队(用户 2026-07-16 原话"忽略ah编队");
- N5 CLI 路 Studio MCP 工具(本决议后置,见关键设计决定 4);
- gateway 状态投影毛刺(含 `route.status` 与 `ui_state` 双状态字段的实体收敛,勘察 2026-07-30 记录);
- `ah attach master` 落到非活跃窗口(ah 仓缺陷,用户 2026-08-02 裁决"不改 ah" ⇒ 搁置):
  attach 是 session 级(`src/bin/ah.rs:1617-1624`),而新 master 以 `new-window -d` 建窗、
  不切换当前窗口,于是 attach 显示的是上一轮 `remain-on-exit` 留下的死窗格。Studio 侧已用
  `lingering` 状态(残留运行时保持可 Close)规避,取证与范围边界见
  `.kiro/specs/studio-ah-state-contract-v1/decision-2026-08-02-lingering-state-and-cli-autoupdate.md`;
- 更早的跨 spec 延期项见 `docs/deferred-items.md`(停更于 2026-06-21,恢复维护待用户排程)。

### 勘察证据存档

三路勘察(引擎执行能力 / Studio 前后端接线 / copilot 工具面×七节旅程矩阵)的结论要点与 file:line 证据已内联在上方各清单"关键坐标"列;勘察发生于 2026-07-30,基线 commit `1afaf27b`(main,2026-07-16)。若清单坐标与未来代码漂移,以重扫为准,不以本文件为设计权威——本文件只是台账,设计权威仍是 MVP1 设计源体系。
