请用中文回答。

你是软件研发流程与文档治理专家。请基于以下客观情况独立判断,不要迎合任何预设结论。

## 背景
一个 monorepo 有三个**独立子系统**,各有自己的设计文档体系和冻结(FROZEN)生命周期:
- studio(`apps/studio`):刚把 63 份设计文档 FROZEN + 哈希锁(改动须 owner 批准走 exemption,否则测试拦截)。
- engine(`packages/graph-agent`):设计文档目前全部 `drafted`,无哈希锁。
- gateway(`packages/graph-agent-gateway`):设计文档 `drafted`/`written`,无哈希锁。

studio 有一个"设计单元索引"(一个 Markdown 表),登记 22 个**横切设计单元**,每个单元由若干"模块切面"组成。切面分两类:
- studio **自己拥有(owner)**的切面 —— 写在 studio 那 63 份已冻结文档里。
- **引用外部系统**的切面(标记 `(引)`,owner = engine 或 gateway 的某契约)—— studio 不拥有、不书写,只引用。

其中 **10 个单元**含有指向 engine/gateway 的 `(引)` 外部切面;而那些 engine/gateway 文档目前全是 `drafted`、无哈希锁。

studio 把这 22 个单元的锁态都标成了 `locked`。studio 自己的写作标准里,单元 `locked` 的定义是"该单元**各切面**审过、锁住"。

## 一次外部审计的发现(客观陈述,供你判断,**不是结论**)
- 发现一:"22/22 locked"过强 —— 10 个单元依赖的外部 engine/gateway 切面根本没冻结/没锁,所以"各切面锁住"不成立。
- 发现二:这个索引文件**本身不在哈希锁里**(它被定义为"活注册表",要能随新单元增长而编辑),所以"哪些单元 locked"这个状态**没有机器强制**,只是文字声明。

## 请你独立判断的问题
1. 在一个有多个独立子系统、各有独立冻结节奏的 monorepo 里,一个子系统(studio)的"设计单元 locked"应该怎么定义才**合理**?是必须等它引用的所有外部契约都冻结了才能 locked,还是"本系统拥有的切面锁住即可、外部引用按外部系统自己的生命周期走"?两种各自的代价和风险是什么?
2. 如果采用"本系统切面锁住即可":怎么防止这个**假信心风险**——"引用了一个还会变的外部契约 → 外部契约后来改了,但本系统的冻结文档字节没变 → 哈希锁抓不到这种语义漂移"?有哪些可落地的机制(例如把外部依赖的版本/哈希也钉住、或建立跨系统引用台账)?
3. 那个登记锁态的"活注册表"文件本身不进哈希锁,但需要保护"哪些单元 locked"这个声明不被静默改动 —— 你会怎么设计机器强制?(选项举例:冻结整个文件 / 结构化不变量测试 / 抽出一个"锁态快照"单独哈希 / 其它。)要同时兼顾"注册表必须能随新单元增长而编辑"。
4. 有没有更好的**第四种**思路,或我漏掉的考量?

## 用户原始指令(原文)
让codex全面audit一遍?

(背景补充:codex 审计 studio 这次 FROZEN 提交后,机械层全 PASS,但报了两个治理层 FAIL:① studio 22 单元全标 locked,但其中 10 个依赖的 engine/gateway 外部契约没冻结;② 登记锁态的索引文件自身不入哈希锁=软锁洞。现在要决定 studio 跨系统"单元 locked"的正确语义 + 机器强制方式。)
