# 定稿 Addendum（#1075 §5.6 + 两项裁定，无条件）

交付方式：聊天消息，你侧落盘。

---

## 0. 冲突核查（先行，两条须裁）

三条裁定与我已核事实**均无冲突**，其中一条获代码自证；但有**两处需要你确认的落点问题**，不改变删除令，只影响工单指向。

**获代码自证：§5.6⑤ 的理由成立。** `adapters/host_native.py:43-46` 的 docstring 原文即为此论据：Phase 3 只支持 root DAG 内可无歧义定位的 Agent 等待点，嵌套 registry 子图 / graph 与相位 iterate / 并联分支「need an address richer than the current single task contract; those shapes are **rejected instead of silently running an embedded model**」。「而不是静默跑 embedded」这半句直接确认：这批拒绝来自 durable handoff 的单任务寻址契约，embedded 本来就能跑这些形状。§5.6⑤ 无需额外论证。

**须裁 A — ah 的 preflight 继承面比 §5.4 轴多一项，且可能带来新缺口。**
被指定继承的形状 `vendor_cli/runtime.py:73-87 _unsupported_phase_capabilities` 实际拒绝**四类**：`:80 tool:` / `:81 subagent:` / `:82 subgraph:` / `:83 context:`，命中即 `:95-102` 抛 `task-capability-missing` + `retryable=False`。你引 §5.4 的轴写的是「tools/subagents/context_access/迭代与并行形状」——**缺 `subgraph:`**。
更实质的是：这四类拒绝的成因写在 `:97-98`——「direct CLI execution does not yet bridge portable Agent tools, subagents, subgraphs, or framework context tools」，即**厂商命令行传输层**的限制，不是 host-native 的。§5.3 定义 ah 同样是「起厂商命令行的受监督进程」。若 ah 逐字继承此形状，则任何声明了 tool 的 Agent 相位都跑不了 ah；§5.6⑤ 关掉的是 host-native 来源的缺口，**不自动处置这一批 CLI 来源的缺口**。请裁定其一：(a) agent-hypervisor 桥接工具面 ⇒ ah 的 preflight 必须**窄于** vendor_cli 现状，须给出实际支持面；(b) 不桥接 ⇒ ah 承接该缺口，需独立 backlog 条目，不能并入已注销的 Phase 3b。

**须裁 B — §5.2 的三个字段路径在现码中不存在。** 工单需按实际路径落，否则改错文件：

| §5.2 措辞 | 现码实际 | 坐标 |
|---|---|---|
| `RunRequest.executor` | `RunRequest.profile.profile.executor`（两层嵌套） | `models.py:483 profile` → `:376 profile` → `:352 executor`；读取点 `adapters/engine.py:232` |
| `RunPreset.node_overrides[...]` | `RunRequest.node_overrides`；**无 `RunPreset` 类型**，只有 `preset_id: Identifier \| None` | `models.py:488`、`:482` |
| 「解析链（两级）」 | 现为 N 级 `RuntimeProfileOverlay` 叠加 + `field_origins` 溯源 | `application/config.py:125-157`；`models.py:325-345`、`:379` |

请裁：两级链是**替换**现有 overlay 机制，还是**叠在其上**（即 overlay 解析出全局默认后，再让 `node_overrides[].executor` 覆盖）。我按后者写工单，如需前者请回一句。

---

## ① 执行域定稿

### ③E1 闭集 — 定稿
应然 `{embedded, ah}`；实然 `{host-native, cli, embedded}`。**两删一建**。

删除坐标：`models.py:248-250 HostNativeExecutorConfig`、`:253-284 CliExecutorConfig`；`adapters/cli.py:84-85`(host-native 分支)、`:88-99`(cli 分支)、`:143 choices`、`:146-159`(vendor/agent-profile/model/executable/timeout flags)；`adapters/engine.py:233-234`(cli 派发)、`:235-236`(host-native 派发)。
新建：`AhExecutorConfig(kind="ah")`；`adapters/engine.py:232` 的 if/elif 换为注册表解析（见 M-E2）。

### ③E2 fallback_executors — 定稿：删除
死字段 + 反设计，闭集降至 2 元后无任何可辩护语义。删 `models.py:337`、`:357`、`:359-366`；`application/config.py:96`、`:107-108`、`:154-157`、`:321`。

### ③E3 相位级覆盖 — 定稿：维持原判，补字段
`models.py:404-414 NodeOverride` 增 `executor: ExecutorConfig | None = None`（闭集 `{embedded, ah}`）。生效顺序须落进 `field_origins`（`models.py:379`），使「这个相位为何用 ah」可溯源。落点依 **须裁 B**。

### ③E4 preflight — 定稿：存活实现数 0，新建，继承工具面形状
两个现有实现全属删除面 ⇒ 无原型可提升，**必须新建**。

- **继承形状**：`vendor_cli/runtime.py:73-87` 的能力清单式拒绝 + `:104-110` 的 `required_capabilities` 差集检查（`RuntimeProfile.required_capabilities` 存活于 `models.py:336,356`，且当前**仅** vendor_cli 校验，ah 必须承接，否则该字段再次沦为死契约）。实际支持面依 **须裁 A**。
- **打捞后废弃**：`host_native.py:39-114` 的「等待点可定位」清单随 durable-pause 模型死亡；其中真实图形约束（并联 `:101-113`、graph 级 iterate `:69-72`、相位级 iterate `:73-82`）按你的口径移交编译期，依引擎 AST/reducer 契约重推，走既有独立工单，不进本批。
- **待定机制**：能力探测归属。现为运行时探测厂商 CLI（`vendor_cli/executor.py:193 capabilities`、`:314/:319` 探测超时、`vendors.py:22 _BASE_CAPABILITIES` 及六份 per-vendor 能力集）。ah 之下应由 agent-hypervisor 上报还是仍探测，须在 T-E1 内定。

### ③E5 主仓 / Studio — 定稿：判定不变，工作量减
主仓 engine 无 executor 概念（全仓仅 `ThreadPoolExecutor`、`artifact_executor` 回调、`loader.py:1494` 占位注释），且从无 host-native ⇒ 无回退工作。Studio 侧 `services/runtime_config.py:37-58` 仍无 executor 键，`app/models/*` 与 `frontend/src/api/types.ts` 零命中 ⇒ 参数链须从 HTTP 边界一路新建（并入 T-E5 runtime_config 类型化）。

### ⑤ handoff 相关 — 定稿：三件全删，原「保留并复用」表述作废
**正式撤回**原报告 T-E2 第 3 条「保留并复用」。三件均**无独立效果**，证据（已核）：

| 对象 | 唯一生产/调用者 | 判定 |
|---|---|---|
| `core/runner.py:1862 _apply_external_phase_completion` | 唯一调用点 `:780`，仅当 `resume_skill(external_phase_completion=...)`（形参 `:667`）非空；实参唯一生产者 `host_native_runtime.py:358`；余者仅测试 `tests/application/test_host_native_handoff.py:596` | 删 |
| `adapters/agent_handoffs.py` 全模块 | 全部消费者仅 `host_native.py:16,230,294,303` 与 `host_native_runtime.py:11-13,79-80,90,103,121,163,165,310,322,325,327` | 删（含 §5.6② 的 sqlite 存储，建表 `:60`；文件名以 #1075 原文为准，我未独立核实） |
| `core/runner.py:591 recover_paused_skill` | 唯一调用点 `host_native_runtime.py:213` | 删 |

连带删除：`core/runner.py:166-179 ExternalPhaseCompletion`；`state.py:220 agent_result_hashes` + `:246` 键列表项（其唯一写者是 `runner.py:1931`，唯一 host-native 字面量 `:1929 f"host-agent:{task_id}"`）；§5.6④ 的 `checkpoint_ref` 形状（生成 `host_native.py:283 f"gskill-handoff-v1:{task_id}"`，装配 `:284 AgentRequired(...)`，读取 `agent_handoffs.py:168 load(checkpoint_ref)`）；§5.6③ 的 submit 入口（`engine.py:249-261`、`adapters/mcp.py` submit tool、`adapters/cli.py:201 submit_parser`）。

**边界确认（与你一致）**：`AgentTask` / `AgentResult` **存活**，不连坐——消费面横跨 18 个文件含公开出口 `sdk.py`、`__init__.py`、`ports/runtime.py:31`、`domain/models.py`、`application/service.py`，且是 `ah` 实现 `AgentExecutor.execute` 的边界类型。`AgentRequired`（durable pause 载荷，`models.py` 内含 `submit_methods:580`）随异步模型删除。

**缺省与快照（采纳你的裁定）**：`models.py:352` 缺省改 `EmbeddedExecutorConfig`；同批升 `RunRequest`/`RuntimeProfile`/`ResolvedRuntimeProfile` 的 `schema_version`；旧快照**显式硬失败拒绝**，不静默改判、不写迁移垫片。`agent_result_hashes` 从 state 模型删除 + 显式声明已落盘 sqlite 检查点不兼容（数据可弃）。

### ⑦ M-E2 `ExecutorPort` — 定稿
- **存在理由**：执行者是运行时参数，需解析点而非 if/elif；两级解析链需一个唯一裁决处。
- **own 事实**：`kind → AgentExecutor` 解析；两级链（全局默认 → 相位覆盖）的生效顺序与溯源；preflight 结论。
- **公开能力**：`resolve(profile, address) -&gt; AgentExecutor`；`preflight(compiled, profile) -&gt; PreflightReport`。**无 submit 半边**（ah 同步，裁定确认）。
- **形状**：`ports/runtime.py:31 AgentExecutor(Protocol)`（`executor_id` + `execute(AgentTask) -&gt; AgentResult`）存活；`ah` 是它的**第一个实现**（当前零实现）。
- **规则动作**：闭集内解析失败直接失败，**不 fallback**；preflight 不过则运行前失败并附不兼容清单。
- **自动消解**：原 V-E3（Port 与 `vendor_cli/runtime.py:41-47 _CliExecutor` 双声明）随删除消失，撤销独立工单。
- **生命周期**：每 run 解析一次并冻结进 `RunRequest`（落点依须裁 B）。

### ⑧ T-E1 — 定稿（executor Port 收口 + 参数链 + preflight）
1. 建 `AhExecutorConfig`；`models.py:295-298` 闭集改 `{embedded, ah}`；删 `HostNativeExecutorConfig`、`CliExecutorConfig`。
2. `engine.py:232-243` 换注册表解析；`else` 隐式兜底改显式 `embedded` 分支 + 未知 kind 硬失败。
3. `NodeOverride` 补 `executor`；实现两级链（依须裁 B），生效顺序入 `field_origins`。
4. 缺省改 embedded + 升 `schema_version` + 旧快照硬拒（同一 PR，不可分批——否则出现「新默认 × 旧快照」静默改判窗口）。
5. 删 `fallback_executors` 全部 8 处坐标。
6. 新建 preflight：继承 `vendor_cli/runtime.py:73-87` 形状 + `:104-110` 差集检查；支持面依须裁 A；能力探测归属须定。
7. 删除前先打捞 `host_native.py:39-114` 的图形约束，移交编译期工单。

### ⑧ T-E2 — 定稿删除面表
删除总量**修正为 ≈3400 行**（原估 ≈3220，上调因 §5.6②③④ 的显式项与 `append_run_event_once` 连带；见观测域）。

| 面 | 行数 | 坐标 |
|---|---|---|
| `adapters/vendor_cli/` 全目录 | 1943 | `__init__.py 17` + `executor.py 765` + `runtime.py 409` + `vendors.py 752` |
| `adapters/host_native.py` | 320 | 全文件 |
| `adapters/host_native_runtime.py` | 410 | 全文件 |
| `adapters/agent_handoffs.py` | 278 | 全文件（§5.6②） |
| `core/runner.py` host-native 区 | ≈157 | `:166-179`、`:591-647`、`:667`、`:780`、`:1862-1941` |
| 6 个 agent 事件类 + union/`__all__` | ≈86 | `callbacks/events.py:773-846` + union 与 `__all__` 条目 |
| `domain/models.py` executor/handoff | ≈60 | `:248-250`、`:253-284`、闭集 6 行、`AgentRequired` 及 `:580` |
| `callbacks/emit.py:59-84` | 26 | `append_run_event_once`（零剩余调用者，连带死亡） |
| `core/state.py` | 2 | `:220`、`:246` |
| engine / cli / mcp / sdk 出口 | ≈120 | `engine.py:233-236,246-261`；`cli.py:84-85,88-99,143,146-159,201`；`mcp.py` submit tool；`application/service.py`、`sdk.py`、`__init__.py` 导出 |
| **合计** | **≈3402** | |

§5.6⑤ Phase 3b backlog 属文档面，不计入行数。

---

## ② 观测域定稿

### ③O5 + T-O2 第 3 条 — 定稿：**6 值删除，前端零改动**
原指令「前端 `ENGINE_EVENT_TYPES` 补 6 个 agent 事件」**正式作废**。6 个事件的全部生产者为 `host_native_runtime.py:105,151,167` 与 `vendor_cli/runtime.py:163,188,212`，皆属删除面 ⇒ 零生产者 ⇒ 从闭集删除。

删除的正是这 6 值：`agent_required` / `agent_dispatched` / `agent_started` / `agent_completed` / `agent_failed` / `agent_result_rejected`（`domain/models.py:13-57` 内对应 6 行；类定义 `callbacks/events.py:773,785,799,813,826,843`）。

&gt; **实现红线**：**不可按 `agent_` 前缀批量删除。** `agent_exit_decision`（`models.py:16`，类 `events.py:695`）与 `agent_loop_iteration`（`models.py:18`，类 `:670`）同前缀但属引擎内部 Agent 循环事件，**主仓 38 值中同样存在**，必须保留。前缀式删除会多删 2 值并使两仓对齐失败。

### 收益（按你要求写入定稿）
**GSR 事件闭集 44 → 38，与主仓完全对齐。** 依据：两仓事件字面量 `diff` 的**唯一**差集就是这 6 值（主仓 `packages/graph-agent/src/graph_agent/callbacks/events.py` 38 类，GSR 44 类，差集 6，无其它出入）。后果：

1. 前端 `apps/studio/frontend/src/utils/engine-event-types.ts:29-67`（38 值）**无需任何改动**即可读 GSR 全量事件。
2. 门禁 `apps/studio/backend/tests/test_engine_event_types_are_mirrored.py` 可**逐字跨仓复用**（它从 `CallbackEvent` union 反射取真相，非正则扫源码，换 import 即可）。
3. Studio 切换的门禁②（事件契约一致）成本降为零；北极星⑤「去黑盒」不因切换而退化。

### 连带治愈（原报告三条缺陷自动消失）
`callbacks/emit.py:59-84 append_run_event_once` 的**全部调用者**就是上述 6 个生产点，删除后该函数成为死码并随之移除。原报告以下三条随之消失，**无需独立工单**：

- **③O2**（事件完整性无保障）：`emit.py:82 except Exception` 只 log 不抛 —— 随函数删除。
- **③O3**（低效）：`:70-80` 每写一条全文重读并逐行 `json.loads` 去重，O(n²) —— 随函数删除。
- **③O4**（去重不可靠）：`:69 _APPEND_EVENT_LOCK` 仅进程内、`handoff_event_id` 机制不通用 —— 随函数删除。

主 trace 写入通路 `_TraceJsonlSink`（`emit.py:67`）经 `callbacks/tracing.py` 存活，不受影响。

### 不受 §5.6 影响、优先级不变
`T-O1`（实时订阅 Port = 门禁③）**仍为观测域最高优先级且完全独立**：`RuntimeEvent`（`models.py:650`，含 `sequence` 游标位，全仓零构造）、`EventSink`（`ports/runtime.py:54`，零实现）、`RunRequest` 无 `event_subscriber` 字段（`:476-491`）、`engine.py:211-244` 不透传 subscriber —— 均与 executor 无关。
`V-O1`（`llm_route_decision` 三 owner、9 值枚举无门禁：engine `events.py:350-360` / gateway `graph_agent_gateway/events.py:18` / 前端 `trace.ts:367-377`，engine docstring 自认「kept in step by hand」）仍为观测域最高危项，`T-O2` 第 1、2 条不变。
`③O1`、`V-O2`~`V-O7`、`M-O1`~`M-O4`、`T-O3`~`T-O5` 全部不变。

---

## ③ 与 #1075 的一致性声明

1. **本定稿以 #1075 §5.6 删除清单五项、§5.3 缺省=embedded 与 ah=同步、§5.2 两级解析链与相位级覆盖保留为唯一准据。** 我此前基于原始任务书（闭集含 host-native）作出的判定，凡与之冲突者已在本文逐条撤回或改判；未在本文出现的原判视为不变。
2. **明确撤回**一处错误结论：原报告 ⑧T-E2 第 3 条「保留并复用 `SqliteAgentHandoffStore` / `canonical_agent_result_hash` / `_apply_external_phase_completion`」。该结论的依据「与 vendor 无关」属实但推论无效——三者与 host-native 强绑定。撤回并改判为全删，证据见 ①⑤ 表。
3. **明确撤回**一处错误指令：原报告 ⑧T-O2 第 3 条「前端补 6 个 agent 事件读法」。改判为 6 值删除、前端零改动、闭集 44→38 对齐。
4. **裁定核查结论**：三条裁定与我已核事实无冲突；§5.6⑤ 的理由获 `host_native.py:43-46` docstring 代码自证。
5. **保留两项未决**，不影响删除令执行，但工单落地前需你回一句：**须裁 A**（ah 的 preflight 实际支持面 —— 是否桥接 tool/subagent/subgraph/context 面；`subgraph:` 是否在轴内；不桥接则需新 backlog 条目）与 **须裁 B**（§5.2 三个字段路径与现码不符，两级链是替换还是叠加于 overlay 之上）。
6. **未独立核实项**（以 #1075 原文为准）：`agent-handoffs.sqlite3` 具体文件名。我核实到的是 `SqliteAgentHandoffStore` 的构造点 `host_native_runtime.py:79-80` 与建表语句 `agent_handoffs.py:60`，未读取路径常量。
7. **删除面数字修正**：≈3220 → **≈3402**，差额来自 §5.6②③④ 的显式项与 `callbacks/emit.py:59-84` 的连带死亡。表内每行附坐标可复核。
8. **本轮未新增代码扫描以外的推断**；除上述两项未决与一项未核实文件名外，所有断言均带 `file:line` 且可复现。
