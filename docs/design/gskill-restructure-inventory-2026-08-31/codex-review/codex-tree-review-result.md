总裁决：**rework**。当前域树和七批队列不应作为实施依据。证据口径已区分静态因果链、实际运行与产物证据。

完整审计已保存至 [adversarial-audit.md](C:/Users/test/AppData/Local/Temp/claude/D--coding-agent-harness/e47fa48b-1904-4f62-aa53-1a12ca33cff7/scratchpad/adversarial-audit.md)。

## P0

[严重度 P0]【依赖、结构风险】Studio 五门禁与整体搬迁不能并行，且草稿漏掉目标仓 CI、分支保护和必过检查生效这一搬迁硬前置。依据：草稿 `:95-103,123-125`；上位决议 `:176,182-195`。修改建议：顺序改为“113 文件落 main → 命名 → 目标仓门禁生效 → 冻结旧 engine 随 gateway/Studio 整体搬迁 → 新仓实施五门禁与模块化 → 五门禁全过后原子切换并删除旧 engine”。

[严重度 P0]【依赖、转述、结构风险】X-T1 将“vendor 新鲜度可验证”越证扩大成“立即重指新 engine”，既无域报告支持，又破坏冻结旧 engine 随迁、五门禁后 cutover 的安排。依据：草稿 `:89-93`；上位决议 `:178,189-195`；G7 `06_a66fac8a014fefd6b.md:229-230,244,302`。修改建议：拆成迁移前的新鲜度门禁，以及五门禁通过后的原子依赖重指；后者与旧包删除、打包版点验和回滚写进同一工单。

[严重度 P0]【八条决定、依赖】草稿接受 `{embedded, ah}`，却仍保留 durable handoff 链，并用无报告依据的“既有记忆”重定 D-T2，正面违反 host-native 删除令。依据：草稿 `:96,142`；上位决议 `:229,253-259,269-274`；旧 G4 `line252_aeed340fbc0847f26.md:22-39,154-158` 不能覆盖后续用户裁决。修改建议：删除 handoff store、`submit_agent_result`、`checkpoint_ref` 和 Phase 3b；`_apply_external_phase_completion` 须证明脱离 handoff 后仍有独立效果，否则删除；D-T2 从 AST 状态合并和 reducer 契约重新推导。

[严重度 P0]【依赖、结构风险】七批队列不是有效依赖序：M9 早于 M4/M6/M8，M4 早于其前置 M3，GoldenVerdict Port 又早于磁盘 schema 统一。依据：草稿 `:96-97,105-106`；G3 `07_ac08659a6d5b556a3.md:232-241`；G5 `line342_ac68cf30433471aa0.md:233-250`。修改建议：废弃手写批次，先生成工单 DAG；至少满足 `M3→M4→M6→M8→M9` 和 `E-T2→E-T1`。

[严重度 P0]【树锁、结构风险】“四带”按仓库/部署形状充当域树父层，已把发布共享域压进 Studio 工作面，并把进程生命周期与 HTTP/API 错误治理塞进同一个底座域，违反按共同不变量分域。依据：草稿 `:19-51`；上位决议 `:106,320-350`；G7 `:131-157`；G1 `08_a2b6b29566a8e3097.md:357-471`。修改建议：四带降为只读导航；发布共享域和运行时底座域重新执行 split-first、三向反事实及膜/原子性分审。

[严重度 P0]【树锁、结构风险】owner 图存在三个真空：`workspace-writer` 没落到域 12-14 的具体域，`node-runtime-overrides` 没进入执行域模块表，MoirAI 既没进入产品 owner 图，也没被声明为非产品治理资产。依据：草稿 `:25,49-51,56,110,116-117`；G5 `:455-458`；G6 `line302_a0f03d30865974364.md:380-389`；G7 `:81,315`。修改建议：补“概念/状态 → 域 → 模块 → Port → 写者/读者 → 当前/目标 owner → 迁移状态”矩阵；MoirAI 必须明确产品或非产品归属、owner、生命周期和迁移门禁。

[严重度 P0]【依赖、树锁】MoirAI 单 owner 被排到最后一批，违背上位决议“立即行动”，并让 KB 冲突持续污染搬迁基线。依据：草稿 `:116-117`；上位决议 `:197-201`；G7 `:315`。修改建议：把资产清单、冲突映射、目标 owner 和迁移校验移到搬家前置；编号由目标 owner 在语义不变条件下分配。

[严重度 P0]【八条决定、树锁】以“新仓已有 `scaffold.py`”和北极星③裁定 scaffold 内容归 engine，混淆了“规范合法最小骨架”与 Studio 产品模板/向导内容。依据：草稿 `:41,57,138`；G5 `:450-453,482`；上位决议 `:328-336`。修改建议：拆成两个候选：engine 只 own serializer、格式和最小合法构造；产品起点、模板选择和向导由 Studio/创作 owner，通过公开 scaffold contract 协作。

## P1

[严重度 P1]【转述、结构风险】证据包没有闭合到“七路报告 + 三份订正”：只能识别六份，G2 缺失，G7 重复，G3/G4 订正没有独立 addendum 和替代范围。依据：草稿 `:3-5`；`line395_unknown.md:45,69,75,78`；G3 正文 `:58-60`。修改建议：生成版本化 evidence manifest，补齐 G2、G3/G4 addendum，并删除无法回指 manifest 的断言。

[严重度 P1]【树锁、结构风险】L3 候选尚未完成三向反事实与膜/原子性分审，却被称为“定稿候选”并直接用于锁树和排批。依据：草稿 `:21-58,73-117,131`；上位决议 `:330-350,397-407`。修改建议：降级为“待资格审查候选”，完成八项义务、反事实、合并证据、膜审和原子性审后才进入 owner 矩阵及 DAG。

[严重度 P1]【结构风险、依赖】标题承诺 Effect 链，正文却只有孤立 Effect 表，没有端到端旅程、域间因果边、失败传播和 cutover/rollback 条件。依据：草稿 `:1,21-58,62-69,73-125`；上位决议 `:322-350,405-407`。修改建议：补系统旅程责任图、typed dependency DAG、owner/Port/状态读写矩阵。

[严重度 P1]【八条决定、待裁】删除五个模板是用户可见能力取舍，报告明确要求先裁，草稿却以 no-backward-compat/YAGNI 自决。依据：草稿 `:139`；G5 `:302-303,331,475-480`。修改建议：把“保留并重写”与“删除并改造三个 Copilot 按钮”上交用户；裁决前只补起点内容必须可编译的门禁。

[严重度 P1]【八条决定、树锁、转述】两册错误码并存方向可能成立，但缺失 G2，且跨册联合导出、命名空间和投影规则没有唯一 owner。依据：草稿 `:58,141`；G1 `:70,172-202,434`。修改建议：先降为候选；定义联合 catalog/export Port、命名空间和允许的投影面后再批准。

[严重度 P1]【待裁、八条决定】`schema_drift` 是否阻断 predict 既未推导，也未进入待裁清单。依据：G5 `:243-256`；草稿 `:135-153`。修改建议：分别定义 compile、predict、golden evaluation 的行为；若无法从既有 Effect 唯一推出，再上交用户。

[严重度 P1]【待裁、依赖】KB-11/12/13 的新编号不应直接上交用户；单 owner 已裁定，编号和重定向属于目标 owner 的迁移细节。依据：草稿 `:117,153`；上位决议 `:197-201`；G7 `:315`。修改建议：由目标 owner 分配稳定 id、维护旧 id 重定向和语义不变对照；只有需要删除或改写语义时才上交。

[严重度 P1]【转述】“空洞通过”只有 schema 与代码路径组成的静态可达链，没有实际 golden run 或结果 artifact。依据：草稿 `:11,27`；G5 `:109-124`。修改建议：改称“代码级可达风险”，补实际输入 baseline、运行结果和 `total_cases=0,status=passed` 证据。

[严重度 P1]【转述】“删 124 条 route”混合了无独立快照的数量、G3 正文已证伪的 URL 根因以及仅存在于会话摘要的订正。依据：草稿 `:11,33,80-81`；G3 `:58-70,232`；`line395_unknown.md:45,75`。修改建议：当前只确认删除机制、角色引用剥离和 30 天跳过；124 需前后快照，根因需正式 addendum。

## P2

[严重度 P2]【转述】“杀活 sidecar（7 环链验实）”没有说明验实仅指静态代码链，与后文仍列真机待验相冲突。依据：草稿 `:11,49,127`；G1 `:147-158,563-566`。修改建议：改成“七环静态因果链已闭合；浏览器行为尚待打包版真机复现”。

未发现：`UrlPlan`/`Verdict`/`ProbeChannel` 归模型域并由媒体复用、`GoldenVerdict` 归 engine 的方向有报告支撑；`workspace-writer` 归平台层有支撑，但精确域仍缺。

未发现：XML/YAML、计费轴、媒体 endpoint、updater、命名五项的上交层级没有问题。

八条决定中，删除两条 501、删除错误信封冗余字段、`checkpoint_validity` 归 engine 未发现问题；GoldenVerdict 方案甲方向成立，但必须遵守 `E-T2→E-T1`。

最小下一步：补齐 G2 和 G3/G4 独立 addendum，建立证据 manifest；随后重建 owner/Port/读写/迁移矩阵、依赖 DAG 与原子 cutover/rollback 条件，完成候选资格审查后重新对抗审计。
