# 问题台账(Problem Ledger)

> 本文件是**用户指出的产品问题的唯一销账台账**:每条问题翻译成"哪个模块的问题",标注它牵动的层
> (事实源 / 中间层 / UI),修一条勾一条,勾销的唯一判据是**最终真机效果**,不是 PR 合并。
> 与 [DELIVERY_LEDGER.md](DELIVERY_LEDGER.md) 分工:交付台账管"正在做什么/到哪一步",本台账管
> "问题全集与销账状态"。全仓已实现功能的全景对照另见 2026-08-19 MVP1 功能对账
> (claude.ai Artifact:https://claude.ai/code/artifact/129408d8-1ffd-4b8c-a2b0-d155074b3ffb,
> 代码级核验);本台账不复制那份全景,只登记**用户点名的问题**。
>
> **建账方法(用户裁决 2026-08-19)**:①问题按模块树归属,一个模块一个包逐个解决,不做大杂烩包;
> ②台账把用户原话翻译成模块问题,原话仅作出处;③每个模块从底层(事实源)修到 UI 一次修完,
> 台账必须把三层受影响面记全,防"修了底层漏了 UI";④对照台账修完一个勾掉一个,判断修完看最终结果。
> 出处标注:全部 36 个历史会话的用户消息(1630 条)已于 2026-08-19 全量挖掘归档。
>
> 状态词汇:
> - `⏳ 未修` —— 已用代码坐实"这件事没做"。
> - `🧩 部分实现` —— 一部分已在,缺口已定位到 file:line;台账里必须写清缺的是哪一半。
> - `🔧 进行中` —— 本会话正在做。
> - `🔎 代码已实证,待真机` —— 逐行核过代码,实现完整;差的只是**最终真机效果**这一道判据。
>   **不许直接标 ✅**——建账方法第 ④ 条说得死:判断修完看最终结果。
> - `✅ 已修` —— 已合并 **且** 真机点验通过,证据写在行内。
> - `🗳 待裁决` —— 需用户定设计取舍。
>
> **2026-08-20 全量核实**:除 1.1.1 外的每一行都由 4 个只读核查 agent 逐条对着当前 `main`
> 的代码坐实过(每条带 file:line)。核实推翻了建账时的大量猜测状态——建账时的 `❓` 是
> "疑似,没查",现在的 `🔎` 是"查过了,代码这一层实现完整"。两者不是同一件事。
>
> **写行时:正文里的每个 `|` 都要写成 `\|`。** markdown 的行是用 `|` 分列的,所以正文里一个
> 没转义的竖线——一个 `str \| None`、一段照抄的报告表格行——会把这一行**悄悄劈成多列**,此后
> 每个格子都排到错的表头下,而渲染出来仍然像一张正常的表。这不是假想:2026-08-20 两本台账
> 各有一处(本表 E7 照抄了报告的两行、交付台账 W2-36 写了 `T \| None`),都是**出处和验收判据
> 整个掉出表外**才被机器发现的。人眼看不出,所以立了门禁
> `apps/studio/backend/tests/docs/test_ledger_tables_hold_their_shape.py`:每一行的格数必须等于
> 它所在表头的列数。

## 模块树(问题归属坐标系)

```
1 运行时观测
  1.0 事实源:运行事件契约(engine + gateway,多个 UI 模块共享的底层)
  1.1 画布运行时观测(区别于 trace)
      1.1.1 node   1.1.2 edge   1.1.3 subgraph   1.1.4 input/output 端点
  1.2 trace 面板
      1.2.1 步骤流时序   1.2.2 步骤内部过程(去黑箱)   1.2.3 长文本呈现
      1.2.4 edge 分段视图   1.2.5 结局卡与报告入口   1.2.6 search 与范围
      1.2.7 LLM 调用细节(路由/降级/配置对比/token)
  1.3 run 汇总(报告内容 / 统计口径 / 时间口径)
2 运行控制(run/pause/resume/stop/compile 按钮状态机)
3 LLM 配置(gateway 真相 + Settings + compare 链路)
4 Copilot(MoirAI)
5 平台与桌面壳(任务状态/重启/登录控制台)
6 发布与分发
7 skill 工作区与 golden
```

修复顺序(按「底座先行 + 统一状态系统」自行推导,不再逐项上呈):
1.0 → 1.1.1 → 1.1.2 → 1.1.3 → 1.1.4 → 1.2.x 逐个 → 1.3 → 2 → 3 → 4/5/6/7;
L1(模型组身份漂移)与 E6(token 汇总)是数据正确性问题,不排在 UI 队列里,并行推进。

---

## 1.0 事实源:运行事件契约

| # | 模块问题 | 层 | 状态 | 出处(用户原话日期) | 验收判据 |
|---|---|---|---|---|---|
| E1 | edge 是"上一 phase end→下一 phase start 之间的过程",引擎必须发 edge 分段事件,trace 中 edge 与 node 平级作为运行分段 | 引擎✅→前端⏳ | ✅ **已修 + 真机点验(2026-08-20)**:本轮 run `2026-08-20T11-30-38_df572662` 的 trace 里 `edge_end` 与 `phase_start` 是**同级行**,各挂在 `SEGMENTATION` / `SETUP` / `SEGMENT` 段头下,文案是完整句(`Transition finished: Input → segmentation`);画布连线 idle 时灰色空心、绑到该 run 后紫色实心。 点验前的核实结论保留如下 —— **引擎侧已完成**:`callbacks/events.py:380-419` 定义 `EdgeStartEvent`/`EdgeEndEvent`(带 `edge_transition_id`/`from_phases`/`to_phase`/`changed_keys`/`blackboard_snapshot`/`operation_count`),`core/edge_transition.py:118-155` 真发。**缺口 100% 在前端**,见 T6 三条 | 08-16"edge指的是一个node到下一个中间的过程…把engine该补的补齐";08-14④;08-19 Q1/Q4 | 跑一次 run,trace 里能看到 edge 分段 |
| E2 | subgraph_path 已随事件上线(#867)但前端零消费 | 前端 | ✅ **已修 + 真机点验(2026-08-20)**:`predict-2026-08-20T11-28-37_092d8eef` 的 **294 条**步骤按 **31 个作用域**分组,每条步骤的 aria-label 形如 `Trace step edge_end in segmentation`,嵌套作用域写成 `story_analysis.analyze_batches`——前端确实在按 `subgraph_path` 分组。 点验前的核实结论保留如下 —— **本 PR 已合**:`phasePathOf` 把 `subgraph_path` + `phase_name` 接成一个键,三条投影全部改用它 | 08-19 Q2 | 见 1.1.3 |
| E3 | prompt_captured 不携带模板原文,只有来源名——"SKILL.md 里写的是什么"无法展示 | 引擎→前端(+vendor 重建) | ✅ **已修(与 T2 同 PR)**:`prompt_captured` 增 `template_text`(认知模板全文)与 `phase_source_path`(相位文档的工作区相对路径);模板正文从 f-string 提成模块常量 `V030_COGNITIVE_TEMPLATE_TEXT`,渲染结果钉在 golden 上(`tests/cognitive/test_template_text_is_a_value.py`,golden 由改动前的 f-string 实现产出)。**真机点验前须重建 vendor** | 08-19"SKILL.md里写的是什么,模板写的是什么" | trace 步骤里能看到模板原文 |
| E4 | agent 节点内部步骤(中间件/md2json/validator 查了什么/protocol 检查)无 print 级语义事件——「只给结果不给过程」 | 引擎→前端 | ✅ **已修 + 真机点验(2026-08-20)**:run `2026-08-20T17-13-16_4e586d15` 的 trace 里,展开 `finish_task_verdict` 读到 VERDICT=accepted、ITEMS=1,外加编号叙述三步(判词 / 「md2json parsed 1 '##' block(s) out of business_data_md.」/ 「Schema check against 'BusinessSchema_102dbdee': all 1 block(s) passed.」);④中间件那条缺口也已补上 —— `agent_exit_decision` 现身且是完整句(「The exit gate accepted a qualified finish_task submission, so the phase ends here.」);全程没有一处回落到原始 JSON。截图 `E4-E16-verdict-narrates-only-what-ran.png`。原始诊断:建账时写的病灶(`machineryNarration` 只读 `details`/`errors`/`violations`)是**前端**的,已随 T4 修掉(单句通道 + `eventFacts` 事实表 + `UnreadEventBody` 明确告警 + 镜像门禁),所以「无原始 JSON 兜底」这半条判据由 F12 结构性保证。**逐个重查四项内部步骤后,真缺口只剩一项**:①md2json ✅ 已在发——`cognitive_flow.py` 的 `story` 连**通过**的检查都写进去(「Schema check against X: all N block(s) passed」);②validator 查了什么 —— **2026-08-20 修正(PR #922)**:这里原本举证的那两句叙述(「Business validator passed N item(s)」/「No business validator is declared」)已随 E16 删除,因为它们是一道**从来没有过校验器的闸**在报告自己的结论。相位 validator 真的查了什么,现在的通道是它失败时那条致命错误的载荷,加上 `phase_end.status`(E17/#921);finish 闸的叙述只留它自己跑过的两步;③protocol 检查 —— 通过时静默,**这是对的**,见下面的边界;④**中间件 ❌ 真缺**:`ExitControlMiddleware` 决定「这个相位继续还是停下」,五个答案里有四个**只写成 `logger.info(...)`**。后果是循环**最常见的结局也最看不见**——相位因提交被接受而正常结束,trace 里只有 `finish_task`、verdict、`phase_end`,没有一行说闸同意了。实测真实 8 相位 run(`2026-08-19T06-58-15_179d1440/trace.jsonl`):77 条事件、4 个 agent 相位,**结束了这四个相位的那个组件贡献 0 条事件**。引擎其实早把这些句子写出来了,只是写在了运行的读者永远不看的地方——这正是本行「print 级语义事件」的字面含义。**修法**:新增 `AgentExitDecisionEvent`,闸的每一个答案都发,取值封闭(`exit_success`/`continue_tool_work`/`continue_nudged`/`continue_open`);**五个决策点全覆盖**,包括写测试时才实测发现的第五个——挂在 `after_model` 的 planning gate,它在 `after_agent` 轮到之前就把循环打回去。前端按 F12 既有契约消费:镜像表 + 事实读法(`outcome: phase ended` / `loop continues` + `iteration`),不需要新设计单元。**边界(同一条规则的另一半,写进 OB8)**:会改变后续控制流的组件是**决定者**,每次决定都报;不改变控制流的检查是**断言**,只报失败——所以 protocol 校验维持只发 `ProtocolViolationEvent`,给一个每次模型调用要跑两遍的断言加「通过」事件是纯噪声 | 08-14②「tracing对用户的目的是去黑箱」;08-19 Q9(说过不止两次) | 展开任一步骤能读到它每一步做了什么,无原始 JSON 兜底 |
| E5 | **DeadEndPrunedEvent 从来没有发出来过**:接线、门禁、前端读法都在,而判定「连续失败」的那个窗口按消息相邻计数,真实 agent loop 里两次失败之间必然夹着一条 AIMessage,于是永远数到 1 | 引擎 | ✅ **已修,待真机**。**先说旧结论错在哪**:本行 2026-08-20 写的是「已收口」——接线确实都在(决议 D7 之后 `Callback.on_event` 是唯一入口、`_safe_emit_event` 是唯一发射器,门禁 `tests/callbacks/test_one_emission_path.py` 用只实现 `on_event` 的消费者接住 `DeadEndPrunedEvent`;`middleware/factory.py:59-97` 八个槽位都收到同一份 run callbacks;前端 `utils/trace.ts` 有 `dead_end_pruned` → `fact('pruned', …)` 的读法),但**当时缺的那个现场,这次做出来了,做出来就发现事件根本不会发**。 **实测(2026-08-21)**:造 `e5-dead-end-fixture`(一个 validator 无条件拒绝的 agent 相位,`max_iterations: 6`)在真 app 里跑完,模型连着 9 次调用 `finish_task`、其中 7 次被拒(`finish_task_verdict` 的 `verdict` 全是 `rejected`),run `2026-08-21T07-25-48_68808cea` 的 `trace.jsonl` 里 121 条事件,`dead_end_pruned` **0 条**。 **根因**:`_summarize_recent_failures`(`middleware/execution_control.py`)倒着走消息,遇到第一条**非工具消息**就 `break`。真实 agent loop 是「一轮一次调用」,每两次失败之间必然夹着**提出下一次调用的那条 AIMessage**,所以这个窗口只能数到 1;它唯一数得出 ≥3 的形状是**同一轮里并发发出的多次调用**,那是另一回事。机械验证:同一份中间件喂「跨 3/5/7 轮的失败」全部返回 `None`,喂「一轮并发 3/5 次」立刻返回 `('finish_task', 3/5, …)`。 **测试为什么没拦住**:`test_execution_control.py` 里每个 dead-end 用例都是**一串裸 ToolMessage 直接相邻**——一个 agent loop 永远产不出的形状。绿的是测试,不是功能。 **改法**:窗口改成**按工具结果计数**,中间的 AIMessage 直接跳过;打断窗口的只有工具结果自己的事实(结果不是 error、或换了别的工具)。窗口的**下界**是本中间件自己注入过的那条 `dead_end_warning`——已经被警告过的失败不再计入,于是下一次警告要再攒够一整个 threshold,而不是此后每失败一次就警告一次;顺带删掉 `_last_dead_end_signature` 这个实例状态(一个中间件实例服务同一相位的所有 batch item / loop 轮 / resume,实例上记的去重会跨着互相压制,和 `_iterations_by_invocation` 当年要按 invocation 分桶是同一个坑)。**参照**:同一条链里的兄弟 `LoopDetectionMiddleware._recent_tool_messages` 本来就是这样读历史的——**借它的窗口,不借它的判据**(它按「同一工具 + 同样内容」认无进展,而死路是「同一工具一直失败」,错误措辞每次不同也算)。设计源 `02-middleware/mvp1-alignment.md` 新增 MW3 同 PR 落盘。 **另做过一次机械审计**:events.py 的 37 个事件类逐个找发射点,零个从未被构造(`LLMRouteDecisionEvent`/`LLMCallSettingsEvent` 由 gateway `call/tracing.py` 构造引擎的类,不是死契约) **真机点验 2026-08-21:事件当场发出来了,改判 ✅**。同一个 `e5-dead-end-fixture` 在真 app 里重跑,run `2026-08-21T08-21-59_b9c5cf65` 的 `trace.jsonl` 记着 8 条 `tool_call_started` / 8 条 `finish_task_verdict`(全部被拒)、7 条 `agent_loop_iteration`,以及 **1 条 `dead_end_pruned`**——跨 7 个轮次的失败第一次被数成了一串,而不是每轮各数到 1。Trace 面板把它渲染成一条 pruned 事实,文案是「工具 `finish_task` 已连续失败 4 次」(截图 E5-01)。**恰好 1 条而不是 4 条**,正是本次改动那条下界的效果:被警告过的失败退出窗口,下一次警告要再攒够一整个 threshold。 | 08-16 | 事件在 trace 正常出现——判据现在有了可复现的现场:重跑 `e5-dead-end-fixture`,`trace.jsonl` 里出现 `dead_end_pruned`,且 Trace 面板把它渲染成一条 pruned 事实 |
| E6 | **run 级 token 汇总漏掉 iterate/batch/loop 下的全部花费(已确诊引擎缺陷)** | 引擎 | ✅ **已修(PR「a run counts every call it made」,2026-08-20)**。**真机点验先推翻了这条早先的 ✅**:装着 #895 的 vendor(`_accounting_from_zero` 在 `graph_assembler.py:502`,快照 11:21 重建)跑出的新 run `2026-08-20T11-30-38_df572662`,`metrics.json` 报 `474586/96642/571228`,而它自己那条 trace 里 80 条 `llm_call` 加起来是 `626900/114065/740965`——少算 **169737(22.9%)**;同屏可见:运行列表写 `1092.7s · 571,228 tokens`,点开它自己的报告写 `740965 total`。**根因不是 #895 没修干净,是同一条错误前提的第二种表现**:每个相位把账写成「继承到的基数 + 自己花的」再塞进 `flow` 通道,而 `flow` 的 dict 字段是**逐 key 合并**——各分支写不同 key 时不丢东西(`phase_execution_ids` 各写各的相位名),但**并联分支写的是同样那两个 token key**,于是退化成「最后一个写入者赢」,一条分支的账变成了整次 run 的账。离线复现:三分支扇出,trace 报 5 次调用 55/35,`metrics` 报 33/21,**只活下来一条**。**修法(引擎裁决 OB10,已写回设计源 `docs/engine/mvp1/02-mechanism/06-seam/02-observability/`)**:**token 计数退出图状态**。一次 run 花了多少,由它报告过的 `LLMCallEvent` 累加得出——run 作用域的 `_RunSpendLedger` 挂在事件 sink 上,`runner._run_metrics` 读它。这条路**在物理上不可能漏**:计数发生在调用发生的那一刻,而不是事后回头读活下来的那份状态;并联、iterate、子图、子代理、以及任何直接 `chat_model.invoke(...)` 而不往消息列表里追加的调用者,一律自动计入。**旧路整条删除**(`account_llm_call`/`fold_spend`/`spend_of`、读消息列表折账那段、`_SpendHarvest`/`_accounting_from_zero`、`FrameworkState.metrics`、无生产调用方的 `collect_metrics`),不留第二份账。**门禁**:三个夹具(batch / loop / **并联**)共用同一条不变量断言 —— run 报出的花费 == 它自己报告过的那些调用之和(`tests/core/_token_spend_invariant.py`)。**真机点验前须重建 vendor** **真机已验(2026-08-20)**:装着本次修复的 vendor 跑完整 skill run `2026-08-20T13-14-59_14582c6b`(15 分钟、63 次 `llm_call`、四个 SUBGRAPH 相位、含 segmentation 与 event_timeline 的并联扇出——正是从前丢数的拓扑),`trace.jsonl` 求和 / `metrics.json` / `report.md` 三处**逐位一致**:613753 in / 95405 out / 709158 total。本行判据「两处数字一致,且等于 trace 里所有 llm_call 之和」达成,状态从 🔎 转 ✅。 | 08-19 截图 | 结局卡 token = Σ调用;`report.md` 与 `metrics.json` 两个 token 源不许打架 |
| E7 | 时间口径:run id/事件 UTC 与本地混用(裁决 08-09:用系统本地时间) | 引擎/后端/前端 | ✅ **已修 + 真机点验(2026-08-20)**:报告实测 `\| Started \| 2026-08-20 11:30:38 -07:00 \|`(带偏移量);run id、predict id、trace 步骤时间戳(`11:28:37`)全为本地时间。 点验前的核实结论保留如下 —— **已修,三层各一处**。先澄清两件不是缺陷的事:**同屏 06:58/11:38 不是串台**(resume 复用同一个 run_id,`routers/runs.py:296-341` 全程用传入 run_id,两簇时间戳本来就该同屏);**存 UTC 也不是缺陷**——带时区的瞬间才是代码能算、换时区仍正确的那个值。真缺陷是**呈现口径**:实测真报告 `2026-08-19T06-58-15_179d1440/report.md` 相隔两行写着 `\| Run \| …T06-58-15… \|`(本地)与 `\| Started \| 2026-08-19T13:58:15.556101Z \|`(UTC),一个瞬间两个读数差七小时。**修法 = 把 D13「UTC 戳对着读它的人就是错的时间」推广到每一个人读得到的时刻,换算只发生在「值变成给人看的文字」那一个边界**:①后端 `services/run_report.py` 的 `_wall_clock`,`Started` 改本机墙钟 + 偏移量;②前端新增唯一换算出口 `utils/wall-clock.ts`,**收编此前散在五处的读法**——其中 Settings 真相源那处(`GeneralTab.formatTimestamp`)是把偏移量从字符串上抹掉、把 UTC 数字当本地显示,是同一缺陷的第二个实例;③导出文件名 `reportTimestamp` 改用同一个 `fileStamp`,与 run 目录同形。门禁钉的是不变量而非格式:同一瞬间的 `Z`/`-07:00`/`+02:00` 三种写法必须渲染成同一段文字。设计源 run-execution F1b 决策 ⑤ + `FRONTEND_UI_SPEC.md` §2.11a | 08-09;08-19 截图 | 全部显示本地时间且互相一致 |
| E8 | 调用前探针缺失 + 最终报告对本次 run 全部路由做一次 warning 汇总 | gateway→报告 | ✅ **已修 + 真机点验(2026-08-20)**:报告 `## Routes` 节把 79 次调用聚合成单行 `\| answered \| 79 \| \|`,设置表同样收敛为`\| max_output_tokens \| 393216 \| sent \| 79 \|` 一行一项。 点验前的核实结论保留如下 —— 报告已有 Routes 节(`services/run_report.py:76-86` `routes_section(events)`) | 08-10 两条 | 报告有路由汇总节 |
| E9 | 框架工具(finish_task / update_working_memory 等)从不被宣告,界面答不出「它现在在跑哪个工具」 | 引擎→前端 | ✅ **已修**。**上一版的定性是错的**,已订正:`ToolCallStartedEvent` 一直在发,只是**只对 skill 自带工具**——`CognitiveFlowMiddleware` 排在 `TracingMiddleware` 前面,对自己拦下的工具直接作答、不调 `handler(request)`,整条 wrapper 链连观察者一起被跳过(实测:两个框架工具产出 2 条 `tool_call`、0 条 `tool_call_started`,两个 wrap 钩子探针各 0 次)。**裁决=观察者放到决策者外层**:顺序契约把 `Tracing` 移到第 1 位(参考 Django MIDDLEWARE / Express app.use);配套把「已报告过的调用不再报告」收进相位唯一那个 `StepReporter`,否则同一次调用会被中间件和 agent 节点各报一遍。UI 侧:节点卡在有工具在跑时直接写工具名,按 `tool_call_id` 配对判断「还开着」 | 08-19(N2 派生) | running 节点与 trace 都能显示正在执行的工具名 |
| E10 | **网关造的两种事件不说自己在哪个子图里**,于是同一个相位在报告里被记成两个节点 | 引擎(回调交界)→报告/trace | ✅ **已修 + 真机点验(2026-08-20)**:**判据达成**:新 run 的报告 Nodes 表每一行都是全限定路径(`segmentation/segment`、`event_timeline.extrac/aggregate`),修复前那条与真身并列、跑了 `0×` 的幽灵 `segment` 行**已消失**;同一条 trace 的 **80 条 `llm_call` 全部带 `subgraph_path`**。 点验前的核实结论保留如下 —— **已修(本 PR)**。**发现方式 = 真机点验**,不是用户报障:run `2026-08-20T10-27-18_a98f6ba5` 的报告里 `segmentation/segment` 有它真实的执行,旁边还多出一个跑了 `0×` 的幽灵 `segment` 行。查 trace 得到确切论据——`llm_call_settings` 与 `llm_route_decision` 是该 run 里**仅有的** `subgraph_path: null`,同相位其它事件都写着 `segmentation`。**根因不是报告记账错了**:网关不依赖引擎、自己造这两种事件,而引擎把回调清单**原样**递给网关,所以它们是全程唯一绕过 `_safe_emit_event` 的事件,也就唯一学不到作用域。**修在交界处**:引擎用 `_GatewayEventSink` 把它们复述成引擎自己的事件再从自己的出口发出去;**拒绝**给网关事件加 `subgraph_path` 字段——「图会嵌套」是引擎概念,塞进底座就是上层概念倒灌。顺带让引擎早就声明在 `CallbackEvent` 联合里、却从来没人构造过的那两个类变成真的。设计源 engine OB9 | 真机点验 2026-08-20(非用户报障) | 同一相位在报告 Nodes 表里只占一行 |
| E11 | **跑失败的 run 把花掉的 token 全丢了** | 引擎 | ✅ **代码已实证,待真机**。离线复现:一个 3 轮后被 `[F-v3-agent-exit-control-failed]` 判死的 run,trace 里有 5 次 `llm_call` 合计 55/35,而 `result.metrics` 是 `0/0/0`。**根因**:`run_skill` 的 `except GraphAgentError` 分支直接新造一份 `WorkflowMetrics(wall_time_sec=...)`(`core/runner.py:496-501`),而记账的 `_RunSpendLedger` 建在更里层的 `_run_v030_skill_dict` 里,失败路径够不着它。`_resume_failed_result`(同文件 ~1954)同样。**这不是 E6 的残留**:E6 修的是成功路径的账本位置,这一条是失败路径压根不去读账本。**修法方向**:把事件 sink 的创建提到 `run_skill` / `resume_skill` 这一层,让成功与失败两个出口读同一个账本——涉及 `_run_skill_dict` / `_run_v030_skill_dict` 两个入口的签名,所以单独一个 PR **已修(PR「every exit of a run reports what it spent」,2026-08-20)**:把 run 的花费账本 `_RunSpendLedger` 提到 `run_skill` / `resume_skill` 这一层持有,再往下传给 `_prepare_v030_event_sink`。**为什么必须提上来**:异常会一路展开、越过建 sink 的那一层,所以账本只要建在里层,报告失败的那个出口就永远看不见它——这跟 2026-08-16 那次「run 身份要在任何东西能失败之前定一次」是同一个形状,现在花费也归进同一组决定。**顺带修好了另外两个出口**:`resume_skill` 的**成功**出口原本写死 `from_mapping({"wall_time_sec": wall_time})`,也就是**每一次 resume 成功都报 0 token**;它的失败出口(`_resume_failed_result`)同样新造一份空 metrics。四个出口现在全部只经 `_run_metrics(spend, ...)` 一处读数。**同时删掉** `_CompositeEventSink.spend` 这个回指——没人再需要从 sink 里把账本掏出来,谁要用谁自己持有(显式优于隐式)。**门禁**:新增 `tests/core/test_failed_run_token_accounting.py` 两条,先 RED(`KeyError: 'total_input_tokens'`)后绿;引擎全套 1614 passed、studio backend 1800 passed、ruff/mypy --strict 全绿。 **第二处、也是 app 真正走的那条路(同 PR 追加)**:Studio 的 run worker 根本不经 `run_skill` —— `services/run_manager.py` → `adapters/engine.py:977` 调的是引擎的 `run_artifact`,它落到 `_run_compiled_artifact_graph`,而**那条路当时连失败出口都没有**:异常一路抛到 `run_artifact` 的 `except Exception`(`core/runner.py:1250`),换回一个不带任何 metrics 的请求级错误,`result.json` / `metrics.json` 一个都不写。**真机现场**:run `2026-08-20T10-27-18_a98f6ba5`,`run_ended: crashed`,trace 里 3 次 `llm_call` 合计 **12582 in / 4103 out**,而 `run_metadata.json` 的 metrics 是 `0/0/0`,Trace 面板列表原样显示「105.6s · 0 tokens」;该 run 目录下**没有** `metrics.json`、`result.json`、`final_state.json`。修法:给 `_run_compiled_artifact_graph` 加失败出口,持有账本、写完整 result 制品、返回 `success=False` 的 `WorkflowResult`。这里**捕 `Exception` 而不是只捕 `GraphAgentError`**——不是吞异常:错误载荷照旧上报,只是多带上花费;真正「跑都没跑起来」的失败(没有 artifact root / workspace)根本到不了这里,仍旧返回 `RunArtifactErrorResult`。**中间层与 UI 无需改动**:`run_manager.py:246-265` 的失败分支本来就读 `_result_metrics(result)` 转存进 `run_metadata.json`,列表直接显示它——底层一修,链路自然通到 UI。第三条测试 `test_a_failed_run_started_through_run_artifact_reports_what_it_spent` 先 RED(`result.json` 根本不存在)后绿。 ✅ **真机点验通过(2026-08-20,重建 vendor 后)**。两次真跑都在 app 里发起、都失败、都报出了真花费:①专门造的失败夹具 `always-fails-fixture` run `2026-08-20T15-44-03_98726d7c`,`run_metadata.json` 报 `input_tokens 1815 / output_tokens 261 / total 2076`,与 trace 里那 1 次 `llm_call` 逐位相等,且 `metrics.json` / `result.json` / `final_state.json` **这次都写出来了**(修前这三个文件在失败 run 下一个都不存在);②真实技能 `2026-08-20T15-50-39_9fd32c27` 在 `review` 相位耗尽 nudge 预算而死,283 条事件、21 次调用,报 `116381/25626/142007`,同样与 trace 之和逐位相等。**界面侧**:trace 面板结局行读作「Run failed · 5.6s · 2,076 tokens」,不再是修前的 0 tokens。 | E6 修复过程中离线复现(2026-08-20) | 让一个 phase 必然失败,跑完看 `metrics.json` 的 token == 该 run trace 里 `llm_call` 之和(非零) |
| E12 | **`phase_end` 事件的 `metrics` 字段永远是空的** | 引擎 | ✅ **代码已实证,待真机**。`PhaseEndEvent` 声明了 `metrics: dict[str, Any]`(`callbacks/events.py:84`),而全仓**唯一**一处构造它的地方(`graph_assembler.py:1315`)从不传这个参数,所以它恒为 `{}`。实测 run `2026-08-20T11-30-38_df572662` 的 **42 条** `phase_end` 全是 `metrics={}`,连真发过 4 次模型调用的 `segmentation/segment` 也是。**后果**:「这个节点花了多少」在事件层面无从得知,前端要按相位显示花费只能自己去 trace 里重算——这正是 diagnostics/SSOT 想避免的「每个界面自己算一遍」。**注意这是契约层面的问题**:一个永远为空的声明字段是一句谎话,读者无法从类型上分辨「这个相位没花钱」和「没人填这个字段」。**修法方向**:E6 的 `_RunSpendLedger` 已经按事件累计,让它同时按 `(subgraph_path, phase_name)` 分桶,`phase_end` 发射时填入该相位这一次执行的花费;要么就删掉这个字段。二选一,不留空壳 **已修(PR「a phase does not answer twice」,2026-08-20):按本行给的二选一,选了删除。****真机现场先补齐**:run `2026-08-20T13-14-59_14582c6b` 的 **42 条** `phase_end` 全部 `metrics={}`,包括真调过 4 次模型的相位 —— 与本行建账时用另一次 run 得到的结论一致。**为什么删不填**:一个相位花了多少**已经有答案** —— 它发出的那些 `LLMCallEvent` 之和,正是 `run_report._account_nodes` 逐节点 `tokens in/out` 那一列的算法(`apps/studio/backend/app/services/run_report.py:355-356`),也是 `TracingCallback` 自己在 `_record_llm_call` 里累加的 `phase["input_tokens"]`。填上它等于让同一个问题有第二个答案,而 OB10 前一天刚把这种结构从 run 级总账里拆掉;填这里就是让它在下一层原样长回来。**连带清掉三处转写**:`TracingCallback` 把它写进 trace 载荷的那一行、`PredictTracingCallback` 那条「把 metrics 清零再传下去」的路径(它顺手用这个 dict 捎带 `mocked_source`,现在直接写在相位上)、以及 `LoggingCallback` 那句永远打印 `metrics={}` 的日志。**门禁**:`tests/core/test_phase_spend_has_one_answer.py` 两条 —— 一条钉住契约(相位生命周期事件不得再声明 metric/token/spend/cost/usage 类字段),一条跑一次两相位 run,证明每个相位的花费能从它自己报告的调用里逐相位还原、且与 run 总账同源。设计源记为 OB11。**顺带发现的真缺口另开 [E15]**:`llm_call` 不带执行 id,报告只能按「此刻哪个执行开着」推断归属;同一个 run 里就有 4 次调用发生在同名相位两次执行同时开着的时刻。 ✅ **真机点验通过(2026-08-20)**。两次新 run 的全部 `phase_end` 事件里,`metrics` / `input_tokens` / `output_tokens` / `total_tokens` / `cost` / `usage` **一个都没有**(机械扫描全 trace);与此同时报告的逐节点 tokens 一列照旧正确 —— 夹具 run 的 `impossible` 节点报 `1815/261`,与 run 总账同源。 | E6 根因排查时的事件普查(2026-08-20) | 跑一次 run,`phase_end` 事件上再也没有 `metrics` 字段,而报告的逐节点 tokens 一列照旧正确 |
| E13 | **引擎会连发两个轮次标记,中间没有任何事件** | 引擎 | ✅ **真机实证,待诊断**。run `2026-08-20T13-14-59_14582c6b` 的 `retroactive` 相位,`trace.jsonl` 第 747、748 两条都是 `agent_loop_iteration`,`iteration` 分别为 3 和 4,**中间一条事件都没有** —— 也就是说第 3 轮被宣告开始,却没有产生 prompt、调用、判定中的任何一样。同一段里 `agent_exit_decision` 在第 746、749、752 三处**连报三次 `iteration=2`**,夹在轮次标记 3、4、5 中间。两个数对不上:退出判定说自己在第 2 轮,轮次标记已经走到第 5。**为什么要管**:轮次是 trace 分层的唯一依据(T3 的修法明确「只认 `agent_loop_iteration`」),它一旦自相矛盾,面板画出来的层级就是引擎的错账,不是渲染的错。**还没查**:是 nudge 分支多推了一次计数,还是退出判定读的是另一个计数器。 ✅ **真机点验通过,且原诊断被推翻(2026-08-20)**。**两个计数器并没有各说各话** —— 对不上的是**读者**:建账时用的那次 run 的事件不带执行 id,而 `aggregate`/`extrac`/`settings` 都有**同名相位的两次执行同时开着**,于是按相位名把两串事件混在一起看,轮次编号自然又跳又倒。[E15] 给每条事件盖上执行 id 之后,同一套机械判据在新 run `2026-08-20T15-50-39_9fd32c27`(283 条事件、多相位多轮)上跑出来:**按执行分组后,没有任何一个轮次标记后面是空的,也没有任何一条 `agent_exit_decision` 报出与当时最新轮次标记不同的编号**。教训与 [E15] 同一条:**在能分清「这是谁的事件」之前,任何按顺序推断出来的结论都可能是把两个人的话拼成了一句**。 | T3 真机点验副产物 | 同一相位内,轮次标记的编号连续且每个编号下至少有一个事件;`agent_exit_decision.iteration` 与当时最新的 `agent_loop_iteration.iteration` 一致 |
| E14 | **通往全局输出节点的那一次交接没有任何事件** | 前端(设计源已定) | ✅ **代码已修(PR「the last edge shows what the run produced」,2026-08-20),待真机。****原判「引擎侧缺口」已推翻**:`__global_output__` 这个 id 在 engine、gateway、studio backend 三处各出现 **0 次**,它是 `components/nodes/buildEdges.ts:16` **画布自己铸的伪节点** —— 没有任何引擎迁移通向它,所以「797 条事件里它出现 0 次」不是漏报,是**它本来就不该出现**。**缺口在前端的读法**:这次 run 交出去的那份东西,run 自己在结束时已经报过一次 —— 实测 `run_ended.final_context.phase_outputs.global_synthesis` 恰为 `{story_framework: …}`,与该 skill `GRAPH.md` 的 `io.outputs.required: [story_framework]` 逐字对应 —— 而 `lib/edge-context.ts` 只扫 `input_dispatch`,读不到,于是 dot 退回静态推断。**修法三条**:①终止边改读 `run_ended` 的 `phase_outputs[<带 output 标记的相位>]`;②判别用伪节点 id 本身,不用「是不是输出边界」—— skill 可以声明一个真名叫 `output` 的相位,那是真节点、有真 `input_dispatch`;③**顺带修掉一个按下去必然报错的操作** —— 终止边过去会渲染 `EdgeTamperEditor`,它的 Resume 请求带的 `resumeFromNodeId` 就是 `__global_output__`,引擎不认识;改为只读的 `Run output` 段并标明产出相位。**不新增引擎事件**,理由与 OB11/OB12 同一条:那份值已经报过一次,再发一条携带同样值的事件就是同一个事实两个发布方。设计源写回 `trace-observability/mvp1-alignment.md` F4。 ✅ **真机点验通过(2026-08-20,#918 合并后)**。选中成功 run `2026-08-20T13-14-59_14582c6b`,点 `global_synthesis → Output` 那颗 dot:范围 chip 显示「global_synthesis → Output」,面板给出 **RUN OUTPUT** 段 —— 「What global_synthesis produced, as this run reported it at the end. Nothing runs past this boundary, so there is nothing to resume.」—— 下面是本次 run 真实的 `story_framework`(climax_ranking 各条带真实 event_id 与强度值)。机械核对:页面里 `Tamper` **0 次**、`Resume downstream` **0 次**、「Run the skill to see」**0 次**。截图 `E14-terminal-edge-shows-run-output.png`。 | T6 真机点验副产物 | 跑完一次 run,点通往 Output 的那颗 dot,看到的是本次 run 的真实产出值与来源相位,不再出现「Run the skill to see the real dispatched values」;这条边上不出现 Tamper / Resume downstream |
| E15 | **调用不说自己属于哪一次执行,报告只能按「此刻哪个开着」猜** | 引擎→报告 | ✅ **真机实证**。`LLMCallEvent` 的字段里没有 `phase_execution_id`(实测字段表:`phase_name` / `step_id` / `parent_node_id` / `subgraph_path` / `group_key` / `sub_run_id` …… 没有执行 id),于是 `run_report._account_nodes` 只能**按顺序**把一次调用记在「此刻这个节点哪一次执行是开着的」头上 —— 它自己的注释写明了这个前提:「an `llm_call` carries no execution id of its own, and the trace is ordered」(`run_report.py:297-300`)。**这个前提在并联下不成立**:实测 run `2026-08-20T13-14-59_14582c6b`,`aggregate`、`extrac`、`settings` 三个相位各出现过**两次执行同时开着**,而 `open_execution[node_id]` 只存得下一个;全 run 有 **4 次** `llm_call` 正好发生在这种时刻,它们记在哪次执行上取决于谁最后开的,不取决于谁真的发起了调用。**这是 E6 同一类错误的下一层**:E6 是「跑完回头读状态」漏账,这里是「事后按位置推断归属」错账 —— 两者都源于**让读者去重建生产者本来就知道的事实**。**修法方向**:被计入某次执行的事件(至少 `llm_call` / `tool_call` / `agent_loop_iteration`)自己带上执行 id,报告按事件说的归属记账,不再按开合猜。 **代码已修(PR「a call names the execution it belongs to」,2026-08-20),待真机。**引擎侧:`_EventBase` 增 `phase_execution_id`,由 `_safe_emit_event` 从 `wrap_edge_transition` 的作用域统一盖章(与 `subgraph_path` 同一处),`PhaseStartEvent`/`PhaseEndEvent` 收窄为必填。宿主侧:`run_report._account_nodes` 按事件自报的执行 id 记账,事件没自报才回落到「当前开着的那一个」;顺带修好 `phase_end` 弹错栈的问题 —— 交错执行下它会把**没关的那次**当成关掉,于是凭空多出一次执行。**门禁两条**:引擎侧跑一次两相位 run,断言每条 `llm_call` 自报的执行 id 等于开它那个相位的 `phase_start`,且相位内**任何**事件都不留空;宿主侧喂一段两次执行交错的 trace,断言两次执行各拿各的调用(100/10 与 200/20),而不是一次吞掉两条。修前实测:两条调用全记在第二次执行上,并多报一次执行。设计源记为 OB12。 ✅ **真机点验通过(2026-08-20)**,并在点验中发现并修好了它带出的一处回归。**引擎侧**:新 run `2026-08-20T15-50-39_9fd32c27` 的 **227** 条相位内事件**全部**自报执行 id,一条不落。**归属侧**:这次 run 真的有交错 —— `extrac`/`aggregate`/`review` 各有两次执行同时开着,**6 次调用**正好落在这种窗口里;报告的逐执行列与 trace 逐位对齐(`segmentation/review` 第一次 2 调用 10721/4495、第二次 3 调用 14384/5002;`segmentation/segment` 1 调用 3689/2059 与 2 调用 8183/1494)。**回归**:盖章作用域覆盖了迁移自己的三条事件(`edge_start`/`input_dispatch`/`edge_end`),它们上面的 `phase_execution_id` 指的是**迁移通向的那个相位执行**,而报告把它们记在**迁移**那一行上,于是每条迁移被劈成两次执行(一次 unfinished、一次 ok),全 run 无一幸免。修法:**决定「这是哪一行」的那个 id,同时决定「这是哪一次执行」** —— 带 `edge_transition_id` 的事件属于迁移行,执行就是那次迁移(PR #919)。用修好的代码重建同一份 trace 的报告,每条迁移回到真实次数、全部 ok、wall 时间补齐。 | E12 修复时的机械审计(2026-08-20) | 构造一个同名相位两次执行重叠的 run,报告里每一次执行的 tokens/工具调用数与该执行实际发起的调用一一对应(不再随两次执行的开合顺序漂移) |
| E16 | **finish_task 判词说「业务校验通过」,而这条路径根本没跑业务校验** | 引擎 | ✅ **已修(PR #922)+ 真机点验(2026-08-20)**:重建 vendor 后跑 `always-fails-fixture`,run `2026-08-20T17-13-16_4e586d15` 的 `finish_task_verdict` 原话已是「Accepted the finish_task submission in phase 'impossible': 1 item(s) passed **the finish_task schema check**.」—— 「business validation」四个字在这一行、在展开的三步叙述里(1 判词 / 2 md2json 解析 1 个 '##' 块 / 3 Schema check against 'BusinessSchema_102dbdee': all 1 block(s) passed)都不再出现,而同一次提交仍然被相位 validator 拒绝、run 仍以 `[F-v3-agent-validator-failed]` 收场 —— 判词与结局不再互相矛盾。截图 `E4-E16-verdict-narrates-only-what-ran.png`。原始实证:原始实证:夹具 run `2026-08-20T15-44-03_98726d7c` 里,`finish_task_verdict` 报 `accepted`,原话是「Accepted the finish_task submission in phase 'impossible': 1 item(s) passed schema and **business validation**」;而**同一份提交**紧接着被该相位声明的 `validator.py` 拒绝,run 以 `[F-v3-agent-validator-failed]` 死亡。**代码举证**:发出这句判词的路径,两行之前把校验器写死成空 —— `middleware/cognitive_flow.py:342` `business_validator=None`、`:350` `validator=None`,而判词文本在 `:659`。真正跑业务校验的是另一处:`runtime/state_mapper.py:301-309` 的 `_run_phase_validator`,它把拒绝当**致命错误**抛出。**于是同一个声明的校验器有两个调用点、两种含义**(可重试的反馈 vs 直接杀 run),而 trace 只报了没跑校验的那一处的结论。**裁定 = 判词只说它真检查过的事(schema),业务校验不接进 finish 闸**,三条举证:①闸里那个 `business_validator` 钩子**唯一传过值的调用点**(`business_validator=phase.validator`)随旧执行家族在 #810 一起删掉,活路径 `graph_assembler` 全文零提及 —— 所以它既不是「被主动下线」也不是「接错了」,是**供给方没了而声明留着**;②相位 validator 的契约由 `core/validator_contract.py` 单独定义(`def validate(output: dict, state_slice: dict, **kwargs)`,失败 = `[F-v3-*-validator-failed]` **致命**),而钩子的签名是 `list[dict] -> (bool, list[str])`,出自已归档的 `docs.backup-2026-05-20/archive/v1-reset/PHASE2_DESIGN.md` A1 §2.4 —— 磁盘上每一个真实 skill 的 validator 都不是这个形状,真接上去当场 TypeError;③接进闸等于让同一个声明的校验器有两个调用点、两种含义(可重试反馈 vs 直接杀 run),正是本轮一路在拆的那种「一个问题两个答案」。**修法(PR #922)**:删掉钩子(`CognitiveFlowMiddleware.__init__` 参数、`_run_business_validator`、`_business_stage_validation`、`factory` 两处透传、静态 schema 闸上那个立刻被 `del` 掉的同名参数),连同只为演练它而存在的测试;判词改成「N item(s) passed the finish_task schema check」;叙述同步去掉那一步 —— 判词与叙述必须同源,否则读者会把留下来的那份当成完整版。设计源落 **CG4**(`docs/engine/mvp1/02-mechanism/05-run-inner/03-cognitive/mvp1-alignment.md`),同一处把旧措辞「业务规则错由 `_run_business_validator` 处理」改成指向真正的 `validator.py`。**同族但另一对象,另记 E19**:`invoke_validator_with_contract` 与 `handle_finish_task_tool_result` 也是活路径零调用的死路径,本次不夹带。 | E11 真机点验副产物(2026-08-20) | 同一份提交的判词与最终结局不再矛盾:要么判词只声称它真做过的检查,要么业务校验在finish_task 处执行且拒绝可重试 |
| E17 | **杀死 run 的那个相位,自己一个字都不说** | 引擎→前端 | ✅ **已修(PR #921 + #924)+ 真机点验(2026-08-20)**。原始实证:夹具 run `2026-08-20T15-44-03_98726d7c` 因 `[F-v3-agent-validator-failed]` 而死,但它的 17 条事件里**没有任何一条说 impossible 这个相位失败了**:判词是 accepted、`agent_exit_decision` 是 exit_success、`phase_end` 照常发出且**不带任何结局字段**(`PhaseEndEvent` 只有 `phase_name` / `phase_execution_id` / `context`),唯一的失败信号是 run 级的 `run_ended status=crashed`。**后果是两处同时说谎**:①画布上 `impossible` 与 `Output` 两个节点都是 **Success**(实测 DOM:`impossible` / `AGENT` / `Success` / `5s` / `1 calls`);②run 报告的 Nodes 表里 `impossible` 的 status 是 **ok**。**前端不是病灶**:`utils/run-status-projection.ts:118-141` 已经会认三种失败证据(事件自报 `status: failed/error`、`protocol_violation`、`finish_task_verdict rejected`),本次一条都没出现;而 `run_ended` 被**有意**排除在逐节点判定之外(:179),因为它说的是整个 run。**这是 OB10/OB12 的同一形状再下一层**:生产者知道是谁死的,却不说,读者只能去猜或者猜错。**修法(PR #921)**:`PhaseEndEvent` 增**必填** `status`(`completed` / `failed`),于是「结束了但没说怎么结束」这种状态写不出来;同时把相位生命周期从被包节点的**外面**搬进 `PhaseWrapper` 内部(`state_mapper`,宿主注入 `PhaseLifecycle`),让判定成败的那一步 —— `wrap_phase_output` 里跑的输出 validator —— 落在这次执行**之内**,否则结局永远在结局事件发出之后才产生。宿主侧 `run_report._Execution.reported_failed` 读它,画布侧**零改动**:`run-status-projection.ts:137-143` 早就无条件相信事件自报的 status,连同它的用例(`run-status-projection.derived.test.ts` 「believes an event that reports its own outcome in a status field」)都已在册。设计落在 observability **OB13**。**这次没覆盖的那一支另记 E18**:相位输入没构建出来时,执行根本没有开过。**真机点验(2026-08-20):画布与报告两个读者已经过关,第三个读者还没有。**重建 vendor 后跑 `always-fails-fixture`,run `2026-08-20T17-13-16_4e586d15`:画布上 `impossible` 是 **Failed / 6s / 1 calls**(修前同一个夹具是 `Success / 5s / 1 calls`)、`Output` 是 **Failed**;run 报告 Nodes 表那一行是 `\| impossible \| failed \| 6.66s \| 1× \| 1 \| 1815/269 \| 1 \| 1 \| — \|`(修前是 `ok`)。截图 `E17-failed-phase-shows-failed.png` / `E17-report-nodes-failed.png`。**第三个读者 trace 面板当时还没读**(写着「Phase finished: impossible」、事实表只列 EXECUTION 不列结局),已由 **PR #924** 收掉,并在合并后**当场复验**:同一个 run `2026-08-20T17-13-16_4e586d15` 的 trace 里,`phase_end` 那一行现在是「**Phase failed: impossible**」,展开的事实表是 `OUTCOME failed` + `EXECUTION b29e2df598cf`,时间轴圆点与徽章都走 `bg-destructive`(和 `run_ended`「Run ended: crashed」同一档;同屏的 `phase_start` 仍是 `bg-muted-foreground/50`)。至此**画布 / 报告 / trace 三个读者对同一次执行的结局说法一致**。截图 `E17-trace-says-phase-failed.png`(同图并可见画布 `impossible` = Failed 6s 1 calls、`Output` = Failed)。 | E11 真机点验副产物(2026-08-20) | 跑一次必然失败的 run,画布上失败的那个节点显示失败、其后的节点不显示 Success,报告 Nodes 表同步;判据以事件自报为准,不靠读者从 run 级状态倒推 |
| E18 | **相位输入没构建出来,那次执行一条事件都没有** | 引擎→前端 | ✅ **已修(PR #926)且真机点验通过(2026-08-20)**。构造 `missing-input-fixture`:上游 `first` 把 `note` 声明为**可选**输出且从不写它,下游 `second` 把 `note` 声明为**必需**输入 —— 编译期挑不出毛病,只在运行时散架。**真机三步实测**:①Studio 点 Compile,控制台 `skill_gate ... compile pass`,画布零诊断 —— 前提坐实:这个缺陷编译看不见;②点 Predict **四个节点全绿**(占位数据满足了 `second` 的必需输入)——顺带现场复现了「predict 全绿弱于真跑一次」;③点 Run:`second` 显示 **`Failed 0s`,不是 idle**,Output 显示 Failed。**run 报告 Nodes 表**(`.workspace/runs/2026-08-20T19-55-51_722f59b0/report.md`)有它这一行,状态为 `failed`、耗时 `0.00s`、跑了 1 次、0 次 LLM 调用(它在够到模型之前就死了),Failure 段点名 `[F-v3-runtime-state-mapping-failed] — phase required input fields missing from blackboard: note`。**引擎事件流**(`trace.jsonl`)是最硬的那层证据:`second` 有 `phase_start` **和** `phase_end status=failed`,两帧共用同一个执行 id —— 修之前它一帧都不发,所以画布只能画 idle、报告里根本没有这一行。判据两半(画布显示失败、报告有行且 failed)全部达成 | 修 E17 时的代码审查(2026-08-20) | 让一个 phase 声明一个上游绝不会产出的 required 输入,跑一次:画布上该节点显示失败(不是 idle),run 报告 Nodes 表里有它这一行且状态为 failed |
| E19 | **finish_task 还留着两条活路径零调用的校验死路** | 引擎 | ✅ **已删(PR #927)**。原始诊断:`middleware/cognitive_flow.py` 的 `handle_finish_task_tool_result`(方法本体,以及它体内的 schema 闸调用和 `invoke_validator_with_contract` 调用)在 `src/` 里**没有任何调用点**,唯一的调用者是测试(`tests/runtime/test_gamma2_state_io_red.py`、`tests/middleware/test_beta_validator_runtime.py`);活的 finish 路径走的是 `wrap_tool_call` → `_validate_finish_args`。于是 `invoke_validator_with_contract` 里「拒绝 → 生成 ToolMessage 让模型重做」那条分支永远走不到,而它用的**恰恰是真 validator 的签名** —— 读者极容易把它当成活着的可重试机制,E16 建账时就是这么读的。发现于修 E16 时(2026-08-20),与 E16 同族、不同对象,不夹带进那次改动。**修法(PR #927)= 删,不是接**。两条路可选,选删的理由由 **CG4** 现成给出:业务规则归相位的 `validator.py`、由 `PhaseWrapper` 在相位跑完之后**致命**执行,接进闸里就等于让同一个声明的校验器有两个调用点、两种含义(可重试反馈 vs 直接杀 run)。**调用点消失的时间点已查实**:唯一调用者是 `graph_assembler` 里那个手写工具执行循环,该循环在 WS-E1 迁往 `create_agent` 时被 `3edd12d0`(2026-06-07)整段替换,处理器留在原地 —— 不是有人特意留的机制,是迁移剩下的。**删掉的全部**:`handle_finish_task_tool_result`、`validate_finish_task_with_schema_gate`、`invoke_validator_with_contract`,加上只有它们到得了的 `FinishTaskSchemaGateResult` / `ValidatorRuntimeResult` / `_schema_gate_reject` / `_validator_runtime_reject` / `_finish_task_accept_response` / `_has_strict_output_schema` / `_coerce_output_schema` / `_parse_finish_task_output_payload`,以及只为它们存在的两个测试文件(`test_beta_cognitive_flow_schema_gate.py` / `test_beta_validator_runtime.py`)和 `test_gamma2_state_io_red.py` 里那一条。**逐条核过没有丢覆盖**:活路径(`wrap_tool_call → _handle_finish_task → _validate_finish_args`)查的是同一份 schema、而且做得更多(重复提交识别、判词叙述、io hoist);被删测试钉的两条规则在活路径上各有对应用例 —— `test_cognitive_flow.py` 的 `test_finish_rejects_schema_validation_errors` 与 `test_finish_without_schema_raises_phase_2_a1`;`phase_outputs` 累积那条归 `wrap_phase_output`,由 `test_gamma2_phase_outputs_flow.py` 钉住。守卫测试 `tests/middleware/test_the_finish_gate_has_one_path.py` 逐个断言这十一个符号不存在,并配一条反向断言(活路径三个入口仍在),否则把整个模块删空也能让前一条变绿。设计落在 cognitive **CG5**。**判据对照**:本行验收判据写的是「要么有真跑覆盖的调用点,要么连同测试一起删除;不留第三种状态」—— 走的是后者,且这是**纯删除**,没有任何运行时行为改变,所以不需要真机点验。 | 修 E16 时的代码审查(2026-08-20) | `src/` 内对这两个符号要么有真跑覆盖的调用点,要么它们连同测试一起删除;不留「只剩测试在用」的第三种状态 |
| E20 | **run 报告把「里面出过错」当成了「它是这么结束的」** | 中间层(run 报告) | ✅ **本轮发现并修掉,证据用真跑记录复算**。**发现方式**:E18 点验时对同一次 run 的三个面读出两个答案 —— `trace.jsonl` 说 `first` 的 `phase_end status=completed`、画布画 Success,而 run 报告 Nodes 表给它 `failed`,**同一行的 `corrections` 列还写着 `1`**。**机制**:`app/services/run_report.py:74` 原本写 `if self.reported_failed or self.errors` —— 而 `:413-418` 把一次**被打回、随后改对**的 `finish_task_verdict`(verdict=rejected)记进 `errors`。被打回去改正是纠错循环**存在的目的**;若它算失败,则任何用过纠错循环的相位都成了失败相位,这个词就不再区分任何东西。同类还有 `builtin_subagent_fallback`,它自己的注释写的是「it finished — on a lesser path」。**讽刺的是意图早就写下来了**:`test_the_report_believes_a_phase_that_failed.py` 的 docstring 明写第三条规则「a rejected submission the model then fixed is still not a failed phase」,却从没有一条测试钉它,代码则反着做。**修法**:状态只由相位自己说的结束方式(`reported_failed`)决定;`errors` 退回它本来的职责 —— 出现在 Failure 段里。真正会掐断 loop 的 `protocol_violation` / `loop_detected` 不需要这一半兜底:E17/OB13 之后 `PhaseEndEvent.status` 是**必填**(`callbacks/events.py:114` 原文「Required, so "a phase ended without saying how" is not representable」),被它们掐死的执行本就以 failed 收尾。**顺带订正两个过期夹具**:两条老测试造的 `phase_end` **不带 status**,即造了一个引擎已经发不出来的事件。**验收证据**:拿 E18 那次**真跑**的 `trace.jsonl` 用修好的代码重算报告 —— `first` 由 `failed` 变 `ok`、`second` 仍是 `failed`,而 Failure 段**两条都还在**(引擎错误码 + 那次被打回的提交)。什么都没藏起来,只是状态列不再回答它没被问到的问题 | 本轮 E18 真机点验中发现(非用户报障) | 一个提交被打回、改对后完成的相位,在 Nodes 表里是 `ok`,而那次打回仍出现在 Failure 段 |

## 1.1 画布运行时观测

**1.1.1 node**
| # | 模块问题 | 层 | 状态 | 出处 | 验收判据 |
|---|---|---|---|---|---|
| N1 | 状态只有小胶囊换字,整卡无状态表达;裁决呈现方式:状态灯闪烁+状态标签(idle/running/success/failed)+边框虚线流动+有空间加运行时间,找成熟参考 | 前端 | ✅ **已修 + 真机点验(2026-08-20)**:本轮 run 中运行节点同时带 `animate-pulse` 状态灯、`studio-running-dash-frame` 虚线框、`Running` 标签与已耗时,四者同屏。 点验前的核实结论保留如下 —— **#875 已合**:状态灯(复用 Settings 路由灯)+ 标签(Idle/Running/Success/Failed)+ running 卡片虚线流动边框(图案与 edge 流动线同源)+ 运行时间(`deriveNodeRuntimes` 投影,running 逐秒推进、终态冻结);设计源 canvas F3 ② 同 PR 改写 | 08-19 Q3 | 扫一眼画布即知进度 |
| N2 | running 节点不显示"正在干什么"(第几次调用/工具/已耗时) | 前端+引擎 | ✅ **已修 + 真机点验(2026-08-20)**:本轮再证,且证到嵌套第三层:`event-extraction` 里的 `aggregate AGENT` 显示 `Running 27s update_working_m…`——**当前工具名就在卡上**。 点验前的核实结论保留如下 —— **三样全齐**:已耗时 #875、第几次调用 #896、**当前工具本 PR**。#896 当时写的「当前工具做不到」是**错判并已订正**——`ToolCallStartedEvent` 一直在发,只是被 `CognitiveFlowMiddleware` 抢答的工具收不到(详见 E9)。现在:有工具在跑时卡片那一行直接写工具名,工具返回后回到 `Call N`,终态回到 `3 calls`;「还开着」按 `tool_call_id` 配对判断,不是「最近听说的那一个」 | 08-19 视觉裁决 | running 卡上有活动说明 |
| N3 | run 结束动画/running 态残留不清("老生常谈"×3);要求组件建立"状态↔显示效果对照表" | 前端 | ✅ **已修 + 真机点验(2026-08-20)**:**判据达成**:run 结束 **11 秒**后,画布内按 class 名与按 computed `animation-name` 两种查法都是 **0 个运动元素**;六个节点全部终态并带耗时与计数。 点验前的核实结论保留如下 —— 对照表已存在且有测试:`utils/run-status-projection.ts:38-45` `NODE_STATUS_AT_RUN_END`,`:188-193` 用 run verdict 无条件关掉所有 running。残留风险见 P1(两条终态通道都没送达时仍永久 running) | 08-04②;08-09⑤;08-14⑥ | run 结束 10s 后画布无任何运动元素 |
| N4 | 完成节点留痕(耗时/调用次数),run 后画布=结果地图 | 前端 | ✅ **已修 + 真机点验(2026-08-20)**:run 结束后画布是一张结果地图:`segmentation Success 58s 3 done` / `event_timeline Success 5m 58s 2 done` / `story_analysis Success 5m 56s 4 done` / `global_synthesis Success 4m 18s 4 done`。 点验前的核实结论保留如下 —— **已合**:耗时 #875,**调用次数**本 PR 与 N2 同源同修——同一个 `deriveNodeActivity` 投影,running 读作序数、终态读作基数。原先标"待裁"的那一项在此自决:它和 N2 的"第几次调用"**是同一个事实**,分两处裁会得到两个数;而"run 后画布=结果地图"这条本行自己的判据,不给调用次数就达不到。**iterate 相位按全部执行累加**(时长仍只取最后一段),否则一个跑了 N 个 item 的节点会把工作量少报 N 倍 | 08-19 讨论中方案 D | run 后每个跑过的节点都留下耗时与调用次数 |
| N5 | **run 的结束判词前端已经拿在手里,却在投影时丢掉,改去再问一次后端**:那一问失败就没有第二条路,徽标停在 running | 前端(gate 投影 → 画布徽标) | ✅ **已修,真机点验通过(2026-08-21,PR #945)**。**修法**:`finalize-run` 效果带上 gate 自己说的判词,`runVerdict` 从两条通道变三条——落章记录 > 终态 gate > 流式 `run_ended`,排序就是各通道能知道多少(记录带全套词汇,分得清 cancelled 与 paused;gate 由写记录的同一步发出,措辞粗但同权威;流只知道 worker 停了)。gate 词表到 verdict 词表的翻译只写一处(`runVerdictFromGateOutcome`:`pass→success`、`stopped→cancelled`),**不把 gate 的词直接塞进决定节点徽标的槽**。判词随 `viewedRun` 三元组变四元组传到画布节点、edge 分段与 trace 面板;**历史视图不带**(旧 run 的状态是它自己记录的事)。回读失败时 run 列表行由 `projectRunStatus` 落到同一个判词——只动 status,token 汇总与报告路径是记录独有的,在这里编出来是同一个缺陷掉个方向;文案也改成据实的「Run ended — could not read its details」,不再把「读不到详情」说成「还在跑」。设计源 F6 同 PR 改写(三通道排序 + 铁律补上「回读失败也不是理由」,哈希已重钉)。**顺手修了一处测试替身缺陷**:`useSkills` 的 mock 每次渲染重建 `skillDetail`,而 SWR 不会——`skillContentSignature` 因此每帧变身份,任何挂在它上面的 effect 会无限重入。 **真机点验(2026-08-21)**:故障注入放在**网络边界**而不是页面 JS——CDP `Fetch` 域按 URL 模式拦截,只让路径以 `/runs/<id>` 结尾的 GET 失败,其余原样放行(脚本只失败了**一次**请求:`/api/skills/p1-slow-fixture/runs/2026-08-21T07-16-19_a471b58d`),被验代码一行未动。跑一个 4 秒会自然结束的 skill:0-5s 画布 `burn` 是 `Running`,**6.1s 三个节点一起收敛成 `Success`**——此刻落章记录这条通道是断的,能给出判词的只剩 gate;6.5s 弹出 `Run ended — could not read its details: Backend unavailable`;trace 面板同时给出 `Run succeeded 4.0s`(截图 `N5-01-settled-from-gate.png`、`N5-02-run-list-terminal.png`)。旧代码在这一步只会停在 `running` 并弹「Could not read archive status」。 | 台账核实 2026-08-21(P1 真机点验后顺链核到底;非用户报障) | gate 事件自带的 pass/fail 判词直接进投影;run 详情拉取失败时徽标仍收敛成终态并说明原因,不停在 running(**已实测**:详情 GET 被网络层拦死,画布仍收敛成 Success 并说出「读不到详情」) |

## 1.2 trace 面板

| # | 模块问题 | 层 | 状态 | 出处 | 验收判据 |
|---|---|---|---|---|---|
| T1 | 步骤流时序倒置:running 时流式输出画在 prompt 段上方,prompt 被压到最底 | 前端 | ✅ **本 PR 已合**:按台账当时给出的正解做了——流式思考/回答并入 `LlmFlowBody` 的 Thinking/Answer 位次(设计 D1 的执行顺序),调用落定后由 `llm_call` 终值接管、流式副本消失;折叠行没有序列可言,仍单独显示流入文本。顺序已加测试锁死 **真机点验 2026-08-20(run `2026-08-20T13-14-59_14582c6b`,CDP 真窗口)**:展开一次 `llm_call` 步骤,`data-trace-flow-entry` 八段的**渲染顺序**逐条读出来是:①`Loaded — phases/segment/SKILL.md` ②`Wrapped — cognitive/v0.3.0` ③`Filled in` ④`Sent — System` ⑤`Sent — User` ⑥`Thinking` ⑦`Answer` ⑧`Tool calls`。判据「装载 prompt→组合→思考→回答」达成:prompt 在最上,流式思考/回答在最下,不再倒置。截图 `T5-view-full-text-opens-editor.png` 同框可见。转 ✅。 | 08-19×2 天,痛斥 | 时序=装载 prompt→组合→思考→回答 |
| T2 | prompt 装载展示不全:要"SKILL.md 原文/模板/组合后/system prompt/user prompt"分段人话 | 引擎+前端 | ✅ **已修**:展开的 LLM 步骤按执行顺序给出四格——`Loaded — <相位文档路径>`(可点开真文件)、`Wrapped — cognitive/v0.3.0`(模板全文)、`Filled in`(变量)、`Sent — System/User`(一条消息一格、正文原样,取代从前那坨 `JSON.stringify`)。标题写**动作**不写数据类别,因此不违反 2026-08-13 D1「废除 TEMPLATE/VARIABLES 容器」。设计源 trace-observability F5 已改写 | 08-19;08-14① | 一步内五段齐 |
| T3 | iteration 区分:机制在(Iteration N 分隔条),部分 phase 事件无轮次标记导致不显示 | 引擎→前端 | ✅ **本 PR 已合,根因不是"缺标记"而是"两个数被当成一个"**。实测(引擎批处理夹具)每个 batch item 花 2 个 ReAct 轮次却发了 **3 次** LLM 调用——被驳回的 finish_task 不触发新 `before_model` 就重试——所以 `prompt_captured.loop_index`(数调用)与 `agent_loop_iteration.iteration`(数轮次)本就是两件事,而前端 `trace-steps.ts` 拿前者当后者的等价数据源。**两处同修**:①引擎 `LLMProviderChatModel` 的调用计数器是个 per-instance 计数,而 chat model 与中间件链一样每个 phase 节点只建一次 → 跨 batch item 一路涨到 6,改为按 `agent_invocation_key()` 记账(`next_invocation_call_index`,与 `ExecutionControlMiddleware` 2026-08-15 那次修复同一条既有配方);②前端轮次分层**只认 `agent_loop_iteration`**,往后顺延,删掉 loop_index 兜底;首条轮次标记之前的调用保持平铺,不猜。设计源同 PR 更正(原文并列写了两个数据源) **真机点验 2026-08-20(run `2026-08-20T13-14-59_14582c6b`)**:该 run 有 **16 个**带循环的 phase,画布 DOM 里 `data-trace-iteration-header` 共 **73 条**,每个循环 phase 都拿到了分隔;判据「全部循环 phase 有分隔」达成,转 ✅。**一个必须说清的观察**:`retroactive` 的 trace 里有 1..6 六个 `agent_loop_iteration`,面板只画了 1、2、4、5、6 —— 缺的第 3 条不是漏画:trace 里第 747、748 两条标记**紧挨着**,中间一个事件都没有,而分隔条只在「下一行步骤的轮次变了」时才画,所以第 3 轮没有任何自己的步骤可归属。这正是本行修法写明的「不猜」。**引擎为什么会连发两个轮次标记**另开 [E13] 追。 | 08-19 Q11 | 全部循环 phase 有分隔 |
| T4 | 非 LLM 步骤展开=原始 JSON 兜底,"只有结果没有过程"(重复≥2 次) | 前端 | ✅ **本 PR 已合**。核查推翻了本行原先的判断「依赖 E4」:D4 要求的引擎自述**大部分已经在发**——`md2json` 的逐阶段叙述走 `FinishTaskVerdictEvent.details`(`cognitive_flow.py` 的 `story`),`loop_detected`/`tool_error_handled`/`tool_history_repaired`/`runtime_input_injected`/`nudge` 各自带整句 `message`。**缺口在前端**:`machineryNarration` 只读 `details`/`errors`/`violations`,于是所有只带 `message` 的决策事件一路掉进 `JSON.stringify`。三处同修:①自述并入单句通道(`message`/`warning`/`reason`)与单数 `error`;②新增 `eventFacts` 逐类型事实表(transition / dispatched / synthesized・dropped / removed / …),长值仍归文本井;③`GenericPayload` 换成**明确告警** `UnreadEventBody`(`data-trace-unread-event`)。**并加了防退化门禁**:`utils/engine-event-types.ts` 镜像引擎事件联合体,后端测试`test_engine_event_types_are_mirrored.py` 两边对读,前端断言每一项都有读法——引擎新加事件而没人给读法,CI 直接红。设计源 F12 **真机点验 2026-08-20(run `2026-08-20T13-14-59_14582c6b`)**:把面板里 **471 条**步骤**全部展开**(逐条 `click()`,展开后 `aria-expanded=true` 计数 471/471),再数兜底渲染:`[data-trace-unread-event]` **0 个**;其中 **346 条**渲染的是语义事实表 `[data-trace-facts]`,其余是 LLM / 工具步骤各自的专用版式。该 run 共 **22 种**事件类型,判据「无 GenericPayload 兜底可见」在全类型全步骤上达成,转 ✅。 | 08-14②;08-19 Q9 | 无 GenericPayload 兜底可见 |
| T5 | 长文本:"View full text"按裁决应打开常规编辑器而非自造 modal | 前端 | ✅ **已实现**:`components/trace/TraceText.tsx:12-21,44-58` → `Workspace.handleFileOpen`(:1083-1096)→ 工作区编辑器,打开的是只读虚拟文档 `trace/<slug>.json`;测试 `TraceText.test.tsx:84-91` 同时断言 payload 与"无 `[role=dialog]`"。**两个会让用户以为没做的触发条件**:①按钮只在文本溢出时出现(`ui/text-well.tsx:27-34` 用 `scrollHeight>clientHeight` 量,被折叠父容器隐藏时两值都是 0);②无 workspace context 时按钮不渲染 **真机点验 2026-08-20(run `2026-08-20T13-14-59_14582c6b`)**:面板里共 **489 个**「View full text」。点第一个,`.monaco-editor` 从 0 变 1,落点在 `SECTION.studio-editor-overlay` —— 即工作区常规编辑器,标题「Prompt template」、语言 plaintext、带行号只读全文;**不是自造 modal**:页面上唯一的 `[role=dialog]` 是那个未打开的 Settings,`dialog.contains(editor)` 为 false。判据「点开=编辑器」达成,转 ✅。截图 `T5-view-full-text-opens-editor.png`。 | 08-14③→08-15 修订三条 | 点开=编辑器 |
| T6 | edge dot 面板:run 成功后仍显示静态推断+"Run the skill to see real dispatched values" | 前端(E1 引擎侧已就绪) | ✅ **#878 已合,三处全修**:①`lib/edge-context.ts:27-30,52` 的 `upstreamIncludes` 要求 `from_phases.includes(fromPhase)`,而首个 phase 的 `from_phases` 是 `[]` → 根边恒不匹配 → 回落静态推断(与 `trace-scope.ts:59-63` 的正确写法分裂,见 IO1);②`Workspace.tsx:2255` 给画布喂的是 `runStream.events`,而 trace 面板读 `viewedTraceEvents`(:738)——**回看历史 run 时画布 dot 永远拿不到真实值**;③`utils/trace-scope.ts:27` 的 `EDGE_OP_TYPES` 不含 `edge_start`/`edge_end`,选中一条 edge 后**它自己的分段步骤被范围过滤掉了**。①→`edge-identity.ts` 一处判定(空 `from_phases` = 根迁移);②→`Workspace.tsx` 的 `viewedRun` 三元组,画布与 trace 面板同源;③→`EDGE_SEGMENT_EVENT_TYPES` 收编两个分段事件 **真机点验 2026-08-20(run `2026-08-20T13-14-59_14582c6b`):五条边里四条达成,第五条不达成,本行留 🔎**。①**达成**:点 `segmentation → event_timeline` 的 dot,面板给出 Scope 标签、本次 run **真实派发的 `segmentation_result` JSON**(章节正文原样在内),以及两个操作 —— `Tamper`(改下游 resume 输入)与 `Resume downstream`;页面上不再出现 `Run the skill to see`。`__global_input__→segmentation`、`event_timeline→story_analysis`、`story_analysis→global_synthesis` 三条同样是run 态措辞「Click to inspect flowing context」。②**不达成**:`global_synthesis → __global_output__` 这条**终止边**仍旧是「INFERRED BLACKBOARD FIELDS … Run the skill to see the real dispatched values.」,尽管 Output 节点本身显示 Success。**根因不在前端**:这次 run 的 797 条事件里,`__global_output__` 出现 **0 次** —— 所有 edge 事件都按「到达的那个 phase」(`to_phase`)记账,而通往全局输出节点的那一次交接**根本没有事件**,面板无从退化以外的选择。当时判为引擎缺口并另开 [E14];**该判断已推翻** —— `__global_output__` 是画布伪节点,引擎从不知道它,本来就不会有事件指向它,该读的是 run 自己报的 `run_ended.final_context.phase_outputs`。E14 已按此修好前端读法,本行随它一起复验。截图 `T6-real-edge-shows-dispatched-values.png` / `T6-terminal-edge-still-inferred.png`。 ✅ **三种边全部真机点验通过(2026-08-20)**。①根边 `Input → segmentation`:面板给出本次 run 真实派发的 chapter 正文;②普通边 `segmentation → event_timeline`:真实 `segmentation_result` + Tamper + Resume downstream;③**终止边 `global_synthesis → Output`**:由 [E14] 补上,现在显示 RUN OUTPUT 与真实产出值,不再回落静态推断。三条边上「Run the skill to see the real dispatched values」均已消失。 | 08-14④;08-19 Q4-2+截图 | 跑完点 dot 见真实值与操作 |
| T7a | 结局卡(Run succeeded)在任何 node/edge 范围收窄下都渲染,与时间线脱节 | 设计源→前端 | ✅ **已修 + 真机点验(2026-08-20)**:Scope 收到 `story_analysis` 后事件只剩该子图四条,面板头部的绿色成功勾**不变**;DOM 读数 `data-trace-verdict` 仍为 `success`、`data-trace-outcome` 变 null。 点验前的核实结论保留如下 —— **本 PR 已合**:设计源先改(`trace-observability/mvp1-alignment.md` F3 的「run 级派生不受范围影响」一刀切措辞被推翻),判据改为**这个派生长在哪里**——身份区(顶条 run_id + 状态徽章,F8)属于运行本身、不受范围影响并加了 `data-trace-verdict` 供锁定;列表区属于当前取景,`TracePanel` 的 `outcome` 在有 scope 时直接为 null。`degradedRouteCount` 一并改读 `scopedEvents`,理由是它**可操作**——点它就是往当前列表里搜,点开找不到的计数比没有计数更坏 | 08-19 Q5 | 收窄范围时不出现 |
| T7b | Open run report 用系统默认程序打开,裁决=app 内编辑器/阅读器(重复≥2 次) | 前端+后端 | ✅ **已修 + 真机点验(2026-08-20)**:点 Report 后中栏以标题 `Run report`、格式徽章 `markdown` 的编辑器页打开,不走外部程序。 点验前的核实结论保留如下 —— **本 PR 已合,两处同修**:`TraceOutcomeRow` 与运行列表行 `TimelinePanel.RunReportLink` 都改走 `onFileOpen(runReportOpenRequest(path))`,以只读 markdown 文档落在工作区编辑器(与 T5 同一条通道);`openLocalPath` 在这两处删净。**根上还改了后端**:`RunMetadata.report_path` 由绝对路径改为**工作区相对路径**(`.workspace/runs/<id>/report.md`)——编辑器按相对路径读文件,绝对路径只有交给 OS 时才用得上,而那正是被推翻的做法 | 08-19 Q6 | 点开=app 内打开 |
| T8 | search:多轮返工仍不符("直接用 shadcn 模板组件不要改";"说了不知道多少次") | 前端 | 🧩 **组件壳子没跑偏**:`TraceSearchBar.tsx:26-31` 100% 用本地 shadcn `ui/input-group`,零样式覆写。用户不满的是**行为**。已列出 11 条行为限制(零事件时搜索框整个不挂载 / 筛选行只在聚焦时可见 / "n filters on" 计数在聚焦时被隐藏 / 匹配面是整个事件 `JSON.stringify` / 纯子串无正则无字段限定 / 无高亮无命中计数无跳转 / 过滤事件而非步骤导致步骤被拆成孤立完成态 / 无防抖 / route-issues chip 与搜索框共用 searchTerm 会覆盖用户输入 / 搜到 0 条时结局卡仍在 / 图标尺寸被测试禁止覆写)——**需与用户逐条对账后一次修对,不再猜** | 08-08④;08-09⑩;08-19 Q8 | 用户点头 |
| T9 | 每个 llm_call 上重复渲染 Probe failed 黄块;"3 route issues" chip 语义 | 前端 | 🔎 **两处分别已合**。①chip:#887 改读 `scopedEvents`——它是**可操作**的(点它就是往当前列表里搜),计数与点开能看到的必须一致。②黄块去重(本 PR):一条降级按「结果+route+endpoint+原因+状态码」认身份,**第一次完整解释,之后只报一行"又一次 + 累计第几次 + 原因见上"**(投影层 `TraceVerdict.occurrence`,渲染层只选版式)。实测依据 run `2026-08-19T06-58-15_179d1440`:一个超时 endpoint 在连续三个 LLM 步骤上各画一整块黄框。参考 syslog 的 `last message repeated N times`,差别是不要求连续(降级会被正常步骤隔开)。`answered` 不折叠——「这次由谁应答」是每次调用各自的事实。设计源 F10 **真机点验 2026-08-20:本 run 没有现场,维持 🔎**。run `2026-08-20T13-14-59_14582c6b` 的 471 条步骤全展开后,页面里 `Probe failed` **0 次**、`route issue` **0 次**、`degrad` 字样 **0 次**、`[data-trace-route-repeat]` **0 个** —— 这次 run 的路由全部正常应答,没有任何降级可供折叠。判据「降级信息一次一处」需要一次**探针失败或超时**的 run 才验得到,留待下一次现场。 **本轮(2026-08-20)仍未取到现场**:这一轮真机验证走的是 Settings 走查与 copilot 审批,没有触发探针失败或超时的 run,折叠逻辑依旧无从当场观察,维持 🔎。 **真机点验 2026-08-21:①已验,②确认在本机造不出现场,维持 🔎**。**①chip 已当场量到**:一次带降级的 run 上,chip 写「2 route issues」,与该 run 里非正常路由判词的条数(`Route used` ×1 正常、`Runtime settings refused` ×1、`Route failed` ×1)一致;点它确实把下面的列表筛到这些步骤上,计数与点开能看到的对得上。**②没有现场,且原因是结构性的,不是运气不好**。想要折叠,得让**同一条降级在一次 run 里重复出现**。逐条查过四种降级各自能不能重复:`dropped_rejected_settings` 的 `refused_settings` 是`call/chat_model.py` 一次调用内的局部量,**跨调用不记忆**,所以一条路由若稳定拒某个设置,本该每次调用都报——但**从界面根本设不出这样的设置**:Properties 的 Custom model params 会按路由已探明的能力上下界锁死,本机真窗口实测 `thinking` 开关 `disabled=true`、`max output tokens` 输入框 `disabled=true`,只有 `temperature` 可改(设计见 `registry/bounds.py` 头注:超出范围的值「spends a round trip to be told something the route had already said about itself」)。`failed_terminal` 直接终结 run,只可能出现一次;`probe_failed` 会 `_mark_down` 该路由,后续调用不再选它;`retried_same_route` 被 `retry_same_used` 限成每次调用至多一次,要重复得靠一条**时好时坏**的路由。结论:**在配置正常的路由上,一次 run 里每种降级对每条路由至多发生一次是这套设计的既定行为**,折叠现场需要一条真的抖动或真的拒设置的第三方路由,造不出来就照实记,不硬凑。**本轮离得最近的一次**:侧跑 `2026-08-21T08-32-45_8055ae4e` 连着出了两条同路由同 400 的判词,但**结果不同**(`dropped_rejected_settings` → `failed_terminal`),按「结果+route+endpoint+原因+状态码」的身份定义本就不是同一条抱怨,`[data-trace-route-repeat]` 因此为 0 个——**这正是身份定义在起作用**,不是漏折叠。**折叠行为本身有单测钉着**:`utils/trace-steps.test.ts:343` 「numbers each repeat of an identical degradation」断言同一身份第二次拿到 `occurrence: 2`,紧邻两条断言换了身份就从 1 重新数;渲染层 `TraceStepRow.tsx:480-503` 只按 `occurrence > 1` 选版式,出 `data-trace-route-repeat` 那一行。**下次要验它,先准备一条会抖的路由**(例如指向一个自建的、按次数交替返回 500 / 200 的本地端点),而不是再去翻正常 run。 | 08-19 截图 | 降级信息一次一处 |
| T10 | run 级 LLM 降级计数 chip 缺;run_id 概要中间层缺 | 前端 | ✅ **本行不成立,关闭**:前半已 live(`TracePanel.tsx:561-586`,设计源亦标 live);后半**已被 D1 裁决作废**(`03_regions/timeline/mvp1-alignment.md:162`「F3 已被 D1 作废,Trace 视图本身就是完整 trace」)。遗留待办:`trace-observability/mvp1-alignment.md:44` 还挂着"仍 target-design:run_id 概要中间层",与 timeline 那份矛盾,删掉 | MVP1 对账 gap(F2/F7) | — |
| T11 | predict 完 timeline 空白 | 前端 | ✅ **已修 + 真机点验(2026-08-20)**:**判据达成**:predict 跑完 timeline 有 **294 条**步骤、31 个作用域,不是空白。 点验前的核实结论保留如下 —— **已实现**,2026-08-09 那次修在案且注释直接引用了这句原话(`services/predictor.py:172-185`);predict 开跑即广播 gate 带人进 trace(`:95-114` + `gate-state.ts:99-107`)。**唯一残留复现路径**:`predictor.py:108` 的 `transport == "in_process"` 判定——切到 `http_loopback` 则不注册瞬态 run 也不推事件,面板停在 "Waiting for run events" | 08-09① | predict 后 trace 有内容 |
| T12 | 跨 run 事件串台 | 前端 | ✅ **已合,两层都已坐实**。①流层面本就干净:`hooks/useRunStream.ts:39-44` 渲染期同步重置、`:63-70` 每次写入都要报出 subject。②真正缺的是**把断点画出来**(本 PR):resume 复用同一个 `run_id`,两簇相隔很远的时间戳同屏,而 `resumed` 事件按普通步骤渲染、一路掉进 `GenericPayload` 原始 JSON——唯一看得出"这里断过"的线索只剩时间跳变。现改为**接缝行**(`TraceResumeSeam`,`data-trace-resume-seam`):左右细线 + 一枚 chip 写清「这是 resume / 从哪个相位接上 / 人回答了什么 / 何时」,不展开、不参与相位缩进——与列表另一端的结局行同理(里面没有执行)。设计源 F11 **真机点验 2026-08-20:只验到一半**。同一次 run 内不串:`df572662` 的 544 条步骤时间戳**全部**落在 11 点。但「切到另一次 run」这半条没做成(点 8-19 那行没切过去),所以不标 ✅。 **真机点验 2026-08-20(run `2026-08-20T13-14-59_14582c6b`)**:面板一次装下 471 条步骤,对应的 `trace.jsonl` 里 `run_started` **1 条**、`run_ended` **1 条**,全文件出现过的 run/thread id **只有这一个**(`run_id` 与 `thread_id` 各 2 次,值相同)。判据「一个 trace 只含一个 run」达成,转 ✅。 | 08-09;08-19 截图 | 一个 trace 只含一个 run |
| T13 | **run 列表被乐观投影顶掉,历史整段不显示** | 前端 | ✅ **已修(PR「a projection adds to a list it cannot invent」)**。**发现方式 = 真机点验**,不是用户报障。**现场**:打开 skill、在 Trace 面板从未挂载过的情况下按一次 Predict,再打开 Trace——表头写着 **`1 run`**,而磁盘上有 32 次 run;点「Refresh run list」全部 32 条立刻回来,所以**数据一直都在,只是没被加载**。**根因不是渲染**:`Workspace.tsx` 在 run 一开始就调 `projectRun`,写进 Trace 面板要读的那个 SWR 键;面板还没冷加载时,投影拿到的 `current` 是 `undefined`,于是它**凭空造了一份完整列表** `{total: 1, runs: [run]}`。这份合成列表是用 `{revalidate: false}` 写进去的,而 `STUDIO_TRUTH_SWR_CONFIG` 把 `revalidateIfStale` / `OnFocus` / `OnReconnect` 全关了——**那次本该带回另外 31 条的加载,从此不会发生**。这正是 SSOT 读取原则反过来:原则说「一个缓存键可以在owning scope 首次需要时冷加载一次,**所有消费者必须共用那一次在飞的请求/结果**」,而一个自己写一份列表的消费者不是真相的只读副本,是第二个只认得一条 run 的真相。**修法**:投影只往**已加载**的列表里加,遇到没加载的就**原样不动**——这不会丢掉那次 run,因为投影发生时它早已存在于服务端,冷加载会连它一起带回来。同文件上一个函数 `projectDeletedRun` 本来就是这么写的。钉住旧行为的那条既有测试(`can seed the shared run-history cache without subscribing to the run list`)本意没错(别丢掉没人看时开始的 run),错在它断言的形状,已按正确形态改写并在文件里写明理由 | 真机点验 2026-08-20(非用户报障) | 打开 skill 后先跑一次 predict/run,再打开 Trace,列表条数等于磁盘上的 run 数 |

## 1.3 run 汇总

| # | 模块问题 | 层 | 状态 | 出处 | 验收判据 |
|---|---|---|---|---|---|
| R1 | run 报告漏答 08-08 规格里的五个问题 | 后端(run 报告投影) | ✅ **五项全修完**(②④⑤ 见 #902,①③ 本 PR)。**①input files**:从代码字体的路径改成**能点开的相对链接**(绑定路径相对 `.workspace`,run 目录在 `<workspace>/runs/<id>`,所以是上两级;**推不出这个布局就不给链接只给路径**——点不开的链接比纯文本更坏);并且不再只读快照的**声明绑定**,补上引擎实发的 `input_file_injected`(它挂在边上,所以「送给了哪个节点」一并报出)——只报声明的话,答案在 run 开始时为真、到结束就过期。**③llm vs 结果链接**:候选 side-run 的报告现在链回**它对照的那次 run** 的 `report.md`。这一条本来是**数据缺口**不只是渲染缺口——side-run 此前根本没记自己对着哪次 run 跑,本 PR 给 `RunMetadata` 补 `compare_base_run_id` 并在 `_spawn_side_run` 写入;没记到的照实说「没记录」,不猜 run id。**基准 run 那一侧仍无 compare 节**,且在报告可重生成之前**不可能有**:compare 发生在基准 run 结束之后,而报告是终态写一次的纯投影;让它去扫兄弟目录会使「这份报告说什么」取决于此后又跑了什么(RUN_EXECUTION-10 已写明这条边界)。**结构缺陷也补完了(#905)**:报告此前**没有重生成入口**,而 RUN_EXECUTION-5 明说它是可随时重生的纯投影——于是历史报告永远停在写它那天的渲染逻辑上,渲染器每改进一次只有新 run 受益。现在**打开报告的那一刻先重渲染**(`POST /api/skills/{skill}/runs/{run}/report`,run 列表行与 Trace 末尾两个入口共用同一个打开动作)。**明确拒绝**渲染器版本号方案:本仓的 vendor 快照已经证明「改了源码还要记得再做一步」会被忘掉,不再添第二个。**没结束的 run 拒绝渲染**(`RUN_NOT_CONCLUDED` 409)——`report_path` 是从文件在不在推出来的,给在跑的 run 写报告等于让列表给它挂上一个几分钟后就自相矛盾的报告入口。重渲染失败仍打开已有那份并 toast 说明,不静默吞。设计源 RUN_EXECUTION-8/9/10/11(哈希已重钉) **真机点验 2026-08-20:五项里四项已当场看到**——①`## Inputs` 第 36 行是可点相对链接 `[import_files/sample_input/sample_input.json](../../import_files/sample_input/sample_input.json)`,磁盘上确有该文件;②④⑤ 的 Nodes 表新列(`status` / `ran` / `agent turns` / `corrections`)与 `## Repeats` 的 17 个逐次块都在。**只剩 ③**(侧跑报告回链到它对照的那次 run)——本轮没有 compare 侧跑,没有现场,所以不标 ✅。 **本轮(2026-08-20)未补上 ③**:没有跑候选对照(compare)侧跑,所以「侧跑报告回链到它对照的那次 run」依旧没有现场。其余四项本轮不重复验,以上一轮的当场观察为准。 **真机点验 2026-08-21:③ 取到现场,五项补齐,改判 ✅**。在真 app 里对 `e5-dead-end-fixture` 起了一次候选对照(Run compare),侧跑 `2026-08-21T08-32-45_8055ae4e` 的 `run_metadata.json` 记着 `compare_base_run_id`,它的 `report.md` 带着回链 `- measured against [2026-08-21T08-31-38_5b78e239](../2026-08-21T08-31-38_5b78e239/report.md)`,而链接指向的那份报告在磁盘上确实存在(相对路径按 `runs/<id>/report.md` 的布局解析,上一级即兄弟 run 目录)。**附带记一个操作事实,免得下次再撞**:Run compare 用的是**当前页面这一次实时 run** 的 `runId`,不是运行历史里选中的那一条;页面刷新后 `runId` 为空,按钮点了没有任何反应也没有提示。 | 08-08 大单 | 逐项在报告中找到 |
| R3 | **报告把 provider 的原始错误原样印进表格**:单个单元格 263,241 字符,一次只发了 3 次模型调用的 run 产出 529 KB 报告 | 后端(run 报告投影) | ✅ **已修 + 真机点验(2026-08-20)**:**判据达成**:新 run 的报告 17,956 字节 / 288 行 / **最长行 280 字符**(修复前那份:529,367 字节 / 87 行 / 最长行 **263,241** 字符)。 点验前的核实结论保留如下 —— **已修(本 PR)**。**发现方式 = 真机点验**,不是用户报障:run `2026-08-20T10-27-18_a98f6ba5` 的报告打开就是一堵墙。**根因是规则只落了一处**——RUN_EXECUTION-9 的 200 字符截断只用在 Failure 节的 `message` 上,而同一份报告另有三处原样打印别人的话:Routes 表的决策 reason、未满足设置表的 reason、Failure 节的 `errors` 列表。**判据改成「这句话是不是报告自己写的」**:不是就走同一道处理(`run_report_text.one_line` / `table_cell`,唯一定义)。顺带修掉**比长度更隐蔽的一种破坏**:含 `\|` 的消息会把自己那行劈成多列、此后每个单元格排到错的表头下,含换行的消息直接把行截断——一张悄悄错了的表比一张明显被截断的表更坏。设计源 RUN_EXECUTION-9(哈希已重钉) | 真机点验 2026-08-20(非用户报障) | 报告里最长一行在正常量级 |
| R2 | compare 侧跑 token 汇总 0 | 引擎 | ✅ **随 E6 同修**。两个源现在由不变量绑在一起:`report.md` 按定义是 trace 里 `llm_call` 事件之和(`run_report.py:200-202`),`metrics.json` 是 `result.metrics`(`runner.py:1980`),而引擎测试要求后者等于前者的定义式,所以「同一个 run 目录里一个写 27009 一个写 0」不再可能 | 08-19 截图 | — |

## 2 运行控制

| # | 模块问题 | 层 | 状态 | 出处 | 验收判据 |
|---|---|---|---|---|---|
| C1 | 暂停表达:有 checkpoint 却无法暂停;暂停态显式 resume+stop 双按钮;节点级暂停缺 | 后端→前端(节点级要引擎) | 🧩 **run 级全有**:`services/run_manager.py:1088` `pause_run` 保 checkpoint,`center-action-bar.tsx:168` paused 态同时给 Resume+Stop(测试钉死),落地 PR #584(08-04 当天)。**节点级暂停 = 全仓零实现**(右键菜单只有缩放/锁定/删除;节点 toolbar 只有 HITL 应答与 Resume;`nodes/types.ts:5` 有 `breakpoint` 状态但**没有任何动作产生它**)——落点 engine 断点能力 → backend `POST /runs/{id}/breakpoints` → frontend 入口。**另有可达性缺陷(会让用户复述"无法暂停")**:`stage` 只来自内存(`Workspace.tsx:561`),刷新/切 skill 后 run 还在后台跑但 Pause 按钮消失;`handlePause` 还要求 `runId` 非空而 `handleSelectRun` 只设 `viewedTrace`;后端 `pause_run` 只认内存 `self._runs`,sidecar 重启后 409 | 08-04×2 | 真机走查 |
| C2 | run 运行中按钮至少变成可停止 | 前端 | ✅ **已修 + 真机点验(2026-08-20)**:run 期间底栏为 `Compile / Predict / Pause / Stop`,Pause 为高亮主按钮、Stop 并列可点。 点验前的核实结论保留如下 —— **本 PR 已合**:`running` 现在同时给 **Pause + Stop**,与 `paused` 的 **Resume + Stop** 对称。后端本就一步到位(`test_run_pause_stop.py::test_stopping_a_run_in_flight_skips_the_pause` 断言在飞的 run 直接 `cancelled`),`handleStop` 也只要求有 `runId`——所以"先暂停再停止"那一步是 UI 自己发明的绕路。设计源 run-execution F7 | 08-04③ | 真机走查 |
| C3 | compile 按钮状态机:compile 成功后再按一次卡死 | 前端 | ✅ **已修 + 真机点验(2026-08-20)**:**判据达成**:连点三次 Compile,三次 `POST /compile` 全 200 OK,按钮每次 <1s 从 disabled 回到可用,没有卡死。 点验前的核实结论保留如下 —— **已修**:PR #676(2026-08-09,正是复现当天)。病灶不在 `gate-state.ts` 而在 `Workspace.applyGateEvent`(:685)——状态落地已无条件且先于任何去重,去重键从 `runId ?? contentHash` 换成整份 projection 指纹(`gate-effect-fold.ts:51-63`),回归测试在案。**残留风险**:`compileSkillById` 无超时兜底,后端永不返回时 stage 永远停在 `compiling` | 08-09 复现 | 复现路径不再卡 |

## 3 LLM 配置

| # | 模块问题 | 层 | 状态 | 出处 | 验收判据 |
|---|---|---|---|---|---|
| L1 | **模型组身份不稳定**:显示层的「同一模型」跨端点合并键被当成组的对外身份 id 发布 | gateway→后端→前端 | ✅ **已修 + 真机点验(2026-08-20)**:Settings → LLM Roles 页上,从前「三张卡全失联」的 `analyst` 现在是**一张**解析成功的 DeepSeek V4 Flash 组,两条 provider(Ark Official / OpenRouter)都是绿点;整页搜 `unresolved` / `unknown model` / `missing` / `orphan` / `失联` **全部 0 命中**(唯一的 `unavailable` 出自 Manual model probing 的说明文案)。截图 `L1-L4-llm-roles.png`。原始结论:**归属更正**:本行原写 gateway→后端→前端,实际 gateway 的`project_model_group_identity` 一直是纯函数、没问题;缺陷**只在 studio 后端发布这一步**——`_model_groups_response` 按语义键把 route 合成组之后,又用 `_representative_canonical_id` **选举**一条 route,拿它的 `canonical_id` 当组的对外 id。前端把这个 id 当不透明键用,不需要改。**实测底数(2026-08-20,开发机真实凭据)**:400 个组发布的 id 与自己的合并键不同;`analyst` 三张卡全失联,存的是 `deepseek-v4-flash-260425`(08-12 加 deepseek-official **之前**选出来的 id)加两个裸模型 id,而**它们的 route 全都还在**、且**全都投影到同一个键** `deepseek-v4-flash`——也就是 `fast` / `copilot_deepseek_v4_flash` 引用得好好的那个。丢的从来只是标签。**修法两半,缺一不可**:①组 id 改成**合并键本身**(纯函数,不选举);②**角色里的组按它持有的 route 重算身份**——没有②的话①会当场打断 `claude-haiku-4-5-20251001` / `claude-opus-4.8` 这两个引用**当前**选举结果的角色(实测同一份文件里就有)。②同时负责合并「同一个模型的两张卡」并丢弃「一条 route 都不列的空组」(真实文件里有一个);注册表不认识其 route 的组**不丢**——那是 route 被删,不是标签过期。**显示名仍可择优**:会变的显示名只是换个说法,会变的 id 让每个写下它的角色失联。设计源 `00_settings-ux-spec.md` §2.2 | 08-19 Q13+截图 | 角色卡三组全绿;增删端点不再翻转任何组身份 |
| L2 | compare 候选的 UI 链路从未走查 | 前端+后端 | 🧩 **UI 全链路都在**(读/增/改/删/测/跑/看结果):候选区挂在 agent 节点属性面板(`PropertiesPanel.tsx:1374` → `LlmNodeCompareField`),整份 PUT 存 `.workspace/runtime_config.json`,有专门的 `Run compare` 按钮(`:3554`)。**三处真空**:①**SUBGRAPH 节点属性面板根本没有这一区**(`:1504-1534` 只渲染名字/路径/validator),而后端接得下 → 能力在、入口无;②必须有**本会话的 live runId**,从历史列表点开的 run 只设 `viewedTrace` → "设完候选点 Run compare 却被告知先跑一次"是必然;③**路由撞车(硬缺陷)**:`routers/runs.py:43` 与 `routers/compare.py:15` 注册同一路径 `POST /skills/{id}/runs/{id}/compare`,`main.py` 先 include runs → **golden 的 POST /compare 被永久遮蔽**(当前前端只用 GET 版所以没炸)。④候选存的是 L1 那个不稳定的 `model_group_id`,同源同修 **③ 已修(PR「one path names one thing」,2026-08-20)**:node-compare 的两个端点改名到自己的资源上 —— `POST /runs/{base_run_id}/node-compare` 与 `GET /runs/node-compare/{group_id}`,golden 的 `GET /runs/{run_id}/compare` 从此不再被遮蔽。同时删掉两条冗余路由:那条**永远到不了**的 `POST /compare`(它还接不了 `against` 参数,而且比较是读不是写),以及 `GET /diff` —— 它是 `GET /compare` 的第二个名字,前端从来没调过。**门禁比修复本身更重要**:新增 `tests/test_every_route_is_reachable.py`,断言没有两个 handler 落在同一个地址上。它必须按**匹配用的地址**比较,而不是按路径字符串 —— 这次相撞的两条是 `/runs/{run_id}/compare` 和 `/runs/{base_run_id}/compare`,字符串不同、Starlette 眼里完全相同,所以第一版按字符串写的门禁**跑绿了**(实证,当场推翻重写)。**顺带一条事实**:`STUDIO_REQUEST_AUDIT.md` 早就把这两条路由**分别列着**,同址这件事谁也没发现——清单能证明「每条路由都被登记过」,证明不了「每条路由都到得了」。**①②④ 仍未做**,本行保持 🧩。 | 08-19 Q12 | UI 全链路走查 |
| L3 | "我添加一个会出现两个" | 前端 | ✅ **已修并真机点验(2026-08-20)**。修法是 PR #866:第三方 provider 身份改为按 name 合并(`provider-utils.ts:439`),回归测试断言同名卡只剩 1 张。**本轮真机证据**:Settings → LLM API-Key 页上 `[data-provider-id]` 共 **9 个,两两不同**(anthropic-official / openai-official / gemini-official / deepseek-official / ark-official,加四个第三方),用户报障的「添加一个出现两个」在页面上不存在。**这里刻意用结构计数而不是文本计数**:页面正文里 `OpenAI` 出现 9 次、`Gemini` 7 次,但那是**协议名**(「OpenAI 协议」)在各 endpoint 行上的复用,拿文本数卡片会数出假重复。用户当初看到的「多出来的」是 **endpoint 行**不是卡片(Qiniu 一张卡里两个 base URL × 三种协议 = 6 行),那一项归 L6 的 provider chip 聚合,不在本行。截图 `L3-api-keys-9-cards.png` | 08-19[b31d7d1f] | 复现→修复 |
| L4 | reasoning_effort UI 入口 + 各家枚举;temperature=模型上限×百分比;越界 clamp 不 400;top_p 不暴露 | gateway→前端 | ✅ **四条全满足,已真机走查(2026-08-20)**。**①reasoning_effort 有入口**:角色设置面板里 `Reasoning effort` 是一个**可用(非禁用)**的 Select,展开后枚举为 **Provider default / Low / Medium / High / Extra high / Max** —— 各家档位的并集(`Extra high` 对应 OpenAI 的 xhigh,`Max` 对应 xAI 的最高档),配文「How hard the model works when it reasons; each model runs the nearest level it sells」正是「就近落档」这条语义。**②temperature 是作者刻度的百分比**:面板显示 `Temperature 30%`,说明文字「Drag to set the role temperature; 70% is the default」——显示的不是某个模型的实际温度值。**③`top_p` 在整个 Settings 对话框正文里 0 次命中**(不只是角色面板里没有)。**④越界 clamp 不 400** 由两层代码承担(`schema.py:341`、`call/settings.py:250-265`);UI 侧是滑杆,**构造不出越界值**,所以这一条只能以代码与回归测试为准,本轮没有、也无法有界面证据。顺带记下同屏看到的一句 `Route max output token range: min 64,000 / max 393,216. 6 route caps unavailable.` —— 上限来自各路由实测而非硬编码。截图 `L4-effort-enum.png` | 08-10 四连 | Settings 走查 |
| L5 | key 掩码位数与真实长度一致 | 后端→前端 | ✅ **已实现 + 真机点验(2026-08-20)**:Settings → LLM API-Key 页上六个已填的 key 字段,掩码点数分别是 **108 / 164 / 39 / 35 / 36 / 52**,逐个等于该字段的 `value.length` —— 既不是定长占位、也没有截断;肉眼上 Anthropic 那条明显长过 OpenAI 那条。截图 `L3-L5-api-keys.png`。实现:`models/llm_config.py:120` `api_key_length` 是 computed_field(永不落盘、不回传 gateway、客户端回传一律丢弃),前端按它重复掩码字符无截断(`ProviderCard.tsx:137`),测试 `api_key_length: 51` → 51 个 `•` | 08-12 | 走查 |
| L6 | 探测能力 T1/T2/T3 整合成可复用接口;provider chip 真聚合等遗留 | gateway | 🧩 **T1/T2 已整合**(`probing/__init__.py` 导出 `probe_provider_endpoint`/`probe_provider_route`/`probe_official_call_method`/effort 三件套,宿主在 `routers/llm.py:3262-3285` 统一接线)。**T3(tool call/ReAct)未实现**——`probing/questions.py:44` 只留了一句 "and later the tool shape",包内无 tool 探测构造器、`bounds.py` 无 tools bounds。"provider chip 真聚合"**台账没给判据,代码单方面判不了**,需用户补一句"聚合到什么粒度算数" | 08-11 | — |
| L7 | 密钥被拒后端点永久砖化 | 后端→前端 | ✅ #872,真机点验通过 | 08-19"为什么点 test 会失效" | 已达成:`deepseek-official` 由 `disabled` 转 `verified`;在仍 `disabled` 的 `gemini-official` 上复验,Test 走完全程并如实报告密钥无效 |
| L8 | 聚焦密钥输入框露出 `**********` 占位符且可就地编辑 | 前端 | ✅ #872 + #873,真机点验通过。**顺带查到的遗留物**:`components/studio/ApiKeyInput.tsx` 是无人引用的死组件,而它实现的正是被 #872 废弃的旧语义(掩码截断到 32、退格砍真实密钥)——留着它,下一个人复用就会把两个缺陷一起复活,建议删 | 08-19"点击激活 api key 的 input 组件为什么显示成这样" | 已达成:9 张卡聚焦后一律 `•` × `api_key_length`、退格无效、键入整把替换 |
| L9 | 改完 Base URL,整张卡到达即死:三个格子全灰、点 Test 报「没有可测项」,而新地址从未被探测过 | 后端→前端 | ✅ #876 + #880,真机点验通过 | 08-19「所以这里的前端交互很有问题啊,需要全面检查一遍」+ jiekou 卡截图 | 已达成:改地址后三个格子回到 `unverified_manual`、密钥保留、旧地址的模型清单清空;普通 Test 真的发出三条 `endpoint_test`;卡片头部按状态分别说「Protocol not supported」/「Not tested」,不再落回「Not configured」。收尾时那张卡由「三格全死」变成「OpenAI 绿、196 个模型、gpt-4o 生成验证通过」。**顺带审计出的独立问题**(整页保存差集删除的三条误删路径 A-1/A-2/A-4)按用户裁决不夹带进本修复,已登记在 [DELIVERY_LEDGER](DELIVERY_LEDGER.md)「审计发现批次 2026-08-20」,并已按用户 2026-08-20 裁决「删除必须显式表达」随 #886 全部销账(交付台账 W2-43) |

## 4 Copilot(MoirAI)

| # | 模块问题 | 层 | 状态 | 出处 | 验收判据 |
|---|---|---|---|---|---|
| CP1 | @mention 节点未实现;图片附件未实现 | 前端→后端 | 🧩 **线上契约与后端已修,composer 还是空壳**(PR TBD)。**已完成(底层)**:设计源立下 COPILOT_ASSIST-8——一次提名的身份是 `(kind, ref)`,两段都由发消息的前端给出,后端不拿显示名反查(那等于把「用户挑了哪一个」变成模糊匹配,正是 F4 ③ 禁的);`label` 只回显不定位(相位改名后,已发出的消息仍该显示当时那个字)。`CopilotWsRequestPayload` 加 `mentions[]` / `attachments[]`;`services/copilot_context.py` 按「这个东西现在还读得出正文吗」分两类——`file`/`phase` 读正文注入(150K 一轮总预算,超了截断并在回显里说明),`dot`/`error`/`trace` 只注入结构化引用(哪一次 run 的哪个值是模型的判断,后端替它取一次等于把判断挪走);解析不到的当场在回显里点名、照常发这一轮(悄悄摘掉会让用户以为它进去了)。图片按值随消息走,走 SDK 的 image content block,不是一条待解引用的路径。**未完成(UI)**:composer 里键入 `@` 的 MentionMenu(五类、模糊过滤、键盘导航)、选中成内联 pill(设计源写明需 tiptap 类富文本,overlay 渲染不了真 DOM pill)、附图入口;`copilot-panel.tsx` 的 placeholder 仍写着 `Use '@' to mention nodes...` 而键盘处理只认发送键 |  | composer 里键入 `@` 弹出可选节点清单,选中成为内联 pill;能附一张图片随消息发出,后端收到 `mentions[]` / `attachments[]` |
| CP2 | 建技能向导(brainstorming)未成型 | 全栈 | ⏳ 设计源自标 target(`copilot-assist/mvp1-alignment.md:61-66`);agent 资产里无 brainstorming skill(`agent-skill-map.json` 七个 skill 里没有);New Skill 对话框只有名字+父目录直接铺模板。现状退化成"MoirAI 自由发挥 + `create_skill` 一把梭" |  | 从 New Skill 进入能走完一轮引导对话并产出骨架,而不是直接铺模板 |
| CP3 | CLI 设置页七条(08-12)残项核对 | 前端 | 🔎 **六条全中**(第 1 条本就是机器环境问题不改代码):全称文案 / codex effort 词表 / 模型下拉非自由输入 / MoirAI worker 三行双下拉+env 注入 / 版本自检并入探测(不加定时器,"查不到 ≠ 版本旧") / 行内更新+登录按钮。唯一遗留是设计源自己记的实测项:worker env 的 `ANTHROPIC_MODEL` 是否吃别名 **本轮(2026-08-20)未验**:唯一遗留项要看 MoirAI worker 进程环境里的 `ANTHROPIC_MODEL` 吃不吃别名,这需要真的拉起一个 worker 并读它的进程环境,不是界面上看得出来的事;本轮的 copilot 验证走的是主会话审批链,没有起 worker。 |  | worker env 的 `ANTHROPIC_MODEL` 吃不吃别名一项有实测结论 |
| CP4 | trace-local"为该节点设计 golden"CTA 缺 | 前端 | 🧩 CTA 已在(`TracePanel.tsx:587-601`),但语义是"从本次 run 提升"(promote)而非"设计",且**无 run 时整个消失**(`:344-347` 依赖 `canCompare = Boolean(runId)`)——而"还没有可用输出时先把期望写下来"恰恰是这条要的路径。与 K1 同族 |  | **没有任何 run 时**也能从该节点进入「先把期望写下来」的入口 |
| CP5 | 回 Home copilot 上下文清空未坐实 | 前端 | ✅ **判据按最终结果判定,已真机点验(2026-08-20)**。测法:在 e18-missing-input 里建一轮会话 → 点 Back to Home(确认已回到 Recent 首页)→ 再进同一工作区。**结果:整段会话原样回来**(提问、回答、耗时全在),说明工作区的会话窗口是**进入时从磁盘 `_window.json` 重新水化**的,不依赖内存 store 里还剩什么。**原记的「隐性缺陷」因此不推翻本行**:`copilotStore.reset(null)`(`useCopilot.ts:212-216`)确实是全局清空、作用域比声明大;但既然每个工作区进入时都重新读回自己的窗口,这条更宽的作用域**产生不了用户可见的差异**——按「判断有没有修完,看最终结果」,本行达成。它剩下的是代码洁癖层面的事:一个签名收 workspace 却不看参数的 `reset`,将来谁改谁踩,已在此点名,不另开一行 |  | 回 Home 只清当前 workspace 的会话窗口,换个 workspace 进去会话还在 |
| CP6 | **卡片决议放错了 owner**:用户的决议只活在组件 `useState` 里,落盘的会话存的是「卡片刚到时的样子」 | 前端(会话记录)+后端(审批生命周期) | ✅ **已修并真机点验(2026-08-20)**。**验在被改坏的那一层**:让 MoirAI 新建一个文件触发工具审批 → 点 Approve → 卡片当场变成已批准、Approve/Reject 两个按钮消失 → **收起 MoirAI 面板再展开**,没有任何「复活成未决议」的卡片,页面上再无可点的 Approve/Reject。**更关键的是落盘那一份**:`.workspace/copilot/sessions/<ws>/session-*.json` 里确实写着 `"decision": "approved"` —— 决议进了**事件本身**,不再只活在组件 `useState` 里,而这正是本行的根因;旧行为下这份 JSON 存的会是 pending。**本轮没能覆盖的一格**:工具执行完成后卡片会正常演进成「结果」,所以「决议了但消息尚未 settle」那个窄窗口没有单独复现,它由该 PR 的 5 条前端测试承担 | 08-19 Q「决议卡片重开面板/切标签/冷启后仍是已决议」 | 决议之后收起面板 / 切标签 / 冷启 Restore chat,卡片仍显示已决议且按钮不可再点 |
| CP7 | 审批挂起不设超时;超时自动拒绝并继续跑是缺陷,应停任务 | 后端 | ✅ **两端都补齐了,2026-08-20**。**后端本来就按裁决实现**:默认 1800s、可环境变量调(`services/copilot.py:388`),**超时 = `interrupt=True` 停任务保会话**,用户主动拒绝才是 `interrupt=False` 继续跑 —— 两条语义分开,正是 07-08 两条裁决的形状,回归测试在案。**缺的从来不是「前端漏接了一个事件」,而是那个事件指不到具体对象**:超时从前发的是一条**泛型 `error`**,它能说「有东西过期了」,说不出**是哪一张卡**;所以哪怕前端去听,也认不出该 settle 哪一张,每张卡照样停在 "Waiting for approval."、按钮照样可点,而背后的任务早已停了。按仓规「事件说不清是哪份数据变了,就去修事件契约」,修的是契约。**改法**:新增 `CopilotEventToolApprovalTimedOut(tool_use_id, tool_name, message)` **取代**那条泛型 error(不并存 —— 两条消息讲同一件事,就是两个随时会打架的答案);前端 `ToolApprovalDecision` 加第四个取值 `timed_out`,**不另开字段** —— 这一个字段回答的正是「这次挂起怎么结束的」,而**没有人决议本身就是一种结局**。卡片据此收起按钮,把 "Waiting for approval." 换成「超时等不到答复,任务已停,发新消息可继续」,并转成 warning 色。**一条竞态被写成规则,而不是留给运气**:用户的决议和超时可能在路上交错,`timeOutToolApproval` 只动仍然是 `pending` 的那一张 —— 用户真的答过的,后到的超时不许把记录改写成没人选过的样子。设计源同 PR 落盘:**COPILOT_ASSIST-8**(哈希已重钉),并写明它与 COPILOT_ASSIST-7 是同一条生命周期的两半:7 管**用户来问**时答哪一种结局,8 管**没人问**时主动说、且说给具体那张卡听。测试:后端 1 条(**改写**了原本钉住泛型 error 形状的那一条)+ 前端 5 条 **本行仍是 🔎 而不是 ✅,理由要说清**:判据要看的是**真机上那张卡**超时后变成什么样,而默认要等 1800 秒。它是**可以真机验的**,不是没办法 —— 给 sidecar 设 `STUDIO_TOOL_APPROVAL_TIMEOUT_S=20` 再触发一次审批、干等 20 秒即可,只是需要一次能独占主 app 的窗口。在那之前按报告纪律:没实测的行不标 ✅。 **真机点验完成(2026-08-20 第二轮),本行转 ✅。** 验法就是本行上一版写下的那条:给 sidecar 设 `STUDIO_TOOL_APPROVAL_TIMEOUT_S=25` 起 app(env 会经 Tauri 继承进 sidecar,`sidecar.rs` 没有 `env_clear`),在 MoirAI 里让它跑一条 Bash,然后**故意不答**。**实测四条**:①落盘的会话事件里那张卡是 `"toolName": "Bash", "detail": "echo cp7-timeout-probe", "decision": "timed_out"` —— 决议值真的写成了新增的第四态;②时间对得上:审批事件 `receivedAt` 1787286865333,工具结果 1787286890498,**相差 25.2 秒**,正是设的超时值;③**任务确实被停了**,不是带着拒绝继续跑 —— SDK 侧收到的是「The user doesn't want to proceed… STOP what you are doing」,该轮以 error 收尾;④界面上那张卡渲染成「timed out waiting for an answer」,**全页面 `Waiting for approval.` 零命中**,卡上没有 Approve/Reject(页面上仅剩的一个 Reject 属于补丁 diff 气泡,不是审批卡),且**冷启重进工作区后仍是这个样子**(从磁盘读回)。截图 `CP7-timed-out-card.png`。**顺带记一个观察,不在本行**:被 interrupt 的那一轮之后,SDK 另发了一条 `SyntaxError: JSON Parse error: Unrecognized token '<'` 的诊断 —— 是 CLI 自己在中断后的解析问题,与本行的超时语义无关,但用户每次超时都会看到它。 |  | 审批超时后卡片不再停在 "Waiting for approval."、按钮不可点,且任务已停不是继续跑 |
| CP8 | 回复语言跟随用户是否已固化进 MoirAI 规则 | 规则资产 | ✅ **已固化并真机点验(2026-08-20)**。规则原文 `app/agents/operating-manual.md:48`「Always reply in the language used by the user in their last message」,装配点 `services/copilot.py:882-897`(会话级 system prompt),三女神 worker 同吃(:942)。**实测**:在 e18-missing-input 里用中文问「请用一句话说明这个 skill 有几个 phase。」,MoirAI **全中文作答**,且答对了(2 个 phase、first 声明可选 note 而 second 把它当必填输入)——语言跟随与内容正确**同时**成立,不是把问题复读成中文 |  | 用中文提问,MoirAI 用中文回 |

## 5 平台与桌面壳

| # | 模块问题 | 层 | 状态 | 出处 | 验收判据 |
|---|---|---|---|---|---|
| P1 | 后台任务状态不收敛,"我看到的状态就是一直在运行啊"(×3) | 后端(run 终态 + artifact 封存)→前端 | ✅ **已修 + 真机点验(2026-08-21,PR #935 + #937)**。**真因在后端,不在前端**——本行第二版的收窄方向是对的,猜的落点是错的:现场观测证明 gate 事件按时到达、`finalize-run` 三个条件全部成立、前端如约发出 `GET /api/skills/{id}/runs/{run}`,而**后端回了 409 `artifact.run_not_sealed`**。封存动作原本写在 `_persist_run_artifacts` 里,它跑在 **worker 进程内**——正是被杀掉的那一个;于是元数据说「已终态」、gate 说「可以读了」、artifact store 说「还在写」,**一个问题三处口径、两个答案**。#937 把封存移到 `_finalize_terminal_run`(一次 run 无论怎么结束都必经的唯一入口),并顺出三条:trace 先提交再封存(否则给一个死在半路的 run 盖上「什么都没发生」的章)、没产出的 run 也写 manifest(读者是**通过** manifest 找到 run 的)、重复封存幂等。**真机实测(2026-08-21)**:自造 `p1-slow-fixture`(一个纯计算的 `burn` 相位)在 app 里点 Run,worker 落地为 pid 6992;外部 `Stop-Process -Force` 杀掉后,徽标在**不到 1 秒**内从 `burn LOGIC Running 9s` 收敛成 **`Failed`**,底部按钮从 Pause/Stop 变回 Compile/Predict/Run,此后连采 30 秒不再变化;sidecar 日志同一秒记着 `GET /api/skills/p1-slow-fixture/runs/2026-08-21T05-50-17_b61a54bf HTTP/1.1" **200 OK**`,整份日志 **0 条 409**。截图 `P1-run-started.png` / `P1-badge-failed.png`。**顺链查到的最后一个单点另立 N5**(判词已到手却丢掉,只靠再问一次)。 以下保留 2026-08-20 第二轮的现场记录 —— **后端已实证收敛,前端这一段已在真机上复现成缺陷(2026-08-20 第二轮)**。**后端(PR #935 的 3 条回归测试已钉住)**:worker 被外部杀死 → `_drain_process_queue` 发现 `is_alive()` 为假 → 拿退出码推终态 → `_finalize_terminal_run` 写元数据、发 `publish_skill_gate`、给流投 `None` 哨兵。**真机实测**:在 app 里跑 e18-missing-input,用一个守候进程在 worker 一出生(pid 42536,21:43:38)就 `Stop-Process -Force` 杀掉。磁盘证据:`runs/2026-08-20T21-43-38_a55e334d/run_metadata.json` 写着 `"status":"failed"`,`report.md` 也生成了 —— 说明 `_finalize_terminal_run` 完整跑完,gate 必然已经发出。**而界面没有跟上**:杀掉之后连续采样 80 秒,四个节点徽标最后停在 `INPUT:Idle / AGENT:Idle / AGENT:Idle / OUTPUT:Idle`,Trace 面板停在 **「Waiting for run events」**、还挂着这次 run 的 id。也就是说这次 run 在界面上**既没变成终态、也没报错,而是退回成「什么都没发生过」** —— 比一直转更难察觉,因为它看起来像「还没开始」。截图 `P1-stuck-after-kill.png`。**缺陷位置已收窄到最后一段**:gate 事件(走 `event_bus` 的 `STUDIO_EVENTS_TOPIC`,**与那条已经关掉的 run 流不是同一条通道**,所以「流关了所以收不到」这个解释不成立)→ `parseSkillGateEvent` → `deriveGateEffects`(`gate-state.ts:113`,`finalize-run` 的三个条件 gate=run、outcome 非 started、runId 存在**全部成立**)→ `Workspace.tsx:702` `setFinalizedRunId` → `getRunDetail` → `setLiveRunRecord`。**还没分清**是事件根本没到、还是 `gateEffectFold.shouldRunEffects` 把它折叠掉了、还是 `event.skillId !== currentSkillIdRef.current` 提前返回了。**下一步是给这条链装一次现场观测**(在页面里 hook 那条 studio-events websocket,重放同一次杀进程),而不是接着读代码猜 —— 本行第一版的诊断就是这么猜错的 |  | worker 被外部杀死后,徽标在有限时间内自行收敛成终态,不再一直转 |
| P2 | **Retry 按钮不重启任何东西**:能启动 sidecar 的配方被关在「已经启动成功的 sidecar」里,首启失败即失去重试能力 | 壳(Rust)→前端 | ✅ **已修,真机点验通过(2026-08-21)**。**根因不是少调一个函数**:`launch_config` 原本存放在 `SidecarManager` 内部,而 `SidecarManager` 只能由一次**成功**的启动构造出来——首启失败,配方跟着陪葬,所以 `restart_sidecar` 在 `manager == None` 时只能直接 Err(只救得了一个还活着的 sidecar);同时 `get_sidecar_config` 回的是**首启那一刻缓存下来的错误字符串**,按一万次字面同一条,用户体验就是「点了没反应」。**修法(三层全改)**:①**壳/Rust**——新增 `SidecarSupervisor`(`sidecar.rs`),它持有**配方**并最多持有一个由配方造出来的 sidecar;状态是 `Running(manager)` / `Absent(原因)` 二选一(不会既有又无);`restart()` 无论从哪个状态出发都去拿一个 sidecar(活的就地重启,没有的就从头启动),且**每次尝试的结果覆盖记录**,所以之后读到的永远是这次尝试说的话。参照 Erlang/OTP supervisor 的 child spec 与 systemd 的 unit 文件——两者都把子进程规格放在**监督者**手里而不是被监督的进程里,`restart_child`/`systemctl restart` 才能作用于一个没在跑的子进程;**不借**它们的自动重启策略(触发器是人按 Retry,永久性失败上自动重试只会把错误刷没)。②**壳/命令层**——`SidecarAppState` 只剩 `supervisor: Option<..>`,`None` 专指 `STUDIO_TAURI_DISABLE_SIDECAR=1`(压根没有配方,与「启动失败」是两回事);`restart_sidecar` 从此有真实调用方。③**前端**——`config/runtime.ts` 新增 `restartSidecar()`,`RuntimeGate` 的 Retry 改调它(首次挂载仍只是读配置,不去踢一个健康的 sidecar);重试期间先回到 `loading`,长达 30s 的 health 超时期间按钮看起来在干活而不是又一次装死。设计源 `03_regions/shell-layout/mvp1-alignment.md` F5 + SHELL_LAYOUT-4(同 PR 落盘,哈希已重钉);顺带订正了同一单元里「RuntimeGate 仍可全屏 gate」这条过期现状(代码里 `RuntimeShell` 无条件渲染 `{children}`,D10 早已满足)。测试:Rust 3 条 + 前端 4 条。 **真机点验(2026-08-21,独占主 app)**:制造首启失败**不改任何文件**——`sidecar.rs:447-476` 的 `allocate_loopback_port_from` 拿到 `STUDIO_SIDECAR_PORT` 就直接返回、**不测可用性**,于是把它钉到一个被独占监听的端口即可;监听方设 `SO_EXCLUSIVEADDRUSE`,否则 Windows 上 uvicorn 的 `SO_REUSEADDR` 会把端口抢过去(实测:不设该标志时抢占成功,设了则报 `WinError 10013`)。① **首启**(8791 被占):红条 `Backend unavailable … sidecar health check timed out on port 8791: … Started server process [22740] … [Errno 10048] … bind on address ('127.0.0.1', 8791)`(截图 `P2-01-first-start-failed.png`)。② **点一次 Retry**(端口仍被占):红条立刻变成 `Connecting to backend…`,90s 后回到报错,而报错里的 uvicorn 进程号**从 `[22740]` 变成 `[5100]`**——**真的另起了一个 sidecar 进程**,文案是这一次尝试自己说的话,不是首启那句缓存(截图 `P2-02-retry-new-attempt.png`)。③ **放开端口再点一次 Retry**:sidecar 起来了(第三个进程 pid 20324 监听 8791),红条消失(截图 `P2-03-retry-recovered.png`)。旧代码在①之后就永远只能重复那一条字符串,②③在旧代码上物理不可能发生。 | 08-19 Q「Try again 点了没反应」 | sidecar **首启失败**时点 Retry 真的重新拉起 sidecar,且报错文案随每次尝试更新(**已实测**:三次尝试三个不同的 uvicorn 进程号,第三次成功后红条消失) |
| P3 | 登录控制台 c/v 快捷键 | 前端/壳 | 🔎 **已实现且多做一步**:`c` 重新复制最近 URL、`v` 把 Windows 剪贴板注入 CLI 输入,首条 URL 零按键自动复制并打一行提示。第二轮"没有 v"的根因已定位并删掉肇事判定——旧的"输入行为空"判定在 Ink TUI 下被终端应答(如光标位置报告)记成"已开始输入",v 从此永久失效;现只剩 60ms 孤立键判定。**限制**:只在 Windows/WSL 生效;CLI 从没吐 URL 时用户永远不知道有这两个键 **本轮(2026-08-20)刻意没验,理由要写清楚**:判据要看的是登录控制台里的 `c` / `v` 行为,而进入那个控制台就得对用户**当前有效的账号**真的发起一次 OAuth 登录流程(真机上 `claude_auth` / `codex_auth` 都是 `ok`)。为一条快捷键提示去冒险扰动用户在用的凭据不划算,所以本行维持 🔎 并写明:它缺的不是实现证据,是一个**可以安全登录的账号现场**。 |  | 登录控制台按 `c` 复制 URL、按 `v` 注入剪贴板,且界面本身告诉用户有这两个键 |
| P4 | 登录按钮常驻(换账号)+显示已登录账号 | 前端/壳 | ✅ **两半都已落地,2026-08-20**。**①登录按钮常驻(真机实测)**:`cliRowAction`(`CliSection.tsx:95-96`)对 `claude_auth` / `codex_auth` **无条件**返回 login 动作;真机上这两行的 `state` 都是 `ok`,`data-cli-row-action="login"` 照样在,账号也都显示。(这一条推翻了设计文档 08-12 写的 `missing`/`broken` 条件,以代码与实测为准。)**②过期仍报账号(本次修)**:**丢字段的是探测端,不是渲染端** —— 渲染层是 `row.account ? … : null`(`CliSection.tsx:279-282`),给了就画、对 `state` 一无所知;病灶在 `apps/studio/tauri/src/lib.rs:384`,`broken` 分支的 `printf` 只发四列,**把上一行刚解析出来的 `$acct` 扔掉了**。第五列照发 `$acct` 即可。**红灯是怎么先亮的**:新增 `test_an_expired_claude_token_still_names_its_account`,把整份探测脚本喂给真 `sh`,fixture 的 `expiresAt` 落在 2001 年;修之前它打出来的正是四列的 `claude_auth`+`broken`+空版本+`token expired`,一个字段都不多。**顺带改写了一条钉住缺陷的旧用例**:`CliSection.test.tsx` 原本给 `broken` 行喂 `account: null` —— 它照抄了缺陷的形状,所以一直是绿的、也永远发现不了这件事;现在两行都带账号,并断言过期那行同样显示。**并把 shell 压制抽成 `run_probe_script_against_home`**:两个登录态用例共用同一套 getent/command/tmux 压制,各写一份迟早会漂,而漂掉的那一份不会报错,只会安静地变成一个更弱的测试。**同一段脚本里发现、但刻意没有顺手改的**:见 **P5** |  | 登录按钮在任何状态下都在;token 过期(`broken`)时仍显示已登录账号 |
| P5 | **codex 登录行表达不出「过期」** | 壳(Rust) | ✅ **已答 + 真机点验(2026-08-21,PR #938),并据实订正了本行原判据**。为回答这一个问题去把两家凭据都量了一遍,**先量出 claude 那行正在做本行明确拒绝的事**(另立 P6)。**codex 的结论是「按规则报 ok」,不是漏写**:`~/.codex/auth.json` 里唯一带到期时刻的是 `id_token`(实测 1 小时档,换个名字的 access token),而 `refresh_token` 是不透明串(实测中段 4 字符,根本不是 JWT),**本地读不出它的到期时刻**;`codex login status` 也帮不上忙——实测 `id_token` 已过期数小时时它照答 "Logged in using ChatGPT",因为它只看文件在不在。故意刷新一次去问,又被 ah 决议 0006 挡着(refresh token 每次使用都轮转,探测一次就把 CLI 自己的凭据链岔开)。所以定的规则是**说不出到期时刻就不下判断**,理由写在探测脚本旁边而不是留在票里;同一条规则也管着 claude 缺字段的情形。**真机实测(2026-08-21)**:Settings → Copilot → CLI 点 Re-check,七行全部拿到,`Codex sign-in \| Ready \| traveluxi.com@gmail.com`、`Claude Code sign-in \| Ready \| traveluxi.com@gmail.com`,没有任何一行谎报过期。**「真的过期」那一支无法在真机上取证**:造出它必须动用户当前有效的凭据,故由三条 Rust 测试(`LONG_PAST_MS` / `FAR_FUTURE_MS`)独家覆盖,本行如实标注而不冒充实测。 以下保留立行时的分析 —— **已坐实,刻意没有并进 P4**。`lib.rs:387-394`:只要 `~/.codex/auth.json` 在,就一律 `printf 'codex_auth'+'ok'+…+邮箱`,**没有任何 broken 分支** —— 也就是说 codex 这一行**永远报已登录**,过没过期根本表达不出来。**为什么它不是「照抄 claude 那行」就能补上的**:claude 手上有 `.credentials.json` 里的 `expiresAt`,是一个可以直接比大小的到期时刻;codex 手上唯一的时间信号是 `id_token` 这个 JWT 的 `exp`(脚本已经在 base64 解它的 payload 取 email,顺手取 `exp` 是同一行的事)。**难的不是取值,是语义**:codex 的 `auth.json` 同时存着 refresh token,`id_token` 过期时 CLI 下次调用会自己刷新,**用户其实没有掉线**。照 `exp` 判 `broken` 等于给一个工作正常的登录扣上「token expired」—— 那是**凭空造一个不存在的故障**,比现在什么都不报更坏。所以这条得先定「codex 的『需要重新登录』以什么为准」(refresh token 也失效?一次真实的探测调用?),再动手 | 台账核实 2026-08-20(修 P4 时在同一段探测脚本里发现;非用户报障) | codex 登录行不把可自动刷新的过期误报成掉线(真机已验);它报不出「需要重新登录」这一点由证据定为**本地不可知**(refresh token 不透明 + 主动探测会岔开凭据链),规则与理由写在探测脚本旁 |
| P6 | **claude 登录行拿 access token 的到期时刻判「过期」**,而那是每天夜里都会发生的常态 | 壳(Rust) | ✅ **已修 + 真机点验(2026-08-21,PR #938)**。`~/.claude/.credentials.json` 里存着**两个**到期时刻:`expiresAt` 属于 access token(这台机器实测 8 小时档),`refreshTokenExpiresAt` 属于 refresh token(实测 14 天档)。探测脚本读的是前一个。access token 过期是**每天都在发生的常态**,CLI 下次调用拿 refresh token 自己换新,用户全程无感——于是这一行**例行地**给一个健康的登录报「token expired」,而它唯一被造出来要报的那个状态(你得重新登录)反倒淹在这份噪声里认不出来。**改法**:只认允许续期的那份凭据,`refreshTokenExpiresAt` 真的过了才报 broken「sign-in expired」;**字段不存在是「不知道」,不是「已登出」**,缺字段报 ok。**真机实测(2026-08-21)**:Settings → Copilot → CLI 的 `Claude Code sign-in` 行是 `Ready` 并带账号;同一时刻直接读凭据里那两个时刻(只取数字,不碰 token):`expiresAt` 距今 **+4 小时**、`refreshTokenExpiresAt` 距今 **+335 小时**——也就是说**再过 4 小时旧代码就会开始谎报过期**,而新判据要到 14 天后才会说话。 | 台账核实 2026-08-21(答 P5 时把两家凭据都量了一遍才发现;非用户报障) | claude 登录行只在**必须重新登录**时报过期;能自动刷新的过期不报 |

## 6 发布与分发

| # | 模块问题 | 层 | 状态 | 出处 | 验收判据 |
|---|---|---|---|---|---|
| D1 | 新用户零安装:"第一次使用者要装多少外部应用?" | 壳/打包 | 🧩 **已内化**:Python 运行时(5 个三元组全 pin)、后端+SDK 闭包、ah/ahd(构建链 `--strict`)。**仍要用户自己装**:tmux、Claude Code CLI、codex,Windows 上还硬依赖 WSL。**且打包版的一键安装必炸**:`lib.rs:606-611` 去仓库里找 `scripts/install-claude-code-wsl.ps1`,而 `tauri.conf.json` 的 `bundle.resources` 根本没带它 → 打包后 `installer script not found (packaged build?)` |  | 打包版点一键安装能真的装上,不报 `installer script not found (packaged build?)` |
| D2 | Windows 发布打包链从未跑通 | 打包 | ✅ **已修(PR #929)且 CI 实证通过**。判据两半在 GitHub runner 上一次跑完 —— `package` workflow run **32440643411**(`windows-installer` job,24 分钟,全部步骤 success):`Build the installer` 产出 `...\target\release\bundle\nsis\Skill Studio_0.1.0_x64-setup.exe`;`Install it and check the sidecar landed inside` 静默装进一次性目录后打印 `OK python interpreter` / `OK vendored site-packages` / `OK vendored backend`、`OK installed interpreter imports graph_agent from C:\Users\runneradmin\...\studio-package-check\vendor\site-packages\graph_agent\__init__.py`,收尾 `PASS: the installed app resolves its sidecar inside ...`。**原诊断两条都已坐实并修掉**:①仓里从来没有任何 CI 跑过 `tauri build`;②`beforeBuildCommand` 调 `python3`,而主力开发机 PATH 上没有 `python3`(实测),改为 `uv run python` —— `build_vendor.py:133` 本来就硬要求 `uv`。**跑起来才发现的第三条**:`download_runtime.js` 把带盘符的绝对路径交给 `tar`,GNU tar 把冒号读成 `host:path` 去连一台叫 `D` 的机器;实测矩阵(GNU tar 1.35 vs bsdtar 3.8.4)显示相对形式两边都收、只有绝对形式劈开两者,故提取统一收进 `download_runtime.js` 一个出口。**为什么它不是必过门禁**:一次冷跑要编译整个 Rust release 栈,代价写进 AGENTS.md,取而代之的是按路径触发,改到打包链才跑 |  | CI 里跑得出 Windows 安装包,且装出来的 app 的 sidecar 路径落在安装目录内 |
| D3 | **打包会把开发机上仓外的私人技能源码装进安装包** | 打包 | ✅ **已修(PR #923)且安装包实测通过(2026-08-20)**。原本卡在「安装包实测要等 D2」,D2 一通,这层证据当天就取到了:拿 PR #929 打出的 `Skill Studio_0.1.0_x64-setup.exe` 静默装进一次性目录,再拿本机 `D:\coding\skills` 下的 **39 个**私人技能目录名去比对装出来的 **637 个**目录 —— **一个都没进去**。唯一命中的 `shared` 是**假阳性**,已逐条查清:装出来的三处 `shared` 全是 Python 包内部(`anthropic/types/shared`、`mcp/shared`、`openai/types/shared`),而同名私人技能装的是 `schemas.py` / `__init__.py`,内容毫不相干。再按技能形状扫一遍 `SKILL.md`/`graph.md`,命中的全部属于产品自己(`vendor/backend/app/agents/skills/*` 七个、以及 fastapi 与 `graph_agent` 自带的),没有一个来自仓外。安装出来的树里**根本不存在** `vendor/resources/skills` —— 空目录不进包。判据「打出的安装包里不含开发机上任何仓外的私人技能源码」达成 | 台账核实 2026-08-20(非用户报障;原记在 D2 行末) | 打出的安装包里不含开发机上任何仓外的私人技能源码 |
| D4 | **安装包里装着它自己的构建缓存** | 打包 | ✅ **已修并实测(2026-08-20)**。**修法是把缓存搬出去,不是在清单里逐个排除**:`bundle.resources` 写的是 `vendor/**/*`,所以 `vendor/` 就是**载荷本身** —— 谁躺在里面谁就发给每一个用户。下载缓存原本在 `vendor/downloads/`,等于把「解压出载荷的那个压缩包」跟载荷放在了一起。缓存现已移到 `apps/studio/tauri/.cache/downloads/`(仓外可弃、已 gitignore),两个 vendoring 脚本共用**同一处定义**(`download_runtime.js` 里,`ensure_ah_vendor.js` import 它 —— 它本来就从那儿取 `download`/`verifySha256`)。**为什么不改成在清单里列举 vendor 的子目录**:那样漏掉的失败方向反了 —— 新增一个载荷目录而没人记得加进列表,它会**静默不发**,而少发比多发严重得多。**顺手删掉的空转**:`bundle.resources` 里的 `python_runtime/**/*` 打包的只是一个 `.keep` 占位符;而 `sidecar.rs:507-517` 找解释器时是拿候选路径**再拼上目标三元组**才 `.exists()`,`python_runtime/<triple>` 从来就不存在 —— 也就是说这条 glob **在运行时从来没被读到过**,删它不改变任何行为(这也正是它安全的理由)。那三条已死的查找路径另立任务清理,不在本次改。**跑起来才发现的坑**:缓存搬出 `vendor/` 后,相对路径从一层变成多层,GNU tar 把 `\` 当转义字符 —— `-C ..\dest` 能过,`-C ..\..\vendor\python\.python-runtime-XXX` 就报「Cannot open」而目录明明在那儿。**之前那次实测只量到一层,不足以覆盖这一档**。改成统一输出 `/` 分隔(两个 tar 在 Windows 上都收),并补了一条多层用例钉住。**实测结果**:安装包 **154.7 MB → 105.9 MB,省 48.8 MB(约三成)**,装出来的 app 依旧 sidecar 三件套齐全、`import graph_agent` 解析在安装目录内。**已变成常驻门禁**:`verify_installed_sidecar.ps1` 现在多断言一条 —— 装出来的树里不得有任何 `.tar.gz`/`.tar.xz`/`.tar.bz2`,CI 的 package workflow 每次都跑 | 台账核实 2026-08-20(非用户报障;D2 的安装包实测中发现) | 安装出来的目录里没有任何 `.tar.gz` / `.tar.xz`,且不存在只含占位符的 `python_runtime/` |
| D5 | **同一份 config 被两套机制各打包一次** | 打包 | ✅ **已修 + 真机核对(2026-08-21,PR #941)**。**答案是「谁都不读」**,所以两份一起删,零份而不是一份——「一份没人读的拷贝」是同一个缺陷的一半大小。两个文件是 `llm_roles.yaml` 与 `llm_canonical_rules.yaml`:运行时后端把它们解析到 `<app settings dir>/llm/`(`llm_paths._env_or_default` → `APP_SETTINGS_DIR/"llm"/<file>`),而 app settings dir 是**用户数据目录**(`lib.rs` 用 `resolve_config_dir()` 覆盖成 `%APPDATA%\AgentStudio`,`sidecar.rs` 以 `STUDIO_CONFIG_DIR` 传下去,`paths.app_settings_dir` 优先认它)。就算走到那条会用安装包的分支,`default_config_dir(STUDIO_RESOURCE_DIR)` 解析出来的是 `<resource>/config/**llm**/<file>`,比安装包放的位置深一层——**两条打包路径下都由构造保证读不到**。首次运行也不拿它们播种(`runtime_truth_init` 直接写代码里的 `RolesData()` 和 `"{}\n"`)。真正读它的是**仓内测试**,按仓根相对路径从源码树读,所以目录留在原处、只是不再被装进包里。**真机核对(2026-08-21)**:`tauri.conf.json` 的 `bundle.resources` 现在只有 `["vendor/**/*"]`,`apps/studio/tauri/vendor/resources/` 下只剩 `skills` 与 `workspaces`,`config` 目录不存在;打包链 `package.yml` 在该 PR 及其合并后的 `main` 推送上各跑一次**全绿**(run 32476992812 / 32477724403)。新增的 node 测试断言同步不再产出 `resources/config`,再加回去会让任务变红而不是悄悄把安装包撑大。 以下保留立行时的分析 —— **已坐实重复,尚未坐实谁读哪一份**。两条路径同源:`sync_resources.js:88` 把 `<repoRoot>/config` 抄进 `vendor/resources/config`,而 `tauri.conf.json` 的 `bundle.resources` 又列了 `../../../config/**/*` —— **同一个源目录,两套机制,各发一份**。安装出来的树里两份都在:`vendor/resources/config/` 和 `_up_/_up_/_up_/config/`(`_up_` 是 Tauri 对父级相对路径的转义)。**已查清的部分**:`sidecar.rs:71-75` 把 `config_dir` 默认推成 `<resource_root>/vendor/resources/config`,但 `lib.rs:4363` 在启动前用 `with_config_dir(resolve_config_dir())` 覆盖掉它,而 `resolve_config_dir()`(`lib.rs:1265-1268`)返回的是**用户配置目录**(`%APPDATA%\AgentStudio`)。后端那边 `paths.py:23-29` 只有在 `STUDIO_CONFIG_DIR` **没设**时才会退回 `default_config_dir(STUDIO_RESOURCE_DIR)`,而 Rust 每次都设(`sidecar.rs:551`)。**没查清、因而不敢删的部分**:既然两份都不经这条路被读,它们到底服务于什么 —— 首次运行播种?某个尚未找到的读取点?**在坐实之前不动它**,因为删错的代价是装出来的 app 少一份它其实需要的文件,而这类缺失只在新机器上才现形 | 台账核实 2026-08-20(非用户报障;D2 的安装包实测中发现) | 安装包里不再有 config(零份),且能指名道姓说出谁在什么时候读它 |

## 7 skill 工作区与 golden

| # | 模块问题 | 层 | 状态 | 出处 | 验收判据 |
|---|---|---|---|---|---|
| K1 | golden 种子化后半截(空模板/坏文件用 run 输出重填) | 后端→前端 | ⏳ **自动种子化整半截零代码**:全仓 `seed`×`golden` 前后端各 0 命中;run 终态路径(`run_manager._finalize_terminal_run:1399-1422`)完全不碰 golden;现存的只有用户点按的显式 promote。"空模板/坏文件"当前语义是**跳过**而非**重填**(`golden_diff.py:541-551` `reason=expected_output_invalid` → skip) |  | 一次 run 结束后,空模板 / 坏文件的 golden 被本次 run 的输出**重填**,而不是被跳过 |
| K2 | 每个 agent node 自动生成桩数据,无 copilot 也能跑 predict | 引擎/后端 | ✅ **已实现并真机点验(2026-08-20)**。**本轮第一手证据**:e18-missing-input(两个 AGENT 相位)的 predict `predict-2026-08-20T19-54-48_c1cb2b0f`,`last_predict.json` 记 `success: true`;trace 里两条 `llm_call` 事件全是 `mock_id_heuristic_stub_first/second_...`,`input_tokens: 0`、`output_tokens: 0`、`resolved_model: null` —— **零次真实 LLM 调用**;桩值形如 `"note": "<mock_note>"`,全文 10 处 `heuristic_stub`。也就是说没有 copilot、没有任何被调用的真实路由,predict 照样跑到底。三级兜底在 `_predict_internal/interception.py:124-136`,连无 schema 也有值;作者 validator 只对 `heuristic_stub` 降级。**判据后半「单列那条边界」已照办:见 K5** |  | 没有 copilot 的 skill 也能跑完 predict;并单列「确定性假值把路由卡在环里」这条边界 |
| K3 | 两套冲突 UX 统一 | 引擎→后端→前端 | ✅ **已修 + 真机点验(2026-08-21,PR #942)**:自造 `k3-overwrite-fixture`(`draft` 与 `revise` 两个相位都声明输出 `summary`),画布上 `revise` 节点的冲突 popover 标题是 **「An upstream output would be overwritten」**,正文 **「Field `summary` is also output by upstream node `draft`. Running this phase replaces that value.」**——字段名是**被覆盖的那一个**`summary`(不再是解决冲突要写的 `allow_sequential_overwrite`),上游 phase 名 `draft` 由新增的结构化槽直接读出;两个按钮是 **Cancel / Overwrite**,与保存冲突对话框逐字同词。点 Overwrite 后 `phases/revise/LOGIC.md` 落盘 `allow_sequential_overwrite: - summary`,节点警告消失,再编译只剩一条无关的 `STUDIO_RUNTIME_INPUT_MISSING`。截图 `K3-popover-correct-field.png` / `K3-conflict-cleared.png`。 以下保留修复前的诊断 —— **一个问题两个答案**:顺序覆盖冲突,engine 编译规则判一次,前端 `canvas-authoring.ts` 里还留着一份 TS 重写的同规则(祖先集合 + 输出字段集 + allow 名单全套)——它在 #219「keep canvas authoring file-backed」里已被摘掉接线,但代码没删,这次连同它的测试一起删。**engine 只把冲突写在英文句子里**:结构化槽 `field_path` 填的是解决冲突要改的 `allow_sequential_overwrite`,不是被覆盖的字段,上游 phase 更是一个槽都没有——于是前端正则拆句子取事实,而它去读结构化槽时读到的是错的(popover 会显示「Field `allow_sequential_overwrite` …」,授权判定问 allow 名单里有没有 `allow_sequential_overwrite`,永远为假,警告清不掉;旧测试 fixture 一律 `field: null`,把这条盖住了)。**改法**:engine `field_path` 改指 `io.outputs.properties.<key>`(对齐同族规则 `[F-v3-parallel-write-conflict]`),`CompileIssue` 加 `conflicting_phase` 报出上游 phase;后端 `CompileError` 与前端类型逐层透传;识别只认 `error_code`,消息串匹配与取事实的正则全删。**UX**:共享词表落在 `components/studio/conflict-vocabulary.ts`——`Overwrite`/`Cancel` 两边同字、标题统一「<什么> would be overwritten」、同一个 warning 严重度(Button 新增 `warning` variant);**容器不合并**,因为设计源 F3 原话就是「canvas popovers, editor conflict dialogs」两个容器,保存冲突要挡住待写入、画布冲突要钉在节点上,合并会弄丢锚点。设计源已同步改写(F3 + 接口契约 + §6/§8) |  | 两条冲突路径用同一套动词与严重度语汇(容器按交互形态分工),顺序覆盖识别吃 `error_code`、字段与上游 phase 吃结构化槽,全链无消息文本正则 |
| K4 | i18n P2 + 后端 skills.py 中文 | 全栈 | 🧩 **账目要改**:后端不是"27 行中文"——`routers/skills.py` 全文只剩 4 行含中日韩字符,其中**用户可见的只有 2 行**(`:496`/`:505` 的 `APP_SETTINGS_INCOMPLETE` 分支 message),其余 router 的中文全是注释。**i18n P2 一步没走**:只有 `settings`/`errors` 两个命名空间(`i18n.ts:11-20,36`),`canvas`/`copilot`/`trace` 都不存在;`useTranslation` 只出现在 16 个文件、全是 settings 家族 |  | `canvas` / `copilot` / `trace` 三个命名空间存在且被使用;`routers/skills.py` 里用户可见的中文消息走 i18n |
| K5 | **确定性假值可能把路由卡在环里(predict 的已知边界)** | 引擎 | ✅ **已修 + 真机点验(2026-08-21,PR #940),并据实订正了本行原判据**。原判据要求「造一个确定性假值必然成环的 skill」,而动手造它的过程坐实了**这个 skill 造不出来**:相位路由全静态(仓内没有 `add_conditional_edges`,agent 自己的中间件之外没有 `Command(goto=...)`),相位成环是编译期 FATAL `[F-v3-graph-phase-cycle]` —— 守卫要防的那条路今天大概率不可达。**真正在发生的是反向误杀**:守卫按相位名在整条路径上计数,分不清 `iterate` 计划内的重复与路由回头,于是一个11 项的普通循环(LOGIC 相位跑两行 Python,附近连模型和桩数据都没有)被判 `PredictDeadlockError`。#940 把计数改成按 **(相位, 迭代轮次)** ——一千项的循环是一千组各一次;顺带把只有引擎在执行的上限常量和只写了两遍的文案都归还引擎。**守卫没有删**:「我造不出来」不等于「它不可能发生」,而它停止误伤之后不要钱。**真机实测(2026-08-21)**:`k5-loop-fixture`(11 项 `iterate` 循环)在 app 里 predict **成功**,`result.json` `success: true`,12 条相位记录里 `collect` 被访问 **11** 次(旧上限 10),`final_state.collected` 11 项齐全。截图 `K5-loop-predicted.png`。死锁真值路径由合成测试(11 次执行不属于任何迭代轮次 = 一组十一次,仍判死锁)继续钉住。 以下保留立行时的判断 —— **已定位、已有兜底、尚未单独验收**。K2 的桩数据是**确定性**的:同一个 agent node 每次拿到同一份假值,于是一条「按上一步输出决定走哪条边」的路由**每轮都会做同一个选择**——真跑里靠模型输出的差异自然跳出的环,在 predict 里跳不出去。引擎的兜底是 `MAX_PHASE_REVISITS = 10`,超出即 422 `PREDICT_DEADLOCK`。**为什么必须单列而不是并进 K2**:K2 的判据是「没有 copilot 也能跑完 predict」,而这条边界恰恰是「predict 合法地跑不完」的情形;两句写在同一行,那一行就自相矛盾。**待验收的是它的行为,不是它的存在**:代码里有常量和错误码不等于用户遇到时能看懂 | 台账核实 2026-08-20(K2 判据后半明确要求单列;非用户报障) | 合法的多轮循环不被守卫误杀(真机已验);死锁守卫按 (相位, 迭代轮次) 计数并保留,其真值路径由合成测试覆盖 —— 现实中不可构造,因相位成环是编译期 FATAL |
| K6 | **子 skill 的诊断跨过 subgraph 边界时会掉轴**(立行时误判为掉的是路径,实测掉的是 `conflicting_phase`) | 引擎→前端 | ✅ **已修,真机点验通过(2026-08-21,PR #946)**。**立行时的诊断被实测推翻了一半**:造一个两层嵌套 fixture(parent → `subgraph/mid` → `subgraph/leaf`,冲突在最深的子 skill)编译父 skill,`source_path` 报的是 **`subgraph/mid/subgraph/leaf/phases/revise/LOGIC.md`** —— 路径轴**早就是对的**(2026-08-15 那次 `_payload_source_path` 重做修的就是它),前端那条 `MESSAGE_LOCATION_RE` 是修好之前留下的死路。**同一次测量抓到了真的缺陷**:同一条诊断的 `conflicting_phase` 是 **`None`** —— K3 刚刚结构化好的「上游 phase 是谁」,一跨 subgraph 就没了,画布在嵌套情形下被逼回去读英文句子。**根因是接缝的形状,不是漏了一个字段**:`_compile_subgraph_children` 把子诊断**逐字段列举**着重建成父诊断,于是 `CompileIssue` 以后每加一个结构化事实,它在别处都对、唯独跨一层 subgraph 消失,而且**没有任何东西会失败**。**改法**:接缝收敛成一个 `_diag_from_child_issue`,明确写清**只有 `source_path` 重算**(它只相对某个被声明的 root 才有意义)、**其余全部携带**;再加一条守卫测试断言 `CompileIssue` 的字段集合 = 携带集 ∪ 重算集 ∪ 「别处说一次」集(`severity`),新加字段必须在这里做一次决定,否则红。前端连带删掉 `MESSAGE_LOCATION_RE`、`relativePathFromMessageLocation` 与 `filePathForRoute`,`file` 轴单独定路由;原来钉 fallback 的那条测试改成钉相反的事。engine 设计源同 PR 补「子诊断带着全部轴到达父」一条(哈希已重钉)。 **真机点验(2026-08-21,vendor 已按仓规第 7 条重建)**:把同一个两层嵌套 fixture 落到 `D:\coding\skills\k6-nested-fixture` 并在真窗口里打开、点 Compile——抽屉第一条就是 `subgraph/mid/subgraph/leaf/phases/revise/LOGIC.md:1 - io.outputs.properties.summary - [F-v3-sequential-overwrite-unauthorized]`(截图 `K6-01-nested-path-in-drawer.png`);再用 app 自己的 sidecar 凭据取回同一次编译的结构化 JSON,那条诊断的 `conflicting_phase` 是 **`"draft"`**——修复前这里是 `None`。 **同一次测量另发现一条,已单开 N6**:这条诊断带着全部轴到达前端后,被 `compileErrorsByNode` 按「只认根节点」滤掉,画布的嵌套路由链因此不触发。 | K3 修复过程中发现(2026-08-21) | 嵌套子图里的诊断,其结构化 `source_path` 是父 skill 可直接寻址的路径(**已实测**),且 `conflicting_phase` 等其余结构化轴跨 subgraph 不丢失(**已实测:`"draft"`**);前端删掉 `MESSAGE_LOCATION_RE` 后嵌套路由测试仍绿(#946 `frontend-gates` 通过) |
| N6 | **嵌套子图的诊断到了前端却进不了画布**:引擎已把全部轴送到,而按「哪个根节点」归属的那一步把它整条丢掉,于是为嵌套冲突写的自动展开 + 弹窗链在真机上永远不会触发 | 前端(诊断归属 → 画布路由) | ✅ **已修,待真机**。**实测(2026-08-21,K6 真机点验顺链发现)**:两层嵌套 fixture 编译后,冲突诊断到达前端时 `file = subgraph/mid/subgraph/leaf/phases/revise/LOGIC.md`、`conflicting_phase = "draft"`(K6 已证),而画布**原地不动**——既没有自动展开 `subgraph/mid` → `subgraph/leaf`,也没有冲突弹窗(画布文本只有 `Input` / `mid_stage` / `Output`,三个都是 `Idle`)。**根因**:`Workspace.tsx` 把 `compileErrorsByNode(...)` 的结果当作路由输入交给画布,而 `compileErrorNodeId`(`node-compile-errors.ts:30-55`)对每条诊断只问一件事——**哪个根节点拥有这个文件**:`rootPhaseIdFromPath`(`diagnostic-paths.ts:30` 的 `^phases\/`)、`boundaryNodeIdFromFile`(只认 `.workspace/`)、`fieldAxisCanNameARootNode`(只认根 `GRAPH.md`),三问皆否则 `return null`,该诊断在 `:63-65` 被 `continue` 掉。于是 `GraphCanvas.tsx` 的 `sequentialOverwriteRoutes` 对嵌套冲突恒为空,自动展开与弹窗都没有输入。**这不是「漏了一个 if」,是两块各自自洽的逻辑对同一个问题给了两个答案**:`diagnostic-paths.ts` 的模块注释明确裁决「子 skill 里的文件不归任何根节点」——那条裁决对**节点徽标**是对的(嵌套相位在根图上没有自己的节点);而 `sequential-overwrite-routing.ts` 整套(含 `subgraphPathChain`、`findNextSubgraphExpansionNode`、`subgraphPreviewChildNodeId`)存在的理由**就是**处理「诊断在子 skill 里、要展开到预览子节点上去画」,它的单测直接钉两层嵌套。两者都对,中间那一段「谁把嵌套诊断交给路由」没有 owner,于是走了徽标那条裁决。 **改法**:路由的输入换成**这一遍编译的诊断本身**——画布的 prop 从 `sequentialOverwriteErrorsByNodeId: Record<nodeId, CompileError[]>` 改成 `manualCompileErrors: readonly CompileError[]`,`sequentialOverwriteRoutesFromNodeErrors` 改成 `sequentialOverwriteRoutesFromCompileErrors`。**这一步没有放宽任何过滤**:分桶那一层对这个消费者本来就只起过滤作用(它 `Object.values(...)` 把键全丢了),而真正的过滤在 `sequentialOverwriteRouteFromCompileError` 里——先按 `error_code` 认冲突,再按相位文件正则定位置;仍然只吃**手动 Compile** 的诊断,首屏/实时 lint 不进这条弹窗通道(那条既有测试改了断言形状、没改意图)。新增两条测试:一条同时钉住两端(同一条嵌套诊断在 `compileErrorsByNode` 里得到空桶、在新入口里得到完整子图链),一条钉住「按相位去重、其他 error_code 不进来」。设计源 `02_capabilities/conflict-overwrite/mvp1-alignment.md` §3 补一条 Routing input(哈希已重钉)。 **真机点验 2026-08-21:改判 ✅**。同一个 `k6-nested-fixture` 在真 app 里手动 Compile 后,画布**自己展开了两层**——`k6_mid` 展开到 `k6_leaf`,冲突弹窗落在最深那一层的节点上,并指名上游相位 `draft`(截图 N6-01)。对照修改前同一操作的观察(画布原地不动、只有 `Input` / `mid_stage` / `Output` 三个 Idle 节点),这一条的因果是闭合的:变的只有路由的输入端。 | K6 真机点验 2026-08-21(非用户报障) | 打开两层嵌套 fixture(冲突在最深的子 skill)点 Compile 后,画布自动展开 `subgraph/mid` → `subgraph/leaf`,并在预览出来的 `revise` 子节点上给出顺序覆写冲突弹窗、点名上游相位 `draft`;真机截图为证 |

---

## 附:方法纪律(不入销账,指向规则文件)

用户反复重申的工作纪律已固化于:AGENTS.md(Development Principles/Coding Standards/铁律)、
CLAUDE.md(交互偏好/汇报纪律/裁决分层)、`.claude/skills/studio-verify`、记忆库。高频重申且需持续自查:
不问选择题、自己测完再说、结论要端到端实测支撑、黑话必解释、报告正文直接贴出、截图走 Artifact、
第一性原理+找成熟工程参考、模块化高内聚低耦合、一个一个模块扎实做完(2026-08-19 建账方法即其落地)。
