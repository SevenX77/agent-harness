{
  "stage": 2,
  "region": "settings:api-keys",
  "note": "轨：官=official / 三=third-party / 共=两类共用。drift 铁律：#24/#25/#27 的官/三测试不对称是现状，非目标——目标=统一 `POST /endpoints/{id}/test` + 批量探测 + 能力探测（§1.2/§6.1）。",
  "atoms": [
    {
      "n": 16,
      "cap": "secret-hydration",
      "track": "共",
      "marker": "live",
      "func": "进 API Keys tab 时把后端 redact 成 '**********' 的密钥换回真值，让用户看到/改自己已存的 key。",
      "action": "切到 API Keys tab（首次进入触发一次加载）。",
      "fe": "`getCredentials({ hydrateSecrets })`（SettingsPage 拉凭证的入口，`SettingsPage.tsx:425/457` 首次 hydrate=true、聚焦重拉 hydrate=false）→ `hydrateEndpointSecrets`（`api/llm.ts` 里逐 endpoint 把掩码换真值的函数）只对 `api_key === redactedSecret` 的 endpoint 逐个发 GET，换回明文后写进 `cachedRegistry`。",
      "be": "`get_registry_endpoint_secret`（`llm.py:348` 按 endpoint_id 返回单条明文 key 的 handler）从 `load_credentials()` 取该 endpoint 的 `api_key.get_secret_value()` 原样返回；不探测、不改状态。",
      "api": "GET /llm/registry/endpoints/{endpoint_id}/secret（逐 endpoint 一次；先 GET /llm/registry 拿 redact 快照）。",
      "gap": "符合设计（§6.1 握手 A10「secret reveal：进 tab 逐个换真值」+ A1「GET registry redacted」）。"
    },
    {
      "n": 17,
      "cap": "provider-partition",
      "track": "共",
      "marker": "live",
      "func": "把页面拆成 official 区（固定 5 厂商预渲染）和 third-party 区（用户自增），用户一眼分清官方/第三方。",
      "fe": "`ApiKeysTab.tsx:73-156`（页面骨架）用 `CatalogAccordion` 两个 section；official 区 `officialProviderDrafts(drafts)`（`provider-utils.ts:78` 把 5 个固定厂商各映射成一张卡，缺则补空卡）、third-party 区 `thirdPartyProviderDrafts(drafts)`（`provider-utils.ts:102` 过滤出非官方 draft）。每张卡渲染 `ProviderCard`（`api-keys/ProviderCard.tsx:722` 单 provider 录入+测试卡）。官方 5 厂商钉死在 `provider-utils.ts:5-11`（anthropic/openai/gemini/deepseek/ark + canonical base_url）。",
      "action": "无（进 tab 自动渲染）。",
      "be": "无（前端按 `officialProviders` 白名单分区；后端只回平铺 registry）。",
      "api": "无（数据来自 atom #16 的 GET /llm/registry）。",
      "gap": "符合设计（§1.1/§1.2 official 固定可控 vs third-party 自增；§7.2 现状 #17 标 ✅）。"
    },
    {
      "n": 18,
      "cap": "official-key-only",
      "track": "官",
      "marker": "live",
      "func": "官方卡只让填 API Key，Base URL/Protocol 用 canonical 默认值且隐藏，用户不会误改官方端点参数。",
      "fe": "`ProviderCard.tsx:1010-1067` 的三段（Protocol Select / Base URL Input / Endpoint test）都包在 `!isOfficial` 条件里——官方卡 `isOfficial=true` 时整段不渲染，只剩 `:946-1009` 的 API Key 行。canonical base_url 由 `officialProviderDrafts`（`provider-utils.ts:78-92`）从白名单 `officialProviders[].baseUrl` 注入、`withOfficialProviderDefaults` 兜底，不暴露成可编辑输入。官方卡也无 ⋮ 菜单（`:902/909` 的 Third-party badge 与 dropdown 同样 `!isOfficial` 门禁），故不可改名/删。",
      "action": "在官方卡只填 API Key（无其它可填项）。",
      "be": "无（前端隐藏字段；后端按 endpoint_id 白名单钉 `provider_kind='official'`，见 §7.2 注）。",
      "api": "无（填写本身不发请求；保存走 PUT /llm/registry/endpoints，测试见 #24）。",
      "gap": "符合设计（§1.1「official 只需要 get models」、§7.2 #18 ✅「canonical 默认+隐藏，不可增删改名」）。"
    },
    {
      "n": 19,
      "cap": "tp-add-provider",
      "track": "三",
      "marker": "live",
      "func": "让用户新增一个第三方 provider 占位卡，之后填它的 URL/协议/key。",
      "action": "点 `+ Add Provider` → 弹框填 provider 名 → 提交。",
      "fe": "`ApiKeysTab.tsx:131-153`：`+ Add Provider` 按钮开 `RoleNameDialog`（复用的命名弹框），提交回调里 `code = custom-{randomUUID}` 后 `onAddProvider({...})`；落到 SettingsPage 的 `addProviderWithData`（`SettingsPage.tsx:607` 用 `draftFromAddProviderSubmission` 建草稿 draft 并入队保存）。新卡此时 base_url/api_key 都空，待用户在卡内补填（atom #20）。",
      "be": "无（新增即建前端草稿；草稿随 debounce 走 PUT /llm/registry/endpoints 落库，无独立 add 端点）。",
      "api": "无（提交本身不发请求；后续草稿保存走 PUT /llm/registry/endpoints）。",
      "gap": "现状=两步（先弹框只填名建空壳，再回卡里填 URL/key），目标=inline 一次把 name/base_url/protocol/key 填全（§1.2，§7.2 #19 标 ⚠️）。`AddProviderForm`（`api-keys/AddProviderForm.tsx` 旧的一次填全表单组件）此处未挂载，属孤儿（§7.2 注）。"
    },
    {
      "n": 20,
      "cap": "tp-credential-edit",
      "track": "三",
      "marker": "live",
      "func": "让用户填/改第三方 provider 的 name、base_url、protocol、api_key 四项凭证。",
      "action": "在第三方卡内编辑 Protocol 下拉 / Base URL 输入 / API Key 输入；改名走 ⋮ → Rename。",
      "fe": "`ProviderCard.tsx`：Protocol `Select`（`:1014` provider_type 下拉，4 协议选项 `:60-65`）、Base URL `Input`（`:1041`）、API Key `Input`（`:954`）各自 `onChange → onFieldChange({...})`；name 由 ⋮ Rename 的 `RoleNameDialog`（`:1166`）回填。每次 patch 落 SettingsPage `updateProviderField`（`SettingsPage.tsx:567` 合并 patch 进 draft 并 `scheduleSave` debounce 保存）。",
      "be": "无（仅前端草稿 + debounce 落库；探测在 #24/#25/#27 才发生）。",
      "api": "无（编辑触发的保存走 PUT /llm/registry/endpoints，非本动作直接调用）。",
      "gap": "符合设计（§1.2「必须填 URL」+ protocol 当前仍可手选；§7.2 #20 ✅）。注：protocol 目标是系统自动探测（§1.2），但「手填 protocol 可用」本身不算 drift，自动探测属 #24/#25 测试路径的 ③b 能力。"
    },
    {
      "n": 21,
      "cap": "credential-key-edit",
      "track": "共",
      "marker": "live",
      "func": "改 API Key（官/三共用）后，让旧测试结果立即失效、状态徽章退回 untested，避免拿旧 key 的「已连通」误导用户。",
      "action": "在 API Key 输入框里修改 key 文本。",
      "fe": "`ProviderCard.tsx:959-962` API Key `onChange → onFieldChange({ api_key })`。SettingsPage `updateProviderField`（`SettingsPage.tsx:570-577`）比对 `providerTestParamsMatch`（`provider-utils.ts:127` 比 api_key/base_url/provider_type 是否一致），参数变了就把该 provider 加进 `invalidatedTestOutcomeIdsRef`（失效集合）；徽章靠 `providerCachedTestResult`（`provider-utils.ts:146` 按参数指纹 `providerTestParamsFingerprint` `:138` 查缓存结果）——指纹对不上则取不到旧结果，回落 not_configured/untested。",
      "be": "无（失效判定纯前端按参数指纹；后端只在重测时写新状态）。",
      "api": "无（改 key 本身不发请求）。",
      "gap": "符合设计（§7.2 #21 ✅「改后旧测试失效→badge 回 untested」；与 §4.2 6 态体系一致）。"
    },
    {
      "n": 22,
      "cap": "secret-mask-toggle",
      "track": "共",
      "marker": "live",
      "func": "用 Eye/EyeOff 在明文与掩码之间切换 API Key 显示，方便核对又默认不外泄。",
      "action": "点 API Key 输入框旁的 Eye / EyeOff 图标。",
      "fe": "`ProviderCard.tsx:978-987` Eye 按钮 `setVisible(v => !v)`；`:957` `type={apiKeyInputType(visible)}`，而 `apiKeyInputType`（`:91`）`return visible ? \"text\" : \"password\"`——掩码态切到原生 `password` input。",
      "be": "无（纯前端显隐）。",
      "api": "无。",
      "gap": "DRIFT（契约层）：现状切原生 `password` type；目标=永远 `type=text + CSS mask`（§6.1 ①前端「API key 输入 type=text + CSS mask + 显隐 + 复制 + 密码管理器抑制属性」）。原生 password 会触发浏览器密码管理器、且与抑制属性语义冲突。§7.2 #22 标 ⚠️「违契约」。"
    },
    {
      "n": 23,
      "cap": "secret-copy",
      "track": "共",
      "marker": "live",
      "func": "一键把 API Key 复制到剪贴板，省去手动选中长串密钥。",
      "action": "点 API Key 旁的 Copy 图标。",
      "fe": "`ProviderCard.tsx:988` `FieldCopyButton`（`:263` 通用复制按钮）→ `copyCredentialValue`（`:103` `navigator.clipboard.writeText` + 成功/失败 toast）；空值时按钮 disabled。",
      "be": "无（浏览器剪贴板 API，无网络）。",
      "api": "无。",
      "gap": "符合设计（§6.1 ①前端「复制」；§7.2 #23 ✅）。"
    },
    {
      "n": 24,
      "cap": "official-test-job",
      "track": "官",
      "marker": "live",
      "func": "官方卡点 Test：异步批量 job 拉全厂商模型目录、提 endpoint 为 verified，给用户进度反馈而不是干等。",
      "action": "在官方卡点 `Test` 按钮。",
      "fe": "官方卡的「Test」即 `ProviderCard.tsx:992-1005` 的 Get Models 按钮（`isOfficial` 时文案=Test、图标=烧瓶）→ `runProviderGetModels`（`SettingsPage.tsx:633`）。`isOfficial` 分支走 `getProviderModels(..., {onProgress})` → `testOfficialProviderModelsWithJob`（`api/llm.ts` 启 job + 轮询的函数）：先 `startEndpointTestJob` POST test-jobs，再 `waitForEndpointTestJob` 每 `delay(750)`（`api/llm.ts:1433`）轮询，progress 经 `handleOfficialProgress`（`SettingsPage.tsx:654`）实时刷卡。",
      "be": "`start_endpoint_test_job`（`llm.py:389` 起官方测试 job 的 handler）建内存 job、`_spawn_background_task(_run_official_endpoint_test_job(...))` 后台跑批量探测，`get_endpoint_test_job`（`llm.py:472`）回进度。endpoint 拉到模型后提 verified（见 `test_endpoint` `llm.py:561-562` official 分支：`discovered_model_ids` 非空即 `status='verified'`）。",
      "api": "POST /llm/endpoints/{endpoint_id}/test-jobs（起 job）+ GET /llm/endpoint-test-jobs/{job_id}（750ms 轮询进度）。",
      "gap": "DRIFT（铁律）：`start_endpoint_test_job`（`llm.py:395-399`）硬门禁 `if endpoint.provider_kind != 'official': raise 400`——批量 job 端点只认官方，第三方被拒。目标=统一 `POST /endpoints/{id}/test` + 批量探测（不分官/三同一套异步批量），见 §1.2「endpoint 真连通验证=批量模型探测」+ §6.1 A5。"
    },
    {
      "n": 25,
      "cap": "tp-getmodels",
      "track": "三",
      "marker": "live",
      "func": "第三方卡点 Get Models：同步单次拉一遍模型清单做发现，回填 Available Models。",
      "action": "在第三方卡点 `Get Models` 按钮。",
      "fe": "`ProviderCard.tsx:992-1005` 非官方时文案=Get Models → `runProviderGetModels`（`SettingsPage.tsx:633`）非 `isOfficial` 分支走 `getProviderModels(...)` 不带 onProgress（同步单次，无 job 轮询）；结果经 `upsertProviderModelsListResponse`（`SettingsPage.tsx:680` 把 models-list 响应并进凭证态）刷卡。",
      "be": "`getProviderModels` 第三方走 `api/llm.ts` 里 POST `/endpoints/{id}/test`（先 PUT upsert 该 endpoint），命中后端 `test_endpoint`（`llm.py:480`）：连通后 `status='unverified_manual'`（`llm.py:521`），official 才在 `:561-562` 升 verified——**第三方停在 unverified_manual**，只证 apikey+URL 可达、不证能生成。",
      "api": "POST /llm/endpoints/{endpoint_id}/test（同步单次 models-list；前置 PUT /llm/registry/endpoints 落 endpoint）。",
      "gap": "DRIFT（同 #24）：第三方 get-models 过了只升 `unverified_manual`、不做批量推理探测，与官方 verified 不对称。目标=第三方也用批量模型探测打推理端点才判 verified（§1.2 item2「为什么必须探模型」+ 自动协议探测；§6.1 A5）。"
    },
    {
      "n": 26,
      "cap": "tp-model-probe",
      "track": "三",
      "marker": "live",
      "func": "在第三方卡的「Endpoint test」填一个 model id，单独探测这个 model 能否真生成，作为 endpoint 验证的轻量入口。",
      "action": "在第三方卡「Endpoint test」输入一个 model id → 点 `Test`。",
      "fe": "`ProviderCard.tsx:1068-1105` 的 `Endpoint test` 段（仅 `!isOfficial`）：`Input` 收 `endpointModelId`，`Test` 按钮 `onEndpointTest(trimmedEndpointModelId)`（`:1091`，未填 model 时 disabled）→ `runProviderEndpointTest`（`SettingsPage.tsx:704` 单模型 endpoint 测试），结果 `upsertProviderTestResponse`（`:734`）刷卡。",
      "be": "`testProviderEndpoint`（`api/llm.ts`）POST `/endpoints/{id}/models/test` body `{model_ids:[单个]}` → 后端 `test_endpoint_models`（`llm.py:599`）第三方分支 `_probe_model`（`llm.py:696-705` 对该 model 打 1 次推理探测），ok 则该 route+endpoint 升 verified（`:737-745`），失败写 endpoint failed（`:746-764`）。",
      "api": "POST /llm/endpoints/{endpoint_id}/models/test（model_ids 含单个；前置 PUT /llm/registry/endpoints）。",
      "gap": "符合设计（§7.2 #26 ✅；是 §1.3「留了入口可批量对单个模型 probe」的单模型形态）。注：单模型探测本身不可靠（§1.2 item2 瞬时抖动），目标主路径是批量，但本入口作为 escape hatch 保留合理。"
    },
    {
      "n": 27,
      "cap": "manual-model-probe",
      "track": "共",
      "marker": "live",
      "func": "在「Manual model probing」面板加多个 model id 一次性批量探测，自动列模型不可用时手动喂候选。",
      "action": "在 Manual model probing 面板点 `Add Model` 填多个 model id → 点 `Test Models`。",
      "fe": "`ManualModelTestPanel.tsx:134`（手动多模型探测面板）：`modelIds` 数组增删（`:218-275` Add Model / 垃圾桶），`runModelTests`（`:172`）调 `testProviderModels({provider_id, model_ids})`，结果 `ManualModelResultList`（`:113` 绿/红 badge 列表）+ `onModelsUpdated` 回写 Available Models。挂载条件 `shouldShowManualModelPanel`（`provider-utils.ts:116`：官方恒显；第三方需已 ok 或已有模型）。",
      "be": "`testProviderModels`（`api/llm.ts`）POST `/endpoints/{id}/models/test` → 后端 `test_endpoint_models`（`llm.py:599`）**按 kind 分叉**：官方（`:627-694`）走 `_probe_official_model_profile_result` 产多候选 `VerifiedProfile`（多能力 profile）；第三方（`:696-784`）走 `_probe_model` + 成功时写死 `_successful_generation_probe_capabilities()`（`llm.py:4173` 硬编码 `input_modalities/output_modalities=['text']`）= **text-only**。",
      "api": "POST /llm/endpoints/{endpoint_id}/models/test（model_ids 多个）。",
      "gap": "DRIFT（铁律）：官→多候选 VerifiedProfile（带真能力探测）/ 三→单次 `_probe_model` 写死 text-only 能力，两条路不对称。目标=统一批量探测 + 统一能力探测（不分官/三都做能力归一化/probe 兜底），见 §1.2 + §6.1 A5/A7「capability 归一化 + 缺则 probe」。§7.2 #27 标 ⚠️。"
    },
    {
      "n": 28,
      "cap": "notable-models",
      "track": "共",
      "marker": "live",
      "func": "Manual 面板自动拉该 provider 的 notable-models 候选，作为 model id 输入框的占位建议，降低用户手填门槛。",
      "action": "无（Manual 面板挂载时自动拉取）。",
      "fe": "`ManualModelTestPanel.tsx:152-170` `useEffect` 调 `getNotableModels(notableProviderKey)`（`api/llm.ts` 拉 doc 维护候选），写入 `notableModels` 供 `modelIdPlaceholder`（`:59` 生成占位文案）；provider key 由 `notableProviderKeyForDraft`（`provider-utils.ts:106` 从 id/name/url 猜 provider 短名）给出。",
      "be": "`get_provider_notable_models`（`llm.py:593` 返回 doc 维护的 model id 建议的 handler）调 `notable_model_ids(provider_key)`，有 note 文件即返、不分官/三。",
      "api": "GET /llm/providers/notable-models?provider_key={key}。",
      "gap": "符合设计（§7.2 #28 ✅「有 note 文件即返，不分官/三」；属 §1.2/§6.1 ①前端的输入建议辅助）。"
    },
    {
      "n": 29,
      "cap": "tp-rename-delete",
      "track": "三",
      "marker": "live",
      "func": "第三方卡的 ⋮ 菜单改名 / 删除（删除带二次确认），让用户管理自增的第三方 provider；官方卡无此菜单不可改名/删。",
      "action": "点第三方卡 `⋮` → Rename（弹框改名）/ Delete（确认 toast 后删）。",
      "fe": "`ProviderCard.tsx:909-944` 的 `DropdownMenu`（仅 `!isOfficial`）：Rename `setRenameOpen(true)` 开 `RoleNameDialog`（`:1166`）→ `onFieldChange({name})`；Delete `requestDeleteConfirmationToast`（`:931` 删除二次确认 toast）确认后 `onDelete` → SettingsPage `deleteProvider`（`SettingsPage.tsx:616` 从 drafts 移除并标记 role 投影需刷新、debounce 落库）。",
      "be": "删除最终经 PUT /llm/registry/endpoints 整表 upsert（缺席 endpoint id 被移除）；若直接删 endpoint 则走 `delete_registry_endpoint`（`llm.py:369` 删 endpoint 并级联清 role 里引用该 endpoint 的 route）。改名只改 display_name，随草稿保存落库。",
      "api": "PUT /llm/registry/endpoints（整表 upsert，删除=缺席即移除）；备用 DELETE /llm/registry/endpoints/{endpoint_id}（级联清 route 引用）。",
      "gap": "符合设计（§6.1 A11「删 endpoint 二次确认 + PUT 整表 upsert」；§7.2 #29 ✅「删除二次确认 toast」、官方不可改名/删）。注：`ProviderDeleteButton`（`ProviderCard.tsx:232` 独立删除按钮）定义未在卡内渲染，属孤儿（§7.2 注）。"
    },
    {
      "n": 30,
      "cap": "test-status-projection",
      "track": "共",
      "marker": "live",
      "func": "把后端测试结果投影成状态标签：第三方卡顶层一个连通徽章（按参数指纹取缓存结果）、官方每条 route 一个彩色 Tag（后端权威状态）。",
      "action": "无（测试/加载后自动投影；含改 key 失效回退见 #21）。",
      "fe": "第三方顶层徽章 `TestMessage`（`ProviderCard.tsx:151` 把 testStatus 渲成 Connected/error/Not configured badge），`testStatus`（`:787-799`）由 `providerCachedTestResult`（按 `providerTestParamsFingerprint` 取缓存）+ hasVerifiedModel 推得。官方每 route 的 `Tag` 由 `renderAvailableModelTag`（`:821`）→ `routeStatusTagVariant`（`:350` 状态转色：verified=success/failed=destructive/disabled=muted…），官方 `backendRouteTagsAreAuthoritative=true`（`:761`）直接信后端 route.status。",
      "be": "状态权威来自后端 `_registry_response`（投影 endpoint/route 的 status 到 DTO）；route status 由 #24/#26/#27 各测试 handler 写（verified/unverified_manual/failed/disabled）。",
      "api": "无（投影本身不发请求；数据来自各测试响应里的 registry + GET /llm/registry）。",
      "gap": "现状用 RouteStatus 枚举（verified/unverified_manual/failed/disabled/...），目标=统一 §4.2 的 6 态（`ready`/`historical_ready`🔵蓝/`untested`/`failed`+reason/`cooling_down`/`off`）——现缺「🔵 蓝=以前联通过」历史态、且把配置缺口与测试失败统一成 failed+reason。§7.2 #30 标「→ 目标 6 态」。"
    },
    {
      "n": 31,
      "cap": "registry-restore",
      "track": "共",
      "marker": "live",
      "func": "刷新/重开页面后，从后端 registry 恢复每个 provider 的 key、测试状态、Available Models，不丢用户已配的凭证与探测结果。",
      "action": "无（刷新或重新进入页面自动恢复；其它窗口改动聚焦回来也重拉）。",
      "fe": "`getCredentials`（`api/llm.ts` 拉凭证）→ `getRegistry`（GET /llm/registry 全量快照）→ `registryToCredentials`（把 registry 转成 UI 凭证态，含 endpoint+route+status+available_models）；SettingsPage 初始与聚焦时各拉一次（`SettingsPage.tsx:425/457/488`）。key 再经 #16 hydrate 换真值。",
      "be": "`get_llm_registry`（`llm.py:340` 返回 endpoint/route/role 联合 redact 快照的 handler）`load_credentials()` + `_registry_response`，api_key 一律 redact 成掩码。",
      "api": "GET /llm/registry（恢复全量；key 真值另走 #16 的 secret 端点）。",
      "gap": "符合设计（§6.1 ①前端「只投影 registry，不持第二份」；§7.2 #31 ✅「刷新后从 registry 恢复 key/状态/Available Models」）。"
    }
  ]
}
