## 回执：#1075 未送达

先说清楚两件事，避免你按错误前提推进：

1. **我上下文里没有 #1075**。本会话我只收到过原始盘点任务，其中写的是「闭集 `host-native|embedded|ah`……这是**已批设计**」。§5.6 删除清单的正文我没有见过。
2. **输出文件为空是预期的**——我的工作约束禁止写报告类 .md，原报告是作为聊天消息整体交付的，不落盘。如果流程需要文件，请指定路径由你侧落盘，或解除该约束。

下面的改判**基于你这条消息给出的前提**（闭集 = `{embedded, ah}`，host-native 属删除面），不是基于 §5.6 正文。凡是取决于 §5.6 细节而我无法判定的，我明确标 `需 §5.6 裁定`。若与正文冲突，按 #1075 覆盖。

---

## ① 原报告条目逐条改判

### ③E1 — 闭集
| | 原判 | 改判 |
|---|---|---|
| 应然 | `host-native\|cli\|embedded` → 缺 `ah`、多 `cli` | 三个 kind **两删一建**：删 `cli` + `host-native`，建 `ah` |

新增删除坐标：`domain/models.py:248-250 HostNativeExecutorConfig`；`adapters/cli.py:84-85`(分支)、`:143`(choices)；`adapters/engine.py:235-236`、`:254-261`(`submit_agent_result` 的 host-native 前置守卫)。

**新增一条隐患（原报告没有，因为原设计保留 host-native）**：`domain/models.py:352 executor: ExecutorConfig = Field(default_factory=HostNativeExecutorConfig)` —— 默认值即将消失。所有**已落盘的 `RunRequest` 快照**在缺 `executor` 字段时会静默改判到新默认。这是行为可见的契约变更，须与 `schema_version` 一起处理，不能只换 `default_factory`。

### ③E2 — fallback_executors
**判定不变**（死字段 + 反设计），**理由增强**：闭集从 3 元降到 2 元后，`fallback_executors`（`models.py:337,357`，校验 `:359-366`，合并 `application/config.py:154-157`，落盘 `:321`）在语义上只剩「embedded↔ah 互为兜底」一种可能，而这正是「无自动 fallback」明令禁止的。删除优先级上调。

### ③E3 — NodeOverride 无 executor 字段
**判定悬置**。原判成立的前提是「相位级可覆盖」仍在设计内。你的消息只提到闭集变更，未提相位级覆盖。`需 §5.6 裁定`：若保留，`models.py:404-414` 仍需补字段；若 §5.6 同时收回相位级覆盖，E3 作废。

### ③E4 — preflight（**改判最重，从"缺一半"变成"归零"**）
| | 原判 | 改判 |
|---|---|---|
| 事实 | preflight 只绑在待删的 `cli` 上；host-native 只透传不校验 | 两个 kind 全删 ⇒ **preflight 存活实现数 = 0** |
| 建议 | 把 `host_native.py:39-114` 的拒绝清单上提复用 | 该清单本身是删除面，**无原型可提升**，须新建 |

删除前必须先**打捞**两份将死原型里的知识，它们记录的是真实的图形约束、不是实现细节：
- `adapters/host_native.py:56/69/78/109` —— 拒 registry 子图内 Agent、graph 级 iterate、相位级 iterate、与其他相位并行。
- `adapters/vendor_cli/runtime.py:73-87 _unsupported_phase_capabilities` —— 拒 tools / subagents / subgraphs / context_access。

这两份约束**不等价**（前者是"能不能定位到唯一等待点"，后者是"能不能桥接工具面"）。`ah` 继承哪一套，取决于 ② 里那个 sync/async 问题。

### ③E5 — 主仓无 executor 概念 / Studio 无 executor 字段
**判定不变**，且**变简单**：主仓 engine 从来没有 host-native，无回退工作量。`services/runtime_config.py:37-58` 仍无 executor 键，`app/models/*` 与 `frontend/src/api/types.ts` 仍零命中。

### ⑤ handoff 相关判定 —— **我原报告有一处明确错误，此处撤回**

原 T-E2 第 3 条我写：「保留并复用：`agent_handoffs.py:38 SqliteAgentHandoffStore`、`:267 canonical_agent_result_hash`、`core/runner.py:1862 _apply_external_phase_completion` —— 这三件**与 vendor 无关**」。

「与 vendor 无关」属实，但我由此推出「可保留」是**错的**：它们与 vendor 无关，是因为它们与 **host-native 强绑定**。证据见 ②。在 §5.6 下三件全部转为删除面。撤回原表述。

同批受影响、原报告未标为删除面的还有：
- `core/runner.py:591 recover_paused_skill`（我原本把它列为 GSR 独有能力）
- `core/runner.py:166 ExternalPhaseCompletion` + `:667` 形参 + `:780` 调用点
- **6 个 agent 生命周期事件**（`callbacks/events.py:773-846`）及 `domain/models.py:13-57` 闭集里对应的 6 个值 —— 详见 ③ 观测域连带。

### ⑦ M-E2 `ExecutorPort`
**保留，重新界定范围**：
- `ports/runtime.py:31 AgentExecutor(Protocol)` 作为**形状**存活，`ah` 是它的第一个实现（当前仍是零实现）。
- `V-E3`（Port 与 `vendor_cli/runtime.py:41-47 _CliExecutor` 双声明）**由删除自动消解**，不再需要单独工单。
- `preflight` 从「上提既有实现」改为「新建」（见 E4）。
- Port 的 `submit_agent_result` 半边是否存在，`需 §5.6 裁定`（见 ②）。
- 「解析失败直接失败、不 fallback」不变。

### ⑧ T-E1 / T-E2
- **T-E1** 增补：删 `HostNativeExecutorConfig` 及其 4 处引用；重定 `models.py:352` 默认值并升 `schema_version`；preflight 新建 + 先打捞两份原型的约束知识。
- **T-E2** 删除面规模改判：

| 面 | 行数 | 坐标 |
|---|---|---|
| vendor_cli（原报告已算） | ≈1943 | `vendor_cli/*` 1943 + `models.py:253-284` + `cli.py:71-99,146-159` |
| host_native.py | 320 | 全文件 |
| host_native_runtime.py | 410 | 全文件 |
| agent_handoffs.py | 278 | 全文件 |
| runner 内 host-native 区 | ≈150 | `:166-179`、`:591-647`、`:667`、`:780`、`:1862-1941` |
| 6 个 agent 事件类 + 闭集值 | ≈80 | `callbacks/events.py:773-846`；`models.py:13-57` 中 6 行 |
| engine/cli/mcp 出口 | ≈40 | `engine.py:235-236,246-261`；`cli.py:201 submit`；`adapters/mcp.py` |
| **合计** | **≈3220** | |

外加一条**检查点兼容**风险（原报告未覆盖）：`core/state.py:220 agent_result_hashes` 是 `FrameworkState` 的持久化字段、`:246` 在其键列表内。删掉唯一写者后该字段成为孤儿；若一并从 state 模型移除，则属**已落盘 sqlite 检查点的 schema 变更**，需迁移或显式声明不兼容。

---

## ② 脱离 handoff 后是否还有独立效果

**结论：三件都没有独立效果。** 证据（本轮定向核实，非推测）：

**`_apply_external_phase_completion`（`core/runner.py:1862`）— 无独立效果**
- 唯一调用点 `core/runner.py:780`，仅在 `resume_skill(..., external_phase_completion=...)`（形参 `:667`）非空时到达。
- 该实参在 src 内**唯一生产者**：`adapters/host_native_runtime.py:358`（`:308` 导入）。src 内再无第二处。
- 唯一其他引用是测试 `tests/application/test_host_native_handoff.py:596`（文件名即证）。
- 函数体本身写死 host-native 语义：`:1929` 生成字面量 `f"host-agent:{completion.task_id}"`；`:1884-1888,:1931` 读写 `flow.agent_result_hashes`，该字段仅由此处写入（全仓 `agent_result_hashes` 命中只有 `state.py:220,246` 定义 + `runner.py` 这一处读写 + `agent_handoffs.py:43` 注释）。

**`agent_handoffs.py` 整个模块 — 无独立效果**
- 三个导出符号的**全部消费者**只有两个文件：`adapters/host_native.py:16,230,294,303` 与 `adapters/host_native_runtime.py:11-13,79-80,90,103,121,163,165,310,322,325,327`。
- src 内零外部消费者。SQLite 表 `agent_handoffs`（`:60`）无其他读写方。

**`recover_paused_skill`（`core/runner.py:591`）— 无独立效果**
- 唯一调用点 `adapters/host_native_runtime.py:213`（`:186` 导入）。src/tests 内无他。

**边界澄清：`AgentTask` / `AgentResult` 不属于同一批，不可连坐删除。**
它们的消费面横跨 18 个文件，含公开出口 `sdk.py`、`__init__.py`、`ports/runtime.py:31`、`domain/models.py`、`adapters/mcp.py`、`application/service.py`。既然 `ah` 要实现 `AgentExecutor.execute(AgentTask) -&gt; AgentResult`，这两个类型应当**存活**。

**因此 §5.6 必须回答的唯一枢纽问题：`ah` 是同步还是异步？**

| | 若 `ah` **同步**（阻塞调出，形如 vendor_cli） | 若 `ah` **异步**（durable pause + 外部 submit，形如 host-native） |
|---|---|---|
| 上述三件 | 全删 | 三件的**能力**必须重建（可复用现有代码，改名去 host-native 语义） |
| `AgentRequired` / `submit_agent_result` / `resume` 提交路径 | 一并删（`engine.py:249-261`、`cli.py:201`、mcp submit tool） | 保留并重新指向 ah |
| 6 个 agent 事件 | 无生产者，删（见 ③） | 保留，且必须补前端读法 |
| preflight 应继承 | `vendor_cli/runtime.py:73-87`（工具面） | `host_native.py:39-114`（等待点可定位性） |

我无法从代码判定这一项——它是设计裁定，不是事实。`需 §5.6 裁定`。

---

## ③ 其余条目确认

**执行域不受影响**：`③E6` 批跑无并发控制 + `_batches` 仅内存 + 错误码串台（`run_manager.py:1103-1129`）；`③E7` run_id 边界零校验（`routers/runs.py` 7 处、`websockets.py:28,50`）；`③E8` Studio 穿透 engine 私有模块（`engine.py:34-37`）；`③E9` GSR 无 import-linter；`V-E1` checkpoint validity 双实现（engine 那份死码）；`V-E2` verdict 双实现；`V-E4` HITL 状态双来源；`⑤` 中 `AgentExecutor` 空 Port / `fallback_executors` / `checkpoint_validity_by_phase` 死码 / `run_skill`·`predict_skill` 未接线；`M-E1`(三切)、`M-E3`(RunIdentity)、`M-E4`(前端 run 控制器)；`T-E3`~`T-E6`。以上一律不变。

**执行域两处微调**：`V-E3`（executor 身份协议双声明）由删除自动消解，撤销独立工单。`V-E5`（trace 目录三处各算）缩为**一处**——`vendor_cli/runtime.py:68` 与 `host_native_runtime.py:85` 两份 `_trace_directory` 随文件删除，只剩主仓 `graph_agent.io.run_layout`。

**观测域绝大部分不受影响**：`V-O1` route decision 三 owner 且枚举无门禁（最高危，不变）；`V-O2`/`V-O3`/`V-O4` 投影双实现；`V-O5` golden 四 owner；`V-O6`/`V-O7`；`③O1`~`O4`（无订阅、写盘失败只 log、O(n²) 去重、进程内锁）；`M-O1`~`M-O4`；`T-O1`（实时订阅 Port = 门禁③）、`T-O3`、`T-O4`、`T-O5`。**`T-O1` 仍是最高优先级，且完全不受 §5.6 影响**——`RuntimeEvent`(`models.py:650`)、`EventSink`(`ports/runtime.py:54`)、游标语义都与 executor 无关。

**观测域两处必须改判（原报告在此处会给出错误指令）**：

- **`③O5` + `T-O2` 第 3 条作废（条件性）**。我原写「前端 `ENGINE_EVENT_TYPES` 补 6 个 agent 事件」。这 6 个事件的**全部生产者**是 `host_native_runtime.py:105,151,167` 与 `vendor_cli/runtime.py:163,188,212` —— §5.6 下两者皆删 ⇒ **零生产者**。若 `ah` 同步，则应从 GSR 的 44 值闭集中**一并删除**这 6 值，前端不需要补任何读法。若 `ah` 异步且重新 emit，则原指令恢复有效。`需 §5.6 裁定`。
- **意外的好消息**：若 6 值删除，GSR 事件闭集 **44 → 38**，与主仓 `packages/graph-agent/src/graph_agent/callbacks/events.py` 的 38 类**完全对齐**，两仓事件契约差异归零，`test_engine_event_types_are_mirrored.py` 可直接跨仓复用。这会显著降低 Studio 切换的门禁②成本——建议 §5.6 决策时把这一项计入收益。

`T-O2` 第 1、2 条（枚举层门禁、gateway 第三份副本门禁）不受影响，仍是最高危项。

---

**待你回复**：把 §5.6 正文（或至少 `ah` 的 sync/async 裁定 + 相位级覆盖是否保留）发我，我把 ①E3、②枢纽表、③观测域两处收敛成确定结论。若你按 #1075 直接覆盖，请以本 addendum 的 ② 段证据（三件无独立效果、`AgentTask`/`AgentResult` 不可连坐）作为删除范围的边界依据。
