{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "chapters": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": true
      },
      "description": "Raw chapter list. Each item should include chapter_number and chapter_content."
    },
    "project_id": {
      "type": "string",
      "description": "Opaque project identifier used by the host storage layer."
    }
  },
  "required": ["chapters", "project_id"],
  "additionalProperties": true
}
