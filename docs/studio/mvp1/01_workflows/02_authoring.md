---
role: workflow-record
---

# Node 2: 编辑与编译 (Authoring & Compile)

> Tier: workflow · Owns: 把业务逻辑装配成严谨 graph_skill 的旅程(宏观契约 / 中观拓扑 / 微观节点编辑 / 实时 compile 门控)· 能力 `graph-authoring` `phase-editing` `file-editing` `compile-lint` `conflict-overwrite` · 区域 `canvas` `editor` `input(→i/o panel)` `properties` `center-action-bar` · 平台 `native-fs`(Rust 写) / `engine`(compile)
> Status: ✅ PM 已确认(批次2 + Half A + G1–G9 + R3–R5;Half B 现在设计)
> 设计权威(最新): 本页 workflow 走查 + **字段/格式 SSOT** = [`engine mvp1 skill-syntax`](../../../engine/mvp1/01-contract/02-skill-syntax/mvp1-alignment.md) + 决策 D7/G2/G3/D12/REQ-10。决策原话就近留底于 §3;canvas/子图细化(T5/T6/G4/G7)见 `canvas`/`graph-authoring` 能力文档。字段唯一真相源 = 能力文档 [`phase-editing`](../02_capabilities/phase-editing/mvp1-alignment.md)(只链接不复制)。(`.kiro/specs/studio-*`、旧 mvp0 FROZEN 仅历史参考、**不作 SSOT**。)
> 本文 = 该节点**最终设计**(atom action 决策 + file:line 依据)。

## 1. 用户旅程目标
把脑子里的业务逻辑转成**严谨可编译的 graph_skill**:宏观全局契约(GRAPH.md `name`/`schema_version`/`llm_role`/`description`/`phases`/`io`,**无 `type` 字段**)→ 中观拓扑(连线/断连/新建 phase/子图 inline 展开+下钻)→ 微观节点编辑(Properties 白名单字段 + L3 步骤)→ 实时 compile/lint 门控(绿灯才解锁 Predict)。

> ⚠️ **节点类型 = 文件名,非 `mode`/`type` 字段**(FROZEN):Agent=`SKILL.md` / Logic=`LOGIC.md` / Subgraph=`SUBGRAPH.md`;"新建节点选类型"=选建哪个文件。旧 UX 散文里的 `type(simple/graph)` / `<system_prompt>` / `<user_prompt_builder>` / `max_retries`/`max_nudges` 等**全部违 FROZEN**,已废。

## 2. atom action 决策表
> **2026-08-24 模块三真机点验复核**:下表 status 与 file:line 依据已逐行按当前代码核实更新(改动明细见 §7 修订记录)。此前记录的「⚠️ 读取层已 v030-aware,但写入/脚手架/子图渲染层多为 V2.x stale-code(违 FROZEN)」**核实后已不成立**:本轮复核的 M1/M2+M3/REQ-1/T1+T2/T3/T4/T5/T6+T7/Properties 两行/L3/(R5)/顺序覆盖/写失败回滚/环阻断/门控等动作绝大多数已翻转为 **live 且合 FROZEN**;剩余缺口收窄为两项,已记入 §6:①**D12 仍是混合态**——文件内容落盘统一走 Rust,但 GRAPH.md 拓扑序列化与 `runtime_config.json` 全套仍是纯 Python 端点,未接 Rust 分支;②**新缺陷 J-03.A**——T4 的 Add action 脚手架生成的函数签名违反引擎入口契约,修复分支在飞。未在本轮核查范围内的动作(「i/o panel 改名 + artifacts 清单」行、「M4 起点 Input/终点 Output」行、「Lint + Compile + 错误面板」行、「Predict 门控解锁」行里「点击进试飞=桩」这一子判断)维持原判,状态未变。

| 动作 | 最终决策 / status | 能力·区域 | 依据(file:line)+ FROZEN |
|---|---|---|---|
| M1 头部 `name/schema_version/llm_role/description/phases`(**无 type**) | **live**,但形态与旧设计不同:图级表单并非独立的 macro-contract 抽屉,而是复用 Properties 面板——空画布点击清空节点选中态后,Properties 渲染 `GraphFrontmatterForm`(可编辑字段 = name/description/llmRole;schema_version 固定、phases 属拓扑,走连线/新建节点操作,不在此表单内)。裁决依据:#219(`c1c1b208`,"fix(studio): keep canvas authoring file-backed")+ kiro spec `studio-source-truth-authoring/design.md:72`「Pane click -> clear selected node -> active panel Properties -> graph frontmatter form」。旧的独立 `MacroContractDrawer` 在同一裁决下被解线,现为孤儿(§6) | graph-authoring · canvas / phase-editing · properties | `PropertiesPanel.tsx:351`(deselect 注释)、`:764-766,834-848`(`!selectedNode` 分支渲染 `GraphFrontmatterForm`)、`:892-896`(`GraphFrontmatterFormData` = name/description/llmRole);`.kiro/specs/studio-source-truth-authoring/design.md:72`;FROZEN `docs/engine/skill-spec/00-FORMAT-GROUND-TRUTH.md` §2 GRAPH.md(旧引用 `02-graph-md-spec.md:12-20` 已失效——该文件已 `status: retired`,只剩一句指回 `00-FORMAT-GROUND-TRUTH.md#2-graphmd`) |
| M2+M3 io schema 展示与编辑入口 | **live**(D-IO-PREVIEW + D-IO-CONFIG-TREE 均已落地):i/o 面板 = 实例预览(只读)+ Configure 入口;**input 侧配置已从弹窗改为面板内联**黑板优先勾选树(`InputConfigInline`,PM 2026-07-03 决策,原设计只写到 07-02 弹窗版,本次补上 07-03 这版决策脉络的落地证据);**output 侧保留 Configure 弹窗**(`OutputConfigDialog`,多卡编辑器过宽,不适合 320px 面板)。双击 IO 开 GRAPH.md 仍保留 | file-editing · input | `IoConfigDialog.tsx:45-55`(头注释:input inline vs output modal 分工)、`:634-635`(PM 2026-07-03 决策注释"Replaces the old modal so the config lives with the selected node like the Properties panel")、`:637`(`InputConfigInline`)、`:771`(`OutputConfigDialog`);`InputPanel.tsx:26`(引入两组件)、`:220`(实例预览注释)、`:392`(挂载 `InputConfigInline`)。真机实测:黑板 context 第一行 + "Import…" 文件导入 + 实例预览空对象 `{}`。原话见 §3 D-IO-CONFIG-TREE + `03_regions/input` §4 |
| i/o panel 改名 + artifacts 清单 | **target-design(r3)**:input→i/o panel;artifacts = **文件清单**(Add artifact,每文件勾选黑板字段,single/per-item,固定命名 `<stem>_latest_<ts>` + `history/`),配置归 Output 节点/GRAPH.md(Input 节点只输入、Output 节点只输出、GRAPH.md 两者都有);普通节点 = 输入配置 + 输出预览 | phase-editing · input(→i/o panel) | **FROZEN/G3**(io.outputs 顶层文件路径+schema、一 schema 多文件、只许 md/json——r3 清单即其成型态);md 源=最终 validated `business_data_md`(不回转);细化落 `03_regions/input` F7 |
| M4 起点 Input / 终点 Output = 画布独立节点 | **live**(已渲染、可双击开 GRAPH.md) | graph-authoring · canvas | `build-nodes.ts:203-217`;FROZEN `02-graph:44` |
| REQ-1 纵向 TB 布局 | **live**:`lib/layout.ts` 已是 `rankdir: 'TB'`,画布纵向自上而下布局;节点连接桩上进下出(target 在 Top、source 在 Bottom)与 TB 方向一致 | graph-authoring · canvas | `layout.ts:33`(`rankdir: 'TB'`)/`SkillNode.tsx:86`(target handle `Position.Top`)、`:280`(source handle `Position.Bottom`)。真机实测:新建 skill 后 Input→init→Output 三节点自上而下排列 |
| T1+T2 连线 / 断连(`depends_on`)| **live,四操作全部实现**(R4 达成):建链(`onConnect`)、改链重连(`onReconnect`)、拖拽断链(`onReconnectEnd`)、右键菜单断(`handleMenuDisconnect`)。写路径为 **D12 混合态**:拓扑序列化由 Python `serializeSkillGraph`(`POST /skills/{id}/graph/serialize`)计算新 GRAPH.md 内容,磁盘落盘统一走 Rust `write_workspace_file`(经 `doWriteSkillFile`) | graph-authoring · canvas | `GraphCanvas.tsx:2152`(`onConnect`)、`:2239`(`onReconnect`)、`:2420`(`onReconnectEnd`)、`:2351`(`handleMenuDisconnect`);`client.ts:362-383`(`serializeSkillGraph` → `api.post`);`Workspace.tsx:1316-1329`(`doWriteSkillFile` → `writeWorkspaceFile` → Rust `write_workspace_file`)、`:1775-1777`(GRAPH.md 写入范例:先 `serializeSkillGraph` 再 `doWriteSkillFile`)。真机实测:连线/菜单断/拖离断三式落盘即时、GRAPH.md body 同步 |
| T3 拓扑校验(环 / 数据断层) | **全 live**(REQ-2 已兑现):环检测在 auto-layout 阶段捕获(`CycleDetectedError`);数据断层已从"类型相等红叉"改为黑板字段可视化——`edge-static-inference.ts` 静态推导每条边到达时应有的黑板字段集,`IoConfigDialog.tsx` 的 `MissingFieldRow` 标记缺失字段。旧的类型不匹配标记已删除(`ContextEdge.tsx` 全文件 grep 零命中 mismatch/type-equal 相关文案) | graph-authoring · canvas | `GraphCanvas.tsx:56`(引入 `staticEdgeInference`)、`:1524-1544`(捕获 `CycleDetectedError` 仍返回可渲染 nodes/edges);`edge-static-inference.ts:1-36`(头注释,PM 2026-07-02 n5 atom #14 trace-observability F4);`IoConfigDialog.tsx:277`(`MissingFieldRow` 定义)、`:389,821`(两处渲染点)。旧引用 `02-graph:89-96` 已失效(该文件已 `status: retired`) |
| T4 新建 phase(Agent/Logic/Subgraph,=选建哪个文件) | **接线与脚手架均 live 且合 FROZEN**:`defaultPhaseMarkdown` 已重写,三类脚手架(agent/logic/subgraph)均不写 `mode`/`system_prompt`/`exit_contract`/`python_callable`,头注释自标"FROZEN-clean per engine skill-syntax §2.3/§2.5/§2.4/§2.1"。真机实测 LOGIC/SUBGRAPH 脚手架均合规、可直接编译。**但**同一次新建 phase 常带的 Add action 脚手架另有一个新缺陷:`phase-actions.ts` 的 `actionStubContent` 生成 `def <name>(context) -> dict:`,违反引擎入口契约(引擎要求恰一个名为 `inputs` 的参数,且不接收 `context`/`ctx`)——**J-03.A**(模块三点验新发现,修复分支 `fix/the-stub-the-engine-would-accept` 在飞),详见 §6 | graph-authoring · canvas | `canvas-authoring.ts:582-594`(`defaultPhaseMarkdown` 三类分发)、`:575-581`(头注释 FROZEN-clean 说明)、`:596-615`(logic 脚手架含 `<action></action>` 占位);`phase-actions.ts:37`(`def ${name}(context) -> dict:`);FROZEN `docs/engine/skill-spec/00-FORMAT-GROUND-TRUTH.md:16,234`(action 签名恰一个 `inputs` 参数,不接收 context/ctx)。旧引用 `05-agent:40-55` 已不存在,该文件现名 `05-agent-md-spec.md`(内容归 `00-FORMAT-GROUND-TRUTH.md` §5) |
| T5 子图 inline 展开(虚线容器、动态 group bbox) | **live,真数据**:`SubgraphInline` 组件已不存在(全仓 grep 零命中),替换为 `SubgraphGroupNode`;展开子图节点通过 `loadChildGraph` 拉取真实子图拓扑(`getChildGraphTopology` API + 子图自身 `SkillDetail`),detail 拉取失败时保留只读拓扑并警告,不静默降级,不再是占位假数据 | graph-authoring · canvas | `SubgraphGroupNode.tsx:15,19`(组件定义);`GraphCanvas.tsx:1234-1266`(`loadChildGraph`)、`:1270-1288`(`drilledPath` 变化触发加载)。真机实测:展开子图节点渲染出该子图的真实拓扑(节点/连线与磁盘一致)。**FROZEN/G2**:子图 io 严格 1:1 已删(不变) |
| T6 子图下钻(就地聚焦,不切工程)+ T7 context_bridge | **T6 live**(不再是"仅就地聚焦"):独立 drill 栈管理(`drill-stack.ts` reducer)+ 面包屑回退(`DrillBreadcrumb.tsx`);子图路径解析走 `subgraph-path.ts`。path 失效时 Properties 面板红字「Path does not resolve to GRAPH.md.」+「Reconnect path」按钮,重新选择文件夹或重新选中节点后自愈。**T7 维持原判 target-design**:FROZEN SUBGRAPH.md 无 `context_bridge` 字段未变,io 仍由 StateMapper 按 schema 自动切片 | graph-authoring · canvas | `drill-stack.ts:43`(`drillStackReducer`)、`:81`(`breadcrumbItems`)、`:96`(`currentLevel`);`DrillBreadcrumb.tsx:26`(组件);`subgraph-path.ts:29`(`resolveSubgraphPath`)、`:69`(`invalidSubgraphPathMessage`)、`:89`(`subgraphPathFieldState`);`PropertiesPanel.tsx:2081-2165`(`SubgraphPathField`:`:2108` 拉取 `getChildGraphTopology` 探测磁盘、`:2147` "Reconnect path" 按钮、`:2158` "Path does not resolve to GRAPH.md." 文案)。旧引用 `04-subgraph:32-38` 已不存在,该文件现名 `04-subgraph-md-spec.md` |
| Properties 编辑保存 | **live**:保存机制(autosave + hash 乐观并发)正常,字段集已按 FROZEN 白名单重建(见下一行),不再与 FROZEN 冲突 | phase-editing · properties | `PropertiesPanel.tsx:790-819`(`PhaseFrontmatterForm` 渲染 + `onFieldChange`/autosave 接线) |
| Properties 白名单重建(按 FROZEN 三类节点字段集) | **live(REQ-10 已兑现)**:`phase-frontmatter.ts` 的 `PhaseFrontmatterFormData` 与三类节点(agent/logic/subgraph)字段集逐项对齐现行 FROZEN 权威。真机逐字段核对:agent 13 字段(name/llm_role/use_graph_llm_role/validator/max_iterations/io/tools/subagents/subgraphs/references/examples/allow_sequential_overwrite/iterate)+ logic/subgraph 白名单,零违禁字段;iterate 的 off/batch/loop 均为合法 `IterateSpec` 取值。**旧 FROZEN 引用文件 `05-agent.md`/`03-logic.md`/`04-subgraph.md` 已不存在**,权威改指 [`engine mvp1 skill-syntax`](../../../engine/mvp1/01-contract/02-skill-syntax/mvp1-alignment.md) + `docs/engine/skill-spec/00-FORMAT-GROUND-TRUTH.md`(§9 Properties Panel 映射) | phase-editing · properties | `phase-frontmatter.ts:41-61`(`PhaseFrontmatterFormData` 接口,agent/logic/subgraph 分区注释);`docs/engine/skill-spec/00-FORMAT-GROUND-TRUTH.md:182`(§3 LOGIC.md)、`:236`(§4 SUBGRAPH.md)、`:291`(§5 SKILL.md 字段表 :360-378)、`:519`(§9 Properties Panel 映射);`docs/engine/mvp1/01-contract/02-skill-syntax/mvp1-alignment.md:1-17`(声明自身非模板 SSOT,指回 `00-FORMAT-GROUND-TRUTH.md`)。**FROZEN/G2**:删 io 1:1(不变);**D7**:子图 path(不变) |
| L3 步骤(`<step>`/`<action>`)增删改序 | **live**,但机制不是 Rust `mutate_phase_body`(全仓 grep 零命中,该函数不存在)——实现为纯 TS body 文本变换(`lib/agent-steps.ts`,保留全部非-step 正文)+ 画布内联 UI(`AgentStepsInline.tsx`,Add step 按钮)+ 统一走 `doWriteSkillFile` 落盘(与其他 phase 内容写入同一 Rust 路径,呼应 D12) | phase-editing · canvas | `agent-steps.ts:36`(`parseAgentSteps`)、`:63`(`reorderAgentSteps`)、`:84`(`removeAgentStep`)、`:95`(`nextStepId`)、`:108`(`addAgentStep`)、`:124`(`updateAgentStep`);`AgentStepsInline.tsx:13`(引入 `agent-steps` 变换)、`:20`(头注释:变换保留全部非-step 正文)、`:55`("Add step" 按钮文案)。真机实测:点 Add step 后 SKILL.md body 出现新增的 `<step id="S2">` |
| Lint + Compile + 错误面板 | **live**(防抖 lint + `compileSkill` 引擎真编译,CompileErrorPanel 渲染) | compile-lint · center-action-bar | `useDebouncedLint.ts:48-49`/`Workspace.tsx:432-435`→`skills.py:109` |
| Predict 门控解锁(Compile 绿灯→解锁 Predict) | **门控 live,真机复核通过**:Compile 通过后 Predict 按钮从锁定变可点、Run 仍锁灰;引入编译错误后 Predict 重新锁定(`deriveButtons` 的 `idle`/`compiling`/`compile-fail` 分支统一锁 Predict)。两条独立路径(点击 vs copilot 经 MCP 触发)投影到同一状态机,不会分叉(决议 2026-08-03「状态对等」D4)。点击进试飞仍是桩,归 04_run-and-verify,原判不变 | compile-lint · center-action-bar | `center-action-bar.tsx:55-84`(`deriveButtons`:`compile-pass`/`predicting`/`predict-fail` 解锁 Predict、锁 Run;`idle`/`compiling`/`compile-fail` 锁 Predict);`gate-state.ts:2-10`(头注释,决议 2026-08-03 D4)、`:78-91`(`STAGE_BY_OUTCOME` 投影) |
| 顺序覆盖冲突保存(overlay Allow/Cancel) | **live**(结论不变,file:line 已刷新) | conflict-overwrite · canvas | `GraphCanvas.tsx:914`(`handleAllowSequentialOverwrite`)、`:1013`(`handleCancelSequentialOverwrite`);`Workspace.tsx:61`(引入 `ConflictDialog`)、`:3251`(挂载 `ConflictDialog`) |
| [失败退路] 写失败回滚 / 拒写 / YAML 坏 error+Open file / ~~环全屏阻断~~ | **前三项 live 不变;环全屏阻断已移除**:2026-07-02「never veils a rendered graph」决议撤销了环的全屏阻断——造环后画布照常渲染、无遮罩,环由引擎 compile(`[F-v3-graph-phase-cycle]`)拦下并回锁 Predict/Run,不再由前端拦截渲染 | graph-authoring · canvas | 回滚:`GraphCanvas.tsx:2314-2334`(`onReconnect` 的 `rollback`,连线/断链写失败还原画布状态);YAML 坏+Open file:`PropertiesPanel.tsx:400,549`(`graphFormState`/`phaseFormState` 解析)、`:820-831,849-859`(渲染 "Frontmatter error" + Open file 按钮);环:`GraphCanvas.tsx:1524-1544`(`CycleDetectedError` 被捕获但仍返回可渲染 nodes/edges)、`:2488-2494`(veil 仅用于"无数据",注释含 2026-07-02 光晕根因)、`GraphCanvas.test.tsx:1426-1445`("never veils a rendered graph on a failed refresh")、`:1447-1461`("keeps rendering the graph when auto-layout detects a cycle",断言不再含旧文案 "SKILL contains cyclic dependency - cannot render graph.") |
| (R5)assets subgraph 类目 ↔ 节点文件同步;子图 path 找不到→标红→OS 选文件夹导入工作区 | **live**:assets 面板子图类目现由后端 topology 真实推导(`mode==="subgraph"` 且带 path 的 phase),头注释明写替换了此前"伪造已注册状态"的内存假缓存;子图 path 失效的标红 + Reconnect 见 T6+D7 行 | graph-authoring · canvas/assets | `subgraph-membership.ts:1-23`(头注释:"This replaces the former in-memory `registeredSubgraphsCache`, which faked 'registered' state...");`PropertiesPanel.tsx:2081-2165`(`SubgraphPathField` 红标 + Reconnect,同 T6+D7 行) |

## 3. 设计决策基础(原话依据,锁定决策)
- **D7 子图按 path** > "subgraph.md里面写path, 直接解析就好了, 随便放哪里. 唯一要注意的是copilot 的工作目录范围要把subgraph的子图path 加进去. 还有一个是注册在agent phase里的子图,也一样写path";`SkillResolverProtocol.resolve` 退化为读 path、无注册表;**copilot cwd 必须纳入被引用子图 path**(否则读不到/改不到子图)。
- **G2 删子图 io 严格 1:1**(FROZEN-1)> "数据流应该是单向不可逆的, 所以没有回写一说. 父子图的io关系不用绑死(伪需求), 和其他节点一样, 子图的input是从state状态机过滤字段拿的; 另外增加一点, 你可以在任何一个i/o界面导入文件, 就和第一个input节点导入文件一样. 导入文件就相当于导入这个文件的字段进状态机";导入时机 = **a 跑到该节点才注入**(后续锁定,非 G2 原句)。
- **G3 artifact 落盘**(FROZEN-2)> "黑板肯定都得进. 落盘是并行需求不影响状态机. 落盘的写法就在io.outputs 的schema顶层再加一个文件路径, xx/xx/xx.json(或者md), 下面是这个文件的schema; 也可以连续两个路径不同文件...一个schema落两个文件; 也可以不同路径各自下面加schema...不同schema落不同文件; 默认不用写前面的路径,只要写一个文件名,代表直接落盘在.workspace的artifacts(artifacts默认怎么组织也要想清楚); 格式:文字只允许md和json";md 用最终 validated `business_data_md` 不回转(后续锁定)。
- **REQ-10 Properties 白名单对齐 FROZEN**(官方确认 batch-2 stale 表单结论);**REQ-1 TB 布局**;**REQ-2 黑板可视化连线**(FROZEN-4,删类型相等红、改 io.inputs 字段勾选)。
- **D-IO-PREVIEW i/o 面板实例预览**(PM 2026-07-02)> "原设计在panel里面做这步太复杂了,有太多的字段以及字段嵌套,会把人搞晕的,还不如清爽的让用户看到这里按照现在的schema大致会得到什么,然后用copilot或者自己直接去改文件"——面板显示 schema 推导的实例预览(只读),schema 编辑走 copilot/编辑器;golden JSON 与该预览同构("golden输出的json和现在推测的json没有区别"),schema→实例生成器与 predict 占位 mock、golden 空模板共用。细化落 `03_regions/input` F2。
- **D-IO-CONFIG-TREE 黑板优先配置树**(PM 2026-07-02 r3)> "导入文件不是node input 数据的主要来源，黑板才是，impot file只是在黑板上额外增加了数据字段" · "输入的配置面板……第一行永远是黑板context，下面才是import附加的文件和文件带来的字段；input节点没有黑板，只有文件；到了input链接的第一个node，input从文件勾选的字段就成了黑板上的字段" · 输出对称:"通过add button添加需要输出的artifacts文件，下面列的是黑板上所有的字段，这个文件在其中勾选需要输出的字段"——G2「导入=导字段进状态机」的成型态;黑板行推导与 dot 静态推断同源;golden 区移出面板。全部原话与 r3b 视觉修订落 `03_regions/input` §4,细化落其 F2/F3/F5/F7/F8。
- **D12 写全量 Rust**;**D-1-4 脚手架 logic→agent 模板**;**R3** i/o panel 加 input files 导入(FROZEN-3);**R4** 连线四操作;**R5** assets 同步 + 子图 path 标红导入。
- **Half B 现在设计**(不留实现期):三类节点字段集 + 子图 path 引用 + io.outputs artifact + 删子图 io 1:1,已落能力文档 [phase-editing](../02_capabilities/phase-editing/mvp1-alignment.md)。

## 4. 失败退路 + 节点间流转
- **失败退路**:写失败→回滚(hash 乐观并发);YAML 坏→error + Open file;拒写(hash 冲突由 ConflictDialog 接管);~~环→全屏阻断~~ **环不阻断渲染**——画布照常渲染,环由 compile 报 `[F-v3-graph-phase-cycle]` 拦下并回锁 Predict/Run(2026-08-24 复核改判,依据同 §2「[失败退路]」行)。
- **上游**:[01_init](./01_init.md)(进入 workspace + copilot 初始化 SKILL.md)。
- **下游**:Compile 绿灯 → [04_run-and-verify](./04_run-and-verify.md)(predict 试飞解锁);copilot inline-diff → copilot-assist;文件树/.workspace → asset-explorer(file-editing)。

## 5. 测试关键点
- 新建 phase 脚手架产出**合 FROZEN**(logic/agent,无 mode/system_prompt/exit_contract/python_callable),可直接编译。
- i/o 面板 = 预览 + Configure + 文件 list(无 schema 表单/golden 区/内联创建表单);配置树黑板行与 dot 静态推断同源;勾选写回 io.inputs JSON Schema,文件路径绑定留在 runtime_config(D-IO-CONFIG-TREE)。
- 子图 io 改动**不**触发严格 1:1 校验(G2);子图按 path 解析、path 找不到→标红+导入入口。
- artifacts:清单声明(文件×黑板字段勾选)→ engine writer 固定格式落 `.workspace`(`<stem>_latest_<ts>` + `history/`,per-item 编号继承输入批量);md 取 `business_data_md` 不回转;旧 per-field target 路径删净。
- 所有写(serialize_graph/mutate_phase_body/新建 phase/Properties 保存)走 **Rust** 文件命令(非 Python)。
- Compile 绿灯才解锁 Predict;改 prompt 不破门控,改 io schema 缺字段触发编译错误。

## 6. 跨切 / 已知债
- **[D12] 混合态(2026-08-24 复核改判)**:新建 phase 写文件 / Properties 保存 / L3 步骤 / 连线序列化后的磁盘落盘均已统一走 Rust `write_workspace_file`(经 `doWriteSkillFile`,`Workspace.tsx:1316-1329`)。**但两处仍是纯 Python、未接 Rust 分支**:①GRAPH.md 拓扑序列化本身(`serializeSkillGraph` → `POST /skills/{id}/graph/serialize`,`client.ts:362-383`);②`runtime_config.json` 全套(`importIoIntoWorkspace` → `POST /skills/{id}/io/import`、`putRuntimeArtifacts` → `PUT /skills/{id}/runtime-config/artifacts`,`client.ts:1154-1181`,后端 `apps/studio/backend/app/routers/runtime_config.py:26,31`)。②是 D-IO-CONFIG-TREE(PM 2026-07-02 r3)出生后未排上 D12 迁移队列的缺口,不是遗漏的旧代码。`mutate_phase_body` 从未存在于本仓(全仓 grep 零命中),此前记录的"待迁 Rust"是对一个从未实现过的函数名的误记。
- **孤儿(2026-08-24 复核发现)——`src/components/macroform/` 整目录**:`MacroContractDrawer.tsx`/`MacroContractForm.tsx`(+ test)/`graph-header.ts` 全仓 grep 除自身互相引用外零外部引用。#219(`c1c1b208`,"fix(studio): keep canvas authoring file-backed")把图级表单裁决为复用 Properties 面板(见 §2 M1 行),这个独立抽屉组件在同一次裁决里被解线但文件未删,`graph-header.ts` 最近一次改动(`446faf44`,#959)仍在这个已死目录内继续维护——待清理。
- **新缺陷 J-03.A(模块三点验新发现,2026-08-24)**:T4 新建 phase 的 Add action 脚手架(`phase-actions.ts` `actionStubContent`)生成 `def <name>(context) -> dict:`,违反引擎入口契约(恰一个名为 `inputs` 的参数,不接收 `context`/`ctx`,FROZEN `docs/engine/skill-spec/00-FORMAT-GROUND-TRUTH.md:16,234`);真机实测 Add action 后下一次 Compile 即报 fatal。修复分支 `fix/the-stub-the-engine-would-accept` 在飞,详见 `docs/development/PROBLEM_LEDGER.md`「旅程点验发现」J-03.A(本文成稿时该行尚未合入台账,行号待补,由点验方登记)。
- **FROZEN 改动落点集中本节点**:FROZEN-1 删子图 io 1:1、FROZEN-2 io.outputs artifact、FROZEN-4 REQ-2 字段勾选(待统一出 FROZEN 新版本;本次复核未涉及)。
- **canvas REQ 覆盖**:REQ-7 结构化 diff → 归 trace(04_run-and-verify);REQ-8 运行时策略开关(prompt_cache/compaction)= engine 未落地 ⏭️ 延后;REQ-9 右键新建节点 = T4(已覆盖)。

## 7. 修订记录
- **2026-08-24**(动因:模块三真机点验 + 代码勘察发现本文件多条 status/file:line 依据系统性滞后于代码)——逐行按当前代码核实更新;FROZEN 决策原文(§3 全部原话)未改一字,仅改了 §2 头部警示、§2 表格的 status/依据列、§4 失败退路、§6 已知债。逐条改动:
  - §2 头部「⚠️ 跨切定性」警示:改判为**不再成立**并重写为现状定性(本轮核查动作绝大多数 live,剩余缺口收窄为 D12 混合态 + 新缺陷 J-03.A)。
  - 「M1 头部」:`target-design` → **live**(图级表单落点从设想中的独立抽屉,改为 #219 裁决后的 Properties 面板复用;`MacroContractDrawer` 解线为孤儿,记入 §6)。
  - 「M2+M3 io schema」:`target-design` → **live**(D-IO-PREVIEW + D-IO-CONFIG-TREE 均已实现;补记 PM 2026-07-03 从弹窗改内联的决策脉络)。
  - 「REQ-1 纵向 TB 布局」:`target-design`(旧证据误记 rankdir LR)→ **live**(`layout.ts` 现为 `rankdir: 'TB'`)。
  - 「T1+T2 连线/断连」:旧判断"现仅菜单断"→ **四操作全部 live**(建链/改链重连/拖拽断链/菜单断);写路径细化为 D12 混合态(拓扑序列化 Python、磁盘落盘 Rust)。
  - 「T3 拓扑校验」:数据断层部分 `target` → **live**(REQ-2 已兑现,旧类型红叉已删,替换为黑板字段可视化)。
  - 「T4 新建 phase」:脚手架 `stale-code` → **live 且合 FROZEN**;同时记录新发现的 Add action 脚手架缺陷 J-03.A(不影响本行 phase 脚手架本身的判定,归入 §6)。
  - 「T5 子图 inline 展开」:`placeholder`(`SubgraphInline` 渲染假数据)→ **live,真数据**(`SubgraphInline` 已不存在,替换为 `SubgraphGroupNode` + `loadChildGraph` 真实拉取)。
  - 「T6 子图下钻+T7 context_bridge」:T6 `target-design`(仅就地聚焦无下钻)→ **live**(独立 drill 栈 + 面包屑 + path 失效标红/Reconnect 自愈);T7 维持原判不变。
  - 「Properties 编辑保存」「Properties 白名单重建」:`stale-code`/`target-design` → **均 live 且与 FROZEN 对齐**(REQ-10 已兑现);旧 FROZEN 引用文件 `05-agent.md`/`03-logic.md`/`04-subgraph.md` 已不存在,权威改指现行 `engine mvp1 skill-syntax` alignment + `00-FORMAT-GROUND-TRUTH.md` §9。
  - 「L3 步骤」:`target-design`(声称走 Rust `mutate_phase_body`)→ **live**(该函数全仓从未存在;真实机制是 `lib/agent-steps.ts` 纯 TS 变换 + `AgentStepsInline.tsx` 画布内联 UI)。
  - 「Predict 门控解锁」:结论不变(门控 live),补真机复核证据(Compile 绿解锁 Predict、Run 仍锁;出错回锁)+ `gate-state.ts` 状态对等依据。
  - 「顺序覆盖冲突保存」:结论不变(live),file:line 因组件重构(`GraphCanvas/GraphCanvas.tsx`、`ConflictDialog.tsx`)刷新。
  - 「[失败退路]」行:写失败回滚/拒写/YAML 坏三项结论不变、file:line 刷新;**环全屏阻断改判为已移除**(2026-07-02「never veils a rendered graph」决议,环由 compile 拦截,不再由前端全屏阻断)。
  - 「(R5)」行:`target-design` → **live**(`subgraph-membership.ts` 头注释自证替换了此前的内存假注册表缓存)。
  - §4:「环→全屏阻断」同步改判为「环不阻断渲染,由 compile 拦」。
  - §6:重写「[D12] 写全量 Rust」为「[D12] 混合态」(两处未接 Rust 分支的具体缺口);「读 v030/写 V2.x stale-code」整条按现状核查结果删除(`SubgraphInline` 已不存在、`phase-frontmatter.ts` 已对齐 FROZEN、`defaultPhaseMarkdown` 已合规);新增孤儿目录 `macroform/` 记录(依据 #219 裁决);新增新缺陷 J-03.A 记录并链问题台账。「FROZEN 改动落点」「canvas REQ 覆盖」两条本次未涉及,原样保留。
  - **未改动、超出授权范围、留待复核**:§1/§3/§5 正文本次未动一字;「i/o panel 改名 + artifacts 清单」「M4 起点 Input/终点 Output」「Lint + Compile + 错误面板」三行本次未核查,状态维持原判,但后两行引用的 `02-graph:44`/`02-graph:89-96` 与已改判行一样,指向的 `02-graph-md-spec.md` 现已 `status: retired`(内容并入 `00-FORMAT-GROUND-TRUTH.md`),这些未触碰行的 FROZEN 引用大概率也已失效,留待下一轮复核;§5 现有「所有写(serialize_graph/mutate_phase_body/新建 phase/Properties 保存)走 Rust 文件命令(非 Python)」一句与本次更新后的 §2 T1+T2 行、§6 D12 行(GRAPH.md 拓扑序列化仍是 Python)已不一致,因超出本次任务的授权编辑范围(仅限 §2 头部警示/status/依据列 + §4 + §6 + 本修订记录)未一并修正,已在交付说明中另行提请复核。
