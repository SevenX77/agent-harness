{
 "apikeys#20": {
  "id": "20",
  "name": "tp-credential-edit (去协议手选下拉)",
  "backend_change_needed": "None in the gateway. The only backend prerequisite is that the test entry point (#24/#25) can auto-detect protocol, so the third-party card no longer needs the manual Protocol dropdown as a fallback. That prerequisite is itself a Studio-shell change (see #25). No new endpoint or contract field is required — PUT /api/llm/registry/endpoints already persists name/base_url/api_key and normalizes base_url (the protocol field can be set by the backend after auto-detection rather than chosen by the user).",
  "module": "frontend-only",
  "doable_without_keepmain": true,
  "evidence": "Manual Protocol Select rendered at apps/studio/frontend/src/components/studio/api-keys/ProviderCard.tsx:1016-1038 (providerProtocolOptions defined at ProviderCard.tsx:60-65; onValueChange writes provider_type). Save path PUT /api/llm/registry/endpoints lives in the Studio shell. No gateway code involved.",
  "fix_plan": "Frontend-only: delete the Protocol Select block in ProviderCard.tsx:1016-1038 for the third-party (provider-kind==='third-party') card, collapsing its editable fields to name/base_url/api_key. The protocol is then filled by the backend protocol auto-detection landed in #25 (POST /endpoints/{id}/test). Sequence this AFTER #25's backend protocol auto-detection so removing the manual fallback never strands a user on an undetectable endpoint. Verify via component test: render provider-kind='third-party' ProviderCard, assert no element with id=`provider-protocol-{id}`."
 },
 "apikeys#24": {
  "id": "24",
  "name": "official-test-job (官/三测试入口对称化)",
  "backend_change_needed": "In the Studio shell only (apps/studio/backend/app/routers/llm.py): make POST /endpoints/{endpoint_id}/test (test_endpoint, llm.py:523) the single official+third-party entry. Official keeps get-models connectivity -> verified; third-party runs protocol auto-detect + batch inference probing -> verified (see #25). Remove the third-party branch that downgrades to 'unverified_manual' (llm.py:544/588-595). The async job path (start_endpoint_test_job, llm.py:437; get_endpoint_test_job, llm.py:515) is already Studio-shell and already free of the old official-only 400 (handbook claim is STALE — it now only 404s on missing endpoint). No gateway contract change: the gateway probe primitives test_provider_endpoint/test_provider_route are already imported in-process (llm.py:51,54 from app.core.adapters.gateway).",
  "module": "studio-backend-shell",
  "doable_without_keepmain": true,
  "evidence": "test_endpoint at apps/studio/backend/app/routers/llm.py:523 sets third-party status='unverified_manual' (llm.py:544) and only promotes official to 'verified' (llm.py:578-587). start_endpoint_test_job (llm.py:437-459) has NO official-only 400 — only a 404 for missing endpoint (llm.py:441-442), confirming the handbook's hard-gate claim is stale. Frontend fork: getProviderModels routes official->testOfficialProviderModelsWithJob async job (apps/studio/frontend/src/api/llm.ts:1356-1357), third-party->sync POST /endpoints/{id}/test (llm.ts:1359). Button text fork: isOfficial?'Test':'Get Models' (ProviderCard.tsx:1010). Gateway probe primitives are in-process via app/core/adapters/gateway.py:81-86.",
  "fix_plan": "Backend (Studio shell, llm.py): unify on POST /endpoints/{id}/test (test_endpoint). For third-party, replace the get-models-only path (which sets unverified_manual) with the #25 protocol-autodetect + batch-inference flow that promotes to verified; keep official on get-models connectivity. Optionally keep the async job wrapper but make both kinds poll GET /endpoint-test-jobs/{job_id}. Frontend (llm.ts): drop the provider_kind fork at llm.ts:1356; both kinds hit the same entry. ProviderCard.tsx:1010: 'Test' for both. Contract test: official and third-party both POST /endpoints/{id}/test; real-key third-party endpoint transitions untested->testing->verified only after a real inference probe succeeds."
 },
 "apikeys#25": {
  "id": "25",
  "name": "tp-getmodels (第三方批量推理探测+协议自动探测)",
  "backend_change_needed": "Studio shell only (llm.py): extend POST /endpoints/{id}/test (test_endpoint, llm.py:523) for third-party to (a) protocol auto-detect — iterate the candidate protocols/auth-header combos by cloning the endpoint with endpoint.model_copy(update={'protocol': candidate}) and calling the EXISTING in-process gateway primitive test_provider_route once per combo, taking the first status=='ok'; (b) batch inference probing — call test_provider_route over batches of discovered/notable model ids, stop on first success, short-circuit on structural error codes; promote to verified only on a real generation success. All of this reuses gateway primitives that already take backend(protocol) and model_id as inputs — NO gateway edit. endpoint_probe_backend (provider_probe.py:232) is a pure protocol->backend mapper; Studio can drive it by varying endpoint.protocol.",
  "module": "studio-backend-shell",
  "doable_without_keepmain": true,
  "evidence": "Third-party path is single-shot get-models: getProviderModels non-official branch POSTs /endpoints/{id}/test (apps/studio/frontend/src/api/llm.ts:1359) and judges ok via modelListReachable=endpoint.status!=='failed' (llm.ts:1364) — proves only key+URL reachability, never runs inference, never reaches verified. Gateway test_provider_route (packages/graph-agent-gateway/src/graph_agent_gateway/registry/provider_probe.py:123-176) DOES a real generation call and is already imported in-process by the Studio adapter (apps/studio/backend/app/core/adapters/gateway.py:84-86; called at llm.py:5012). endpoint_probe_backend derives backend purely from endpoint.protocol (provider_probe.py:232-245), so cloning the endpoint with a different protocol and re-probing is a Studio-shell loop. Button text 'Get Models' at ProviderCard.tsx:1010.",
  "fix_plan": "Backend (Studio shell, llm.py test_endpoint third-party branch): add a protocol-detection loop (for each candidate protocol: clone endpoint via model_copy, call the imported gateway test_provider_route, accept first ok) then a batch model-probe loop (chunk model ids, call test_provider_route per id, stop on first ok, short-circuit structural error codes); on success persist detected protocol + set status='verified'; on all-fail set 'failed' with reason. Reuse normalize_route_capabilities for caps. Frontend (llm.ts/ProviderCard.tsx): change third-party verdict from modelListReachable (llm.ts:1364) to verified-on-batch-probe-success; button text 'Get Models'->'Test' (ProviderCard.tsx:1010). Verify with a real key + a real generating model: assert endpoint reaches verified."
 },
 "apikeys#27": {
  "id": "27",
  "name": "manual-model-probe (6 态呈现 + 能力探测统一)",
  "backend_change_needed": "Studio shell only (llm.py test_endpoint_models, llm.py:624): unify the provider_kind fork. The third-party branch hard-codes capabilities to text-only via _successful_generation_probe_capabilities() (llm.py:756, body at llm.py:4281-4287). Replace that with capabilities normalized from the endpoint's list-models rich fields using normalize_route_capabilities (already a Studio service, app.services.llm_route_capabilities, re-exported via the adapter), falling back to a probe only when list-models lacks the data. This removes the official(multi-profile)/third-party(write-text-only) asymmetry. All edited code is in the Studio router + Studio services. No gateway change — normalize_route_capabilities and the probe primitives are already in-process.",
  "module": "studio-backend-shell",
  "doable_without_keepmain": true,
  "evidence": "models/test forks on provider_kind: official branch at apps/studio/backend/app/routers/llm.py:652 (_probe_official_model_profile_result -> real multi-profile capabilities); third-party else-branch at llm.py:721+ uses _gateway_test_provider_model then writes hard-coded caps via _successful_generation_probe_capabilities() at llm.py:756 (definition llm.py:4281-4287 = text-only model_type=language_reasoning, input/output_modalities=['text']). normalize_route_capabilities is a Studio service imported at llm.py:38 and used at llm.py:841/3415/3443/4297. Frontend: ManualModelResultList still uses 2-state Badge variant={result.status==='ok'?'success':'destructive'} (apps/studio/frontend/src/components/studio/api-keys/ManualModelTestPanel.tsx:121-129). The 6-state component already exists: ProviderStateBadge at apps/studio/frontend/src/components/studio/settings/llm-roles/provider-state-badge.tsx:61 (map includes historical_ready/cooling_down).",
  "fix_plan": "Step 1 (frontend, can ship first): replace ManualModelResultList's 2-state Badge (ManualModelTestPanel.tsx:113-132) with 6-state inline rendering reusing ProviderStateBadge (provider-state-badge.tsx:61). Step 2 (Studio backend): in test_endpoint_models, drop the provider_kind fork; for third-party replace _successful_generation_probe_capabilities() (llm.py:756) with normalize_route_capabilities(protocol=..., provider_model_id=..., raw_capabilities=<list-models rich fields>) and probe-fallback, matching the official side's real-capability output so both kinds return a symmetric capability structure. Verify: contract test asserts symmetric capability shape for official vs third-party on the same models/test entry."
 },
 "apikeys#30": {
  "id": "30",
  "name": "test-status-projection (API Keys 卡接后端 6 态 ui_state)",
  "backend_change_needed": "Studio shell only (llm.py _registry_response, llm.py:1644): before returning, project each provider_route's 6-state ui_state and set it on the route. The projection machinery is ALREADY in-process and already used by LLM Roles: build_gateway_adapter().project_route_state({endpoint, route, circuits, now, evidence_records}) is called at llm.py:2016 inside _provider_model_option and emits ui_state at llm.py:2038. Apply the identical call per route in _registry_response and route.model_copy(update={'ui_state': projection.ui_state}). The Studio ProviderRoute DTO already carries ui_state (it subclasses GatewayProviderRoute which defines ui_state at registry/schema.py:219), so no schema edit. Gateway is untouched: project_route_state (state_projection.py:73-111) is a pure function over status strings/booleans. The blue historical_ready state additionally needs real draft evidence — its blocker probe_import_draft (llm.py:905-909) is a Studio-shell stub, so turning it into a real probe (call gateway test_provider_route + write probe-verified evidence records) is also Studio-shell work, not gateway.",
  "module": "studio-backend-shell",
  "doable_without_keepmain": true,
  "evidence": "_registry_response returns raw provider_routes=credentials.provider_routes without projecting ui_state (apps/studio/backend/app/routers/llm.py:1669-1671), so route.ui_state stays at the schema default 'untested'. The SAME projector is already wired for LLM Roles: _provider_model_option calls adapter.project_route_state(...) (llm.py:2016) and emits projection.ui_state (llm.py:2038); the adapter delegates to the canonical gateway projector in-process (apps/studio/backend/app/core/adapters/gateway.py:321-364, calling gateway_project_route_state). Studio ProviderRoute inherits ui_state from GatewayProviderRoute (apps/studio/backend/app/models/llm_config.py:81; field defined at packages/graph-agent-gateway/src/graph_agent_gateway/registry/schema.py:219, ProviderUiState 6-state Literal at schema.py:22). Frontend TS ProviderRoute interface (apps/studio/frontend/src/api/llm.ts:67-78) lacks ui_state; ProviderStateBadge (provider-state-badge.tsx:61) exists but only LLM Roles consumes it. Blue-state root cause: probe_import_draft is a stub that only flips status='probed' (llm.py:905-909).",
  "fix_plan": "Backend (Studio shell, llm.py): in _registry_response (llm.py:1644), for each route in credentials.provider_routes build the projection via the existing build_gateway_adapter().project_route_state({endpoint, route, circuits, now, evidence_records}) (mirroring llm.py:2009-2024) and set route=route.model_copy(update={'ui_state': projection.ui_state}) before assembling RegistryResponse — no extra HTTP, no gateway edit. Frontend: add ui_state:ProviderUiState to the TS ProviderRoute interface (llm.ts:67-78), migrate ProviderCard's status tag from the old RouteStatus map (routeStatusTagVariant, ProviderCard.tsx:356-374) to ProviderStateBadge (provider-state-badge.tsx) rendered inline next to key/Base URL, covering all 6 states. For blue historical_ready end-to-end: convert probe_import_draft (llm.py:905-909) from stub to a real probe that calls gateway test_provider_route and writes probe-verified evidence records, so project_route_state's draft_history leg lights up (state_projection.py:108-109). All edits land in apps/studio/backend + frontend."
 },
 "n4-golden#29": {
  "id": "n4-golden#29",
  "name": "golden-list (golden-content READ endpoint)",
  "backend_change_needed": "Add a golden-content READ endpoint in the Studio shell that returns the persisted per-node golden case content (the expected_output stored in cases/{case_id}.json) so the frontend can open/edit an EXISTING baseline's content. Today the golden router (apps/studio/backend/app/routers/golden.py) exposes GET list / POST set / POST /plan / GET /template / POST /manual/plan / DELETE only — there is NO endpoint that returns the actual saved expected_output of an existing golden case. The GoldenBaseline DTO already projects per-node `cases` metadata (case_id/node_id/phase_id/expected_output_ref) but never the content the ref points at.",
  "module": "studio-backend-shell",
  "doable_without_keepmain": true,
  "fix_plan": "BE (Studio shell, no KEEP-MAIN): 1) In apps/studio/backend/app/services/golden_diff.py add a read service e.g. read_golden_case_content(skill_id, golden_id, node_id) -> dict (the expected_output) and/or read_golden_baseline_detail(skill_id, golden_id). It reuses the existing on-disk layout: _golden_dir_for(skill_id, golden_id) (golden_diff.py:433) + the baseline.json `cases[].expected_output_ref` (golden_diff.py:103/325/371) + _read_json (golden_diff.py:495) to load cases/{case_id}.json and return its `expected_output`. This mirrors exactly how golden_headless.py:352-372 already resolves expected_output_ref -> case file -> expected_output (so the read logic already exists and just needs a non-headless, content-returning variant). Add a small response model (e.g. GoldenCaseContent{node_id, case_id, expected_output: dict}) in app/models/golden.py. 2) In apps/studio/backend/app/routers/golden.py add GET '/{golden_id}/content' (optionally ?node_id=) returning that model (404 golden.baseline_not_found reusing the existing pattern at golden.py/delete; 422 for missing case). No write guard needed (read-only). FE: in GoldenSection.tsx make each listed baseline/case row open an editable JSON view by SWR-fetching this content endpoint, reusing the existing GoldenTemplateRow Textarea + the saveManualGolden write path (POST /golden/manual/plan -> Rust native-fs / browser fallback) to persist edits. Engine/gateway untouched: golden storage and all readers live entirely in the Studio shell.",
  "evidence": "apps/studio/backend/app/routers/golden.py:28-107 (6 endpoints; NO content-read route); golden_diff.py:103,325,371 (expected_output_ref points at cases/{case_id}.json), golden_diff.py:433 _golden_dir_for, golden_diff.py:495 _read_json; golden_headless.py:352-372 already reads expected_output_ref -> case file -> expected_output (proves the read path exists in-shell); app/models/golden.py:11-33 GoldenBaseline/GoldenBaselineCase project case metadata but no content."
 },
 "n4-golden#35": {
  "id": "n4-golden#35",
  "name": "golden-invalidation (compile-time golden field validation)",
  "backend_change_needed": "Add a compile-time golden-vs-output-schema field check in the Studio shell's compile path: when an agent node has a persisted golden case but the node's current io.outputs schema requires fields the golden case is missing (output-schema drift), the compile must FAIL with a CompileError so predict is blocked. Today compile_skill_for_studio (apps/studio/backend/app/services/skills.py:386-417) only surfaces engine GraphCompileError/lint diagnostics and does NO golden-vs-output-schema check; schema drift is detected only at COMPARE time inside the headless path (golden_headless.py:137-155 computes schema_status valid/stale/missing + stale_fields) and never gates compile/predict.",
  "module": "studio-backend-shell",
  "doable_without_keepmain": true,
  "fix_plan": "BE (Studio shell, no KEEP-MAIN): Add a golden-field-validation step to compile_skill_for_studio (apps/studio/backend/app/services/skills.py:386-417), or a helper it calls, that runs AFTER the engine compile succeeds: (a) enumerate persisted golden cases for the skill via list_golden_baselines_for_skill / read the per-node golden expected_output using the same readers as #29 (golden_diff cases + cases/{case_id}.json expected_output); (b) for each agent node that has a golden case, resolve its current output schema via the ALREADY-ALLOWLISTED engine adapter method build_engine_adapter().resolve_agent_node_output_schema(skill_dir, node_id) (engine.py:1224) — no new engine code, it is an existing port; (c) compute required-field gaps (a field required by io.outputs but absent in the golden expected_output) — reuse/lift the stale-field logic already implemented in golden_headless (_compare_node_outputs schema_status/stale_fields at golden_headless.py:137-155); (d) if any gap exists, raise CompileFailedError(CompileFailure(...)) with a CompileError{severity:'fatal', field/message describing the node + missing golden field} — CompileError/CompileFailure already exist (models/skills.py:103-130) and CompileFailedError + _compile_failure_from_exception already exist (skills.py:111,1556). The compile router (routers/skills.py:365-375) already turns CompileFailedError into a 422 with failure.errors, so the FE gating mechanism is already wired. FE: surface this CompileError in the compile/predict gate (coordinate with N3 compile gating) and block predict until golden fields are refilled. KEEP-MAIN untouched: schema resolution is via the existing adapter port (engine.py:32 imports generate_heuristic_stub, engine.py:1224 resolve_agent_node_output_schema already exist), and the gap computation + error raising live in the Studio shell. NOTE: only the SHELL orchestrates this; the engine compile itself (GraphCompileError) is not changed — the golden-vs-schema rule is a Studio-layer business gate, which is the correct placement (engine compile has no knowledge of Studio golden files).",
  "evidence": "apps/studio/backend/app/services/skills.py:386-417 compile_skill_for_studio (surfaces only engine compile + lint; no golden field check); skills.py:111 CompileFailedError, skills.py:1556-1563 _compile_failure_from_exception, models/skills.py:103-130 CompileError/CompileFailure (failure contract already exists); routers/skills.py:365-375 maps CompileFailedError -> 422 (gate already wired); engine.py:1224 resolve_agent_node_output_schema + engine.py:32 generate_heuristic_stub (output-schema resolution already exposed via allowlisted adapter, no engine change); golden_headless.py:137-155 stale_fields/schema_status logic already exists (only used in compare path today, can be reused for the compile gate)."
 },
 "n2-iopanel#28": {
  "id": "n2-iopanel#28",
  "name": "any-io-import-file (import a file as any-node io)",
  "backend_change_needed": "NONE in engine/gateway. The runtime injection contract the handbook says must be defined already exists in the engine: _wrap_declared_input_files (packages/graph-agent/src/graph_agent/core/graph_assembler.py:1066) wraps each phase node, and _inject_declared_input_files (graph_assembler.py:1126) reads that phase's io.inputs fields carrying source:file+path, reads the file into the blackboard right before the node runs, and emits InputFileInjectedEvent(to_phase=phase_id). Config persistence is the existing Studio shell endpoints POST /{skill_id}/files/{file_path:path} (apps/studio/backend/app/routers/skills.py:877) and POST /{skill_id}/graph/serialize (skills.py:453). The only optional Studio-shell-or-frontend change: make the import writeback emit a source:file field instead of inline source:runtime props.",
  "module": "frontend-only",
  "doable_without_keepmain": true,
  "fix_plan": "No engine/gateway change. Frontend: the per-node import affordance is ALREADY live — SchemaInferPanel renders at apps/studio/frontend/src/components/studio/panels/InputPanel.tsx:655 with a per-node relPath from resolveIoEditTarget (apps/studio/frontend/src/components/studio/panels/io-target.ts:61) that resolves a selected phase node to phases/<id>/SKILL.md|LOGIC.md|SUBGRAPH.md, writing imported fields to that node's frontmatter io.inputs via writeSkillFile -> POST /{skill_id}/files/{file_path:path}. The remaining residual: applyInputSchemaToGraph (apps/studio/frontend/src/lib/schema-infer.ts:53, line 61 `io.inputs = inputSchema`) writes the dropped JSON as inline source=runtime properties. To realize the design's literal 'import file = inject the file's fields into the state machine at that node' (engine triggers injection only on fields with source:file+path), extend the import writeback to optionally tag the imported field(s) as {source:'file', path:<ref>} so the existing engine _wrap_declared_input_files injection fires per-node. This is entirely in the frontend lib + InputPanel; no Studio backend contract change is even required for the inline-schema case, and the file-ref case rides the already-shipped engine injection. Add FE unit test (drop file at a selected mid-node -> writes phases/<id>/<KIND>.md io.inputs) and an engine-untouched check.",
  "evidence": "Engine per-node injection already exists: packages/graph-agent/src/graph_agent/core/graph_assembler.py:1066 (_wrap_declared_input_files) and :1126-1166 (_inject_declared_input_files emits InputFileInjectedEvent to_phase=phase_id). FE per-node import already wired: apps/studio/frontend/src/components/studio/panels/InputPanel.tsx:655 (SchemaInferPanel render) + :619 (resolveIoEditTarget) + apps/studio/frontend/src/components/studio/panels/io-target.ts:61-80 (resolves phase file). Studio shell write endpoint: apps/studio/backend/app/routers/skills.py:877. Residual inline-vs-source:file writeback: apps/studio/frontend/src/lib/schema-infer.ts:53,61. KEEP-MAIN clean: `git status --short packages/graph-agent packages/graph-agent-gateway` empty."
 },
 "n2-iopanel#30": {
  "id": "n2-iopanel#30",
  "name": "subgraph-io-no-1to1 (drop parent/child outputs 1:1 FATAL)",
  "backend_change_needed": "Delete/relax the engine compile-time FATAL that enforces parent subgraph-node io.outputs == child skill manifest io.outputs. Specifically remove the `if parent_outputs != child_outputs: _fatal([F-v3-subgraph-io-mismatch])` block in _validate_subgraph_io_contracts so a subgraph's outputs need not equal the parent node's declared outputs (per G2/§2.10: subgraph input is sliced from the blackboard by schema like any node, parent/child io not bound 1:1). Then remove the [F-v3-subgraph-io-mismatch] FATAL registration and update/delete the corresponding red tests/fixtures. This is inside packages/graph-agent = engine = KEEP-MAIN.",
  "module": "engine-KEEPMAIN",
  "doable_without_keepmain": false,
  "fix_plan": "BLOCKED on KEEP-MAIN. Required engine change (packages/graph-agent): (1) remove the FATAL at packages/graph-agent/src/graph_agent/core/loader.py:631-640 (the `parent_outputs = doc.ast.io.outputs; child_outputs = child.manifest.io.outputs; if parent_outputs != child_outputs: _fatal(... [F-v3-subgraph-io-mismatch] ...)` inside _validate_subgraph_io_contracts), called active at loader.py:213; replace with schema-based field slicing per §2.10 (subgraph input drawn from parent blackboard by schema, no equality requirement). (2) De-register/relabel the error at packages/graph-agent/src/graph_agent/core/error_registry.py:92. (3) Update/remove fixtures+tests (e.g. packages/graph-agent/tests/core/test_ws_e1_subgraph_io_contract_red.py and the round14 compiler e2e + features.yaml entry). No Studio-shell slice can substitute: this is a compile-time FATAL the engine raises during compile_skill, so the Studio backend (which only orchestrates HTTP/DTO and calls the engine) cannot bypass or downgrade it without modifying the frozen engine. Subgraph absolute-path resolution and deprecated target_skill-key rejection are already correct (manifest.py:187-193 Field(alias='path'); loader.py rejects literal target_skill) — no change there. Frontend has no such validation and needs no change.",
  "evidence": "packages/graph-agent/src/graph_agent/core/loader.py:631-640 contains `parent_outputs = doc.ast.io.outputs / child_outputs = child.manifest.io.outputs / if parent_outputs != child_outputs: _fatal(... [F-v3-subgraph-io-mismatch] ...)`; active call site at loader.py:213 (_validate_subgraph_io_contracts inside compile_skill); FATAL registered at packages/graph-agent/src/graph_agent/core/error_registry.py:92 (ErrorCodeMetadata '[F-v3-subgraph-io-mismatch]','FATAL','编译期'). KEEP-MAIN clean: `git status --short packages/graph-agent packages/graph-agent-gateway` returns empty."
 },
 "n4-trace#19": {
  "id": "n4-trace#19",
  "name": "trace-dot-blackboard (dot 节点间操作记录流)",
  "backend_change_needed": "NONE. The handbook's blocker ('数据源依赖引擎补推节点间 micro 操作事件到 EventEnvelope') is STALE/false. The engine package (packages/graph-agent/src/graph_agent/callbacks/events.py) already defines AND emits every micro-op event the design wants: BlackboardReduceEvent/event_type='blackboard_reduce' (events.py:256-262, emitted at graph_assembler.py:715/864/988), InputDispatchEvent/'input_dispatch' (events.py:265-273, emitted graph_assembler.py:750), InputFileInjectedEvent/'input_file_injected' (events.py:275-282, emitted graph_assembler.py:1156), ArtifactSavedEvent/'artifact_saved' (events.py:350-358, emitted io/storage.py:190). The Studio shell relays ALL event types verbatim — _event_envelope_from_callback (run_manager.py:994-1005) wraps any raw callback dict into the EventEnvelope with no type filtering. So the micro-op operation stream is ALREADY on the WS wire reaching the frontend; only the frontend selector ignores it.",
  "module": "frontend-only",
  "doable_without_keepmain": true,
  "fix_plan": "Pure frontend, two files. (1) apps/studio/frontend/src/lib/edge-context.ts: edgeContextFromEvents (line 56-118) currently scans ONLY input_dispatch/edge_transition (isInputDispatch line 26-28 + isEdgeTransition line 30-32, filtered at line 70-73) and returns just snapshot+changed_keys. Add a second pass that collects, for the same from_phase→to_phase pair, the ordered sequence of blackboard_reduce / input_file_injected / artifact_saved (and the input_dispatch itself) events occurring between the upstream phase_end and downstream phase_start, and attach them as a new `operations` array on the returned object. (2) apps/studio/frontend/src/components/studio/WorkspaceContext.tsx: extend the EdgeContextJson interface (line 7-15, currently inputs/phase_outputs/changed_keys only) with an optional `operations` field. (3) apps/studio/frontend/src/components/studio/panels/EdgeContextView.tsx: replace the hardcoded placeholder at lines 262-264 ('Detailed reducer / filter / inject / persist operations are part of the run trace (target-design)...') with a rendered operation-record list driven by contextJson.operations (reducer name from blackboard_reduce.reducer, injected file_ref/target_field from input_file_injected, persisted name/path/size from artifact_saved, dispatched_keys from input_dispatch). The dot-click consumer is already wired: ContextEdge.tsx:158 calls edgeContextFromEvents and feeds EdgeContextView; CallbackEvent is an open Record type (types.ts:628) so no type plumbing is needed for the new event_types. Verify: real run, click a dot, assert EdgeContextView shows the end→start reduce/inject/persist log column.",
  "evidence": "STALE blocker disproven: packages/graph-agent/src/graph_agent/callbacks/events.py:256-282,350-358 (event classes exist) + graph_assembler.py:715,750,864,988,1156 and io/storage.py:190 (engine actually emits them); apps/studio/backend/app/services/run_manager.py:994-1005 (shell relays all event types unfiltered). Frontend gap: apps/studio/frontend/src/lib/edge-context.ts:70-73 only matches input_dispatch/edge_transition and drops the other three; apps/studio/frontend/src/components/studio/panels/EdgeContextView.tsx:262-264 hardcoded target-design placeholder; apps/studio/frontend/src/components/studio/WorkspaceContext.tsx:7-15 EdgeContextJson has no operations field."
 },
 "n4-trace#23": {
  "id": "n4-trace#23",
  "name": "trace-model-compare (P8 模型对比纵切)",
  "backend_change_needed": "Studio-shell-only backend contract additions (NOT gateway/engine). Needed: (a) extend RunRequest DTO with candidate fields; (b) add a shell orchestration path that materializes each candidate into a temporary RoleEntry and runs it; (c) group results by candidate. The gateway already provides the temporary-role primitive — RouteResolver.resolve_temporary_role / resolve_temporary_roles (packages/graph-agent-gateway/src/graph_agent_gateway/resolver.py:194-235) inject an unsaved RoleEntry into a copied registry snapshot and resolve it to a route chain — so NO gateway change is required. Even simpler, the shell already owns a per-run role-injection seam: the engine resolver loads roles from the STUDIO_LLM_ROLES_PATH env / llm_roles.yaml (apps/studio/backend/app/core/adapters/engine.py:231-235), and runs execute in a separate worker process (run_manager.py:493-496), so the shell can hand each candidate worker a temporary roles file/env without touching the engine. NOTE: phase.model_override is baked per-phase at compile time (llm_phase_node.py:178/186/199), so candidate selection rides on the temporary-ROLE path (override the role's resolved endpoint), not a runtime model_override field.",
  "module": "mixed-studio+keepmain",
  "doable_without_keepmain": true,
  "fix_plan": "Entire vertical slice lands in apps/studio/backend (shell) + frontend; gateway/engine untouched. BACKEND-FIRST: (1) apps/studio/backend/app/models/runs.py — RunRequest (line 26-31, currently extra='forbid' with only input_data/golden_id/paste_json) gains an optional `candidates: list[RunCandidate]` field where RunCandidate references an existing settings entity (role name OR model_group key OR model_bundle id) — these data sources already exist (llm_model_groups service + llm_roles service). (2) New shell service (e.g. app/services/run_compare.py) — for each candidate, call materialize_role/materialize_model_bundle (app/services/llm_role_materializer.py:16-32, already wraps the engine materializer) to produce a temporary RoleEntry, then resolve it via the gateway's resolve_temporary_role (resolver.py:194) OR write a per-candidate temporary llm_roles.yaml and point that worker's STUDIO_LLM_ROLES_PATH at it. (3) run_manager.start_run (run_manager.py:456-521) — when request.candidates is present, spawn one worker per candidate against the same compiled artifact/inputs, tagging each run with a compare-group key + candidate id in RunMetadata. (4) Expose group results: extend RunListResponse / a new GET compare endpoint in routers/runs.py so the frontend can fetch results grouped by candidate (including per-candidate failure state). FRONTEND: run page candidate-select UI referencing settings model-group/bundle/role (reuse existing settings components, no new bespoke widgets), and TracePanel top tabs grouped by candidate. Verify: unit test run request carrying candidates → temporary role resolved → N runs grouped → Trace top tabs switch candidates incl. failure tab; real run picking 2 roles, switch tabs.",
  "evidence": "Missing in shell DTO: apps/studio/backend/app/models/runs.py:26-31 RunRequest has extra='forbid' + no candidate/compare/model_group/bundle field (grep zero-hit). Building blocks PRESENT (no KEEP-MAIN change): packages/graph-agent-gateway/src/graph_agent_gateway/resolver.py:194-235 resolve_temporary_role(s); apps/studio/backend/app/services/llm_role_materializer.py:16-32 materialize_role/materialize_model_bundle; apps/studio/backend/app/core/adapters/engine.py:231-235 roles loaded from STUDIO_LLM_ROLES_PATH (shell-controllable per worker); apps/studio/backend/app/services/run_manager.py:493-496 runs spawn in separate worker processes. Not model-compare today: apps/studio/backend/app/models/compare.py:18 CompareResult is golden_value diff (golden), not per-model; frontend candidate/compare hits are all settings/api-keys (route_candidate, copilot role derivation), TracePanel.tsx has zero candidate/compare references."
 },
 "n2-canvas#10": {
  "id": "n2-canvas#10",
  "name": "data-gap-viz",
  "backend_change_needed": "Expose a STRUCTURED per-phase io field supply/demand projection (which upstream phase's io.outputs field feeds which downstream phase's io.inputs field), instead of only the whole-skill io_schema. Today get_skill_detail returns (a) io_schema = compiled.raw['io']['inputs'/'outputs'] which is the ENTIRE-skill graph-level io (apps/studio/backend/app/services/skills.py:1453-1457 _io_schema), and (b) graph_topology rows that carry only id/src/depends_on/mode/path (skills.py:1426-1441 _topology_row) with NO per-phase field schema. Add per-phase inputs/outputs (and optionally a resolved supply→demand edge list) so the FE can compute and render data-gaps at design time.",
  "module": "studio-backend-shell",
  "doable_without_keepmain": true,
  "fix_plan": "BACKEND (Studio shell, no engine edit): The engine's CompiledSkill already carries per-node io: each node exposes node.frontmatter['io']['inputs'/'outputs']['properties'] (proven in packages/graph-agent/.../runner.py:1312-1314 _node_output_fields and :259-260 node.ast.io.outputs). The Studio shell already iterates compiled.nodes (skills.py:1409). Extend _topology_row / _graph_topology (skills.py:1408-1441) — or add a sibling projector — to attach each phase's io.inputs and io.outputs field schema, and optionally precompute the supply→demand mapping (for each downstream io.inputs field, which upstream phase produces a matching io.outputs field, honoring depends_on order — same producer_by_field pattern the engine uses at runner.py:1330-1335). Add these fields to SkillDetail/graph_topology DTO in apps/studio/backend/app/models/skills.py. FRONTEND: in canvas-authoring.ts add the supply/demand/data-gap analysis (grep confirms zero such code today) and render the blackboard-field-checkbox UI + canvas gap markers in the node io panel. No engine/gateway change: all source data is already in the compiled graph the Studio shell loads via _load_compiled (skills.py:1302).",
  "evidence": "apps/studio/backend/app/services/skills.py:1453-1457 (_io_schema returns whole-skill io, not per-phase); skills.py:1426-1441 (_topology_row exposes only id/src/depends_on/mode/path, no per-phase io fields); skills.py:1409 (Studio shell already iterates compiled.nodes); packages/graph-agent/src/graph_agent/core/runner.py:1312-1314 + 259-260 (engine compiled nodes already carry frontmatter['io'].outputs.properties / node.ast.io.outputs — the per-phase supply data exists); backend grep supply/field_supply/io_supply/data_gap in apps/studio/backend/app = ZERO; frontend grep dataGap/supplyGap/fieldSupply/typeMismatch in canvas-authoring.ts = ZERO (no FE viz, no stale red-X to delete)."
 },
 "n5-node#3": {
  "id": "n5-node#3",
  "name": "dirty-downstream-graying",
  "backend_change_needed": "Make resume_validity per-node aware. Today the dirty decision is a GLOBAL artifact-level compare: the Studio shell adapter computes dirty_fields = ['content_hash'/'execution_fingerprint'] by comparing the whole-skill snapshot artifact vs a fresh ephemeral compile (apps/studio/backend/app/core/adapters/engine.py:1184-1198), and resume_from_node_id is accepted+echoed but NEVER participates (engine.py:1095, 1198). Need: given resume_from_node_id, slice dirtiness by node dependency so unrelated side-branches return resume_allowed=true. The DTO must also carry per-node dirty info — ResumeValidityResponse.dirty_fields is currently typed Literal['content_hash','execution_fingerprint'] (apps/studio/backend/app/models/runs.py), too narrow.",
  "module": "studio-backend-shell",
  "doable_without_keepmain": true,
  "fix_plan": "BACKEND (Studio shell, no engine edit): The whole-skill compare lives in the Studio adapter EngineAdapter.resume_validity (engine.py:1079-1198), NOT in the frozen engine — even the production loopback path routes back to EngineAdapter(transport='in_process').resume_validity (loopback_host.py:22). The engine already exposes everything to slice per node: the compiled graph gives per-node io.outputs fields (_node_output_fields runner.py:1319), the producer-field→phase map and phase dependency order (runner.py:1326-1335 / _compiled_phase_names_in_order). The Studio adapter already calls self.compile() and holds the compiled graph (engine.py:1164). So in engine.py resume_validity, when resume_from_node_id is set, build the affected-downstream set from depends_on order + producer mapping and return resume_allowed=true for unrelated branches. Widen ResumeValidityResponse (models/runs.py) and _resume_validity_payload (engine.py:504-532) to carry per-node/affected-node dirty data (e.g. dirty_node_ids / affected_downstream). NOTE the engine LACKS per-node content hashes (artifacts.py:181/214 hash the whole skill; per-node hash grep = ZERO), so the slice must be dependency-graph-based (which downstream phases depend on the changed upstream), not per-node re-hashing — sufficient for the graying UX and stays in the Studio shell. FRONTEND: Workspace.tsx:983-1020 validity effect currently fires for a single selectedNodeId only when selectedNodeStatus==='error'; change to compute the affected-downstream set on upstream change → batch-query validity → paint graying onto canvas [Resume] nodes (depends on #2's node-level Resume entry landing first).",
  "evidence": "apps/studio/backend/app/core/adapters/engine.py:1184-1198 (dirty_fields = whole-skill content_hash/execution_fingerprint compare); engine.py:1095,1198 (resume_from_node_id only passed through, never drives the decision); engine.py:1164 (Studio adapter already has compiled graph via self.compile); apps/studio/backend/app/core/adapters/loopback_host.py:22 (loopback → EngineAdapter in_process resume_validity, so the decision is shell-side even in prod); apps/studio/backend/app/routers/runs.py:176-180 (router forwards resume_from_node_id to adapter); packages/graph-agent/src/graph_agent/core/runner.py:1319-1349 (_validate_resume_context_override_scope already builds producer_by_field + phase order — the slicing primitives exist); packages/graph-agent/src/graph_agent/core/artifacts.py:181,214 (content_hash/execution_fingerprint are whole-skill; no per-node hash); apps/studio/frontend/src/components/studio/Workspace.tsx:985 (validity effect gated on single selectedNodeId && selectedNodeStatus==='error', no batch downstream)."
 },
 "n2-properties#20": {
  "id": "n2-properties#20",
  "name": "subgraph-whitelist",
  "backend_change_needed": "NONE — already done. The handbook flags slice ③ (copilot cwd must validate the referenced subgraph path actually resolves on disk) as backend-blocked on a 'GET child-graph-topology' contract, but that endpoint already exists and is wired end-to-end: GET /skills/{skill_id}/subgraph (apps/studio/backend/app/routers/skills.py:353-362) resolves the child GRAPH.md by absolute path within the boundary and returns ChildGraphTopology, raising SUBGRAPH_PATH_NOT_FOUND when the path does not resolve. No engine/gateway change is needed (the underlying load_child_graph_topology_projection is engine-owned but already imported and unchanged).",
  "module": "frontend-only",
  "doable_without_keepmain": true,
  "fix_plan": "NO backend change required. ① + ② are pure frontend and can land now: in apps/studio/frontend/src/components/studio/panels/PropertiesPanel.tsx import + call resolveSubgraphReference (subgraph-path.ts:35) for the selected subgraph node (currently only legacySubgraphTargetSkill is imported at line 22); when status==='missing' give the Path input destructive styling + error hint; in the subgraph branch (PropertiesPanel.tsx:582-598) add a 'select folder to import subgraph' button calling selectSkillDirectory (tauri.ts:109, Desktop-only safe) and setField('path', ...). ③ ('path given but does not resolve on disk') is ALSO doable without KEEP-MAIN now: the validating endpoint GET /skills/{skill_id}/subgraph already exists (skills.py:353) and is already exposed in the FE client as getChildGraphTopology (client.ts:594-602) and consumed by SubgraphInline.tsx:132 / GraphCanvas.tsx:368. resolveSubgraphReference (subgraph-path.ts:39-42) today returns 'resolved' for ANY non-empty absolute path without checking disk — the fix is to drive the missing/destructive state off the existing getChildGraphTopology 404 (SUBGRAPH_PATH_NOT_FOUND), not to add a new backend contract. So the entire item is frontend-only.",
  "evidence": "apps/studio/backend/app/routers/skills.py:353-362 (GET /{skill_id}/subgraph already exists, returns ChildGraphTopology); apps/studio/backend/app/services/skills.py:1271-1295 (get_child_graph_topology resolves child by path, raises SUBGRAPH_PATH_NOT_FOUND); packages/graph-agent/src/graph_agent/core/topology_projection.py:133-134 (load_child_graph_topology_projection raises SUBGRAPH_PATH_NOT_FOUND when child GRAPH.md is absent on disk — already implemented, unchanged); apps/studio/frontend/src/api/client.ts:594-602 (getChildGraphTopology already wired to that endpoint); apps/studio/frontend/src/components/studio/SubgraphInline.tsx:132 + GraphCanvas.tsx:368 (already consumed); apps/studio/frontend/src/components/studio/panels/PropertiesPanel.tsx:22 (only legacySubgraphTargetSkill imported, resolveSubgraphReference NOT used) and :582-598 (subgraph branch: path+legacy hint only, no missing-destructive, no folder-picker import); apps/studio/frontend/src/components/studio/subgraph-path.ts:35-50 (resolveSubgraphReference exists; :39-42 returns resolved for any non-empty path, no disk check); apps/studio/frontend/src/lib/tauri.ts:109 (selectSkillDirectory ready, Desktop-only fallback)."
 }
}