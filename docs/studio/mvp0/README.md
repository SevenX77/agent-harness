# Studio docs — MVP0 / 旧设计(当前实现 baseline)

> 描述 **当前已实现的 Studio(MVP0 / 旧设计)** 的 baseline 文档。新设计(MVP1 重设计目标)在 [`../mvp1/`](../mvp1/)。
> ⚠️ 多处与重设计方向、与 FROZEN 相反; **新设计权威已移交** `.kiro/specs/studio-feature-*` + [`../mvp1/`](../mvp1/) + [`../INDEX.md`](../INDEX.md)。迁移完成后按需删/归档。**不要据此实现。**

- `02_features/` — 旧 5 feature baseline(asset-explorer / canvas-topology / copilot-chat / skill-lifecycle / trace-inspector)。新权威 = 同名 `.kiro/specs/studio-feature-*`。
- `03_platform/` — 旧 4 platform baseline(llm-gateway / state-engine / system-layout / workspace-fs)。新权威 = INDEX §6 平台三分(D10)。
